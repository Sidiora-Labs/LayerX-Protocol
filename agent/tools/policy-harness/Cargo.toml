[package]
name = "layerx-policy-harness"
version = "0.1.0"
edition = "2021"
rust-version = "1.91.1"
license = "LicenseRef-Centra-ai-Protocol"
publish = false

[dependencies]
layerx-agentd = { path = "../../crates/layerx-agentd" }
layerx-types = { path = "../../crates/layerx-types" }

[workspace]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
