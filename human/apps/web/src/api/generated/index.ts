// GENERATED by layerx-human-api-gen from human/schema/human-api. DO NOT EDIT BY HAND.
// Regenerate with: cargo run --manifest-path human/tools/api-gen/Cargo.toml --locked -- human/schema/human-api human/apps/web/src/api/generated

export const schemaVersion = { major: 1, minor: 0 } as const;

export type JsonValue = null | boolean | number | string | JsonValue[] | JsonObject;

export interface JsonObject {
  [name: string]: JsonValue;
}

export class HumanApiDecodeError extends Error {
  constructor(message: string) {
    super(message);
    this.name = "HumanApiDecodeError";
  }
}

export function requireValue(value: JsonValue | undefined, at: string): JsonValue {
  if (value === undefined) {
    throw new HumanApiDecodeError(at + " is missing");
  }
  return value;
}

export function expectObject(value: JsonValue | undefined, at: string): JsonObject {
  const present = requireValue(value, at);
  if (present === null || typeof present !== "object" || Array.isArray(present)) {
    throw new HumanApiDecodeError(at + " must be a JSON object");
  }
  return present;
}

export function expectArray(value: JsonValue | undefined, at: string): JsonValue[] {
  const present = requireValue(value, at);
  if (!Array.isArray(present)) {
    throw new HumanApiDecodeError(at + " must be a JSON array");
  }
  return present;
}

export function expectString(value: JsonValue | undefined, at: string): string {
  const present = requireValue(value, at);
  if (typeof present !== "string") {
    throw new HumanApiDecodeError(at + " must be a JSON string");
  }
  return present;
}

export function expectBoolean(value: JsonValue | undefined, at: string): boolean {
  const present = requireValue(value, at);
  if (typeof present !== "boolean") {
    throw new HumanApiDecodeError(at + " must be a JSON boolean");
  }
  return present;
}

export function expectInteger(value: JsonValue | undefined, at: string): number {
  const present = requireValue(value, at);
  if (typeof present !== "number" || !Number.isSafeInteger(present)) {
    throw new HumanApiDecodeError(at + " must be a JSON integer");
  }
  return present;
}

export function decodeArray<Item>(
  value: JsonValue | undefined,
  at: string,
  item: (value: JsonValue | undefined, at: string) => Item,
): Item[] {
  return expectArray(value, at).map((entry, index) => item(entry, at + "[" + String(index) + "]"));
}

const consensusIntegerShape = /^[0-9]+$/;

export function decodeConsensusInteger(value: JsonValue | undefined, at: string): bigint {
  const text = expectString(value, at);
  if (!consensusIntegerShape.test(text)) {
    throw new HumanApiDecodeError(at + " must be a decimal string of base units");
  }
  return BigInt(text);
}

export function encodeConsensusInteger(value: bigint): string {
  if (value < 0n) {
    throw new HumanApiDecodeError("a consensus integer is never negative");
  }
  return value.toString(10);
}

export type AccountId = string;

export type ActivityEntryId = string;

export type AgentId = string;

export type Amount = bigint;

export type ApprovalId = string;

export type AssertionId = string;

export type CopyKey = string;

export type CurrencyCode = string;

export type Cursor = string;

export type DeviceId = string;

export type EmailAddress = string;

export type EvidenceId = string;

export type EvmAddress = string;

export type EvmSignature = string;

export type ExportId = string;

export type JourneyId = string;

export type NotificationId = string;

export type OpaqueCredential = string;

export type OperationDigest = string;

export type PasskeyId = string;

export type QuoteId = string;

export type RegistrationId = string;

export type SessionId = string;

export type StageId = string;

export type StepUpChallengeId = string;

export type Timestamp = string;

export type TraceId = string;

export type WalletAddress = string;

export type WalletSignature = string;

export type WalletTxId = string;

export type ActivityEntryKind = "deposit" | "withdrawal" | "movement" | "agent-action" | "approval" | "security-event";

export const activityEntryKindVariants: readonly ActivityEntryKind[] = ["deposit", "withdrawal", "movement", "agent-action", "approval", "security-event"];

export function decodeActivityEntryKind(value: JsonValue | undefined, at: string): ActivityEntryKind {
  const text = expectString(value, at);
  for (const variant of activityEntryKindVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared ActivityEntryKind variant");
}

export type AgentState = "creating" | "active" | "paused" | "archiving" | "archived";

export const agentStateVariants: readonly AgentState[] = ["creating", "active", "paused", "archiving", "archived"];

export function decodeAgentState(value: JsonValue | undefined, at: string): AgentState {
  const text = expectString(value, at);
  for (const variant of agentStateVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared AgentState variant");
}

export type ApprovalState = "pending" | "approved" | "rejected" | "expired" | "defective";

export const approvalStateVariants: readonly ApprovalState[] = ["pending", "approved", "rejected", "expired", "defective"];

export function decodeApprovalState(value: JsonValue | undefined, at: string): ApprovalState {
  const text = expectString(value, at);
  for (const variant of approvalStateVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared ApprovalState variant");
}

export type BindingState = "none" | "binding" | "bound" | "rebinding";

export const bindingStateVariants: readonly BindingState[] = ["none", "binding", "bound", "rebinding"];

export function decodeBindingState(value: JsonValue | undefined, at: string): BindingState {
  const text = expectString(value, at);
  for (const variant of bindingStateVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared BindingState variant");
}

export type ErrorCode = "unauthenticated" | "session-expired" | "step-up-required" | "forbidden" | "not-found" | "invalid-request" | "conflict" | "rate-limited" | "cursor-expired" | "unavailable" | "upstream-degraded" | "challenge-expired" | "refused-by-policy" | "refused-by-budget" | "refused-by-capability" | "refused-by-protocol" | "refused-by-limit" | "quote-expired" | "wallet-not-bound" | "exit-unavailable" | "already-decided" | "hold-expired" | "hold-defective" | "archive-needs-disposition" | "confirmation-mismatch" | "not-suppressible";

export const errorCodeVariants: readonly ErrorCode[] = ["unauthenticated", "session-expired", "step-up-required", "forbidden", "not-found", "invalid-request", "conflict", "rate-limited", "cursor-expired", "unavailable", "upstream-degraded", "challenge-expired", "refused-by-policy", "refused-by-budget", "refused-by-capability", "refused-by-protocol", "refused-by-limit", "quote-expired", "wallet-not-bound", "exit-unavailable", "already-decided", "hold-expired", "hold-defective", "archive-needs-disposition", "confirmation-mismatch", "not-suppressible"];

export function decodeErrorCode(value: JsonValue | undefined, at: string): ErrorCode {
  const text = expectString(value, at);
  for (const variant of errorCodeVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared ErrorCode variant");
}

export type EvidenceClass = "local-journey-state" | "submission-record" | "layerx-receipt" | "checkpoint-proof" | "paxeer-finality" | "typed-refusal" | "approval-hold" | "wallet-ack";

export const evidenceClassVariants: readonly EvidenceClass[] = ["local-journey-state", "submission-record", "layerx-receipt", "checkpoint-proof", "paxeer-finality", "typed-refusal", "approval-hold", "wallet-ack"];

export function decodeEvidenceClass(value: JsonValue | undefined, at: string): EvidenceClass {
  const text = expectString(value, at);
  for (const variant of evidenceClassVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared EvidenceClass variant");
}

export type ExportKind = "statement" | "evidence-bundle";

export const exportKindVariants: readonly ExportKind[] = ["statement", "evidence-bundle"];

export function decodeExportKind(value: JsonValue | undefined, at: string): ExportKind {
  const text = expectString(value, at);
  for (const variant of exportKindVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared ExportKind variant");
}

export type JourneyKind = "onboarding" | "wallet-binding" | "deposit" | "withdraw" | "exit" | "move" | "agent-create" | "agent-fund" | "agent-pause" | "agent-retire";

export const journeyKindVariants: readonly JourneyKind[] = ["onboarding", "wallet-binding", "deposit", "withdraw", "exit", "move", "agent-create", "agent-fund", "agent-pause", "agent-retire"];

export function decodeJourneyKind(value: JsonValue | undefined, at: string): JourneyKind {
  const text = expectString(value, at);
  for (const variant of journeyKindVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared JourneyKind variant");
}

export type JourneyState = "getting-ready" | "sending" | "processing" | "done" | "done-finalised" | "still-checking" | "refused" | "waiting-for-you";

export const journeyStateVariants: readonly JourneyState[] = ["getting-ready", "sending", "processing", "done", "done-finalised", "still-checking", "refused", "waiting-for-you"];

export function decodeJourneyState(value: JsonValue | undefined, at: string): JourneyState {
  const text = expectString(value, at);
  for (const variant of journeyStateVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared JourneyState variant");
}

export type KeyChallengeKind = "rotate" | "recover";

export const keyChallengeKindVariants: readonly KeyChallengeKind[] = ["rotate", "recover"];

export function decodeKeyChallengeKind(value: JsonValue | undefined, at: string): KeyChallengeKind {
  const text = expectString(value, at);
  for (const variant of keyChallengeKindVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared KeyChallengeKind variant");
}

export type LimitEnforcement = "protocol" | "app";

export const limitEnforcementVariants: readonly LimitEnforcement[] = ["protocol", "app"];

export function decodeLimitEnforcement(value: JsonValue | undefined, at: string): LimitEnforcement {
  const text = expectString(value, at);
  for (const variant of limitEnforcementVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared LimitEnforcement variant");
}

export type MoneyDirection = "in" | "out";

export const moneyDirectionVariants: readonly MoneyDirection[] = ["in", "out"];

export function decodeMoneyDirection(value: JsonValue | undefined, at: string): MoneyDirection {
  const text = expectString(value, at);
  for (const variant of moneyDirectionVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared MoneyDirection variant");
}

export type MoveMechanism = "fund" | "allocate" | "return" | "transfer";

export const moveMechanismVariants: readonly MoveMechanism[] = ["fund", "allocate", "return", "transfer"];

export function decodeMoveMechanism(value: JsonValue | undefined, at: string): MoveMechanism {
  const text = expectString(value, at);
  for (const variant of moveMechanismVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared MoveMechanism variant");
}

export type NotificationClass = "approval-waiting" | "money-arrived" | "journey-finished" | "claim-ready" | "security-new-device" | "security-recovery" | "security-wallet-rebinding" | "security-key-rotation" | "service-status";

export const notificationClassVariants: readonly NotificationClass[] = ["approval-waiting", "money-arrived", "journey-finished", "claim-ready", "security-new-device", "security-recovery", "security-wallet-rebinding", "security-key-rotation", "service-status"];

export function decodeNotificationClass(value: JsonValue | undefined, at: string): NotificationClass {
  const text = expectString(value, at);
  for (const variant of notificationClassVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared NotificationClass variant");
}

export type NotificationDetailLevel = "full" | "summary" | "minimal";

export const notificationDetailLevelVariants: readonly NotificationDetailLevel[] = ["full", "summary", "minimal"];

export function decodeNotificationDetailLevel(value: JsonValue | undefined, at: string): NotificationDetailLevel {
  const text = expectString(value, at);
  for (const variant of notificationDetailLevelVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared NotificationDetailLevel variant");
}

export type NotificationRecency = "today" | "yesterday" | "this-week" | "earlier";

export const notificationRecencyVariants: readonly NotificationRecency[] = ["today", "yesterday", "this-week", "earlier"];

export function decodeNotificationRecency(value: JsonValue | undefined, at: string): NotificationRecency {
  const text = expectString(value, at);
  for (const variant of notificationRecencyVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared NotificationRecency variant");
}

export type RefusedBy = "policy" | "budget" | "capability" | "protocol" | "limit";

export const refusedByVariants: readonly RefusedBy[] = ["policy", "budget", "capability", "protocol", "limit"];

export function decodeRefusedBy(value: JsonValue | undefined, at: string): RefusedBy {
  const text = expectString(value, at);
  for (const variant of refusedByVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared RefusedBy variant");
}

export type Retriability = "retriable" | "retriable-after" | "structural" | "final";

export const retriabilityVariants: readonly Retriability[] = ["retriable", "retriable-after", "structural", "final"];

export function decodeRetriability(value: JsonValue | undefined, at: string): Retriability {
  const text = expectString(value, at);
  for (const variant of retriabilityVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared Retriability variant");
}

export type StreamEventKind = "journey-progress" | "approval-created" | "approval-approved" | "approval-rejected" | "approval-expired" | "notification";

export const streamEventKindVariants: readonly StreamEventKind[] = ["journey-progress", "approval-created", "approval-approved", "approval-rejected", "approval-expired", "notification"];

export function decodeStreamEventKind(value: JsonValue | undefined, at: string): StreamEventKind {
  const text = expectString(value, at);
  for (const variant of streamEventKindVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared StreamEventKind variant");
}

export type VerificationLevel = "unverified" | "receipt-verified" | "checkpoint-finalised" | "paxeer-finalised";

export const verificationLevelVariants: readonly VerificationLevel[] = ["unverified", "receipt-verified", "checkpoint-finalised", "paxeer-finalised"];

export function decodeVerificationLevel(value: JsonValue | undefined, at: string): VerificationLevel {
  const text = expectString(value, at);
  for (const variant of verificationLevelVariants) {
    if (variant === text) {
      return variant;
    }
  }
  throw new HumanApiDecodeError(at + " must be a declared VerificationLevel variant");
}

export interface AccountCreateRequest {
  email: EmailAddress;
  display_name: string;
}

export function decodeAccountCreateRequest(value: JsonValue | undefined, at: string): AccountCreateRequest {
  const object = expectObject(value, at);
  const result: AccountCreateRequest = {
    email: expectString(object["email"], at + ".email"),
    display_name: expectString(object["display_name"], at + ".display_name"),
  };
  return result;
}

export function encodeAccountCreateRequest(value: AccountCreateRequest): JsonValue {
  const result: JsonObject = {
    email: value.email,
    display_name: value.display_name,
  };
  return result;
}

export interface AccountCreation {
  account_id: AccountId;
  onboarding: Journey;
}

export function decodeAccountCreation(value: JsonValue | undefined, at: string): AccountCreation {
  const object = expectObject(value, at);
  const result: AccountCreation = {
    account_id: expectString(object["account_id"], at + ".account_id"),
    onboarding: decodeJourney(object["onboarding"], at + ".onboarding"),
  };
  return result;
}

export function encodeAccountCreation(value: AccountCreation): JsonValue {
  const result: JsonObject = {
    account_id: value.account_id,
    onboarding: encodeJourney(value.onboarding),
  };
  return result;
}

export interface ActivityEntry {
  entry_id: ActivityEntryId;
  kind: ActivityEntryKind;
  state: JourneyState;
  state_copy_key: CopyKey;
  summary_copy_key: CopyKey;
  occurred_at: Timestamp;
  money?: Money;
  direction?: MoneyDirection;
  agent_id?: AgentId;
  journey_id?: JourneyId;
  approval_id?: ApprovalId;
}

export function decodeActivityEntry(value: JsonValue | undefined, at: string): ActivityEntry {
  const object = expectObject(value, at);
  const result: ActivityEntry = {
    entry_id: expectString(object["entry_id"], at + ".entry_id"),
    kind: decodeActivityEntryKind(object["kind"], at + ".kind"),
    state: decodeJourneyState(object["state"], at + ".state"),
    state_copy_key: expectString(object["state_copy_key"], at + ".state_copy_key"),
    summary_copy_key: expectString(object["summary_copy_key"], at + ".summary_copy_key"),
    occurred_at: expectString(object["occurred_at"], at + ".occurred_at"),
  };
  if (object["money"] !== undefined) {
    result.money = decodeMoney(object["money"], at + ".money");
  }
  if (object["direction"] !== undefined) {
    result.direction = decodeMoneyDirection(object["direction"], at + ".direction");
  }
  if (object["agent_id"] !== undefined) {
    result.agent_id = expectString(object["agent_id"], at + ".agent_id");
  }
  if (object["journey_id"] !== undefined) {
    result.journey_id = expectString(object["journey_id"], at + ".journey_id");
  }
  if (object["approval_id"] !== undefined) {
    result.approval_id = expectString(object["approval_id"], at + ".approval_id");
  }
  return result;
}

export function encodeActivityEntry(value: ActivityEntry): JsonValue {
  const result: JsonObject = {
    entry_id: value.entry_id,
    kind: value.kind,
    state: value.state,
    state_copy_key: value.state_copy_key,
    summary_copy_key: value.summary_copy_key,
    occurred_at: value.occurred_at,
  };
  if (value.money !== undefined) {
    result["money"] = encodeMoney(value.money);
  }
  if (value.direction !== undefined) {
    result["direction"] = value.direction;
  }
  if (value.agent_id !== undefined) {
    result["agent_id"] = value.agent_id;
  }
  if (value.journey_id !== undefined) {
    result["journey_id"] = value.journey_id;
  }
  if (value.approval_id !== undefined) {
    result["approval_id"] = value.approval_id;
  }
  return result;
}

export interface ActivityEntryDetail {
  entry_id: ActivityEntryId;
  kind: ActivityEntryKind;
  state: JourneyState;
  state_copy_key: CopyKey;
  summary_copy_key: CopyKey;
  occurred_at: Timestamp;
  stages: JourneyStage[];
  evidence: EvidenceRef[];
  money?: Money;
  fees?: Money;
  direction?: MoneyDirection;
  agent_id?: AgentId;
  journey_id?: JourneyId;
  approval_id?: ApprovalId;
}

export function decodeActivityEntryDetail(value: JsonValue | undefined, at: string): ActivityEntryDetail {
  const object = expectObject(value, at);
  const result: ActivityEntryDetail = {
    entry_id: expectString(object["entry_id"], at + ".entry_id"),
    kind: decodeActivityEntryKind(object["kind"], at + ".kind"),
    state: decodeJourneyState(object["state"], at + ".state"),
    state_copy_key: expectString(object["state_copy_key"], at + ".state_copy_key"),
    summary_copy_key: expectString(object["summary_copy_key"], at + ".summary_copy_key"),
    occurred_at: expectString(object["occurred_at"], at + ".occurred_at"),
    stages: decodeArray(object["stages"], at + ".stages", decodeJourneyStage),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
  };
  if (object["money"] !== undefined) {
    result.money = decodeMoney(object["money"], at + ".money");
  }
  if (object["fees"] !== undefined) {
    result.fees = decodeMoney(object["fees"], at + ".fees");
  }
  if (object["direction"] !== undefined) {
    result.direction = decodeMoneyDirection(object["direction"], at + ".direction");
  }
  if (object["agent_id"] !== undefined) {
    result.agent_id = expectString(object["agent_id"], at + ".agent_id");
  }
  if (object["journey_id"] !== undefined) {
    result.journey_id = expectString(object["journey_id"], at + ".journey_id");
  }
  if (object["approval_id"] !== undefined) {
    result.approval_id = expectString(object["approval_id"], at + ".approval_id");
  }
  return result;
}

export function encodeActivityEntryDetail(value: ActivityEntryDetail): JsonValue {
  const result: JsonObject = {
    entry_id: value.entry_id,
    kind: value.kind,
    state: value.state,
    state_copy_key: value.state_copy_key,
    summary_copy_key: value.summary_copy_key,
    occurred_at: value.occurred_at,
    stages: value.stages.map(encodeJourneyStage),
    evidence: value.evidence.map(encodeEvidenceRef),
  };
  if (value.money !== undefined) {
    result["money"] = encodeMoney(value.money);
  }
  if (value.fees !== undefined) {
    result["fees"] = encodeMoney(value.fees);
  }
  if (value.direction !== undefined) {
    result["direction"] = value.direction;
  }
  if (value.agent_id !== undefined) {
    result["agent_id"] = value.agent_id;
  }
  if (value.journey_id !== undefined) {
    result["journey_id"] = value.journey_id;
  }
  if (value.approval_id !== undefined) {
    result["approval_id"] = value.approval_id;
  }
  return result;
}

export interface ActivityFilter {
  kinds?: ActivityEntryKind[];
  agent_id?: AgentId;
  from?: Timestamp;
  to?: Timestamp;
}

export function decodeActivityFilter(value: JsonValue | undefined, at: string): ActivityFilter {
  const object = expectObject(value, at);
  const result: ActivityFilter = {};
  if (object["kinds"] !== undefined) {
    result.kinds = decodeArray(object["kinds"], at + ".kinds", decodeActivityEntryKind);
  }
  if (object["agent_id"] !== undefined) {
    result.agent_id = expectString(object["agent_id"], at + ".agent_id");
  }
  if (object["from"] !== undefined) {
    result.from = expectString(object["from"], at + ".from");
  }
  if (object["to"] !== undefined) {
    result.to = expectString(object["to"], at + ".to");
  }
  return result;
}

export function encodeActivityFilter(value: ActivityFilter): JsonValue {
  const result: JsonObject = {};
  if (value.kinds !== undefined) {
    result["kinds"] = value.kinds;
  }
  if (value.agent_id !== undefined) {
    result["agent_id"] = value.agent_id;
  }
  if (value.from !== undefined) {
    result["from"] = value.from;
  }
  if (value.to !== undefined) {
    result["to"] = value.to;
  }
  return result;
}

export interface ActivityGroup {
  month: string;
  subtotal_in: Money;
  subtotal_out: Money;
  entries: ActivityEntry[];
}

export function decodeActivityGroup(value: JsonValue | undefined, at: string): ActivityGroup {
  const object = expectObject(value, at);
  const result: ActivityGroup = {
    month: expectString(object["month"], at + ".month"),
    subtotal_in: decodeMoney(object["subtotal_in"], at + ".subtotal_in"),
    subtotal_out: decodeMoney(object["subtotal_out"], at + ".subtotal_out"),
    entries: decodeArray(object["entries"], at + ".entries", decodeActivityEntry),
  };
  return result;
}

export function encodeActivityGroup(value: ActivityGroup): JsonValue {
  const result: JsonObject = {
    month: value.month,
    subtotal_in: encodeMoney(value.subtotal_in),
    subtotal_out: encodeMoney(value.subtotal_out),
    entries: value.entries.map(encodeActivityEntry),
  };
  return result;
}

export interface ActivityPage {
  groups: ActivityGroup[];
  next_cursor: Cursor;
  filter: ActivityFilter;
}

export function decodeActivityPage(value: JsonValue | undefined, at: string): ActivityPage {
  const object = expectObject(value, at);
  const result: ActivityPage = {
    groups: decodeArray(object["groups"], at + ".groups", decodeActivityGroup),
    next_cursor: expectString(object["next_cursor"], at + ".next_cursor"),
    filter: decodeActivityFilter(object["filter"], at + ".filter"),
  };
  return result;
}

export function encodeActivityPage(value: ActivityPage): JsonValue {
  const result: JsonObject = {
    groups: value.groups.map(encodeActivityGroup),
    next_cursor: value.next_cursor,
    filter: encodeActivityFilter(value.filter),
  };
  return result;
}

export interface ActivityQueryRequest {
  cursor?: Cursor;
  filter?: ActivityFilter;
  page_limit?: number;
}

export function decodeActivityQueryRequest(value: JsonValue | undefined, at: string): ActivityQueryRequest {
  const object = expectObject(value, at);
  const result: ActivityQueryRequest = {};
  if (object["cursor"] !== undefined) {
    result.cursor = expectString(object["cursor"], at + ".cursor");
  }
  if (object["filter"] !== undefined) {
    result.filter = decodeActivityFilter(object["filter"], at + ".filter");
  }
  if (object["page_limit"] !== undefined) {
    result.page_limit = expectInteger(object["page_limit"], at + ".page_limit");
  }
  return result;
}

export function encodeActivityQueryRequest(value: ActivityQueryRequest): JsonValue {
  const result: JsonObject = {};
  if (value.cursor !== undefined) {
    result["cursor"] = value.cursor;
  }
  if (value.filter !== undefined) {
    result["filter"] = encodeActivityFilter(value.filter);
  }
  if (value.page_limit !== undefined) {
    result["page_limit"] = value.page_limit;
  }
  return result;
}

export interface Agent {
  agent_id: AgentId;
  name: string;
  purpose: string;
  state: AgentState;
  state_copy_key: CopyKey;
  limit: SpendLimit;
  spend: AgentSpend;
  evidence: EvidenceRef[];
  created_at: Timestamp;
  updated_at: Timestamp;
  creation_journey_id?: JourneyId;
}

export function decodeAgent(value: JsonValue | undefined, at: string): Agent {
  const object = expectObject(value, at);
  const result: Agent = {
    agent_id: expectString(object["agent_id"], at + ".agent_id"),
    name: expectString(object["name"], at + ".name"),
    purpose: expectString(object["purpose"], at + ".purpose"),
    state: decodeAgentState(object["state"], at + ".state"),
    state_copy_key: expectString(object["state_copy_key"], at + ".state_copy_key"),
    limit: decodeSpendLimit(object["limit"], at + ".limit"),
    spend: decodeAgentSpend(object["spend"], at + ".spend"),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
    created_at: expectString(object["created_at"], at + ".created_at"),
    updated_at: expectString(object["updated_at"], at + ".updated_at"),
  };
  if (object["creation_journey_id"] !== undefined) {
    result.creation_journey_id = expectString(object["creation_journey_id"], at + ".creation_journey_id");
  }
  return result;
}

export function encodeAgent(value: Agent): JsonValue {
  const result: JsonObject = {
    agent_id: value.agent_id,
    name: value.name,
    purpose: value.purpose,
    state: value.state,
    state_copy_key: value.state_copy_key,
    limit: encodeSpendLimit(value.limit),
    spend: encodeAgentSpend(value.spend),
    evidence: value.evidence.map(encodeEvidenceRef),
    created_at: value.created_at,
    updated_at: value.updated_at,
  };
  if (value.creation_journey_id !== undefined) {
    result["creation_journey_id"] = value.creation_journey_id;
  }
  return result;
}

export interface AgentArchiveRequest {
  confirm_name: string;
}

export function decodeAgentArchiveRequest(value: JsonValue | undefined, at: string): AgentArchiveRequest {
  const object = expectObject(value, at);
  const result: AgentArchiveRequest = {
    confirm_name: expectString(object["confirm_name"], at + ".confirm_name"),
  };
  return result;
}

export function encodeAgentArchiveRequest(value: AgentArchiveRequest): JsonValue {
  const result: JsonObject = {
    confirm_name: value.confirm_name,
  };
  return result;
}

export interface AgentCreateRequest {
  name: string;
  purpose: string;
  monthly_limit: Money;
}

export function decodeAgentCreateRequest(value: JsonValue | undefined, at: string): AgentCreateRequest {
  const object = expectObject(value, at);
  const result: AgentCreateRequest = {
    name: expectString(object["name"], at + ".name"),
    purpose: expectString(object["purpose"], at + ".purpose"),
    monthly_limit: decodeMoney(object["monthly_limit"], at + ".monthly_limit"),
  };
  return result;
}

export function encodeAgentCreateRequest(value: AgentCreateRequest): JsonValue {
  const result: JsonObject = {
    name: value.name,
    purpose: value.purpose,
    monthly_limit: encodeMoney(value.monthly_limit),
  };
  return result;
}

export interface AgentLimitRequest {
  monthly_limit: Money;
}

export function decodeAgentLimitRequest(value: JsonValue | undefined, at: string): AgentLimitRequest {
  const object = expectObject(value, at);
  const result: AgentLimitRequest = {
    monthly_limit: decodeMoney(object["monthly_limit"], at + ".monthly_limit"),
  };
  return result;
}

export function encodeAgentLimitRequest(value: AgentLimitRequest): JsonValue {
  const result: JsonObject = {
    monthly_limit: encodeMoney(value.monthly_limit),
  };
  return result;
}

export interface AgentPage {
  agents: Agent[];
  next_cursor: Cursor;
}

export function decodeAgentPage(value: JsonValue | undefined, at: string): AgentPage {
  const object = expectObject(value, at);
  const result: AgentPage = {
    agents: decodeArray(object["agents"], at + ".agents", decodeAgent),
    next_cursor: expectString(object["next_cursor"], at + ".next_cursor"),
  };
  return result;
}

export function encodeAgentPage(value: AgentPage): JsonValue {
  const result: JsonObject = {
    agents: value.agents.map(encodeAgent),
    next_cursor: value.next_cursor,
  };
  return result;
}

export interface AgentReclaimRequest {
  money: Money;
}

export function decodeAgentReclaimRequest(value: JsonValue | undefined, at: string): AgentReclaimRequest {
  const object = expectObject(value, at);
  const result: AgentReclaimRequest = {
    money: decodeMoney(object["money"], at + ".money"),
  };
  return result;
}

export function encodeAgentReclaimRequest(value: AgentReclaimRequest): JsonValue {
  const result: JsonObject = {
    money: encodeMoney(value.money),
  };
  return result;
}

export interface AgentSpend {
  period_start: Timestamp;
  period_end: Timestamp;
  spent: Money;
  remaining: Money;
  verification: VerificationLevel;
  reconciliation_copy_key?: CopyKey;
}

export function decodeAgentSpend(value: JsonValue | undefined, at: string): AgentSpend {
  const object = expectObject(value, at);
  const result: AgentSpend = {
    period_start: expectString(object["period_start"], at + ".period_start"),
    period_end: expectString(object["period_end"], at + ".period_end"),
    spent: decodeMoney(object["spent"], at + ".spent"),
    remaining: decodeMoney(object["remaining"], at + ".remaining"),
    verification: decodeVerificationLevel(object["verification"], at + ".verification"),
  };
  if (object["reconciliation_copy_key"] !== undefined) {
    result.reconciliation_copy_key = expectString(object["reconciliation_copy_key"], at + ".reconciliation_copy_key");
  }
  return result;
}

export function encodeAgentSpend(value: AgentSpend): JsonValue {
  const result: JsonObject = {
    period_start: value.period_start,
    period_end: value.period_end,
    spent: encodeMoney(value.spent),
    remaining: encodeMoney(value.remaining),
    verification: value.verification,
  };
  if (value.reconciliation_copy_key !== undefined) {
    result["reconciliation_copy_key"] = value.reconciliation_copy_key;
  }
  return result;
}

export interface ApiError {
  code: ErrorCode;
  copy_key: CopyKey;
  retry: Retriability;
  retry_after_ms?: number;
  field?: string;
}

export function decodeApiError(value: JsonValue | undefined, at: string): ApiError {
  const object = expectObject(value, at);
  const result: ApiError = {
    code: decodeErrorCode(object["code"], at + ".code"),
    copy_key: expectString(object["copy_key"], at + ".copy_key"),
    retry: decodeRetriability(object["retry"], at + ".retry"),
  };
  if (object["retry_after_ms"] !== undefined) {
    result.retry_after_ms = expectInteger(object["retry_after_ms"], at + ".retry_after_ms");
  }
  if (object["field"] !== undefined) {
    result.field = expectString(object["field"], at + ".field");
  }
  return result;
}

export function encodeApiError(value: ApiError): JsonValue {
  const result: JsonObject = {
    code: value.code,
    copy_key: value.copy_key,
    retry: value.retry,
  };
  if (value.retry_after_ms !== undefined) {
    result["retry_after_ms"] = value.retry_after_ms;
  }
  if (value.field !== undefined) {
    result["field"] = value.field;
  }
  return result;
}

export interface ApprovalApproveRequest {
  step_up_evidence: EvidenceId;
}

export function decodeApprovalApproveRequest(value: JsonValue | undefined, at: string): ApprovalApproveRequest {
  const object = expectObject(value, at);
  const result: ApprovalApproveRequest = {
    step_up_evidence: expectString(object["step_up_evidence"], at + ".step_up_evidence"),
  };
  return result;
}

export function encodeApprovalApproveRequest(value: ApprovalApproveRequest): JsonValue {
  const result: JsonObject = {
    step_up_evidence: value.step_up_evidence,
  };
  return result;
}

export interface ApprovalDecision {
  approval_id: ApprovalId;
  state: ApprovalState;
  state_copy_key: CopyKey;
  money_moved: boolean;
  moved_copy_key: CopyKey;
  evidence: EvidenceRef[];
}

export function decodeApprovalDecision(value: JsonValue | undefined, at: string): ApprovalDecision {
  const object = expectObject(value, at);
  const result: ApprovalDecision = {
    approval_id: expectString(object["approval_id"], at + ".approval_id"),
    state: decodeApprovalState(object["state"], at + ".state"),
    state_copy_key: expectString(object["state_copy_key"], at + ".state_copy_key"),
    money_moved: expectBoolean(object["money_moved"], at + ".money_moved"),
    moved_copy_key: expectString(object["moved_copy_key"], at + ".moved_copy_key"),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
  };
  return result;
}

export function encodeApprovalDecision(value: ApprovalDecision): JsonValue {
  const result: JsonObject = {
    approval_id: value.approval_id,
    state: value.state,
    state_copy_key: value.state_copy_key,
    money_moved: value.money_moved,
    moved_copy_key: value.moved_copy_key,
    evidence: value.evidence.map(encodeEvidenceRef),
  };
  return result;
}

export interface ApprovalDetail {
  approval_id: ApprovalId;
  agent_id: AgentId;
  agent_name: string;
  state: ApprovalState;
  state_copy_key: CopyKey;
  reason_copy_key: CopyKey;
  facts: ApprovalFacts;
  budget_remaining_after: VerifiedMoney;
  created_at: Timestamp;
  evidence: EvidenceRef[];
}

export function decodeApprovalDetail(value: JsonValue | undefined, at: string): ApprovalDetail {
  const object = expectObject(value, at);
  const result: ApprovalDetail = {
    approval_id: expectString(object["approval_id"], at + ".approval_id"),
    agent_id: expectString(object["agent_id"], at + ".agent_id"),
    agent_name: expectString(object["agent_name"], at + ".agent_name"),
    state: decodeApprovalState(object["state"], at + ".state"),
    state_copy_key: expectString(object["state_copy_key"], at + ".state_copy_key"),
    reason_copy_key: expectString(object["reason_copy_key"], at + ".reason_copy_key"),
    facts: decodeApprovalFacts(object["facts"], at + ".facts"),
    budget_remaining_after: decodeVerifiedMoney(object["budget_remaining_after"], at + ".budget_remaining_after"),
    created_at: expectString(object["created_at"], at + ".created_at"),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
  };
  return result;
}

export function encodeApprovalDetail(value: ApprovalDetail): JsonValue {
  const result: JsonObject = {
    approval_id: value.approval_id,
    agent_id: value.agent_id,
    agent_name: value.agent_name,
    state: value.state,
    state_copy_key: value.state_copy_key,
    reason_copy_key: value.reason_copy_key,
    facts: encodeApprovalFacts(value.facts),
    budget_remaining_after: encodeVerifiedMoney(value.budget_remaining_after),
    created_at: value.created_at,
    evidence: value.evidence.map(encodeEvidenceRef),
  };
  return result;
}

export interface ApprovalFacts {
  amount: Money;
  counterparty: string;
  asset: CurrencyCode;
  fees: Money;
  expires_at: Timestamp;
}

export function decodeApprovalFacts(value: JsonValue | undefined, at: string): ApprovalFacts {
  const object = expectObject(value, at);
  const result: ApprovalFacts = {
    amount: decodeMoney(object["amount"], at + ".amount"),
    counterparty: expectString(object["counterparty"], at + ".counterparty"),
    asset: expectString(object["asset"], at + ".asset"),
    fees: decodeMoney(object["fees"], at + ".fees"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodeApprovalFacts(value: ApprovalFacts): JsonValue {
  const result: JsonObject = {
    amount: encodeMoney(value.amount),
    counterparty: value.counterparty,
    asset: value.asset,
    fees: encodeMoney(value.fees),
    expires_at: value.expires_at,
  };
  return result;
}

export interface ApprovalPage {
  approvals: ApprovalSummary[];
  next_cursor: Cursor;
}

export function decodeApprovalPage(value: JsonValue | undefined, at: string): ApprovalPage {
  const object = expectObject(value, at);
  const result: ApprovalPage = {
    approvals: decodeArray(object["approvals"], at + ".approvals", decodeApprovalSummary),
    next_cursor: expectString(object["next_cursor"], at + ".next_cursor"),
  };
  return result;
}

export function encodeApprovalPage(value: ApprovalPage): JsonValue {
  const result: JsonObject = {
    approvals: value.approvals.map(encodeApprovalSummary),
    next_cursor: value.next_cursor,
  };
  return result;
}

export interface ApprovalSummary {
  approval_id: ApprovalId;
  agent_id: AgentId;
  agent_name: string;
  counterparty: string;
  amount: Money;
  reason_copy_key: CopyKey;
  expires_at: Timestamp;
  state: ApprovalState;
  budget_remaining_after: VerifiedMoney;
}

export function decodeApprovalSummary(value: JsonValue | undefined, at: string): ApprovalSummary {
  const object = expectObject(value, at);
  const result: ApprovalSummary = {
    approval_id: expectString(object["approval_id"], at + ".approval_id"),
    agent_id: expectString(object["agent_id"], at + ".agent_id"),
    agent_name: expectString(object["agent_name"], at + ".agent_name"),
    counterparty: expectString(object["counterparty"], at + ".counterparty"),
    amount: decodeMoney(object["amount"], at + ".amount"),
    reason_copy_key: expectString(object["reason_copy_key"], at + ".reason_copy_key"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
    state: decodeApprovalState(object["state"], at + ".state"),
    budget_remaining_after: decodeVerifiedMoney(object["budget_remaining_after"], at + ".budget_remaining_after"),
  };
  return result;
}

export function encodeApprovalSummary(value: ApprovalSummary): JsonValue {
  const result: JsonObject = {
    approval_id: value.approval_id,
    agent_id: value.agent_id,
    agent_name: value.agent_name,
    counterparty: value.counterparty,
    amount: encodeMoney(value.amount),
    reason_copy_key: value.reason_copy_key,
    expires_at: value.expires_at,
    state: value.state,
    budget_remaining_after: encodeVerifiedMoney(value.budget_remaining_after),
  };
  return result;
}

export interface BindingStatement {
  statement: string;
  address: EvmAddress;
  expires_at: Timestamp;
}

export function decodeBindingStatement(value: JsonValue | undefined, at: string): BindingStatement {
  const object = expectObject(value, at);
  const result: BindingStatement = {
    statement: expectString(object["statement"], at + ".statement"),
    address: expectString(object["address"], at + ".address"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodeBindingStatement(value: BindingStatement): JsonValue {
  const result: JsonObject = {
    statement: value.statement,
    address: value.address,
    expires_at: value.expires_at,
  };
  return result;
}

export interface BindingStatementRequest {
  address: EvmAddress;
}

export function decodeBindingStatementRequest(value: JsonValue | undefined, at: string): BindingStatementRequest {
  const object = expectObject(value, at);
  const result: BindingStatementRequest = {
    address: expectString(object["address"], at + ".address"),
  };
  return result;
}

export function encodeBindingStatementRequest(value: BindingStatementRequest): JsonValue {
  const result: JsonObject = {
    address: value.address,
  };
  return result;
}

export interface BindingSubmission {
  address: EvmAddress;
  statement: string;
  signature: EvmSignature;
}

export function decodeBindingSubmission(value: JsonValue | undefined, at: string): BindingSubmission {
  const object = expectObject(value, at);
  const result: BindingSubmission = {
    address: expectString(object["address"], at + ".address"),
    statement: expectString(object["statement"], at + ".statement"),
    signature: expectString(object["signature"], at + ".signature"),
  };
  return result;
}

export function encodeBindingSubmission(value: BindingSubmission): JsonValue {
  const result: JsonObject = {
    address: value.address,
    statement: value.statement,
    signature: value.signature,
  };
  return result;
}

export interface ChannelPreference {
  enabled: boolean;
  classes: ClassToggle[];
}

export function decodeChannelPreference(value: JsonValue | undefined, at: string): ChannelPreference {
  const object = expectObject(value, at);
  const result: ChannelPreference = {
    enabled: expectBoolean(object["enabled"], at + ".enabled"),
    classes: decodeArray(object["classes"], at + ".classes", decodeClassToggle),
  };
  return result;
}

export function encodeChannelPreference(value: ChannelPreference): JsonValue {
  const result: JsonObject = {
    enabled: value.enabled,
    classes: value.classes.map(encodeClassToggle),
  };
  return result;
}

export interface ClassToggle {
  class: NotificationClass;
  enabled: boolean;
}

export function decodeClassToggle(value: JsonValue | undefined, at: string): ClassToggle {
  const object = expectObject(value, at);
  const result: ClassToggle = {
    class: decodeNotificationClass(object["class"], at + ".class"),
    enabled: expectBoolean(object["enabled"], at + ".enabled"),
  };
  return result;
}

export function encodeClassToggle(value: ClassToggle): JsonValue {
  const result: JsonObject = {
    class: value.class,
    enabled: value.enabled,
  };
  return result;
}

export interface DepositConfirmRequest {
  wallet_transaction: WalletTxId;
}

export function decodeDepositConfirmRequest(value: JsonValue | undefined, at: string): DepositConfirmRequest {
  const object = expectObject(value, at);
  const result: DepositConfirmRequest = {
    wallet_transaction: expectString(object["wallet_transaction"], at + ".wallet_transaction"),
  };
  return result;
}

export function encodeDepositConfirmRequest(value: DepositConfirmRequest): JsonValue {
  const result: JsonObject = {
    wallet_transaction: value.wallet_transaction,
  };
  return result;
}

export interface DepositStartRequest {
  money: Money;
}

export function decodeDepositStartRequest(value: JsonValue | undefined, at: string): DepositStartRequest {
  const object = expectObject(value, at);
  const result: DepositStartRequest = {
    money: decodeMoney(object["money"], at + ".money"),
  };
  return result;
}

export function encodeDepositStartRequest(value: DepositStartRequest): JsonValue {
  const result: JsonObject = {
    money: encodeMoney(value.money),
  };
  return result;
}

export interface Device {
  device_id: DeviceId;
  label: string;
  platform: string;
}

export function decodeDevice(value: JsonValue | undefined, at: string): Device {
  const object = expectObject(value, at);
  const result: Device = {
    device_id: expectString(object["device_id"], at + ".device_id"),
    label: expectString(object["label"], at + ".label"),
    platform: expectString(object["platform"], at + ".platform"),
  };
  return result;
}

export function encodeDevice(value: Device): JsonValue {
  const result: JsonObject = {
    device_id: value.device_id,
    label: value.label,
    platform: value.platform,
  };
  return result;
}

export type Empty = Record<string, never>;

export function decodeEmpty(value: JsonValue | undefined, at: string): Empty {
  expectObject(value, at);
  return {};
}

export function encodeEmpty(value: Empty): JsonValue {
  return { ...value };
}

export interface EvidenceMaterial {
  evidence_id: EvidenceId;
  class: EvidenceClass;
  verification: VerificationLevel;
  content_type: string;
  bytes_base64: string;
}

export function decodeEvidenceMaterial(value: JsonValue | undefined, at: string): EvidenceMaterial {
  const object = expectObject(value, at);
  const result: EvidenceMaterial = {
    evidence_id: expectString(object["evidence_id"], at + ".evidence_id"),
    class: decodeEvidenceClass(object["class"], at + ".class"),
    verification: decodeVerificationLevel(object["verification"], at + ".verification"),
    content_type: expectString(object["content_type"], at + ".content_type"),
    bytes_base64: expectString(object["bytes_base64"], at + ".bytes_base64"),
  };
  return result;
}

export function encodeEvidenceMaterial(value: EvidenceMaterial): JsonValue {
  const result: JsonObject = {
    evidence_id: value.evidence_id,
    class: value.class,
    verification: value.verification,
    content_type: value.content_type,
    bytes_base64: value.bytes_base64,
  };
  return result;
}

export interface EvidenceRef {
  evidence_id: EvidenceId;
  class: EvidenceClass;
  verification: VerificationLevel;
}

export function decodeEvidenceRef(value: JsonValue | undefined, at: string): EvidenceRef {
  const object = expectObject(value, at);
  const result: EvidenceRef = {
    evidence_id: expectString(object["evidence_id"], at + ".evidence_id"),
    class: decodeEvidenceClass(object["class"], at + ".class"),
    verification: decodeVerificationLevel(object["verification"], at + ".verification"),
  };
  return result;
}

export function encodeEvidenceRef(value: EvidenceRef): JsonValue {
  const result: JsonObject = {
    evidence_id: value.evidence_id,
    class: value.class,
    verification: value.verification,
  };
  return result;
}

export interface ExitEligibility {
  eligible: boolean;
  copy_key: CopyKey;
  withdraw_instead_path?: string;
}

export function decodeExitEligibility(value: JsonValue | undefined, at: string): ExitEligibility {
  const object = expectObject(value, at);
  const result: ExitEligibility = {
    eligible: expectBoolean(object["eligible"], at + ".eligible"),
    copy_key: expectString(object["copy_key"], at + ".copy_key"),
  };
  if (object["withdraw_instead_path"] !== undefined) {
    result.withdraw_instead_path = expectString(object["withdraw_instead_path"], at + ".withdraw_instead_path");
  }
  return result;
}

export function encodeExitEligibility(value: ExitEligibility): JsonValue {
  const result: JsonObject = {
    eligible: value.eligible,
    copy_key: value.copy_key,
  };
  if (value.withdraw_instead_path !== undefined) {
    result["withdraw_instead_path"] = value.withdraw_instead_path;
  }
  return result;
}

export interface ExitStartRequest {
  confirmation: string;
}

export function decodeExitStartRequest(value: JsonValue | undefined, at: string): ExitStartRequest {
  const object = expectObject(value, at);
  const result: ExitStartRequest = {
    confirmation: expectString(object["confirmation"], at + ".confirmation"),
  };
  return result;
}

export function encodeExitStartRequest(value: ExitStartRequest): JsonValue {
  const result: JsonObject = {
    confirmation: value.confirmation,
  };
  return result;
}

export interface ExportArtefact {
  export_id: ExportId;
  kind: ExportKind;
  download_path: string;
  content_type: string;
  created_at: Timestamp;
  evidence: EvidenceRef[];
}

export function decodeExportArtefact(value: JsonValue | undefined, at: string): ExportArtefact {
  const object = expectObject(value, at);
  const result: ExportArtefact = {
    export_id: expectString(object["export_id"], at + ".export_id"),
    kind: decodeExportKind(object["kind"], at + ".kind"),
    download_path: expectString(object["download_path"], at + ".download_path"),
    content_type: expectString(object["content_type"], at + ".content_type"),
    created_at: expectString(object["created_at"], at + ".created_at"),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
  };
  return result;
}

export function encodeExportArtefact(value: ExportArtefact): JsonValue {
  const result: JsonObject = {
    export_id: value.export_id,
    kind: value.kind,
    download_path: value.download_path,
    content_type: value.content_type,
    created_at: value.created_at,
    evidence: value.evidence.map(encodeEvidenceRef),
  };
  return result;
}

export interface ExportEvidenceRequest {
  filter?: ActivityFilter;
  entry_ids?: ActivityEntryId[];
}

export function decodeExportEvidenceRequest(value: JsonValue | undefined, at: string): ExportEvidenceRequest {
  const object = expectObject(value, at);
  const result: ExportEvidenceRequest = {};
  if (object["filter"] !== undefined) {
    result.filter = decodeActivityFilter(object["filter"], at + ".filter");
  }
  if (object["entry_ids"] !== undefined) {
    result.entry_ids = decodeArray(object["entry_ids"], at + ".entry_ids", expectString);
  }
  return result;
}

export function encodeExportEvidenceRequest(value: ExportEvidenceRequest): JsonValue {
  const result: JsonObject = {};
  if (value.filter !== undefined) {
    result["filter"] = encodeActivityFilter(value.filter);
  }
  if (value.entry_ids !== undefined) {
    result["entry_ids"] = value.entry_ids;
  }
  return result;
}

export interface ExportStatementRequest {
  filter?: ActivityFilter;
}

export function decodeExportStatementRequest(value: JsonValue | undefined, at: string): ExportStatementRequest {
  const object = expectObject(value, at);
  const result: ExportStatementRequest = {};
  if (object["filter"] !== undefined) {
    result.filter = decodeActivityFilter(object["filter"], at + ".filter");
  }
  return result;
}

export function encodeExportStatementRequest(value: ExportStatementRequest): JsonValue {
  const result: JsonObject = {};
  if (value.filter !== undefined) {
    result["filter"] = encodeActivityFilter(value.filter);
  }
  return result;
}

export interface Journey {
  journey_id: JourneyId;
  kind: JourneyKind;
  state: JourneyState;
  state_copy_key: CopyKey;
  stages: JourneyStage[];
  evidence: EvidenceRef[];
  started_at: Timestamp;
  updated_at: Timestamp;
  refusal?: Refusal;
  wallet_request?: WalletSignRequest;
}

export function decodeJourney(value: JsonValue | undefined, at: string): Journey {
  const object = expectObject(value, at);
  const result: Journey = {
    journey_id: expectString(object["journey_id"], at + ".journey_id"),
    kind: decodeJourneyKind(object["kind"], at + ".kind"),
    state: decodeJourneyState(object["state"], at + ".state"),
    state_copy_key: expectString(object["state_copy_key"], at + ".state_copy_key"),
    stages: decodeArray(object["stages"], at + ".stages", decodeJourneyStage),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
    started_at: expectString(object["started_at"], at + ".started_at"),
    updated_at: expectString(object["updated_at"], at + ".updated_at"),
  };
  if (object["refusal"] !== undefined) {
    result.refusal = decodeRefusal(object["refusal"], at + ".refusal");
  }
  if (object["wallet_request"] !== undefined) {
    result.wallet_request = decodeWalletSignRequest(object["wallet_request"], at + ".wallet_request");
  }
  return result;
}

export function encodeJourney(value: Journey): JsonValue {
  const result: JsonObject = {
    journey_id: value.journey_id,
    kind: value.kind,
    state: value.state,
    state_copy_key: value.state_copy_key,
    stages: value.stages.map(encodeJourneyStage),
    evidence: value.evidence.map(encodeEvidenceRef),
    started_at: value.started_at,
    updated_at: value.updated_at,
  };
  if (value.refusal !== undefined) {
    result["refusal"] = encodeRefusal(value.refusal);
  }
  if (value.wallet_request !== undefined) {
    result["wallet_request"] = encodeWalletSignRequest(value.wallet_request);
  }
  return result;
}

export interface JourneyPage {
  journeys: Journey[];
  next_cursor: Cursor;
}

export function decodeJourneyPage(value: JsonValue | undefined, at: string): JourneyPage {
  const object = expectObject(value, at);
  const result: JourneyPage = {
    journeys: decodeArray(object["journeys"], at + ".journeys", decodeJourney),
    next_cursor: expectString(object["next_cursor"], at + ".next_cursor"),
  };
  return result;
}

export function encodeJourneyPage(value: JourneyPage): JsonValue {
  const result: JsonObject = {
    journeys: value.journeys.map(encodeJourney),
    next_cursor: value.next_cursor,
  };
  return result;
}

export interface JourneyStage {
  stage_id: StageId;
  copy_key: CopyKey;
  state: JourneyState;
  evidence: EvidenceRef[];
}

export function decodeJourneyStage(value: JsonValue | undefined, at: string): JourneyStage {
  const object = expectObject(value, at);
  const result: JourneyStage = {
    stage_id: expectString(object["stage_id"], at + ".stage_id"),
    copy_key: expectString(object["copy_key"], at + ".copy_key"),
    state: decodeJourneyState(object["state"], at + ".state"),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
  };
  return result;
}

export function encodeJourneyStage(value: JourneyStage): JsonValue {
  const result: JsonObject = {
    stage_id: value.stage_id,
    copy_key: value.copy_key,
    state: value.state,
    evidence: value.evidence.map(encodeEvidenceRef),
  };
  return result;
}

export interface KeyChallenge {
  agent_id: AgentId;
  kind: KeyChallengeKind;
  delay_copy_key: CopyKey;
  delay_seconds: number;
  ready_at: Timestamp;
  evidence: EvidenceRef[];
}

export function decodeKeyChallenge(value: JsonValue | undefined, at: string): KeyChallenge {
  const object = expectObject(value, at);
  const result: KeyChallenge = {
    agent_id: expectString(object["agent_id"], at + ".agent_id"),
    kind: decodeKeyChallengeKind(object["kind"], at + ".kind"),
    delay_copy_key: expectString(object["delay_copy_key"], at + ".delay_copy_key"),
    delay_seconds: expectInteger(object["delay_seconds"], at + ".delay_seconds"),
    ready_at: expectString(object["ready_at"], at + ".ready_at"),
    evidence: decodeArray(object["evidence"], at + ".evidence", decodeEvidenceRef),
  };
  return result;
}

export function encodeKeyChallenge(value: KeyChallenge): JsonValue {
  const result: JsonObject = {
    agent_id: value.agent_id,
    kind: value.kind,
    delay_copy_key: value.delay_copy_key,
    delay_seconds: value.delay_seconds,
    ready_at: value.ready_at,
    evidence: value.evidence.map(encodeEvidenceRef),
  };
  return result;
}

export interface Money {
  amount: Amount;
  currency: CurrencyCode;
}

export function decodeMoney(value: JsonValue | undefined, at: string): Money {
  const object = expectObject(value, at);
  const result: Money = {
    amount: decodeConsensusInteger(object["amount"], at + ".amount"),
    currency: expectString(object["currency"], at + ".currency"),
  };
  return result;
}

export function encodeMoney(value: Money): JsonValue {
  const result: JsonObject = {
    amount: encodeConsensusInteger(value.amount),
    currency: value.currency,
  };
  return result;
}

export interface MoveCommitRequest {
  quote_id: QuoteId;
}

export function decodeMoveCommitRequest(value: JsonValue | undefined, at: string): MoveCommitRequest {
  const object = expectObject(value, at);
  const result: MoveCommitRequest = {
    quote_id: expectString(object["quote_id"], at + ".quote_id"),
  };
  return result;
}

export function encodeMoveCommitRequest(value: MoveCommitRequest): JsonValue {
  const result: JsonObject = {
    quote_id: value.quote_id,
  };
  return result;
}

export interface MoveQuote {
  quote_id: QuoteId;
  description_copy_key: CopyKey;
  mechanism: MoveMechanism;
  money: Money;
  fee_estimate: Money;
  fee_ceiling: Money;
  arrival_estimate: Timestamp;
  expires_at: Timestamp;
  irreversibility_copy_key?: CopyKey;
}

export function decodeMoveQuote(value: JsonValue | undefined, at: string): MoveQuote {
  const object = expectObject(value, at);
  const result: MoveQuote = {
    quote_id: expectString(object["quote_id"], at + ".quote_id"),
    description_copy_key: expectString(object["description_copy_key"], at + ".description_copy_key"),
    mechanism: decodeMoveMechanism(object["mechanism"], at + ".mechanism"),
    money: decodeMoney(object["money"], at + ".money"),
    fee_estimate: decodeMoney(object["fee_estimate"], at + ".fee_estimate"),
    fee_ceiling: decodeMoney(object["fee_ceiling"], at + ".fee_ceiling"),
    arrival_estimate: expectString(object["arrival_estimate"], at + ".arrival_estimate"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  if (object["irreversibility_copy_key"] !== undefined) {
    result.irreversibility_copy_key = expectString(object["irreversibility_copy_key"], at + ".irreversibility_copy_key");
  }
  return result;
}

export function encodeMoveQuote(value: MoveQuote): JsonValue {
  const result: JsonObject = {
    quote_id: value.quote_id,
    description_copy_key: value.description_copy_key,
    mechanism: value.mechanism,
    money: encodeMoney(value.money),
    fee_estimate: encodeMoney(value.fee_estimate),
    fee_ceiling: encodeMoney(value.fee_ceiling),
    arrival_estimate: value.arrival_estimate,
    expires_at: value.expires_at,
  };
  if (value.irreversibility_copy_key !== undefined) {
    result["irreversibility_copy_key"] = value.irreversibility_copy_key;
  }
  return result;
}

export interface MoveQuoteRequest {
  source: string;
  destination: string;
  money: Money;
}

export function decodeMoveQuoteRequest(value: JsonValue | undefined, at: string): MoveQuoteRequest {
  const object = expectObject(value, at);
  const result: MoveQuoteRequest = {
    source: expectString(object["source"], at + ".source"),
    destination: expectString(object["destination"], at + ".destination"),
    money: decodeMoney(object["money"], at + ".money"),
  };
  return result;
}

export function encodeMoveQuoteRequest(value: MoveQuoteRequest): JsonValue {
  const result: JsonObject = {
    source: value.source,
    destination: value.destination,
    money: encodeMoney(value.money),
  };
  return result;
}

export interface NotificationGroup {
  recency: NotificationRecency;
  notifications: NotificationSummary[];
}

export function decodeNotificationGroup(value: JsonValue | undefined, at: string): NotificationGroup {
  const object = expectObject(value, at);
  const result: NotificationGroup = {
    recency: decodeNotificationRecency(object["recency"], at + ".recency"),
    notifications: decodeArray(object["notifications"], at + ".notifications", decodeNotificationSummary),
  };
  return result;
}

export function encodeNotificationGroup(value: NotificationGroup): JsonValue {
  const result: JsonObject = {
    recency: value.recency,
    notifications: value.notifications.map(encodeNotificationSummary),
  };
  return result;
}

export interface NotificationPage {
  groups: NotificationGroup[];
  next_cursor: Cursor;
  unread_count: number;
}

export function decodeNotificationPage(value: JsonValue | undefined, at: string): NotificationPage {
  const object = expectObject(value, at);
  const result: NotificationPage = {
    groups: decodeArray(object["groups"], at + ".groups", decodeNotificationGroup),
    next_cursor: expectString(object["next_cursor"], at + ".next_cursor"),
    unread_count: expectInteger(object["unread_count"], at + ".unread_count"),
  };
  return result;
}

export function encodeNotificationPage(value: NotificationPage): JsonValue {
  const result: JsonObject = {
    groups: value.groups.map(encodeNotificationGroup),
    next_cursor: value.next_cursor,
    unread_count: value.unread_count,
  };
  return result;
}

export interface NotificationPreferences {
  push: ChannelPreference;
  email: ChannelPreference;
  in_app: ChannelPreference;
  detail: NotificationDetailLevel;
}

export function decodeNotificationPreferences(value: JsonValue | undefined, at: string): NotificationPreferences {
  const object = expectObject(value, at);
  const result: NotificationPreferences = {
    push: decodeChannelPreference(object["push"], at + ".push"),
    email: decodeChannelPreference(object["email"], at + ".email"),
    in_app: decodeChannelPreference(object["in_app"], at + ".in_app"),
    detail: decodeNotificationDetailLevel(object["detail"], at + ".detail"),
  };
  return result;
}

export function encodeNotificationPreferences(value: NotificationPreferences): JsonValue {
  const result: JsonObject = {
    push: encodeChannelPreference(value.push),
    email: encodeChannelPreference(value.email),
    in_app: encodeChannelPreference(value.in_app),
    detail: value.detail,
  };
  return result;
}

export interface NotificationSummary {
  notification_id: NotificationId;
  class: NotificationClass;
  title_copy_key: CopyKey;
  body_copy_key: CopyKey;
  deep_link: string;
  read: boolean;
  created_at: Timestamp;
  money?: Money;
  agent_id?: AgentId;
  approval_id?: ApprovalId;
  journey_id?: JourneyId;
  action_copy_key?: CopyKey;
}

export function decodeNotificationSummary(value: JsonValue | undefined, at: string): NotificationSummary {
  const object = expectObject(value, at);
  const result: NotificationSummary = {
    notification_id: expectString(object["notification_id"], at + ".notification_id"),
    class: decodeNotificationClass(object["class"], at + ".class"),
    title_copy_key: expectString(object["title_copy_key"], at + ".title_copy_key"),
    body_copy_key: expectString(object["body_copy_key"], at + ".body_copy_key"),
    deep_link: expectString(object["deep_link"], at + ".deep_link"),
    read: expectBoolean(object["read"], at + ".read"),
    created_at: expectString(object["created_at"], at + ".created_at"),
  };
  if (object["money"] !== undefined) {
    result.money = decodeMoney(object["money"], at + ".money");
  }
  if (object["agent_id"] !== undefined) {
    result.agent_id = expectString(object["agent_id"], at + ".agent_id");
  }
  if (object["approval_id"] !== undefined) {
    result.approval_id = expectString(object["approval_id"], at + ".approval_id");
  }
  if (object["journey_id"] !== undefined) {
    result.journey_id = expectString(object["journey_id"], at + ".journey_id");
  }
  if (object["action_copy_key"] !== undefined) {
    result.action_copy_key = expectString(object["action_copy_key"], at + ".action_copy_key");
  }
  return result;
}

export function encodeNotificationSummary(value: NotificationSummary): JsonValue {
  const result: JsonObject = {
    notification_id: value.notification_id,
    class: value.class,
    title_copy_key: value.title_copy_key,
    body_copy_key: value.body_copy_key,
    deep_link: value.deep_link,
    read: value.read,
    created_at: value.created_at,
  };
  if (value.money !== undefined) {
    result["money"] = encodeMoney(value.money);
  }
  if (value.agent_id !== undefined) {
    result["agent_id"] = value.agent_id;
  }
  if (value.approval_id !== undefined) {
    result["approval_id"] = value.approval_id;
  }
  if (value.journey_id !== undefined) {
    result["journey_id"] = value.journey_id;
  }
  if (value.action_copy_key !== undefined) {
    result["action_copy_key"] = value.action_copy_key;
  }
  return result;
}

export interface Passkey {
  passkey_id: PasskeyId;
  label: string;
  created_at: Timestamp;
  last_used_at?: Timestamp;
}

export function decodePasskey(value: JsonValue | undefined, at: string): Passkey {
  const object = expectObject(value, at);
  const result: Passkey = {
    passkey_id: expectString(object["passkey_id"], at + ".passkey_id"),
    label: expectString(object["label"], at + ".label"),
    created_at: expectString(object["created_at"], at + ".created_at"),
  };
  if (object["last_used_at"] !== undefined) {
    result.last_used_at = expectString(object["last_used_at"], at + ".last_used_at");
  }
  return result;
}

export function encodePasskey(value: Passkey): JsonValue {
  const result: JsonObject = {
    passkey_id: value.passkey_id,
    label: value.label,
    created_at: value.created_at,
  };
  if (value.last_used_at !== undefined) {
    result["last_used_at"] = value.last_used_at;
  }
  return result;
}

export interface PasskeyAssertion {
  assertion_id: AssertionId;
  passkey_id: PasskeyId;
  completed_at: Timestamp;
  expires_at: Timestamp;
}

export function decodePasskeyAssertion(value: JsonValue | undefined, at: string): PasskeyAssertion {
  const object = expectObject(value, at);
  const result: PasskeyAssertion = {
    assertion_id: expectString(object["assertion_id"], at + ".assertion_id"),
    passkey_id: expectString(object["passkey_id"], at + ".passkey_id"),
    completed_at: expectString(object["completed_at"], at + ".completed_at"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodePasskeyAssertion(value: PasskeyAssertion): JsonValue {
  const result: JsonObject = {
    assertion_id: value.assertion_id,
    passkey_id: value.passkey_id,
    completed_at: value.completed_at,
    expires_at: value.expires_at,
  };
  return result;
}

export interface PasskeyAssertionBegin {
  email?: EmailAddress;
}

export function decodePasskeyAssertionBegin(value: JsonValue | undefined, at: string): PasskeyAssertionBegin {
  const object = expectObject(value, at);
  const result: PasskeyAssertionBegin = {};
  if (object["email"] !== undefined) {
    result.email = expectString(object["email"], at + ".email");
  }
  return result;
}

export function encodePasskeyAssertionBegin(value: PasskeyAssertionBegin): JsonValue {
  const result: JsonObject = {};
  if (value.email !== undefined) {
    result["email"] = value.email;
  }
  return result;
}

export interface PasskeyAssertionChallenge {
  assertion_id: AssertionId;
  ceremony: OpaqueCredential;
  expires_at: Timestamp;
}

export function decodePasskeyAssertionChallenge(value: JsonValue | undefined, at: string): PasskeyAssertionChallenge {
  const object = expectObject(value, at);
  const result: PasskeyAssertionChallenge = {
    assertion_id: expectString(object["assertion_id"], at + ".assertion_id"),
    ceremony: expectString(object["ceremony"], at + ".ceremony"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodePasskeyAssertionChallenge(value: PasskeyAssertionChallenge): JsonValue {
  const result: JsonObject = {
    assertion_id: value.assertion_id,
    ceremony: value.ceremony,
    expires_at: value.expires_at,
  };
  return result;
}

export interface PasskeyAssertionFinish {
  credential: OpaqueCredential;
}

export function decodePasskeyAssertionFinish(value: JsonValue | undefined, at: string): PasskeyAssertionFinish {
  const object = expectObject(value, at);
  const result: PasskeyAssertionFinish = {
    credential: expectString(object["credential"], at + ".credential"),
  };
  return result;
}

export function encodePasskeyAssertionFinish(value: PasskeyAssertionFinish): JsonValue {
  const result: JsonObject = {
    credential: value.credential,
  };
  return result;
}

export interface PasskeyRegistrationBegin {
  account_id: AccountId;
}

export function decodePasskeyRegistrationBegin(value: JsonValue | undefined, at: string): PasskeyRegistrationBegin {
  const object = expectObject(value, at);
  const result: PasskeyRegistrationBegin = {
    account_id: expectString(object["account_id"], at + ".account_id"),
  };
  return result;
}

export function encodePasskeyRegistrationBegin(value: PasskeyRegistrationBegin): JsonValue {
  const result: JsonObject = {
    account_id: value.account_id,
  };
  return result;
}

export interface PasskeyRegistrationChallenge {
  registration_id: RegistrationId;
  ceremony: OpaqueCredential;
  expires_at: Timestamp;
}

export function decodePasskeyRegistrationChallenge(value: JsonValue | undefined, at: string): PasskeyRegistrationChallenge {
  const object = expectObject(value, at);
  const result: PasskeyRegistrationChallenge = {
    registration_id: expectString(object["registration_id"], at + ".registration_id"),
    ceremony: expectString(object["ceremony"], at + ".ceremony"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodePasskeyRegistrationChallenge(value: PasskeyRegistrationChallenge): JsonValue {
  const result: JsonObject = {
    registration_id: value.registration_id,
    ceremony: value.ceremony,
    expires_at: value.expires_at,
  };
  return result;
}

export interface PasskeyRegistrationFinish {
  credential: OpaqueCredential;
}

export function decodePasskeyRegistrationFinish(value: JsonValue | undefined, at: string): PasskeyRegistrationFinish {
  const object = expectObject(value, at);
  const result: PasskeyRegistrationFinish = {
    credential: expectString(object["credential"], at + ".credential"),
  };
  return result;
}

export function encodePasskeyRegistrationFinish(value: PasskeyRegistrationFinish): JsonValue {
  const result: JsonObject = {
    credential: value.credential,
  };
  return result;
}

export interface Profile {
  display_name: string;
  avatar_url?: string;
}

export function decodeProfile(value: JsonValue | undefined, at: string): Profile {
  const object = expectObject(value, at);
  const result: Profile = {
    display_name: expectString(object["display_name"], at + ".display_name"),
  };
  if (object["avatar_url"] !== undefined) {
    result.avatar_url = expectString(object["avatar_url"], at + ".avatar_url");
  }
  return result;
}

export function encodeProfile(value: Profile): JsonValue {
  const result: JsonObject = {
    display_name: value.display_name,
  };
  if (value.avatar_url !== undefined) {
    result["avatar_url"] = value.avatar_url;
  }
  return result;
}

export interface ProfileUpdate {
  display_name?: string;
  avatar_url?: string;
}

export function decodeProfileUpdate(value: JsonValue | undefined, at: string): ProfileUpdate {
  const object = expectObject(value, at);
  const result: ProfileUpdate = {};
  if (object["display_name"] !== undefined) {
    result.display_name = expectString(object["display_name"], at + ".display_name");
  }
  if (object["avatar_url"] !== undefined) {
    result.avatar_url = expectString(object["avatar_url"], at + ".avatar_url");
  }
  return result;
}

export function encodeProfileUpdate(value: ProfileUpdate): JsonValue {
  const result: JsonObject = {};
  if (value.display_name !== undefined) {
    result["display_name"] = value.display_name;
  }
  if (value.avatar_url !== undefined) {
    result["avatar_url"] = value.avatar_url;
  }
  return result;
}

export interface RebindingSubmission {
  address: EvmAddress;
  statement: string;
  signature: EvmSignature;
  step_up: StepUpEvidence;
}

export function decodeRebindingSubmission(value: JsonValue | undefined, at: string): RebindingSubmission {
  const object = expectObject(value, at);
  const result: RebindingSubmission = {
    address: expectString(object["address"], at + ".address"),
    statement: expectString(object["statement"], at + ".statement"),
    signature: expectString(object["signature"], at + ".signature"),
    step_up: decodeStepUpEvidence(object["step_up"], at + ".step_up"),
  };
  return result;
}

export function encodeRebindingSubmission(value: RebindingSubmission): JsonValue {
  const result: JsonObject = {
    address: value.address,
    statement: value.statement,
    signature: value.signature,
    step_up: encodeStepUpEvidence(value.step_up),
  };
  return result;
}

export interface Refusal {
  refused_by: RefusedBy;
  copy_key: CopyKey;
  money_left: boolean;
  change_path?: string;
}

export function decodeRefusal(value: JsonValue | undefined, at: string): Refusal {
  const object = expectObject(value, at);
  const result: Refusal = {
    refused_by: decodeRefusedBy(object["refused_by"], at + ".refused_by"),
    copy_key: expectString(object["copy_key"], at + ".copy_key"),
    money_left: expectBoolean(object["money_left"], at + ".money_left"),
  };
  if (object["change_path"] !== undefined) {
    result.change_path = expectString(object["change_path"], at + ".change_path");
  }
  return result;
}

export function encodeRefusal(value: Refusal): JsonValue {
  const result: JsonObject = {
    refused_by: value.refused_by,
    copy_key: value.copy_key,
    money_left: value.money_left,
  };
  if (value.change_path !== undefined) {
    result["change_path"] = value.change_path;
  }
  return result;
}

export interface ResponseEnvelope {
  ok: boolean;
  result: JsonObject;
  trace: TraceId;
}

export function decodeResponseEnvelope(value: JsonValue | undefined, at: string): ResponseEnvelope {
  const object = expectObject(value, at);
  const result: ResponseEnvelope = {
    ok: expectBoolean(object["ok"], at + ".ok"),
    result: expectObject(object["result"], at + ".result"),
    trace: expectString(object["trace"], at + ".trace"),
  };
  return result;
}

export function encodeResponseEnvelope(value: ResponseEnvelope): JsonValue {
  const result: JsonObject = {
    ok: value.ok,
    result: value.result,
    trace: value.trace,
  };
  return result;
}

export interface SchemaVersion {
  major: number;
  minor: number;
}

export function decodeSchemaVersion(value: JsonValue | undefined, at: string): SchemaVersion {
  const object = expectObject(value, at);
  const result: SchemaVersion = {
    major: expectInteger(object["major"], at + ".major"),
    minor: expectInteger(object["minor"], at + ".minor"),
  };
  return result;
}

export function encodeSchemaVersion(value: SchemaVersion): JsonValue {
  const result: JsonObject = {
    major: value.major,
    minor: value.minor,
  };
  return result;
}

export interface Session {
  session_id: SessionId;
  device: Device;
  opened_at: Timestamp;
  last_active_at: Timestamp;
  current: boolean;
}

export function decodeSession(value: JsonValue | undefined, at: string): Session {
  const object = expectObject(value, at);
  const result: Session = {
    session_id: expectString(object["session_id"], at + ".session_id"),
    device: decodeDevice(object["device"], at + ".device"),
    opened_at: expectString(object["opened_at"], at + ".opened_at"),
    last_active_at: expectString(object["last_active_at"], at + ".last_active_at"),
    current: expectBoolean(object["current"], at + ".current"),
  };
  return result;
}

export function encodeSession(value: Session): JsonValue {
  const result: JsonObject = {
    session_id: value.session_id,
    device: encodeDevice(value.device),
    opened_at: value.opened_at,
    last_active_at: value.last_active_at,
    current: value.current,
  };
  return result;
}

export interface SessionList {
  sessions: Session[];
}

export function decodeSessionList(value: JsonValue | undefined, at: string): SessionList {
  const object = expectObject(value, at);
  const result: SessionList = {
    sessions: decodeArray(object["sessions"], at + ".sessions", decodeSession),
  };
  return result;
}

export function encodeSessionList(value: SessionList): JsonValue {
  const result: JsonObject = {
    sessions: value.sessions.map(encodeSession),
  };
  return result;
}

export interface SessionOpenRequest {
  assertion_id: AssertionId;
}

export function decodeSessionOpenRequest(value: JsonValue | undefined, at: string): SessionOpenRequest {
  const object = expectObject(value, at);
  const result: SessionOpenRequest = {
    assertion_id: expectString(object["assertion_id"], at + ".assertion_id"),
  };
  return result;
}

export function encodeSessionOpenRequest(value: SessionOpenRequest): JsonValue {
  const result: JsonObject = {
    assertion_id: value.assertion_id,
  };
  return result;
}

export interface SessionRevocation {
  revoked_session_ids: SessionId[];
  revoked_at: Timestamp;
}

export function decodeSessionRevocation(value: JsonValue | undefined, at: string): SessionRevocation {
  const object = expectObject(value, at);
  const result: SessionRevocation = {
    revoked_session_ids: decodeArray(object["revoked_session_ids"], at + ".revoked_session_ids", expectString),
    revoked_at: expectString(object["revoked_at"], at + ".revoked_at"),
  };
  return result;
}

export function encodeSessionRevocation(value: SessionRevocation): JsonValue {
  const result: JsonObject = {
    revoked_session_ids: value.revoked_session_ids,
    revoked_at: value.revoked_at,
  };
  return result;
}

export interface SpendLimit {
  monthly: Money;
  enforcement: LimitEnforcement;
  enforcement_copy_key: CopyKey;
}

export function decodeSpendLimit(value: JsonValue | undefined, at: string): SpendLimit {
  const object = expectObject(value, at);
  const result: SpendLimit = {
    monthly: decodeMoney(object["monthly"], at + ".monthly"),
    enforcement: decodeLimitEnforcement(object["enforcement"], at + ".enforcement"),
    enforcement_copy_key: expectString(object["enforcement_copy_key"], at + ".enforcement_copy_key"),
  };
  return result;
}

export function encodeSpendLimit(value: SpendLimit): JsonValue {
  const result: JsonObject = {
    monthly: encodeMoney(value.monthly),
    enforcement: value.enforcement,
    enforcement_copy_key: value.enforcement_copy_key,
  };
  return result;
}

export interface StepUpChallenge {
  challenge_id: StepUpChallengeId;
  confirms: OperationDigest;
  ceremony: OpaqueCredential;
  expires_at: Timestamp;
}

export function decodeStepUpChallenge(value: JsonValue | undefined, at: string): StepUpChallenge {
  const object = expectObject(value, at);
  const result: StepUpChallenge = {
    challenge_id: expectString(object["challenge_id"], at + ".challenge_id"),
    confirms: expectString(object["confirms"], at + ".confirms"),
    ceremony: expectString(object["ceremony"], at + ".ceremony"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodeStepUpChallenge(value: StepUpChallenge): JsonValue {
  const result: JsonObject = {
    challenge_id: value.challenge_id,
    confirms: value.confirms,
    ceremony: value.ceremony,
    expires_at: value.expires_at,
  };
  return result;
}

export interface StepUpEvidence {
  challenge_id: StepUpChallengeId;
  confirms: OperationDigest;
  passkey_id: PasskeyId;
  completed_at: Timestamp;
  expires_at: Timestamp;
}

export function decodeStepUpEvidence(value: JsonValue | undefined, at: string): StepUpEvidence {
  const object = expectObject(value, at);
  const result: StepUpEvidence = {
    challenge_id: expectString(object["challenge_id"], at + ".challenge_id"),
    confirms: expectString(object["confirms"], at + ".confirms"),
    passkey_id: expectString(object["passkey_id"], at + ".passkey_id"),
    completed_at: expectString(object["completed_at"], at + ".completed_at"),
    expires_at: expectString(object["expires_at"], at + ".expires_at"),
  };
  return result;
}

export function encodeStepUpEvidence(value: StepUpEvidence): JsonValue {
  const result: JsonObject = {
    challenge_id: value.challenge_id,
    confirms: value.confirms,
    passkey_id: value.passkey_id,
    completed_at: value.completed_at,
    expires_at: value.expires_at,
  };
  return result;
}

export interface StepUpFinish {
  credential: OpaqueCredential;
}

export function decodeStepUpFinish(value: JsonValue | undefined, at: string): StepUpFinish {
  const object = expectObject(value, at);
  const result: StepUpFinish = {
    credential: expectString(object["credential"], at + ".credential"),
  };
  return result;
}

export function encodeStepUpFinish(value: StepUpFinish): JsonValue {
  const result: JsonObject = {
    credential: value.credential,
  };
  return result;
}

export interface StepUpRequest {
  confirms: OperationDigest;
}

export function decodeStepUpRequest(value: JsonValue | undefined, at: string): StepUpRequest {
  const object = expectObject(value, at);
  const result: StepUpRequest = {
    confirms: expectString(object["confirms"], at + ".confirms"),
  };
  return result;
}

export function encodeStepUpRequest(value: StepUpRequest): JsonValue {
  const result: JsonObject = {
    confirms: value.confirms,
  };
  return result;
}

export interface StreamEvent {
  cursor: Cursor;
  kind: StreamEventKind;
  observed_at: Timestamp;
  journey?: Journey;
  approval?: ApprovalSummary;
  notification?: NotificationSummary;
}

export function decodeStreamEvent(value: JsonValue | undefined, at: string): StreamEvent {
  const object = expectObject(value, at);
  const result: StreamEvent = {
    cursor: expectString(object["cursor"], at + ".cursor"),
    kind: decodeStreamEventKind(object["kind"], at + ".kind"),
    observed_at: expectString(object["observed_at"], at + ".observed_at"),
  };
  if (object["journey"] !== undefined) {
    result.journey = decodeJourney(object["journey"], at + ".journey");
  }
  if (object["approval"] !== undefined) {
    result.approval = decodeApprovalSummary(object["approval"], at + ".approval");
  }
  if (object["notification"] !== undefined) {
    result.notification = decodeNotificationSummary(object["notification"], at + ".notification");
  }
  return result;
}

export function encodeStreamEvent(value: StreamEvent): JsonValue {
  const result: JsonObject = {
    cursor: value.cursor,
    kind: value.kind,
    observed_at: value.observed_at,
  };
  if (value.journey !== undefined) {
    result["journey"] = encodeJourney(value.journey);
  }
  if (value.approval !== undefined) {
    result["approval"] = encodeApprovalSummary(value.approval);
  }
  if (value.notification !== undefined) {
    result["notification"] = encodeNotificationSummary(value.notification);
  }
  return result;
}

export interface StreamPage {
  events: StreamEvent[];
  next_cursor: Cursor;
}

export function decodeStreamPage(value: JsonValue | undefined, at: string): StreamPage {
  const object = expectObject(value, at);
  const result: StreamPage = {
    events: decodeArray(object["events"], at + ".events", decodeStreamEvent),
    next_cursor: expectString(object["next_cursor"], at + ".next_cursor"),
  };
  return result;
}

export function encodeStreamPage(value: StreamPage): JsonValue {
  const result: JsonObject = {
    events: value.events.map(encodeStreamEvent),
    next_cursor: value.next_cursor,
  };
  return result;
}

export interface StreamPosition {
  cursor: Cursor;
}

export function decodeStreamPosition(value: JsonValue | undefined, at: string): StreamPosition {
  const object = expectObject(value, at);
  const result: StreamPosition = {
    cursor: expectString(object["cursor"], at + ".cursor"),
  };
  return result;
}

export function encodeStreamPosition(value: StreamPosition): JsonValue {
  const result: JsonObject = {
    cursor: value.cursor,
  };
  return result;
}

export interface VerifiedMoney {
  money: Money;
  verification: VerificationLevel;
}

export function decodeVerifiedMoney(value: JsonValue | undefined, at: string): VerifiedMoney {
  const object = expectObject(value, at);
  const result: VerifiedMoney = {
    money: decodeMoney(object["money"], at + ".money"),
    verification: decodeVerificationLevel(object["verification"], at + ".verification"),
  };
  return result;
}

export function encodeVerifiedMoney(value: VerifiedMoney): JsonValue {
  const result: JsonObject = {
    money: encodeMoney(value.money),
    verification: value.verification,
  };
  return result;
}

export interface VersionInfo {
  schema: SchemaVersion;
  service: string;
}

export function decodeVersionInfo(value: JsonValue | undefined, at: string): VersionInfo {
  const object = expectObject(value, at);
  const result: VersionInfo = {
    schema: decodeSchemaVersion(object["schema"], at + ".schema"),
    service: expectString(object["service"], at + ".service"),
  };
  return result;
}

export function encodeVersionInfo(value: VersionInfo): JsonValue {
  const result: JsonObject = {
    schema: encodeSchemaVersion(value.schema),
    service: value.service,
  };
  return result;
}

export interface WalletBinding {
  state: BindingState;
  address?: EvmAddress;
  bound_at?: Timestamp;
  evidence?: EvidenceRef;
}

export function decodeWalletBinding(value: JsonValue | undefined, at: string): WalletBinding {
  const object = expectObject(value, at);
  const result: WalletBinding = {
    state: decodeBindingState(object["state"], at + ".state"),
  };
  if (object["address"] !== undefined) {
    result.address = expectString(object["address"], at + ".address");
  }
  if (object["bound_at"] !== undefined) {
    result.bound_at = expectString(object["bound_at"], at + ".bound_at");
  }
  if (object["evidence"] !== undefined) {
    result.evidence = decodeEvidenceRef(object["evidence"], at + ".evidence");
  }
  return result;
}

export function encodeWalletBinding(value: WalletBinding): JsonValue {
  const result: JsonObject = {
    state: value.state,
  };
  if (value.address !== undefined) {
    result["address"] = value.address;
  }
  if (value.bound_at !== undefined) {
    result["bound_at"] = value.bound_at;
  }
  if (value.evidence !== undefined) {
    result["evidence"] = encodeEvidenceRef(value.evidence);
  }
  return result;
}

export interface WalletSignRequest {
  stage_id: StageId;
  copy_key: CopyKey;
  from_address: WalletAddress;
  to_sign_base64: string;
}

export function decodeWalletSignRequest(value: JsonValue | undefined, at: string): WalletSignRequest {
  const object = expectObject(value, at);
  const result: WalletSignRequest = {
    stage_id: expectString(object["stage_id"], at + ".stage_id"),
    copy_key: expectString(object["copy_key"], at + ".copy_key"),
    from_address: expectString(object["from_address"], at + ".from_address"),
    to_sign_base64: expectString(object["to_sign_base64"], at + ".to_sign_base64"),
  };
  return result;
}

export function encodeWalletSignRequest(value: WalletSignRequest): JsonValue {
  const result: JsonObject = {
    stage_id: value.stage_id,
    copy_key: value.copy_key,
    from_address: value.from_address,
    to_sign_base64: value.to_sign_base64,
  };
  return result;
}

export interface WithdrawClaimRequest {
  claim_signature: WalletSignature;
}

export function decodeWithdrawClaimRequest(value: JsonValue | undefined, at: string): WithdrawClaimRequest {
  const object = expectObject(value, at);
  const result: WithdrawClaimRequest = {
    claim_signature: expectString(object["claim_signature"], at + ".claim_signature"),
  };
  return result;
}

export function encodeWithdrawClaimRequest(value: WithdrawClaimRequest): JsonValue {
  const result: JsonObject = {
    claim_signature: value.claim_signature,
  };
  return result;
}

export interface WithdrawStartRequest {
  money: Money;
  destination: WalletAddress;
}

export function decodeWithdrawStartRequest(value: JsonValue | undefined, at: string): WithdrawStartRequest {
  const object = expectObject(value, at);
  const result: WithdrawStartRequest = {
    money: decodeMoney(object["money"], at + ".money"),
    destination: expectString(object["destination"], at + ".destination"),
  };
  return result;
}

export function encodeWithdrawStartRequest(value: WithdrawStartRequest): JsonValue {
  const result: JsonObject = {
    money: encodeMoney(value.money),
    destination: value.destination,
  };
  return result;
}

export class HumanApiError extends Error {
  readonly status: number;
  readonly trace: TraceId;
  readonly detail: ApiError;

  constructor(status: number, trace: TraceId, detail: ApiError) {
    super(detail.code + ": " + detail.copy_key);
    this.name = "HumanApiError";
    this.status = status;
    this.trace = trace;
    this.detail = detail;
  }
}

export type HttpMethod = "DELETE" | "GET" | "PATCH" | "POST" | "PUT";

export interface OperationShape {
  readonly method: HttpMethod;
  readonly path: string;
  readonly pathParams: readonly string[];
  readonly request: string;
  readonly response: string;
  readonly idempotency: boolean;
  readonly bodyless: boolean;
}

export const operationNames = [
  "account.create",
  "activity.entry",
  "activity.export.evidence",
  "activity.export.statement",
  "activity.query",
  "agent.archive",
  "agent.create",
  "agent.get",
  "agent.limit",
  "agent.list",
  "agent.pause",
  "agent.reclaim",
  "agent.recover",
  "agent.resume",
  "agent.rotate",
  "approval.approve",
  "approval.get",
  "approval.list",
  "approval.reject",
  "binding.rebind",
  "binding.statement",
  "binding.status",
  "binding.submit",
  "deposit.confirm",
  "deposit.start",
  "evidence.get",
  "exit.eligibility",
  "exit.start",
  "journey.get",
  "journey.list",
  "move.commit",
  "move.quote",
  "notification.list",
  "notification.preferences.get",
  "notification.preferences.set",
  "notification.read",
  "onboarding.resume",
  "onboarding.status",
  "passkey.assert.begin",
  "passkey.assert.finish",
  "passkey.register.begin",
  "passkey.register.finish",
  "profile.get",
  "profile.update",
  "session.list",
  "session.open",
  "session.refresh",
  "session.revoke",
  "session.revoke-all",
  "stepup.begin",
  "stepup.finish",
  "stream.next",
  "stream.open",
  "version",
  "withdraw.claim",
  "withdraw.start",
] as const;

export type OperationName = (typeof operationNames)[number];

export const operations: { readonly [name in OperationName]: OperationShape } = {
  "account.create": { method: "POST", path: "/v1/accounts", pathParams: [], request: "AccountCreateRequest", response: "AccountCreation", idempotency: true, bodyless: false },
  "activity.entry": { method: "GET", path: "/v1/activity/{entry_id}", pathParams: ["entry_id"], request: "Empty", response: "ActivityEntryDetail", idempotency: false, bodyless: true },
  "activity.export.evidence": { method: "POST", path: "/v1/activity/exports/evidence", pathParams: [], request: "ExportEvidenceRequest", response: "ExportArtefact", idempotency: true, bodyless: false },
  "activity.export.statement": { method: "POST", path: "/v1/activity/exports/statement", pathParams: [], request: "ExportStatementRequest", response: "ExportArtefact", idempotency: true, bodyless: false },
  "activity.query": { method: "POST", path: "/v1/activity/query", pathParams: [], request: "ActivityQueryRequest", response: "ActivityPage", idempotency: false, bodyless: false },
  "agent.archive": { method: "POST", path: "/v1/agents/{agent_id}/archive", pathParams: ["agent_id"], request: "AgentArchiveRequest", response: "Journey", idempotency: true, bodyless: false },
  "agent.create": { method: "POST", path: "/v1/agents", pathParams: [], request: "AgentCreateRequest", response: "Journey", idempotency: true, bodyless: false },
  "agent.get": { method: "GET", path: "/v1/agents/{agent_id}", pathParams: ["agent_id"], request: "Empty", response: "Agent", idempotency: false, bodyless: true },
  "agent.limit": { method: "POST", path: "/v1/agents/{agent_id}/limit", pathParams: ["agent_id"], request: "AgentLimitRequest", response: "Agent", idempotency: true, bodyless: false },
  "agent.list": { method: "GET", path: "/v1/agents", pathParams: [], request: "Empty", response: "AgentPage", idempotency: false, bodyless: true },
  "agent.pause": { method: "POST", path: "/v1/agents/{agent_id}/pause", pathParams: ["agent_id"], request: "Empty", response: "Agent", idempotency: true, bodyless: true },
  "agent.reclaim": { method: "POST", path: "/v1/agents/{agent_id}/reclaim", pathParams: ["agent_id"], request: "AgentReclaimRequest", response: "Journey", idempotency: true, bodyless: false },
  "agent.recover": { method: "POST", path: "/v1/agents/{agent_id}/recover", pathParams: ["agent_id"], request: "Empty", response: "KeyChallenge", idempotency: true, bodyless: true },
  "agent.resume": { method: "POST", path: "/v1/agents/{agent_id}/resume", pathParams: ["agent_id"], request: "Empty", response: "Agent", idempotency: true, bodyless: true },
  "agent.rotate": { method: "POST", path: "/v1/agents/{agent_id}/rotate", pathParams: ["agent_id"], request: "Empty", response: "KeyChallenge", idempotency: true, bodyless: true },
  "approval.approve": { method: "POST", path: "/v1/approvals/{approval_id}/approve", pathParams: ["approval_id"], request: "ApprovalApproveRequest", response: "ApprovalDecision", idempotency: true, bodyless: false },
  "approval.get": { method: "GET", path: "/v1/approvals/{approval_id}", pathParams: ["approval_id"], request: "Empty", response: "ApprovalDetail", idempotency: false, bodyless: true },
  "approval.list": { method: "GET", path: "/v1/approvals", pathParams: [], request: "Empty", response: "ApprovalPage", idempotency: false, bodyless: true },
  "approval.reject": { method: "POST", path: "/v1/approvals/{approval_id}/reject", pathParams: ["approval_id"], request: "Empty", response: "ApprovalDecision", idempotency: true, bodyless: true },
  "binding.rebind": { method: "POST", path: "/v1/wallet-binding/rebind", pathParams: [], request: "RebindingSubmission", response: "Journey", idempotency: true, bodyless: false },
  "binding.statement": { method: "POST", path: "/v1/wallet-binding/statement", pathParams: [], request: "BindingStatementRequest", response: "BindingStatement", idempotency: false, bodyless: false },
  "binding.status": { method: "GET", path: "/v1/wallet-binding", pathParams: [], request: "Empty", response: "WalletBinding", idempotency: false, bodyless: true },
  "binding.submit": { method: "POST", path: "/v1/wallet-binding", pathParams: [], request: "BindingSubmission", response: "Journey", idempotency: true, bodyless: false },
  "deposit.confirm": { method: "POST", path: "/v1/deposits/{journey_id}/confirm", pathParams: ["journey_id"], request: "DepositConfirmRequest", response: "Journey", idempotency: false, bodyless: false },
  "deposit.start": { method: "POST", path: "/v1/deposits", pathParams: [], request: "DepositStartRequest", response: "Journey", idempotency: true, bodyless: false },
  "evidence.get": { method: "GET", path: "/v1/evidence/{evidence_id}", pathParams: ["evidence_id"], request: "Empty", response: "EvidenceMaterial", idempotency: false, bodyless: true },
  "exit.eligibility": { method: "GET", path: "/v1/exit/eligibility", pathParams: [], request: "Empty", response: "ExitEligibility", idempotency: false, bodyless: true },
  "exit.start": { method: "POST", path: "/v1/exit", pathParams: [], request: "ExitStartRequest", response: "Journey", idempotency: true, bodyless: false },
  "journey.get": { method: "GET", path: "/v1/journeys/{journey_id}", pathParams: ["journey_id"], request: "Empty", response: "Journey", idempotency: false, bodyless: true },
  "journey.list": { method: "GET", path: "/v1/journeys", pathParams: [], request: "Empty", response: "JourneyPage", idempotency: false, bodyless: true },
  "move.commit": { method: "POST", path: "/v1/moves", pathParams: [], request: "MoveCommitRequest", response: "Journey", idempotency: true, bodyless: false },
  "move.quote": { method: "POST", path: "/v1/moves/quote", pathParams: [], request: "MoveQuoteRequest", response: "MoveQuote", idempotency: false, bodyless: false },
  "notification.list": { method: "GET", path: "/v1/notifications", pathParams: [], request: "Empty", response: "NotificationPage", idempotency: false, bodyless: true },
  "notification.preferences.get": { method: "GET", path: "/v1/notifications/preferences", pathParams: [], request: "Empty", response: "NotificationPreferences", idempotency: false, bodyless: true },
  "notification.preferences.set": { method: "POST", path: "/v1/notifications/preferences", pathParams: [], request: "NotificationPreferences", response: "NotificationPreferences", idempotency: false, bodyless: false },
  "notification.read": { method: "POST", path: "/v1/notifications/{notification_id}/read", pathParams: ["notification_id"], request: "Empty", response: "NotificationSummary", idempotency: false, bodyless: true },
  "onboarding.resume": { method: "POST", path: "/v1/onboarding/resume", pathParams: [], request: "Empty", response: "Journey", idempotency: false, bodyless: true },
  "onboarding.status": { method: "GET", path: "/v1/onboarding", pathParams: [], request: "Empty", response: "Journey", idempotency: false, bodyless: true },
  "passkey.assert.begin": { method: "POST", path: "/v1/passkeys/assertions", pathParams: [], request: "PasskeyAssertionBegin", response: "PasskeyAssertionChallenge", idempotency: false, bodyless: false },
  "passkey.assert.finish": { method: "POST", path: "/v1/passkeys/assertions/{assertion_id}", pathParams: ["assertion_id"], request: "PasskeyAssertionFinish", response: "PasskeyAssertion", idempotency: false, bodyless: false },
  "passkey.register.begin": { method: "POST", path: "/v1/passkeys/registrations", pathParams: [], request: "PasskeyRegistrationBegin", response: "PasskeyRegistrationChallenge", idempotency: false, bodyless: false },
  "passkey.register.finish": { method: "POST", path: "/v1/passkeys/registrations/{registration_id}", pathParams: ["registration_id"], request: "PasskeyRegistrationFinish", response: "Passkey", idempotency: false, bodyless: false },
  "profile.get": { method: "GET", path: "/v1/profile", pathParams: [], request: "Empty", response: "Profile", idempotency: false, bodyless: true },
  "profile.update": { method: "PATCH", path: "/v1/profile", pathParams: [], request: "ProfileUpdate", response: "Profile", idempotency: false, bodyless: false },
  "session.list": { method: "GET", path: "/v1/sessions", pathParams: [], request: "Empty", response: "SessionList", idempotency: false, bodyless: true },
  "session.open": { method: "POST", path: "/v1/sessions", pathParams: [], request: "SessionOpenRequest", response: "Session", idempotency: false, bodyless: false },
  "session.refresh": { method: "POST", path: "/v1/sessions/refresh", pathParams: [], request: "Empty", response: "Session", idempotency: false, bodyless: true },
  "session.revoke": { method: "DELETE", path: "/v1/sessions/{session_id}", pathParams: ["session_id"], request: "Empty", response: "SessionRevocation", idempotency: false, bodyless: true },
  "session.revoke-all": { method: "POST", path: "/v1/sessions/revoke-all", pathParams: [], request: "Empty", response: "SessionRevocation", idempotency: false, bodyless: true },
  "stepup.begin": { method: "POST", path: "/v1/step-up", pathParams: [], request: "StepUpRequest", response: "StepUpChallenge", idempotency: false, bodyless: false },
  "stepup.finish": { method: "POST", path: "/v1/step-up/{challenge_id}", pathParams: ["challenge_id"], request: "StepUpFinish", response: "StepUpEvidence", idempotency: false, bodyless: false },
  "stream.next": { method: "GET", path: "/v1/stream/{cursor}", pathParams: ["cursor"], request: "Empty", response: "StreamPage", idempotency: false, bodyless: true },
  "stream.open": { method: "POST", path: "/v1/stream", pathParams: [], request: "Empty", response: "StreamPosition", idempotency: false, bodyless: true },
  "version": { method: "GET", path: "/v1/version", pathParams: [], request: "Empty", response: "VersionInfo", idempotency: false, bodyless: true },
  "withdraw.claim": { method: "POST", path: "/v1/withdrawals/{journey_id}/claim", pathParams: ["journey_id"], request: "WithdrawClaimRequest", response: "Journey", idempotency: false, bodyless: false },
  "withdraw.start": { method: "POST", path: "/v1/withdrawals", pathParams: [], request: "WithdrawStartRequest", response: "Journey", idempotency: true, bodyless: false },
};

export type FetchLike = (input: string, init: RequestInit) => Promise<Response>;

export interface HumanApiClientOptions {
  readonly baseUrl?: string;
  readonly fetch?: FetchLike;
  readonly headers?: { readonly [name: string]: string };
}

export interface HumanApiClient {
  accountCreate(request: AccountCreateRequest, idempotencyKey: string): Promise<AccountCreation>;
  activityEntry(entry_id: string): Promise<ActivityEntryDetail>;
  activityExportEvidence(request: ExportEvidenceRequest, idempotencyKey: string): Promise<ExportArtefact>;
  activityExportStatement(request: ExportStatementRequest, idempotencyKey: string): Promise<ExportArtefact>;
  activityQuery(request: ActivityQueryRequest): Promise<ActivityPage>;
  agentArchive(agent_id: string, request: AgentArchiveRequest, idempotencyKey: string): Promise<Journey>;
  agentCreate(request: AgentCreateRequest, idempotencyKey: string): Promise<Journey>;
  agentGet(agent_id: string): Promise<Agent>;
  agentLimit(agent_id: string, request: AgentLimitRequest, idempotencyKey: string): Promise<Agent>;
  agentList(): Promise<AgentPage>;
  agentPause(agent_id: string, idempotencyKey: string): Promise<Agent>;
  agentReclaim(agent_id: string, request: AgentReclaimRequest, idempotencyKey: string): Promise<Journey>;
  agentRecover(agent_id: string, idempotencyKey: string): Promise<KeyChallenge>;
  agentResume(agent_id: string, idempotencyKey: string): Promise<Agent>;
  agentRotate(agent_id: string, idempotencyKey: string): Promise<KeyChallenge>;
  approvalApprove(approval_id: string, request: ApprovalApproveRequest, idempotencyKey: string): Promise<ApprovalDecision>;
  approvalGet(approval_id: string): Promise<ApprovalDetail>;
  approvalList(): Promise<ApprovalPage>;
  approvalReject(approval_id: string, idempotencyKey: string): Promise<ApprovalDecision>;
  bindingRebind(request: RebindingSubmission, idempotencyKey: string): Promise<Journey>;
  bindingStatement(request: BindingStatementRequest): Promise<BindingStatement>;
  bindingStatus(): Promise<WalletBinding>;
  bindingSubmit(request: BindingSubmission, idempotencyKey: string): Promise<Journey>;
  depositConfirm(journey_id: string, request: DepositConfirmRequest): Promise<Journey>;
  depositStart(request: DepositStartRequest, idempotencyKey: string): Promise<Journey>;
  evidenceGet(evidence_id: string): Promise<EvidenceMaterial>;
  exitEligibility(): Promise<ExitEligibility>;
  exitStart(request: ExitStartRequest, idempotencyKey: string): Promise<Journey>;
  journeyGet(journey_id: string): Promise<Journey>;
  journeyList(): Promise<JourneyPage>;
  moveCommit(request: MoveCommitRequest, idempotencyKey: string): Promise<Journey>;
  moveQuote(request: MoveQuoteRequest): Promise<MoveQuote>;
  notificationList(): Promise<NotificationPage>;
  notificationPreferencesGet(): Promise<NotificationPreferences>;
  notificationPreferencesSet(request: NotificationPreferences): Promise<NotificationPreferences>;
  notificationRead(notification_id: string): Promise<NotificationSummary>;
  onboardingResume(): Promise<Journey>;
  onboardingStatus(): Promise<Journey>;
  passkeyAssertBegin(request: PasskeyAssertionBegin): Promise<PasskeyAssertionChallenge>;
  passkeyAssertFinish(assertion_id: string, request: PasskeyAssertionFinish): Promise<PasskeyAssertion>;
  passkeyRegisterBegin(request: PasskeyRegistrationBegin): Promise<PasskeyRegistrationChallenge>;
  passkeyRegisterFinish(registration_id: string, request: PasskeyRegistrationFinish): Promise<Passkey>;
  profileGet(): Promise<Profile>;
  profileUpdate(request: ProfileUpdate): Promise<Profile>;
  sessionList(): Promise<SessionList>;
  sessionOpen(request: SessionOpenRequest): Promise<Session>;
  sessionRefresh(): Promise<Session>;
  sessionRevoke(session_id: string): Promise<SessionRevocation>;
  sessionRevokeAll(): Promise<SessionRevocation>;
  stepupBegin(request: StepUpRequest): Promise<StepUpChallenge>;
  stepupFinish(challenge_id: string, request: StepUpFinish): Promise<StepUpEvidence>;
  streamNext(cursor: string): Promise<StreamPage>;
  streamOpen(): Promise<StreamPosition>;
  version(): Promise<VersionInfo>;
  withdrawClaim(journey_id: string, request: WithdrawClaimRequest): Promise<Journey>;
  withdrawStart(request: WithdrawStartRequest, idempotencyKey: string): Promise<Journey>;
}

export function createHumanApiClient(options: HumanApiClientOptions = {}): HumanApiClient {
  const baseUrl = options.baseUrl ?? "";
  const transport: FetchLike = options.fetch ?? ((input, init) => globalThis.fetch(input, init));
  const baseHeaders = options.headers ?? {};
  async function execute(
    method: HttpMethod,
    path: string,
    body: JsonValue | undefined,
    idempotencyKey: string | undefined,
  ): Promise<JsonValue> {
    const headers: { [name: string]: string } = { ...baseHeaders, Accept: "application/json" };
    if (body !== undefined) {
      headers["Content-Type"] = "application/json";
    }
    if (idempotencyKey !== undefined) {
      headers["Idempotency-Key"] = idempotencyKey;
    }
    const init: RequestInit = { method, headers };
    if (body !== undefined) {
      init.body = JSON.stringify(body);
    }
    const response = await transport(baseUrl + path, init);
    let parsed: JsonValue;
    try {
      parsed = (await response.json()) as JsonValue;
    } catch {
      throw new HumanApiDecodeError("the response body is not JSON");
    }
    const envelope = expectObject(parsed, "response");
    const ok = expectBoolean(envelope["ok"], "response.ok");
    const trace = expectString(envelope["trace"], "response.trace");
    if (!ok) {
      throw new HumanApiError(response.status, trace, decodeApiError(envelope["error"], "response.error"));
    }
    return requireValue(envelope["result"], "response.result");
  }
  const client: HumanApiClient = {
    accountCreate: async (request, idempotencyKey) =>
      decodeAccountCreation(await execute("POST", "/v1/accounts", encodeAccountCreateRequest(request), idempotencyKey), "account.create result"),
    activityEntry: async (entry_id) =>
      decodeActivityEntryDetail(await execute("GET", "/v1/activity/" + encodeURIComponent(entry_id), undefined, undefined), "activity.entry result"),
    activityExportEvidence: async (request, idempotencyKey) =>
      decodeExportArtefact(await execute("POST", "/v1/activity/exports/evidence", encodeExportEvidenceRequest(request), idempotencyKey), "activity.export.evidence result"),
    activityExportStatement: async (request, idempotencyKey) =>
      decodeExportArtefact(await execute("POST", "/v1/activity/exports/statement", encodeExportStatementRequest(request), idempotencyKey), "activity.export.statement result"),
    activityQuery: async (request) =>
      decodeActivityPage(await execute("POST", "/v1/activity/query", encodeActivityQueryRequest(request), undefined), "activity.query result"),
    agentArchive: async (agent_id, request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/archive", encodeAgentArchiveRequest(request), idempotencyKey), "agent.archive result"),
    agentCreate: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/agents", encodeAgentCreateRequest(request), idempotencyKey), "agent.create result"),
    agentGet: async (agent_id) =>
      decodeAgent(await execute("GET", "/v1/agents/" + encodeURIComponent(agent_id), undefined, undefined), "agent.get result"),
    agentLimit: async (agent_id, request, idempotencyKey) =>
      decodeAgent(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/limit", encodeAgentLimitRequest(request), idempotencyKey), "agent.limit result"),
    agentList: async () =>
      decodeAgentPage(await execute("GET", "/v1/agents", undefined, undefined), "agent.list result"),
    agentPause: async (agent_id, idempotencyKey) =>
      decodeAgent(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/pause", undefined, idempotencyKey), "agent.pause result"),
    agentReclaim: async (agent_id, request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/reclaim", encodeAgentReclaimRequest(request), idempotencyKey), "agent.reclaim result"),
    agentRecover: async (agent_id, idempotencyKey) =>
      decodeKeyChallenge(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/recover", undefined, idempotencyKey), "agent.recover result"),
    agentResume: async (agent_id, idempotencyKey) =>
      decodeAgent(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/resume", undefined, idempotencyKey), "agent.resume result"),
    agentRotate: async (agent_id, idempotencyKey) =>
      decodeKeyChallenge(await execute("POST", "/v1/agents/" + encodeURIComponent(agent_id) + "/rotate", undefined, idempotencyKey), "agent.rotate result"),
    approvalApprove: async (approval_id, request, idempotencyKey) =>
      decodeApprovalDecision(await execute("POST", "/v1/approvals/" + encodeURIComponent(approval_id) + "/approve", encodeApprovalApproveRequest(request), idempotencyKey), "approval.approve result"),
    approvalGet: async (approval_id) =>
      decodeApprovalDetail(await execute("GET", "/v1/approvals/" + encodeURIComponent(approval_id), undefined, undefined), "approval.get result"),
    approvalList: async () =>
      decodeApprovalPage(await execute("GET", "/v1/approvals", undefined, undefined), "approval.list result"),
    approvalReject: async (approval_id, idempotencyKey) =>
      decodeApprovalDecision(await execute("POST", "/v1/approvals/" + encodeURIComponent(approval_id) + "/reject", undefined, idempotencyKey), "approval.reject result"),
    bindingRebind: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/wallet-binding/rebind", encodeRebindingSubmission(request), idempotencyKey), "binding.rebind result"),
    bindingStatement: async (request) =>
      decodeBindingStatement(await execute("POST", "/v1/wallet-binding/statement", encodeBindingStatementRequest(request), undefined), "binding.statement result"),
    bindingStatus: async () =>
      decodeWalletBinding(await execute("GET", "/v1/wallet-binding", undefined, undefined), "binding.status result"),
    bindingSubmit: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/wallet-binding", encodeBindingSubmission(request), idempotencyKey), "binding.submit result"),
    depositConfirm: async (journey_id, request) =>
      decodeJourney(await execute("POST", "/v1/deposits/" + encodeURIComponent(journey_id) + "/confirm", encodeDepositConfirmRequest(request), undefined), "deposit.confirm result"),
    depositStart: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/deposits", encodeDepositStartRequest(request), idempotencyKey), "deposit.start result"),
    evidenceGet: async (evidence_id) =>
      decodeEvidenceMaterial(await execute("GET", "/v1/evidence/" + encodeURIComponent(evidence_id), undefined, undefined), "evidence.get result"),
    exitEligibility: async () =>
      decodeExitEligibility(await execute("GET", "/v1/exit/eligibility", undefined, undefined), "exit.eligibility result"),
    exitStart: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/exit", encodeExitStartRequest(request), idempotencyKey), "exit.start result"),
    journeyGet: async (journey_id) =>
      decodeJourney(await execute("GET", "/v1/journeys/" + encodeURIComponent(journey_id), undefined, undefined), "journey.get result"),
    journeyList: async () =>
      decodeJourneyPage(await execute("GET", "/v1/journeys", undefined, undefined), "journey.list result"),
    moveCommit: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/moves", encodeMoveCommitRequest(request), idempotencyKey), "move.commit result"),
    moveQuote: async (request) =>
      decodeMoveQuote(await execute("POST", "/v1/moves/quote", encodeMoveQuoteRequest(request), undefined), "move.quote result"),
    notificationList: async () =>
      decodeNotificationPage(await execute("GET", "/v1/notifications", undefined, undefined), "notification.list result"),
    notificationPreferencesGet: async () =>
      decodeNotificationPreferences(await execute("GET", "/v1/notifications/preferences", undefined, undefined), "notification.preferences.get result"),
    notificationPreferencesSet: async (request) =>
      decodeNotificationPreferences(await execute("POST", "/v1/notifications/preferences", encodeNotificationPreferences(request), undefined), "notification.preferences.set result"),
    notificationRead: async (notification_id) =>
      decodeNotificationSummary(await execute("POST", "/v1/notifications/" + encodeURIComponent(notification_id) + "/read", undefined, undefined), "notification.read result"),
    onboardingResume: async () =>
      decodeJourney(await execute("POST", "/v1/onboarding/resume", undefined, undefined), "onboarding.resume result"),
    onboardingStatus: async () =>
      decodeJourney(await execute("GET", "/v1/onboarding", undefined, undefined), "onboarding.status result"),
    passkeyAssertBegin: async (request) =>
      decodePasskeyAssertionChallenge(await execute("POST", "/v1/passkeys/assertions", encodePasskeyAssertionBegin(request), undefined), "passkey.assert.begin result"),
    passkeyAssertFinish: async (assertion_id, request) =>
      decodePasskeyAssertion(await execute("POST", "/v1/passkeys/assertions/" + encodeURIComponent(assertion_id), encodePasskeyAssertionFinish(request), undefined), "passkey.assert.finish result"),
    passkeyRegisterBegin: async (request) =>
      decodePasskeyRegistrationChallenge(await execute("POST", "/v1/passkeys/registrations", encodePasskeyRegistrationBegin(request), undefined), "passkey.register.begin result"),
    passkeyRegisterFinish: async (registration_id, request) =>
      decodePasskey(await execute("POST", "/v1/passkeys/registrations/" + encodeURIComponent(registration_id), encodePasskeyRegistrationFinish(request), undefined), "passkey.register.finish result"),
    profileGet: async () =>
      decodeProfile(await execute("GET", "/v1/profile", undefined, undefined), "profile.get result"),
    profileUpdate: async (request) =>
      decodeProfile(await execute("PATCH", "/v1/profile", encodeProfileUpdate(request), undefined), "profile.update result"),
    sessionList: async () =>
      decodeSessionList(await execute("GET", "/v1/sessions", undefined, undefined), "session.list result"),
    sessionOpen: async (request) =>
      decodeSession(await execute("POST", "/v1/sessions", encodeSessionOpenRequest(request), undefined), "session.open result"),
    sessionRefresh: async () =>
      decodeSession(await execute("POST", "/v1/sessions/refresh", undefined, undefined), "session.refresh result"),
    sessionRevoke: async (session_id) =>
      decodeSessionRevocation(await execute("DELETE", "/v1/sessions/" + encodeURIComponent(session_id), undefined, undefined), "session.revoke result"),
    sessionRevokeAll: async () =>
      decodeSessionRevocation(await execute("POST", "/v1/sessions/revoke-all", undefined, undefined), "session.revoke-all result"),
    stepupBegin: async (request) =>
      decodeStepUpChallenge(await execute("POST", "/v1/step-up", encodeStepUpRequest(request), undefined), "stepup.begin result"),
    stepupFinish: async (challenge_id, request) =>
      decodeStepUpEvidence(await execute("POST", "/v1/step-up/" + encodeURIComponent(challenge_id), encodeStepUpFinish(request), undefined), "stepup.finish result"),
    streamNext: async (cursor) =>
      decodeStreamPage(await execute("GET", "/v1/stream/" + encodeURIComponent(cursor), undefined, undefined), "stream.next result"),
    streamOpen: async () =>
      decodeStreamPosition(await execute("POST", "/v1/stream", undefined, undefined), "stream.open result"),
    version: async () =>
      decodeVersionInfo(await execute("GET", "/v1/version", undefined, undefined), "version result"),
    withdrawClaim: async (journey_id, request) =>
      decodeJourney(await execute("POST", "/v1/withdrawals/" + encodeURIComponent(journey_id) + "/claim", encodeWithdrawClaimRequest(request), undefined), "withdraw.claim result"),
    withdrawStart: async (request, idempotencyKey) =>
      decodeJourney(await execute("POST", "/v1/withdrawals", encodeWithdrawStartRequest(request), idempotencyKey), "withdraw.start result"),
  };
  return client;
}
