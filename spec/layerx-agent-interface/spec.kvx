# spec.kvx - LayerX Agent Interface. SINGLE SOURCE OF TRUTH for this feature.
#
# requirements.md / design.md / tasks.md next to this file are GENERATED from it
# by spec/specgen. Never hand-edit those; edit this file and regenerate.
#
# Drive the work with:  cg spec next -> cg spec start <id> -> cg spec done <id>
# Provenance for every statement here: docs/00-source-brief.md
# Upstream normative source: spec/layerx-protocol/spec.kvx (the protocol wins).

[meta]
feature      = "layerx-agent-interface"
title        = "LayerX Agent Interface - The Rust Interaction Layer"
kind         = "interaction-layer-specification"
version      = "1.0.0"
status       = "draft"
language     = "Rust"
repo_root    = "agent/"
upstream     = "spec/layerx-protocol (C17 core; normative for all protocol behaviour)"
intro        = "The LayerX Agent Interface is the Rust interaction layer through which autonomous agents reach the LayerX protocol. It provides agent identity, sessions, capabilities, budgets and policy enforcement; activity preparation, local signing, submission and receipt tracking; verified balances, history, checkpoints, proofs and data-availability retrieval; streaming events and durable subscriptions; MCP servers exposing scoped read and write tools; a Rust SDK plus generated TypeScript and Python SDKs; and rate limits, idempotency, audit trails, observability and tenant isolation. One rule governs the entire layer: it never invents or directly changes protocol state. Every mutation becomes canonical signed LayerX bytes submitted to the core, and every claimed result is backed by a core-produced receipt or proof that this layer verified for itself. layerx-agentd reaches the core only through a stable, versioned protocol boundary - never by opening the node database and never by binding internal C structures - so that the two sides upgrade independently and the agent layer never becomes a second consensus implementation. These requirements are normative; the design and the task plan beside them are compiled from this file."

# Decisions locked before implementation begins. These supersede any ambiguity
# in docs/00-source-brief.md. Where they touch protocol behaviour they defer to
# spec/layerx-protocol/spec.kvx, which is normative for the protocol itself.
[decision]
language            = "Rust 2021 for the entire interaction layer, in its own workspace at agent/. The protocol runtime stays C17. No Rust translation unit links into the consensus runtime, and no crate in agent/ is a build dependency of src/."
placement           = "The agent workspace lives in this repository at agent/ so spec, core and interaction layer share one Codify graph, but it builds, tests, versions and releases independently of the C core."
non_authority       = "The interaction layer holds no protocol authority. It can refuse, prepare, sign with an explicitly provisioned protocol session key, submit, verify and report. It can never author an effect, never write state, and never assert an outcome the core did not produce."
boundary            = "layerx-agentd speaks to the core only over the LayerX Node Interface (LNI): a versioned, canonical-binary request, response and streaming protocol carrying canonical LayerX bytes plus the proofs required to verify them. Unix domain socket is the default transport; TCP with mutual TLS is permitted for remote deployment."
boundary_prohibits  = "No crate in agent/ may open the node SQLite projection, link a SQLite driver, read the append-only log files directly, or bind any struct layout declared in include/layerx/. An optional stable C ABI transport is permitted only with opaque handles and canonical byte buffers, never with shared struct layouts."
no_second_consensus = "The agent layer decodes, verifies, re-hashes and compares. It never executes a state transition, never applies a transfer, never derives a state root from activities and never predicts admission as though it were a decision. Where it must estimate, the estimate is labelled a projection and carries no receipt."
policy_is_negative  = "Local policy, capability scopes and budget mirrors are restrictions only. They can refuse an activity that the protocol would have accepted; they can never authorise one the protocol would reject, and their approval is never evidence of authority."
signing             = "Signing is local to the key holder. agentd does not hold agent primary keys. It signs only under a protocol-level session key an operator explicitly provisioned to it, whose scope and expiry the state machine enforces independently of anything agentd believes."
unknown_is_terminal = "A submission whose fate cannot be determined is reported as unknown, never as success and never as failure, and is resolved only by looking up the receipt for its idempotency key."
sdk_generation      = "The Rust SDK is authored; the TypeScript and Python SDKs are generated from the same contract schema by the same generator. Hand-editing generated output is a build failure."
mcp_scope           = "MCP servers expose the agent-api contract as tools under an explicit scope. Read tools return verified core bytes; write tools prepare and submit canonical activities and return receipts. No MCP tool returns a claimed outcome that is not backed by a receipt or proof."

[design]
include = "design.body.md"

[tasks]
heading          = "Implementation Plan: LayerX Agent Interface"
overview_include = "tasks.overview.md"
notes_include    = "tasks.notes.md"

# --------------------------------------------------------------------------
# REQUIREMENTS
# --------------------------------------------------------------------------

[req.1]
title = "Non-Authority of the Interaction Layer"
story = "As an autonomous agent operator, I want the Rust interaction layer to be structurally incapable of inventing protocol state or asserting an unbacked result, so that every effect my agents cause is a protocol effect and every number they act on is evidence rather than an opinion held by middleware."
ac_1 = "WHEN any component of the interaction layer effects a change of protocol state THE layer SHALL do so exclusively by constructing a canonical LayerX activity, obtaining a signature over its canonical bytes from a key the LayerX state machine binds to the acting identity, and submitting those exact bytes to the core over the node interface, and SHALL possess no other path by which protocol state can change."
ac_2 = "THE layer SHALL NOT write, cache as authoritative, patch, reconcile or otherwise mutate any protocol state store, SHALL NOT open the node append-only log or SQLite projection for reading or writing, and SHALL treat every protocol value it holds as a copy of core-produced bytes whose provenance and verification status travel with it."
ac_3 = "WHEN the layer reports a balance, a transfer outcome, an escrow or budget state, a service lifecycle state, a position, a fee charged or any other protocol fact THE layer SHALL return only values decoded from core-produced bytes and SHALL attach a verification status recording exactly which of sequencer signature, activity inclusion proof, state inclusion proof and checkpoint certificate were verified locally for that value."
ac_4 = "IF a value cannot be verified to the level a caller requested THEN THE layer SHALL either return it explicitly marked unverified together with the reason, or refuse the request, and SHALL NOT silently downgrade the verification level or omit the status."
ac_5 = "THE layer SHALL compute no protocol outcome of its own, SHALL NOT apply transfers, evaluate module transitions, derive state roots from activities, or determine admissibility as a decision, and WHERE it estimates a prospective result for user guidance THE layer SHALL label the value a projection, SHALL exclude it from every receipt-shaped structure and SHALL never return it in a field a caller could mistake for an executed outcome."
ac_6 = "IF a submission is acknowledged, times out, fails in transport, or completes with an indeterminate response THEN THE layer SHALL record the terminal state as unknown, SHALL NOT report success and SHALL NOT report failure, and SHALL resolve the state only by retrieving the receipt for that idempotency key from the core."
ac_7 = "IF the core returns a failure result code for an activity THEN THE layer SHALL surface that exact code and its canonical meaning, SHALL NOT reinterpret, soften, retry-until-success or substitute a locally derived explanation for it, and SHALL NOT present a failed activity as pending merely because a retry is scheduled."
ac_8 = "THE layer SHALL NOT hold, mint, escrow, net, aggregate or sweep value on behalf of an agent outside protocol accounts, SHALL NOT maintain any internal ledger of owed or owing amounts that is not a mirror of core-produced receipts, and SHALL make any locally tracked amount reconstructible from receipts alone."
ac_9 = "WHEN the layer is stopped, its databases deleted and its process restarted against the same core THE layer SHALL reconstruct every protocol-derived answer it previously served from the core alone, and SHALL lose only local artefacts such as policy configuration, subscription cursors, audit history and idempotency records that it declares as local."
ac_10 = "WHERE a component would need to be trusted for a protocol fact to be believed THE layer SHALL provide the caller with the underlying core-produced bytes and proofs sufficient to verify that fact independently of the layer, and the qualification suite SHALL demonstrate that verification succeeding against a hostile layer implementation that alters the reported value."

[req.2]
title = "The Stable Node Interface Boundary"
story = "As a protocol operator, I want the agent layer to reach the core only through one versioned protocol boundary, so that the core can be refactored, re-indexed or upgraded without breaking agents, and the agent layer can never quietly grow into a second implementation of consensus."
ac_1 = "THE layer SHALL communicate with the core exclusively through the LayerX Node Interface, a versioned request, response and streaming protocol whose messages are canonically encoded and whose payloads are canonical LayerX bytes plus the proofs required to verify them, and SHALL define no other channel to the core."
ac_2 = "THE layer SHALL NOT link a SQLite driver, open the node database file, read the append-only activity log or its segment files, memory-map node state, or depend on any file layout private to the node, and CI SHALL fail the build when a dependency or symbol reference violating this appears anywhere in the agent workspace."
ac_3 = "THE layer SHALL NOT declare, generate or bind any struct, union or enum layout from include/layerx, SHALL NOT depend on C field order, padding, packing or type widths, and WHERE the optional C ABI transport is used THE layer SHALL exchange only opaque handles and canonical byte buffers through a published stable ABI header that carries its own version."
ac_4 = "WHEN a connection to the core is established THE layer SHALL perform a version handshake exchanging node interface version, protocol_version, network_id, node role, chain head sequence, latest sealed batch number, latest finalised checkpoint and the sequencer public key currently authorised, and SHALL refuse to operate against a network_id or protocol_version it was not configured for."
ac_5 = "IF the core advertises a node interface version whose major component differs from the one the layer was built against THEN THE layer SHALL refuse the connection with an explicit incompatibility error naming both versions, and SHALL NOT attempt best-effort interpretation of its messages."
ac_6 = "WHERE the core advertises a capability the layer does not know, or omits a capability the layer knows, THE layer SHALL continue to operate using the intersection, SHALL report every unavailable capability explicitly through its own status surface, and SHALL fail requests that need a missing capability with an unavailable error rather than emulating the capability locally."
ac_7 = "IF the core is unreachable, is behind, or reports a degraded mode THEN THE layer SHALL continue to serve reads it can still verify while marking their staleness with the head sequence and checkpoint they are relative to, SHALL refuse to acknowledge submissions it cannot deliver, and SHALL NOT invent an admission decision on the core behalf."
ac_8 = "WHEN the layer performs any read whose answer is protocol-visible THE layer SHALL request the accompanying proof material and verify it locally through layerx-proof before returning the value, and SHALL treat a core response that lacks requested proof material as an unverified answer rather than as a verified one."
ac_9 = "THE layer SHALL define the node interface schema as a versioned artefact under source control with golden encoding vectors, SHALL require any change to it to be additive within a major version, and SHALL run a boundary conformance suite against a real running node rather than against any simulated or in-process substitute."
ac_10 = "IF the boundary does not yet expose a capability this specification requires THEN THE layer SHALL report that gap explicitly in its capability report and SHALL fail the dependent request as unavailable, and SHALL NOT satisfy it by reading node internals, by reconstructing the answer from partial data, or by returning a plausible substitute."

[req.3]
title = "layerx-types: Canonical Domain Types"
story = "As an SDK author, I want one Rust type vocabulary that mirrors the protocol structures exactly and refuses to represent anything the protocol cannot express, so that an invalid activity is unconstructible rather than merely rejected later."
ac_1 = "THE crate layerx-types SHALL define the LayerX domain vocabulary, including the activity envelope with its exact twelve fields, module payload types, the activity receipt, the 402LXP receipt, batch headers, checkpoint certificates, account identifiers, asset identifiers, result codes and verification status, and SHALL derive each definition from spec/layerx-protocol rather than restating it independently."
ac_2 = "THE crate SHALL represent every consensus-critical numeric value as an exact fixed-width integer type, SHALL provide the 128-bit and 256-bit widths the protocol requires, and SHALL make it impossible to construct a protocol value from a floating-point number anywhere in the public API."
ac_3 = "THE crate SHALL represent account identifiers only through the protocol namespaces agent main, agent budget, agent escrow, agent margin, system liquidity, system insurance, system fees, system paxeer reserve and system paxeer withdrawals, and SHALL reject construction of any identifier outside that namespace at the type boundary."
ac_4 = "THE crate SHALL define result codes as an exhaustive typed taxonomy partitioned by the same domains the protocol defines, SHALL preserve the exact numeric code across the boundary, and SHALL provide a total mapping from numeric code to a typed variant that preserves unknown codes verbatim instead of collapsing them into a generic error."
ac_5 = "WHEN a domain type carries a length-bounded element THE crate SHALL enforce the protocol declared maximum at construction, SHALL reject an over-long value with a typed error rather than truncating it, and SHALL make the bound a compile-time or constructor-time property rather than a runtime convention."
ac_6 = "THE crate SHALL make every protocol-derived value carry its provenance, so that a value obtained from a verified receipt, a value obtained from an unverified read and a value produced by a local projection are distinguishable by type or by an inseparable status field."
ac_7 = "THE crate SHALL contain no input or output, no clock read, no network access and no global mutable state, and SHALL depend on no crate that performs any of those, so that types can be used inside signing devices and constrained hosts."
ac_8 = "WHEN the protocol adds a module, an activity type or a result code THE crate SHALL accommodate it by extension without changing the meaning of existing variants, and SHALL record the protocol version each definition was derived from."
ac_9 = "THE crate SHALL provide conversions to and from the canonical wire representation only through layerx-wire, SHALL expose no ad-hoc serialisation of consensus-critical structures such as a derived JSON or debug format used for hashing or signing, and SHALL mark any human-readable rendering as non-canonical."
ac_10 = "THE crate SHALL be validated against the protocol conformance vectors so that every type it defines round-trips the published vectors exactly, and a vector the crate cannot represent SHALL be a build-breaking failure rather than a skipped test."

[req.4]
title = "layerx-wire: Byte-Exact Canonical Codec"
story = "As an agent operator, I want the Rust encoder to produce exactly the bytes the C core would produce and the Rust decoder to reject exactly what the core rejects, so that a signature made on this side is valid on that side and no representation gap exists between the two implementations."
ac_1 = "THE crate layerx-wire SHALL implement the canonical binary encoding of every protocol structure such that encoding a value produces bytes byte-identical to those the C reference implementation produces for the same logical value, and SHALL be validated against the published conformance corpora on every build."
ac_2 = "WHEN layerx-wire decodes bytes it SHALL reject every non-canonical form the protocol rejects, including non-minimal integer encodings, indefinite-length items, duplicate map keys, out-of-order map keys, unused padding and trailing bytes, and SHALL return the same result-code domain the core returns for each rejection class."
ac_3 = "WHEN a value decodes successfully THE crate SHALL guarantee that re-encoding it reproduces the original bytes exactly, and the round-trip property SHALL be enforced by both the conformance corpora and a property test over generated values."
ac_4 = "THE crate SHALL compute activity_id, payload_hash and every other identifier as the domain-separated hash of canonical bytes using the same domain tags as the core, and SHALL be proven equal to the core output for every identifier in the conformance corpora."
ac_5 = "THE crate SHALL enforce every declared length limit during decoding, SHALL reject an over-long element before allocating memory to hold it, and SHALL bound total allocation per decode so that a hostile message cannot exhaust memory."
ac_6 = "THE crate SHALL contain no panicking path reachable from decoding arbitrary bytes, SHALL return typed errors instead, and SHALL be fuzzed continuously with corpora seeded from the protocol conformance vectors, with any panic, hang, or unbounded allocation treated as a build-breaking defect."
ac_7 = "IF the crate encounters an unknown protocol version, an unknown activity type or an unknown field identifier in a consensus-critical structure THEN THE crate SHALL reject the input deterministically rather than skipping or best-effort interpreting the unrecognised content."
ac_8 = "THE crate SHALL provide the exact byte string that a signature covers for every signable structure, SHALL expose it as the only input a signer is given, and SHALL guarantee that the bytes submitted to the core are byte-identical to the bytes that were signed."
ac_9 = "THE crate SHALL treat any JSON, text or debug rendering as non-canonical and SHALL forbid its use as a hashing, signing or comparison input, and CI SHALL fail if a consensus-critical hash is computed over anything other than canonical bytes."
ac_10 = "WHEN the differential harness runs THE crate SHALL be compared against the C implementation over both the published vectors and randomly generated structures, and any divergence in produced bytes, computed digests or rejection classification SHALL fail the build."

[req.5]
title = "layerx-crypto: Keys, Signing and Custody"
story = "As an agent operator, I want signing to happen where the key lives, under an explicit disclosure of what is being signed, so that neither the daemon nor a compromised tool can obtain signing power it was not deliberately given."
ac_1 = "THE crate layerx-crypto SHALL implement Ed25519 signing and verification for agent identity over domain-separated canonical bytes, SHALL implement secp256k1 verification for Paxeer-facing certificates, and SHALL reject non-canonical or malleable signature encodings exactly as the core does."
ac_2 = "THE crate SHALL define a signer abstraction with at least a local in-process signer, an operating-system keystore signer and a remote signer that never exports private key material, and SHALL make the signing interface identical across them so that key location is a deployment choice rather than a code change."
ac_3 = "WHEN a signer is asked to sign THE crate SHALL provide the exact canonical byte string together with a structured disclosure of what those bytes mean, including activity type, actor, authority, amounts, counterparties, fee limit, expiry and idempotency key, and SHALL make signing opaque bytes without a disclosure impossible through the public API."
ac_4 = "IF the structured disclosure does not decode back to byte-identical canonical bytes THEN THE crate SHALL refuse to sign, because a disclosure that does not match the bytes is a lie about what is being authorised."
ac_5 = "THE crate SHALL hold private key material only in memory that is zeroized on drop, SHALL exclude key material from every debug rendering, log, metric, trace, error message and panic payload, and SHALL fail a build-time check if a key type implements an unredacted debug format."
ac_6 = "WHEN keys are stored at rest THE crate SHALL encrypt them under an operator-supplied secret with an authenticated cipher and a memory-hard key derivation, SHALL bind the ciphertext to the identity and network it belongs to, and SHALL refuse to load a keystore whose authenticated data does not match the expected identity or network."
ac_7 = "WHEN a session key is issued for delegated use THE crate SHALL require an explicit expiry, an explicit permitted activity type set and an explicit revocation sequence to accompany it, SHALL refuse to issue an unbounded session key, and SHALL make the resulting authority exactly the protocol session-key authority rather than a layer-local notion."
ac_8 = "THE crate SHALL enforce domain separation for every signature purpose, SHALL make it impossible to reuse a signature produced for one purpose or network in another, and SHALL bind every signature preimage to network_id and protocol_version."
ac_9 = "THE crate SHALL provide constant-time comparison for secrets and digests and SHALL avoid secret-dependent branching and secret-dependent memory access in its own code paths."
ac_10 = "WHEN a remote signer is used THE crate SHALL treat a refusal, a timeout or an unreachable signer as a refusal to sign, SHALL never fall back to a lower-assurance key, and SHALL surface the failure without producing any partially signed artefact."

[req.6]
title = "layerx-proof: Independent Verification"
story = "As an agent operator, I want every proof the protocol offers to be checkable by my own process, so that trust in a receipt derives from arithmetic I performed rather than from the reputation of whoever handed it to me."
ac_1 = "THE crate layerx-proof SHALL verify a signed receipt from its bytes alone by recomputing its canonical encoding, checking the sequencer signature against the sequencer key authorised for that batch, and confirming internal consistency of amounts, before and after balances and chained state roots."
ac_2 = "THE crate SHALL verify an activity inclusion proof by recomputing activity_merkle_root from the leaf and the proof path under the protocol domain tags, and SHALL reject any proof that relies on reinterpreting an internal node as a leaf or that carries a path length inconsistent with the committed tree."
ac_3 = "THE crate SHALL verify a state inclusion proof against a named resulting_state_root, SHALL verify that root against the batch header it claims, and SHALL verify the batch header signature against the authorised sequencer identity for that batch number."
ac_4 = "THE crate SHALL verify a checkpoint certificate by checking that at least the configured guarantor threshold of distinct bonded guarantor signatures verify over the checkpoint identifier, SHALL reject duplicate signers counted twice, and SHALL report the achieved count and threshold rather than a bare boolean."
ac_5 = "WHEN data availability bytes are retrieved THE crate SHALL verify them by re-hashing the served bytes and comparing against the committed data_availability_root and the corresponding chunk inclusion proof, and SHALL classify a mismatch as an availability failure with the mismatching commitment and the served bytes retained as evidence."
ac_6 = "THE crate SHALL express the outcome of verification as an explicit level, distinguishing at minimum unverified, sequencer-signed, batch-included, state-proven, checkpoint-finalised and settlement-anchored, and SHALL never collapse those levels into a single verified flag."
ac_7 = "THE crate SHALL perform verification without contacting the network, without reading a clock for any verification decision, and without requiring any index or database, so that verification succeeds from bytes on disk in an air-gapped environment."
ac_8 = "IF any verification input is missing, malformed or internally inconsistent THEN THE crate SHALL return a typed verification failure naming the exact check that failed, and SHALL NOT return a partial success or a downgraded level as though the check had been performed."
ac_9 = "THE crate SHALL be tested against negative corpora containing altered amounts, altered recipients, re-signed receipts, truncated proof paths, sub-threshold certificates, duplicated guarantor signatures, swapped leaf and node domains and mismatched roots, and SHALL reject every one of them."
ac_10 = "THE crate SHALL expose verification as a public API usable independently of the rest of the layer, so that a counterparty holding only receipt bytes and the published keys can confirm a payment without running agentd, the SDKs or any LayerX node."

[req.7]
title = "layerx-client: Boundary Client"
story = "As an SDK author, I want one Rust client that speaks the node interface, submits activities, tracks receipts and returns only verified reads, so that every higher layer inherits the same guarantees instead of reimplementing them."
ac_1 = "THE crate layerx-client SHALL implement the node interface for submission, receipt retrieval, verified reads, proof retrieval, data-availability retrieval and event streaming, and SHALL be the only crate in the workspace that opens a connection to the core."
ac_2 = "WHEN the client submits an activity THE client SHALL send exactly the signed canonical bytes it was given, SHALL NOT re-encode, normalise, reorder or augment them, and SHALL fail rather than transmit bytes that differ in any way from the bytes covered by the signature."
ac_3 = "WHEN a submission is acknowledged THE client SHALL treat the acknowledgement as admission evidence only, SHALL NOT treat it as execution, and SHALL consider the activity resolved only when a core-produced receipt for it has been retrieved and verified."
ac_4 = "WHEN a submission is retried THE client SHALL resend byte-identical bytes under the same idempotency key, SHALL treat the return of an existing receipt as the correct outcome of the retry, and SHALL NOT construct a new activity, a new idempotency key or a new sequence for a retry of the same intent."
ac_5 = "IF a submission attempt ends without a determinate outcome THEN THE client SHALL classify it as unknown, SHALL resolve it by receipt lookup under the idempotency key, and SHALL apply bounded jittered backoff to that resolution rather than resubmitting indefinitely."
ac_6 = "WHEN the client performs a read THE client SHALL request the proof material appropriate to the caller requested verification level, SHALL verify it through layerx-proof before returning, and SHALL return the achieved level with every value."
ac_7 = "WHEN the client streams events THE client SHALL deliver them in strict global_sequence order, SHALL detect and report any gap in sequence rather than closing it silently, and SHALL expose a resumable cursor that survives disconnection and process restart."
ac_8 = "THE client SHALL apply explicit deadlines, connection limits and message size limits to every boundary call, SHALL surface backpressure to the caller instead of buffering without bound, and SHALL never let a slow read starve submission delivery."
ac_9 = "THE client SHALL treat every transport-level error class distinctly, separating connection failure, deadline exceeded, protocol incompatibility, unavailable capability, core-side rejection and verification failure, and SHALL never map a verification failure onto a transport error or the reverse."
ac_10 = "THE client SHALL be usable directly by an embedding application with no daemon present, so that the guarantees of this layer do not depend on running agentd."

[req.8]
title = "Agent Identity and Sessions"
story = "As an agent operator, I want the daemon to model agents and sessions explicitly and bind them to protocol identities and session keys, so that authority is always traceable to a DID and a key the state machine can revoke."
ac_1 = "WHEN an agent is registered with the daemon THE daemon SHALL bind it to exactly one LayerX DID that exists in current protocol state, SHALL verify that binding against the core rather than accepting it as configuration, and SHALL refuse registration for an unknown or frozen identity."
ac_2 = "WHEN a session is opened THE daemon SHALL record the agent DID, the tenant, the authority the session may present, the permitted activity types, the expiry, the client that opened it and the policy version in force, and SHALL refuse to open a session that omits any of those."
ac_3 = "THE daemon SHALL derive session authority exclusively from a protocol session key or capability grant already registered in protocol state, SHALL verify that the presented authority is currently valid and unrevoked at open time, and SHALL NOT invent an authority record that has no protocol counterpart."
ac_4 = "WHILE a session is open THE daemon SHALL re-evaluate the underlying protocol authority before every write it prepares, and SHALL refuse to prepare or submit under an authority that has expired, been rotated out, been revoked, or fallen outside its declared scope since the session opened."
ac_5 = "WHEN a session expires, is closed, or its underlying authority is revoked THE daemon SHALL immediately refuse further use of it, SHALL cancel pending prepared activities bound to it that have not yet been submitted, and SHALL NOT retroactively affect activities the protocol already executed."
ac_6 = "THE daemon SHALL issue session tokens that are bound to the tenant, the agent, the scope and the expiry, SHALL treat a token as an authenticator to the daemon only and never as protocol authority, and SHALL make clear in every response which protocol authority was actually used."
ac_7 = "IF a caller presents a token for an agent in a different tenant, or a token whose scope does not name the requested operation, THEN THE daemon SHALL refuse with an explicit authorisation error and SHALL record the refusal in the audit trail."
ac_8 = "THE daemon SHALL support many concurrent sessions per agent and many agents per tenant, SHALL keep per-session state independent, and SHALL ensure that closing one session never disturbs another session sequence, budget accounting or subscription cursor."
ac_9 = "WHEN a primary key rotation or account recovery is observed in protocol state THE daemon SHALL invalidate every session whose authority no longer verifies, SHALL report the invalidation reason to the affected clients, and SHALL NOT continue to serve writes under a superseded key."
ac_10 = "THE daemon SHALL persist session and identity records durably, SHALL restore them on restart, and SHALL revalidate every restored session against protocol state before honouring it rather than trusting its own stored copy."

[req.9]
title = "Capabilities and Scoped Authority"
story = "As an agent operator, I want to hand a sub-agent or a tool a narrow capability and know the narrowing is real, so that the worst case of a compromised tool is bounded by both the daemon and the protocol."
ac_1 = "THE daemon SHALL express a capability as an explicit set of permitted activity types, permitted counterparties, permitted assets, amount ceilings, rate ceilings, purpose constraints and an expiry, and SHALL reject a capability that leaves any of those dimensions unspecified rather than defaulting it open."
ac_2 = "WHEN a capability is used THE daemon SHALL enforce that the prepared activity falls inside every dimension of the capability, SHALL refuse on the first dimension that is exceeded, and SHALL name the exceeded dimension in the refusal."
ac_3 = "THE daemon SHALL guarantee that a capability can only narrow the underlying protocol authority, SHALL reject at creation time any capability that would permit an activity type, asset, counterparty or amount the underlying protocol authority does not already permit, and SHALL never present a capability as authorisation to the core."
ac_4 = "WHEN a capability is attenuated to derive a narrower capability THE daemon SHALL compute the intersection of every dimension, SHALL forbid a derived capability from exceeding its parent in any dimension, and SHALL record the derivation chain so that any exercised capability is traceable to the root authority."
ac_5 = "WHEN a capability, its parent, or the underlying protocol authority is revoked THE daemon SHALL refuse every subsequent use of the whole subtree, and SHALL apply the revocation to prepared but unsubmitted activities."
ac_6 = "THE daemon SHALL evaluate capability checks deterministically from recorded state and the prepared activity, SHALL NOT consult ambient context such as caller network address, process identity or wall-clock convenience for the decision, and SHALL produce the identical decision for the identical inputs."
ac_7 = "IF a capability names a spending ceiling THEN THE daemon SHALL account consumption against it from verified receipts rather than from submission attempts, SHALL count a failed activity as unconsumed, and SHALL never let concurrent uses of the same capability exceed the ceiling in aggregate."
ac_8 = "WHERE the protocol offers an equivalent enforceable construct, including capability grants, session key scopes, budgets and payer grants, THE daemon SHALL prefer to express the restriction in protocol state so that it is enforced by the state machine, and SHALL record which restrictions are protocol-enforced and which are daemon-enforced only."
ac_9 = "THE daemon SHALL make every capability decision visible in the audit trail with the capability identifier, its derivation chain, the decision, the deciding dimension and the resulting activity identifier when one was produced."
ac_10 = "THE daemon SHALL treat a daemon-enforced-only restriction as advisory in its reporting, SHALL state plainly in the capability report that it binds only while the daemon is in the path, and SHALL NOT describe it as a protocol guarantee."

[req.10]
title = "Budgets and Spend Control"
story = "As an agent operator, I want spending caps that hold even when the daemon is bypassed, restarted or raced, so that a runaway agent cannot spend past the limit I set by exploiting a gap between my controls and the ledger."
ac_1 = "WHEN an operator sets a spending limit THE daemon SHALL offer to express it as a protocol budget or capability grant so that the state machine enforces it, SHALL create that protocol object through an ordinary signed activity, and SHALL report the resulting protocol object identifier."
ac_2 = "WHERE a limit exists only in the daemon THE daemon SHALL label it explicitly as daemon-enforced, SHALL state that bypassing the daemon bypasses the limit, and SHALL NOT report it as equivalent to a protocol budget."
ac_3 = "THE daemon SHALL derive budget consumption exclusively from verified receipts and protocol budget state, SHALL treat its own local accounting as a cache that is reconciled against the core, and SHALL correct itself to the protocol figure whenever the two differ."
ac_4 = "WHEN a prepared activity would exceed a limit THE daemon SHALL refuse to prepare or submit it, SHALL name the limit, the ceiling, the consumed amount and the requested amount in the refusal, and SHALL leave no reservation behind for a refused request."
ac_5 = "WHILE multiple concurrent requests draw on one limit THE daemon SHALL serialise the reservation decision so that concurrent or reordered requests cannot exceed the ceiling in aggregate, and SHALL release a reservation deterministically when the activity reaches a terminal state or the reservation window expires."
ac_6 = "IF the daemon cannot determine the terminal state of an activity that held a reservation THEN THE daemon SHALL keep the reservation held and the amount unavailable until the receipt is resolved, and SHALL NOT release it optimistically."
ac_7 = "WHEN a recurring budget window rolls over THE daemon SHALL take the window boundary and the remaining allowance from protocol state rather than from a local timer, so that its view of the window matches the deterministic protocol view exactly."
ac_8 = "THE daemon SHALL support limits scoped to a tenant, an agent, a session, a capability and a counterparty simultaneously, SHALL apply every applicable limit, and SHALL refuse if any single one is exceeded."
ac_9 = "THE daemon SHALL expose the reconciliation state of every budget, including the last verified receipt applied, the protocol figure, the local figure and any divergence, and SHALL raise an explicit alert rather than silently adopting the local figure when they diverge."
ac_10 = "WHEN the daemon restarts THE daemon SHALL rebuild spend accounting from persisted receipts and protocol state before accepting a write, and SHALL refuse writes against a limit whose accounting has not yet been reconciled."

[req.11]
title = "Policy Enforcement"
story = "As an agent operator, I want a deterministic, explainable policy engine that refuses by default, so that I can reason about what my agents may do and prove afterwards why any particular action was allowed or refused."
ac_1 = "THE daemon SHALL evaluate every write request against a policy set before preparation, SHALL deny by default when no rule permits the request, and SHALL never allow a request merely because no rule denied it."
ac_2 = "THE policy engine SHALL evaluate deterministically from the request, the session, the capability, the reconciled budget state and the loaded policy version alone, SHALL NOT consult wall-clock convenience, random values, load or non-deterministic ordering, and SHALL yield the identical decision for identical inputs."
ac_3 = "WHEN a policy decision is produced THE daemon SHALL record the decision, the policy version, every rule that matched, the deciding rule and the reason, and SHALL make that record retrievable for the lifetime of the audit retention window."
ac_4 = "THE daemon SHALL support a dry-run evaluation that returns the decision and its explanation without preparing, signing or submitting anything, and SHALL guarantee dry-run has no side effect other than an audit entry."
ac_5 = "WHEN a policy set is updated THE daemon SHALL version it, SHALL apply a new version only to requests received after activation, SHALL retain prior versions for audit reconstruction, and SHALL validate a policy set before activation, rejecting one that fails validation while continuing under the previous version."
ac_6 = "IF policy evaluation fails, times out or encounters an internal error THEN THE daemon SHALL deny the request, because a policy engine that fails open is not a control."
ac_7 = "THE policy engine SHALL be able to constrain at least activity type, counterparty, asset, amount, cumulative rate, purpose, capability, session, agent, tenant, time window and required approval, and SHALL express thresholds as exact integers rather than floating-point values."
ac_8 = "WHERE a policy requires human or external approval THE daemon SHALL hold the request in an explicit awaiting-approval state, SHALL expire it deterministically if approval does not arrive within its declared window, and SHALL never auto-approve on expiry."
ac_9 = "THE daemon SHALL make plain in every policy decision that policy is a local restriction, SHALL NOT represent a policy allow as protocol authorisation, and SHALL still submit only activities the protocol independently authorises."
ac_10 = "THE daemon SHALL ship a policy test harness that evaluates a policy set against recorded request corpora and reports the decisions, so that an operator can verify a policy change before activating it."

[req.12]
title = "Activity Preparation and Local Signing"
story = "As an agent operator, I want preparation and signing to be separate steps with a disclosure in between, so that whatever signs can see exactly what it is authorising and the daemon can never obtain a signature for something it did not show."
ac_1 = "WHEN a caller requests preparation THE daemon SHALL construct the complete unsigned canonical activity, including actor, authority, account_sequence, timestamp_bound, idempotency_key, fee_limit, payload and payload_hash, SHALL return its exact canonical bytes, and SHALL return the domain-separated byte string a signature must cover."
ac_2 = "WHEN preparation returns THE daemon SHALL accompany the bytes with a structured disclosure of their meaning, including activity type, actor, authority, every counterparty, every amount, the asset, the fee limit, the expiry and the idempotency key, and SHALL guarantee the disclosure was decoded from the same bytes rather than assembled from the request."
ac_3 = "THE daemon SHALL take account_sequence and every other protocol-derived preparation input from core state, SHALL NOT guess or increment a sequence locally beyond what the core reports, and SHALL fail preparation when the required core state is unavailable rather than assuming a value."
ac_4 = "WHEN an activity is signed THE signing component SHALL sign exactly the bytes preparation produced, and THE daemon SHALL verify before submission that the signature verifies over the exact bytes it is about to send, refusing to submit otherwise."
ac_5 = "IF any field of a prepared activity is altered after signing THEN THE daemon SHALL detect the alteration through signature verification against the exact bytes and SHALL refuse submission, so that a modified activity can never be submitted under an earlier signature."
ac_6 = "THE daemon SHALL support external signing, in which preparation output leaves the daemon, a signature is produced elsewhere and returned, and the daemon submits without ever holding the key, and SHALL treat this as the default posture for agent primary keys."
ac_7 = "WHERE the daemon is provisioned with a protocol session key it MAY sign directly, and in that case THE daemon SHALL confine its signing to the activity types, expiry and scope the protocol session key declares, and SHALL record every self-signed activity distinctly in the audit trail."
ac_8 = "WHEN preparation sets timestamp_bound and fee_limit THE daemon SHALL derive them from explicit caller input or explicit configured defaults, SHALL include them in the disclosure, and SHALL never widen a caller supplied bound."
ac_9 = "THE daemon SHALL expire prepared activities that are not submitted within their declared window, SHALL release any reservation they held, and SHALL refuse to submit a prepared activity whose timestamp_bound has already passed relative to the core reported batch time."
ac_10 = "THE daemon SHALL NOT store signed activity bytes beyond the retention needed to complete submission and receipt resolution, SHALL store no private key material at any point in this flow, and SHALL redact payload contents in logs according to the configured redaction policy while keeping the activity identifier."

[req.13]
title = "Submission, Idempotency and Receipt Tracking"
story = "As an agent operator, I want submission to be exactly-once in effect and honest about uncertainty, so that a network failure costs me a retry rather than a duplicate payment or a false report of success."
ac_1 = "WHEN an activity is submitted THE daemon SHALL record it in a durable outbox before transmission, SHALL transmit the exact signed bytes, and SHALL retain the record until the activity reaches a terminal state of executed, failed, expired or superseded."
ac_2 = "THE daemon SHALL track each submission through an explicit state machine of prepared, signed, queued, submitted, acknowledged, unknown, executed, failed and expired, SHALL record every transition with its cause, and SHALL expose the current state and its evidence to the caller."
ac_3 = "WHEN a submission is retried THE daemon SHALL resend byte-identical bytes carrying the original idempotency_key, SHALL NOT re-prepare or re-sign the activity, and SHALL treat an existing receipt returned by the core as the authoritative outcome of the retry."
ac_4 = "IF the daemon cannot determine whether a submission was executed THEN THE daemon SHALL hold the submission in unknown, SHALL continue resolution attempts under bounded jittered backoff, SHALL keep any associated reservation held, and SHALL report unknown to the caller rather than success or failure."
ac_5 = "WHEN a receipt is obtained THE daemon SHALL verify it through layerx-proof before recording a terminal state, SHALL store the exact receipt bytes, and SHALL make the stored bytes retrievable unchanged by activity identifier, idempotency key and global sequence."
ac_6 = "THE daemon SHALL provide caller-supplied idempotency keys for its own request surface, SHALL return the original result for a repeated request carrying a key already settled, and SHALL reject with a conflict error a repeated key whose request body differs from the original."
ac_7 = "WHEN the daemon restarts THE daemon SHALL recover its outbox, SHALL resume resolution of every non-terminal submission, and SHALL neither resubmit an activity already executed nor drop an activity that was signed and queued but never transmitted."
ac_8 = "WHEN a receipt is later covered by a checkpoint THE daemon SHALL augment its stored record with the activity inclusion proof, the state inclusion proof, the checkpoint identifier, the guarantor certificate and the settlement reference, SHALL verify each before recording it, and SHALL NOT alter any pre-checkpoint field of the receipt."
ac_9 = "THE daemon SHALL expose the verification level of every tracked submission, SHALL allow a caller to wait for a declared level with an explicit deadline, and SHALL return the level actually reached when the deadline elapses rather than an assumed one."
ac_10 = "IF an activity is rejected by the core THEN THE daemon SHALL record the exact result code, SHALL classify it as retriable or terminal from the protocol taxonomy rather than by guesswork, SHALL NOT retry a terminal rejection, and SHALL surface the code unmodified to the caller."

[req.14]
title = "Verified Reads: Balances, History, Checkpoints and Proofs"
story = "As an agent operator, I want every read to arrive with the evidence that makes it true, so that my agents act on protocol facts rather than on a cache the middleware assembled."
ac_1 = "WHEN a balance is requested THE layer SHALL return the value derived from core-produced state together with the account identifier, the asset, the global sequence and the batch or checkpoint the value is relative to, and SHALL return the verification level actually achieved."
ac_2 = "WHERE a caller requests a proven balance THE layer SHALL retrieve and verify a state inclusion proof against the named state root and verify that root through its batch header signature or its checkpoint certificate, and SHALL refuse to answer at that level rather than downgrade silently."
ac_3 = "WHEN history is requested THE layer SHALL return core-produced canonical bytes for activities, receipts and events in strict global_sequence order, SHALL provide stable cursors for pagination, and SHALL guarantee that a resumed page continues exactly where the previous page ended with no gap and no repetition."
ac_4 = "WHEN a caller re-hashes served history bytes THE layer SHALL have preserved them exactly, so that recomputation reproduces the committed activity, receipt, event and oracle roots for the batches covered."
ac_5 = "WHEN a checkpoint is requested THE layer SHALL return the checkpoint certificate, its header commitments, the guarantor signature set with the achieved count and threshold, and the settlement reference where one exists, and SHALL verify the certificate before returning it."
ac_6 = "THE layer SHALL expose the current chain head, the latest sealed batch and the latest finalised checkpoint with every read that is sensitive to freshness, so that staleness is a value the caller can see rather than an assumption."
ac_7 = "WHERE the layer caches a protocol value THE layer SHALL cache the core-produced bytes together with their verification evidence, SHALL revalidate against the core before serving a value past its declared freshness bound, and SHALL never serve a cached value at a verification level higher than the evidence it holds."
ac_8 = "IF the core cannot serve a read THEN THE layer SHALL return an unavailable error naming the missing capability or range, and SHALL NOT reconstruct the answer from partial data, from its own projection, or from a previous unrelated response."
ac_9 = "THE layer SHALL never return a computed aggregate as a protocol fact without labelling it, and WHERE it offers derived views such as totals, spend summaries or position rollups THE layer SHALL identify each contributing receipt so the aggregate is reconstructible and auditable."
ac_10 = "THE layer SHALL provide an export that emits the receipts, proofs, certificates and headers needed for a third party to verify a stated set of facts offline with layerx-proof and no access to the layer."

[req.15]
title = "Data Availability Retrieval"
story = "As an agent operator, I want to fetch the underlying data behind a checkpoint and confirm it hashes to what was committed, so that I can replay history myself instead of trusting that somebody still has it."
ac_1 = "THE layer SHALL retrieve availability data by checkpoint identifier, batch number, global sequence range or activity identifier through the node interface, and SHALL return the canonical bytes as served."
ac_2 = "WHEN availability bytes are retrieved THE layer SHALL verify each chunk against its inclusion proof under the committed data_availability_root and SHALL verify the reassembled bytes re-hash to the committed roots before treating the retrieval as successful."
ac_3 = "IF served bytes do not verify THEN THE layer SHALL classify the retrieval as an availability failure, SHALL retain the served bytes and the mismatching commitment as evidence, SHALL report which check failed, and SHALL NOT return the bytes as valid history."
ac_4 = "THE layer SHALL retrieve the five availability classes the protocol names, being the activity batch, the receipts, the oracle inputs, the state-diff material and the recovery metadata, and SHALL report which classes were obtained and which were missing."
ac_5 = "THE layer SHALL support retrieval from multiple providers, SHALL treat a provider that serves non-verifying bytes as failed for that request, and SHALL never merge bytes from two providers into one unverified result."
ac_6 = "WHEN retrieval is used to support replay THE layer SHALL emit the retrieved history in a form an independent verifier can consume without any index or database, and SHALL document the exact ordering and framing it produces."
ac_7 = "THE layer SHALL bound retrieval by explicit size, chunk-count and deadline limits, SHALL stream large retrievals rather than buffering them whole, and SHALL surface progress and partial completion honestly."
ac_8 = "IF availability data behind an unfinalised checkpoint cannot be obtained THEN THE layer SHALL report the checkpoint as unavailable for verification purposes and SHALL NOT report any fact at checkpoint-finalised level against it."
ac_9 = "THE layer SHALL record every availability failure in the audit trail with the checkpoint, the batch, the chunk index, the provider and the failing check, so that repeated withholding is visible rather than lost in retries."
ac_10 = "THE layer SHALL make retrieval and verification available as a standalone operation, so that an operator can audit availability without submitting any activity or holding any key."

[req.16]
title = "Streaming Events and Durable Subscriptions"
story = "As an agent operator, I want an event stream that is ordered, resumable and honest about gaps, so that my agents can react to protocol effects without polling and without silently missing one."
ac_1 = "THE layer SHALL stream protocol events derived only from core-produced event records, SHALL deliver them in strict global_sequence order, and SHALL NOT synthesize, infer, reorder or enrich an event with a value the core did not produce."
ac_2 = "WHEN a subscription is created THE layer SHALL record it durably with its filter, its start position, its delivery target and its owning tenant, agent and scope, and SHALL restore and resume it after a daemon restart from its last acknowledged cursor."
ac_3 = "WHEN events are delivered THE layer SHALL guarantee at-least-once delivery with a monotonic cursor, SHALL include a deduplication identifier derived from the event identity, and SHALL document plainly that consumers must deduplicate on it."
ac_4 = "IF a gap in global_sequence is detected THEN THE layer SHALL surface it as an explicit gap event naming the missing range, SHALL attempt backfill from the core, and SHALL NOT continue delivering past the gap as though the stream were contiguous."
ac_5 = "WHEN a subscriber is slow or unreachable THE layer SHALL apply bounded buffering and explicit backpressure, SHALL retry with bounded jittered backoff, SHALL mark the subscription as lagging with its lag in sequences, and SHALL never drop an event silently to keep up."
ac_6 = "THE layer SHALL support filtering by agent, account, activity type, module, asset, counterparty and result class, SHALL evaluate filters deterministically, and SHALL apply tenant and scope restrictions before any filter so that a filter can never widen visibility."
ac_7 = "WHEN a subscription is created from a historical position THE layer SHALL backfill from durable history and transition to live delivery without a gap or a duplicate at the seam, and SHALL make the transition point observable."
ac_8 = "WHERE events are delivered to an external endpoint THE layer SHALL authenticate the delivery, SHALL sign or otherwise bind the payload so a receiver can confirm origin, SHALL include the underlying receipt or event bytes needed for verification, and SHALL retry failures without duplicating side effects on the receiving side beyond at-least-once semantics."
ac_9 = "THE layer SHALL expose subscription health including cursor position, lag, last delivery, failure count and last error, and SHALL retain undelivered events only within a declared, enforced retention bound after which the subscription is marked truncated rather than silently repaired."
ac_10 = "WHEN a subscription is deleted or its owning session, capability or tenant is revoked THE layer SHALL stop delivery immediately, SHALL retain its audit record, and SHALL NOT deliver buffered events after revocation."

[req.17]
title = "MCP Servers and Scoped Tools"
story = "As an agent operator, I want model-facing tools that are scoped, auditable and incapable of asserting an unbacked result, so that connecting an LLM to my funds is bounded by the same controls as any other client."
ac_1 = "THE crate layerx-mcp SHALL expose the agent-api contract as MCP tools, SHALL separate read tools from write tools explicitly, and SHALL declare for every tool the scope it requires, the mutations it can cause and the evidence it returns."
ac_2 = "WHEN an MCP server starts THE server SHALL bind to exactly one tenant and one scope set, SHALL derive its authority from a session and capability created through the normal daemon path, and SHALL expose no tool outside that scope."
ac_3 = "WHEN a read tool returns THE tool SHALL return only core-produced values with their verification level attached, and SHALL NOT return a summary, an estimate or an inference presented as a protocol fact."
ac_4 = "WHEN a write tool is invoked THE tool SHALL follow the prepare, disclose, sign, submit and track path exactly as any other client, SHALL return the receipt or the honest non-terminal state, and SHALL NOT report success before a verified receipt exists."
ac_5 = "WHERE a write tool would exceed a configured threshold THE server SHALL require explicit approval before submission, SHALL present the disclosure to the approver, and SHALL expire an unapproved request deterministically rather than proceeding."
ac_6 = "THE server SHALL treat all tool arguments and all model-supplied text as untrusted input, SHALL validate them against the contract schema before use, and SHALL NOT allow instructions embedded in data, resource content or tool results to widen scope, alter a capability, change an approval requirement or redirect a counterparty."
ac_7 = "THE server SHALL apply the same policy, capability, budget, rate limit and audit path as every other client, SHALL be incapable of bypassing them, and SHALL record every tool invocation with its arguments digest, decision and outcome."
ac_8 = "IF a tool cannot complete THEN THE tool SHALL return a typed error naming the failing stage and the protocol result code where one exists, and SHALL NOT return prose that a model could mistake for a completed action."
ac_9 = "THE server SHALL bound tool result size, SHALL paginate large results with stable cursors, and SHALL never truncate a result silently in a way that could read as completeness."
ac_10 = "THE server SHALL support read-only deployment in which every write tool is absent rather than merely refused, so that a read-only integration cannot be talked into a mutation."

[req.18]
title = "Rust SDK and Generated TypeScript and Python SDKs"
story = "As an application developer, I want three SDKs that behave identically because they are generated from one contract, so that a guarantee I rely on in Rust is not quietly missing in TypeScript or Python."
ac_1 = "THE workspace SHALL define the agent-api contract as a single versioned schema artefact from which the Rust SDK types, the TypeScript SDK and the Python SDK are all derived, and SHALL treat that schema as the source of truth for the client surface."
ac_2 = "THE Rust SDK SHALL expose identity and session management, capability and budget management, policy dry-run, activity preparation, signing integration, submission and tracking, verified reads, proof verification, availability retrieval and subscriptions, and SHALL be usable with or without a running daemon."
ac_3 = "THE TypeScript and Python SDKs SHALL be generated from the schema by a generator in this workspace, SHALL be regenerated and diffed in CI, and a hand-edit to generated output SHALL fail the build."
ac_4 = "THE generated SDKs SHALL preserve the guarantees of this layer, including verification levels on every read, honest unknown submission state, idempotency key handling, typed protocol result codes and refusal to present unverified values as results."
ac_5 = "WHEN a generated SDK represents an amount or any consensus-critical integer THE SDK SHALL use an exact integer representation in that language, SHALL NOT use a floating-point number for it, and SHALL fail rather than lose precision at the language boundary."
ac_6 = "THE workspace SHALL run a cross-SDK parity suite that exercises the identical scenarios through the Rust, TypeScript and Python SDKs against the same daemon and asserts identical observable behaviour, including identical error taxonomy and identical verification levels."
ac_7 = "WHEN the contract schema changes THE workspace SHALL enforce compatibility rules, SHALL permit only additive changes within a major version, SHALL fail CI on a breaking change that does not carry a major version increment, and SHALL publish a compatibility matrix of SDK version against contract version against node interface version."
ac_8 = "THE SDKs SHALL NOT embed private key handling that exports key material, SHALL integrate with external signers through an explicit interface, and SHALL make the default posture one in which the SDK never holds an agent primary key."
ac_9 = "THE SDKs SHALL ship runnable examples covering payment with receipt verification, an HTTP 402 style settlement, budget-constrained delegated spending, a service lifecycle interaction and offline verification of a receipt by a counterparty."
ac_10 = "THE SDKs SHALL document precisely which guarantees are protocol-enforced and which are daemon-enforced, and SHALL NOT describe a daemon-enforced restriction as a protocol guarantee anywhere in generated documentation."

[req.19]
title = "Rate Limits, Quotas and Backpressure"
story = "As a platform operator, I want limits that protect the node and separate tenants from each other, so that one agent cannot degrade the service for everyone or push load into the core that the core did not ask for."
ac_1 = "THE daemon SHALL apply configurable rate limits per tenant, per agent, per session, per capability and per operation class, SHALL apply every applicable limit, and SHALL refuse when any one is exceeded."
ac_2 = "WHEN a limit is exceeded THE daemon SHALL return a typed rate-limit error carrying the limit, the window, the remaining quota and a retry-after hint, and SHALL NOT queue the request indefinitely in place of refusing it."
ac_3 = "THE daemon SHALL enforce limits deterministically from recorded counters and the request, SHALL make the decision independent of instance-local timing artefacts where the deployment is multi-instance, and SHALL document the consistency model of its counters plainly."
ac_4 = "THE daemon SHALL apply admission control to outbound boundary traffic so that retries, backfills and subscription catch-up cannot saturate the node interface, and SHALL prioritise submission delivery and receipt resolution over bulk reads under contention."
ac_5 = "WHEN the core signals backpressure or unavailability THE daemon SHALL propagate it to callers as backpressure rather than absorbing it into unbounded internal queues, and SHALL bound every internal queue with an explicit capacity and a defined overflow behaviour."
ac_6 = "THE daemon SHALL bound every request by an explicit deadline, SHALL cancel downstream work when a caller disconnects, and SHALL never leave an orphaned submission whose fate nobody is resolving."
ac_7 = "THE daemon SHALL apply quotas to durable resources including subscriptions, prepared activities, outbox entries, audit retention and stored receipts, and SHALL refuse creation past a quota rather than degrading an existing tenant."
ac_8 = "IF a retry storm, a hot loop or a pathological client is detected THEN THE daemon SHALL shed load for that client specifically, SHALL record the shedding decision, and SHALL NOT shed load for unrelated tenants to compensate."
ac_9 = "THE daemon SHALL never let a rate limit, a quota or shedding cause a duplicate economic effect, so that a refused retry leaves the idempotency record and the outbox in a state that resolves to exactly one outcome."
ac_10 = "THE daemon SHALL expose current limit configuration and live utilisation per tenant so that a limit refusal is diagnosable without reading logs."

[req.20]
title = "Audit Trail and Observability"
story = "As a compliance owner, I want an append-only record of every decision and every mutation attempt with its evidence, so that I can reconstruct what happened without trusting a narrative assembled after the fact."
ac_1 = "THE daemon SHALL record in an append-only audit log every authentication, session lifecycle event, capability decision, policy decision, budget reservation, preparation, signature request, submission, terminal outcome, subscription change, configuration change and administrative action."
ac_2 = "WHEN an audit entry is written THE entry SHALL carry the tenant, the agent, the session, the capability, the policy version, the request identifier, the idempotency key, the decision, the reason, the resulting activity identifier where one exists and the verification level reached."
ac_3 = "THE audit log SHALL be append-only and tamper-evident through a hash chain over its entries, SHALL make any excision or reordering detectable, and SHALL be verifiable by a tool shipped with the workspace."
ac_4 = "THE daemon SHALL never write private key material, full signed activity payload contents beyond the configured retention, session token values, or unredacted secret configuration into audit entries, logs, metrics, traces or error messages."
ac_5 = "WHEN an activity reaches a terminal state THE audit trail SHALL be sufficient to reconstruct why it was allowed, what exact bytes were submitted, which authority was used, and what the core returned, using the audit entries and stored receipt bytes alone."
ac_6 = "THE daemon SHALL emit metrics covering submission outcomes by result class, unknown-state population and age, verification levels achieved, boundary latency and error classes, policy and capability decisions, budget utilisation, subscription lag and rate-limit refusals, and SHALL label them by tenant without embedding high-cardinality identifiers such as activity identifiers in metric labels."
ac_7 = "THE daemon SHALL emit distributed traces that span the request from caller through policy, preparation, signing, submission and receipt resolution, and SHALL propagate a request identifier that appears in logs, traces and audit entries alike."
ac_8 = "THE daemon SHALL expose health surfaces distinguishing liveness, readiness to accept writes, boundary connectivity, verification backlog, unknown-state backlog and degraded modes, and SHALL report not-ready rather than accepting writes it cannot deliver."
ac_9 = "IF the audit log cannot be written THEN THE daemon SHALL refuse the operation that would have been audited, because an unaudited mutation attempt is not permitted."
ac_10 = "THE daemon SHALL support exporting an audit slice for a tenant, an agent or a time range together with the receipts and proofs it references, so that an external reviewer can verify the protocol facts independently."

[req.21]
title = "Tenant Isolation"
story = "As a platform operator, I want tenants separated in keys, data, limits and failure domains, so that one tenant can neither observe another nor degrade another through any path the layer exposes."
ac_1 = "THE daemon SHALL scope every stored object, including identities, sessions, capabilities, budgets, policies, prepared activities, outbox entries, receipts, subscriptions and audit entries, to exactly one tenant, and SHALL make a cross-tenant read or write structurally impossible in the storage access path rather than filtered at the edge."
ac_2 = "WHEN any request is served THE daemon SHALL resolve the tenant from the authenticated principal, SHALL ignore any tenant identifier supplied in the request body or headers as authoritative, and SHALL refuse a request whose target object belongs to another tenant."
ac_3 = "THE daemon SHALL isolate key material and signer configuration per tenant, SHALL never allow a tenant to use another tenant signer, and SHALL scope every external signer binding to its tenant."
ac_4 = "THE daemon SHALL apply limits, quotas and load shedding per tenant so that one tenant exhausting its quota does not consume another tenant capacity, and SHALL reserve capacity for submission delivery and receipt resolution across tenants under contention."
ac_5 = "THE daemon SHALL scope subscriptions, streams and MCP servers to a tenant, and SHALL guarantee that no filter, cursor, backfill or error message can expose an event, an identifier or an existence fact from another tenant."
ac_6 = "IF an internal error occurs THEN THE daemon SHALL ensure the error message, the trace and the metric labels carry no data belonging to another tenant, and SHALL NOT include raw object identifiers from a failed cross-tenant lookup."
ac_7 = "THE daemon SHALL support per-tenant configuration of policy, redaction, retention, verification level defaults and approval requirements, and SHALL apply a tenant configuration only to that tenant."
ac_8 = "THE workspace SHALL include an isolation test suite that attempts cross-tenant access through every public surface, including the API, the SDKs, the MCP tools, subscriptions, exports and error paths, and SHALL treat any leakage, including a distinguishable existence or timing signal in the API surface, as a build-breaking defect."
ac_9 = "WHEN a tenant is deleted THE daemon SHALL remove or irreversibly key-destroy its data according to the configured retention policy, SHALL retain only what an explicitly configured legal retention requires, and SHALL record the deletion in the audit trail."
ac_10 = "THE daemon SHALL make tenancy visible in every audit entry, metric and trace so that isolation is observable rather than asserted."

[req.22]
title = "Deployment, Configuration and Degraded Modes"
story = "As a platform operator, I want the daemon to be explicit about its configuration and honest when it is degraded, so that a partial outage produces refusals I can see rather than answers I should not have trusted."
ac_1 = "THE daemon SHALL load configuration from explicit files and environment with a documented precedence, SHALL validate it fully at startup, and SHALL refuse to start on an invalid, ambiguous or unsafe configuration rather than substituting a default."
ac_2 = "THE daemon SHALL require explicit configuration of network_id, node endpoint, expected protocol version, tenant set, policy source, signer configuration and verification level defaults, and SHALL treat an unset security-relevant setting as a startup failure rather than a permissive default."
ac_3 = "WHEN the daemon starts THE daemon SHALL perform the boundary handshake, SHALL report the node interface version, protocol version, network identifier, capability intersection and any missing capability, and SHALL refuse writes until the handshake succeeds."
ac_4 = "IF the core becomes unreachable THEN THE daemon SHALL enter a degraded read mode in which it serves only values it can still verify, marks their staleness, refuses preparation that needs live core state, refuses submission acknowledgement, and continues resolving unknown submissions when connectivity returns."
ac_5 = "IF the core reports a halted, emergency or data-unavailable mode THEN THE daemon SHALL propagate that state in its own health and read responses, SHALL refuse to report facts at a verification level the core state no longer supports, and SHALL NOT continue to advertise finality it cannot verify."
ac_6 = "WHEN the daemon is upgraded THE daemon SHALL migrate its local stores forward with an explicit versioned migration, SHALL refuse to start against a store from a newer version, and SHALL never require a receipt, proof or protocol fact to be re-derived from anything other than the core during migration."
ac_7 = "THE workspace SHALL publish a compatibility matrix of daemon version against node interface version against contract version against SDK version, and SHALL verify at least the supported range in CI against a real node."
ac_8 = "THE daemon SHALL support graceful shutdown that stops accepting new work, completes or durably records in-flight submissions, flushes audit entries, and exits without leaving a submission whose state is unrecoverable from the outbox."
ac_9 = "THE daemon SHALL provide an operator surface to inspect and act on unknown submissions, stalled subscriptions, budget divergence and verification backlog, and SHALL make every operator action itself audited and non-mutating with respect to protocol state."
ac_10 = "THE daemon SHALL expose no administrative path that alters a recorded receipt, a verification level, an audit entry or a protocol-derived value, so that no operator action can make the layer assert something the core did not produce."

[req.23]
title = "Agent Layer Qualification and Conformance"
story = "As a protocol steward, I want the interaction layer qualified against the real core under adversarial conditions, so that its guarantees are demonstrated rather than described."
ac_1 = "THE workspace SHALL run a differential conformance suite that compares layerx-wire encodings, digests, identifiers and rejection classifications against the C reference implementation over the published vectors and generated inputs, and SHALL fail the build on any divergence."
ac_2 = "THE workspace SHALL run a boundary conformance suite against a real running node covering every node interface message, its error cases, its streaming behaviour, its version negotiation and its capability reporting, and SHALL NOT satisfy it with an in-process substitute for the node."
ac_3 = "THE workspace SHALL run a no-fabrication suite in which a deliberately hostile node returns altered balances, altered receipts, re-signed receipts, sub-threshold certificates, truncated proofs, reordered events and withheld availability data, and SHALL demonstrate that the layer reports a verification failure or unavailability in every case and never surfaces the altered value as a result."
ac_4 = "THE workspace SHALL run a fault-injection suite covering connection loss at every stage of submission, process kill between signing and transmission, duplicate delivery, delayed acknowledgement, restart mid-resolution and clock disturbance, and SHALL demonstrate exactly-once economic effect and honest unknown reporting in every case."
ac_5 = "THE workspace SHALL prove idempotency end to end by submitting the same intent through repeated retries, concurrent duplicates and post-restart resubmission and asserting exactly one receipt and one economic effect."
ac_6 = "THE workspace SHALL run the cross-tenant isolation suite, the policy adversarial suite and the MCP untrusted-input suite as build gates, and SHALL treat any escape as a build-breaking defect rather than a tracked issue."
ac_7 = "THE workspace SHALL fuzz every parser and every externally reachable decoder, including the wire codec, the node interface framing, the contract request surface and the policy loader, and SHALL treat a panic, hang or unbounded allocation as a defect."
ac_8 = "THE workspace SHALL run a soak test that sustains submission, streaming and verification load for an extended period and asserts no unbounded memory growth, no unbounded queue growth, no leaked reservations and no growing population of unresolved unknown submissions."
ac_9 = "THE workspace SHALL forbid unsafe code outside an explicitly enumerated and justified allowlist, SHALL run the test suite under the address and thread sanitizers where the toolchain supports it, and SHALL fail the build on a sanitizer report."
ac_10 = "THE workspace SHALL publish, as a release gate, a qualification report stating which guarantees are protocol-enforced, which are daemon-enforced, which suites were run, what the results were and which capabilities the node did not expose, and SHALL make an unmet gate block release rather than being waived silently."

# --------------------------------------------------------------------------
# TASKS
# --------------------------------------------------------------------------

[task.1]
section = "Wave 1 - Foundations and Domain Types"
title = "Establish the Rust agent workspace and its engineering gates"
status = "pending"

[task.1.1]
title = "Create the agent workspace and its build entry points"
status = "done"
wave = 1
do_1 = "Create the Cargo workspace at agent/ with the nine member crates layerx-types, layerx-wire, layerx-crypto, layerx-proof, layerx-client, layerx-agent-api, layerx-agentd, layerx-mcp and layerx-sdk, each with its declared purpose and no member depending on a crate above it in that order."
do_2 = "Pin the Rust edition, the minimum supported toolchain and the resolver in the workspace manifest, and commit a toolchain file so every machine and CI runner builds with the identical compiler."
do_3 = "Add the agent-build, agent-test, agent-lint, agent-fuzz and agent-check make targets to the repository Makefile, each delegating to cargo inside agent/ and none of them entering the C build."
do_4 = "Keep the C build untouched: prove that make build and the existing protocol test targets succeed with the agent workspace present and that no C target gains a Rust dependency."
do_5 = "Add a workspace README fragment that states the non-authority rule and the boundary rule as the first two paragraphs a contributor reads."
verify_cmd = "make agent-build"
symbols = ["agent_workspace_manifest","agent_toolchain_pin"]
touches = ["agent/Cargo.toml","agent/rust-toolchain.toml","agent/README.md","Makefile"]
reqs = ["1.1","2.2"]

[task.1.2]
title = "Lock the dependency, unsafe and supply-chain policy"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Forbid unsafe code across the workspace by default and enumerate every permitted exception with its justification in a single allowlist file that CI reads, so a new unsafe block cannot appear without editing that file."
do_2 = "Configure the dependency policy to deny unmaintained, yanked and vulnerable crates, to require a license allowlist, and to fail the build rather than warn."
do_3 = "Forbid by policy any dependency that provides SQLite, that links the C core, that performs process-wide global initialisation, or that reads the ambient clock inside a verification path."
do_4 = "Enable the strict lint set including deny on unwrap and expect outside tests, on panicking arithmetic in protocol paths, and on implicit float conversion, and fail the build on warnings."
do_5 = "Add the supply-chain and lint gates to make agent-lint and to CI so the policy is enforced on every change rather than at review time."
verify_cmd = "make agent-lint"
symbols = ["agent_unsafe_allowlist","agent_dependency_policy"]
touches = ["agent/deny.toml","agent/clippy.toml","agent/unsafe-allowlist.toml",".github/workflows/agent.yml"]
reqs = ["23.9","2.2","5.5"]

[task.1.3]
title = "Define the layer error model and the protocol result-code mapping"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Define the layer error taxonomy with disjoint classes for transport failure, deadline, protocol incompatibility, unavailable capability, core rejection, verification failure, policy refusal, capability refusal, budget refusal, rate limit and internal fault."
do_2 = "Define the protocol result-code type that preserves the exact numeric code and its domain partition, mapping known codes to typed variants and preserving unknown codes verbatim rather than collapsing them."
do_3 = "Classify every protocol result code as terminal or retriable from the protocol taxonomy, and make the classification a data table derived from the protocol spec rather than scattered conditionals."
do_4 = "Forbid by construction the mapping of a verification failure onto a transport error or the reverse, and add a test that enumerates every class and asserts the mapping is total and injective where it must be."
do_5 = "Add tests asserting that an unknown numeric result code survives a round trip through the error model with its value intact."
verify_cmd = "make agent-test-errors"
symbols = ["layerx_types::result::ResultCode","layerx_types::error::LayerError","layerx_types::result::Retriability"]
touches = ["agent/crates/layerx-types/src/result.rs","agent/crates/layerx-types/src/error.rs","agent/crates/layerx-types/tests/errors.rs"]
reqs = ["3.4","7.9","13.10"]

[task.1.4]
title = "Build the boundary-purity CI gate"
status = "done"
wave = 1
requires = ["1.2"]
do_1 = "Implement a checker that fails the build if any crate in agent/ declares a dependency on a SQLite driver, on a bindgen-style binding to include/layerx, or on any crate that links the C core."
do_2 = "Extend the checker to fail on any source reference to node-private file layouts, including log segment paths, projection database paths and snapshot directories."
do_3 = "Extend the checker to fail on any declaration that mirrors a C struct layout, including repr C types whose fields correspond to an include/layerx structure, unless the type is listed in the published stable ABI allowlist."
do_4 = "Wire the checker into make agent-check and CI, and prove it by adding fixtures that each violation class is detected and that a clean tree passes."
do_5 = "Document in the workspace README that this gate exists because the boundary is what preserves independent upgrades, and that suppressing it requires a spec change rather than a code comment."
verify_cmd = "make agent-check-boundary"
symbols = ["agent_boundary_purity_check"]
touches = ["agent/tools/boundary-check/src/main.rs","agent/tools/boundary-check/tests/fixtures.rs","Makefile"]
reqs = ["2.2","2.3","1.2"]

[task.1.5]
title = "Stand up the test, property and fuzz harness"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Establish the test layout for unit, integration, property, differential, fault-injection and conformance suites, with a naming convention that maps a suite to the make target that runs it."
do_2 = "Add the property-testing and fuzzing infrastructure with a committed corpus directory per target and a seed policy that makes a failing case reproducible from the recorded seed."
do_3 = "Add the sanitizer build variants the toolchain supports and wire them to make agent-test-sanitize, treating any report as a failure."
do_4 = "Add a deterministic test clock and a deterministic test randomness source so that no test depends on ambient time or entropy, and forbid ambient clock reads in library code through the lint set."
do_5 = "Add the CI workflow that runs build, lint, boundary check, unit and property suites on every change and the longer suites on a schedule."
verify_cmd = "make agent-test"
symbols = ["agent_test_harness","agent_fuzz_corpus_policy"]
touches = ["agent/crates/layerx-types/tests/harness.rs","agent/fuzz/Cargo.toml",".github/workflows/agent.yml","Makefile"]
reqs = ["23.7","23.9","4.6"]

[task.2]
title = "Build layerx-types, the canonical domain vocabulary"
status = "pending"

[task.2.1]
title = "Define identifiers, account namespaces and asset types"
status = "done"
wave = 1
requires = ["1.3"]
do_1 = "Define DID, activity identifier, batch identifier, checkpoint identifier, transaction identifier, idempotency key and asset identifier as distinct fixed-width types that cannot be interchanged by assignment."
do_2 = "Define the account identifier as a closed enumeration over exactly the protocol namespaces agent main, agent budget, agent escrow, agent margin, system liquidity, system insurance, system fees, system paxeer reserve and system paxeer withdrawals, and make an identifier outside that set unconstructible."
do_3 = "Define the amount type over the exact fixed-width integers the protocol uses including the 128-bit width, with checked arithmetic only, and make construction from any floating-point value impossible in the public API."
do_4 = "Enforce every protocol declared length limit at construction and return a typed error rather than truncating, with the limit constants derived from the protocol spec in one place."
do_5 = "Add tests proving namespace rejection, limit rejection, type non-interchangeability and the absence of any float conversion path."
verify_cmd = "make agent-test-types-ids"
symbols = ["layerx_types::ids::ActivityId","layerx_types::account::AccountId","layerx_types::amount::Amount"]
touches = ["agent/crates/layerx-types/src/ids.rs","agent/crates/layerx-types/src/account.rs","agent/crates/layerx-types/src/amount.rs","agent/crates/layerx-types/tests/ids.rs"]
reqs = ["3.2","3.3","3.5"]

[task.2.2]
title = "Define the activity envelope and the module payload types"
status = "done"
wave = 1
requires = ["2.1"]
do_1 = "Define the activity envelope with exactly the twelve protocol fields protocol_version, network_id, activity_type, actor_did, authority, account_sequence, timestamp_bound, idempotency_key, fee_limit, payload_hash, payload and signature, with no extra field and no optional field the protocol declares mandatory."
do_2 = "Define the authority type as a closed enumeration over the six protocol authorisation kinds, so an unknown authorisation kind cannot be represented as a valid value."
do_3 = "Define the payload types for the asset, escrow, budget, stream, service, perps, governance and bridge modules, each tagged by its declared activity type, and reject construction of a payload whose activity type is not declared by a registered module."
do_4 = "Define the unsigned envelope and the signed envelope as distinct types so that a signature can only be attached through the path that binds it to the exact canonical bytes."
do_5 = "Add tests proving an envelope cannot be constructed with a missing, repeated or undeclared field and that unsigned and signed forms are not interchangeable."
verify_cmd = "make agent-test-types-activity"
symbols = ["layerx_types::activity::Envelope","layerx_types::activity::Authority","layerx_types::payload::Payload"]
touches = ["agent/crates/layerx-types/src/activity.rs","agent/crates/layerx-types/src/payload.rs","agent/crates/layerx-types/tests/activity.rs"]
reqs = ["3.1","3.5","12.1"]

[task.2.3]
title = "Define receipts, batch headers and checkpoint certificates"
status = "done"
wave = 1
requires = ["2.1"]
do_1 = "Define the activity receipt with exactly its protocol fields and the 402LXP receipt with exactly its protocol fields, including before and after balances, roots, batch identifier and sequencer signature."
do_2 = "Define the batch header with exactly its fifteen protocol commitments and the checkpoint certificate with its guarantor signature set, threshold and settlement reference."
do_3 = "Define the proof types for activity inclusion, state inclusion and availability chunk inclusion, each carrying the root it is claimed against so a proof cannot be verified against an unrelated root by accident."
do_4 = "Make every receipt type constructible only by decoding core-produced bytes, so no code path can build a receipt from local values."
do_5 = "Add tests proving a receipt cannot be constructed from arbitrary local input and that field sets match the protocol spec exactly."
verify_cmd = "make agent-test-types-receipt"
symbols = ["layerx_types::receipt::LxpReceipt","layerx_types::batch::BatchHeader","layerx_types::checkpoint::CheckpointCertificate"]
touches = ["agent/crates/layerx-types/src/receipt.rs","agent/crates/layerx-types/src/batch.rs","agent/crates/layerx-types/src/checkpoint.rs","agent/crates/layerx-types/tests/receipt.rs"]
reqs = ["3.1","1.3","6.1"]

[task.2.4]
title = "Define verification status and value provenance"
status = "done"
wave = 1
requires = ["2.3"]
do_1 = "Define the verification level as an ordered lattice with the levels unverified, sequencer-signed, batch-included, state-proven, checkpoint-finalised and settlement-anchored, and expose comparison rather than a boolean."
do_2 = "Define the verified value wrapper that binds a decoded protocol value to its achieved level, the evidence identifiers it rests on, and the head sequence and checkpoint it is relative to."
do_3 = "Define the projection wrapper for locally estimated values, make it structurally distinct from a verified value, and forbid a projection from being placed in any receipt-shaped structure."
do_4 = "Make the level unforgeable within the crate by constructing it only through layerx-proof verification outcomes, so no caller can assert a level it did not achieve."
do_5 = "Add tests proving a projection cannot be returned where a verified value is expected and that level cannot be raised without evidence."
verify_cmd = "make agent-test-types-verification"
symbols = ["layerx_types::verify::VerificationLevel","layerx_types::verify::Verified","layerx_types::verify::Projection"]
touches = ["agent/crates/layerx-types/src/verify.rs","agent/crates/layerx-types/tests/verification.rs"]
reqs = ["1.3","1.5","6.6"]

[task.2.5]
title = "Load and enforce the protocol conformance vectors"
status = "done"
wave = 1
requires = ["2.2","2.3"]
do_1 = "Implement a loader for the protocol conformance corpora that reads the published vectors from the repository rather than copying them, so a protocol vector change is visible to this workspace immediately."
do_2 = "Assert that every vector representing a structure this crate defines can be represented by the crate types, and treat an unrepresentable vector as a build failure naming the vector and the missing capability."
do_3 = "Record for each type the protocol version its definition derives from and fail the build when the corpora declare a protocol version the crate has not been updated for."
do_4 = "Add the corpora coverage report listing which vector classes are exercised and which are not, and fail if a class is silently unused."
do_5 = "Wire the vector suite into make agent-test-vectors so it runs on every change."
verify_cmd = "make agent-test-vectors"
symbols = ["layerx_types::vectors::Corpus","layerx_types::vectors::coverage_report"]
touches = ["agent/crates/layerx-types/src/vectors.rs","agent/crates/layerx-types/tests/vectors.rs","Makefile"]
reqs = ["3.10","3.8","23.1"]

[task.3]
section = "Wave 2 - Canonical Bytes"
title = "Build layerx-wire, the byte-exact canonical codec"
status = "pending"

[task.3.1]
title = "Implement the canonical primitive encoder and decoder"
status = "done"
wave = 2
requires = ["2.5"]
do_1 = "Implement the canonical encoding of unsigned integers, byte strings, tagged unions, ordered maps and sequences exactly as the protocol codec defines them, with one legal encoding per logical value."
do_2 = "Implement the decoder to reject non-minimal integers, indefinite-length items, duplicate map keys, out-of-order map keys, padding and trailing bytes, returning the protocol result-code domain for each rejection class."
do_3 = "Enforce declared length limits during decode before allocating, and bound total allocation per decode so a hostile message cannot exhaust memory."
do_4 = "Guarantee that decoding then re-encoding reproduces the original bytes exactly, and prove it as a property test over generated values as well as over the corpora."
do_5 = "Remove every panicking path reachable from decoding arbitrary bytes and return typed errors instead, asserted by a test that decodes adversarial inputs without unwinding."
verify_cmd = "make agent-test-wire-primitives"
symbols = ["layerx_wire::encode::Encoder","layerx_wire::decode::Decoder","layerx_wire::limits"]
touches = ["agent/crates/layerx-wire/src/encode.rs","agent/crates/layerx-wire/src/decode.rs","agent/crates/layerx-wire/tests/primitives.rs"]
reqs = ["4.1","4.2","4.5","4.6"]

[task.3.2]
title = "Encode and decode envelopes, payloads and receipts"
status = "done"
wave = 2
requires = ["3.1"]
do_1 = "Implement canonical encoding and decoding for the activity envelope, every module payload, the activity receipt, the 402LXP receipt, batch headers, checkpoint certificates and proof structures."
do_2 = "Implement the signed and unsigned envelope encodings such that the signing preimage is derived from the unsigned form and the submitted bytes are the signed form containing exactly those preimage bytes."
do_3 = "Reject an unknown protocol version, an unknown activity type and an unknown field identifier deterministically rather than skipping or best-effort interpreting them."
do_4 = "Assert against the corpora that every structure encodes byte-identically to the published vector and decodes to the identical logical value."
do_5 = "Add a test that decodes every vector, re-encodes it, and compares bytes, failing on the first divergence with the offending offset."
verify_cmd = "make agent-test-wire-structures"
symbols = ["layerx_wire::activity::encode_unsigned","layerx_wire::activity::encode_signed","layerx_wire::receipt::decode"]
touches = ["agent/crates/layerx-wire/src/activity.rs","agent/crates/layerx-wire/src/receipt.rs","agent/crates/layerx-wire/tests/structures.rs"]
reqs = ["4.1","4.3","4.7","4.8"]

[task.3.3]
title = "Enforce non-canonical rejection and declared limits as a suite"
status = "done"
wave = 2
requires = ["3.1"]
do_1 = "Consume the protocol non-canonical rejection corpus and assert this implementation rejects every case with the same rejection class the core assigns."
do_2 = "Add generated mutations of valid vectors, including reordered map keys, re-encoded lengths, appended bytes and shortened bytes, and assert rejection for every mutation that leaves the canonical form."
do_3 = "Assert that every declared maximum is enforced, using a generated case per bounded element that sits exactly at and exactly one past the limit."
do_4 = "Assert that no rejection path consumes unbounded memory or time, with a bounded-resource harness around the decoder."
do_5 = "Report coverage of rejection classes and fail if a class in the protocol taxonomy has no case."
verify_cmd = "make agent-test-wire-rejection"
symbols = ["layerx_wire::tests::rejection_corpus","layerx_wire::limits::enforce"]
touches = ["agent/crates/layerx-wire/tests/rejection.rs","agent/crates/layerx-wire/src/limits.rs"]
reqs = ["4.2","4.5","4.7"]

[task.3.4]
title = "Compute identifiers and signing preimages under domain tags"
status = "done"
wave = 2
requires = ["3.2"]
do_1 = "Implement domain-separated hashing with the exact domain tag set the protocol defines for activity identifiers, payload hashes, signature preimages, authorization hashes, context hashes, merkle leaves, merkle internal nodes, batch headers and checkpoint certificates."
do_2 = "Implement activity_id and payload_hash computation over canonical bytes and prove equality with the corpora values for every vector."
do_3 = "Expose the signing preimage as the single byte string a signer is given, and make it impossible to obtain a preimage that is not derived from canonical bytes."
do_4 = "Add a gate that fails the build if any consensus-critical hash input is a debug, text or JSON rendering rather than canonical bytes."
do_5 = "Add tests proving that identical bytes hashed under two different domains produce different digests and that a preimage for one network or protocol version does not verify for another."
verify_cmd = "make agent-test-wire-hashing"
symbols = ["layerx_wire::hash::domain","layerx_wire::hash::activity_id","layerx_wire::sign::preimage"]
touches = ["agent/crates/layerx-wire/src/hash.rs","agent/crates/layerx-wire/src/sign.rs","agent/crates/layerx-wire/tests/hashing.rs"]
reqs = ["4.4","4.8","4.9","5.8"]

[task.3.5]
title = "Run the byte-parity differential harness against the C core"
status = "done"
wave = 2
requires = ["3.3","3.4"]
do_1 = "Build a differential harness that drives the C reference encoder and this encoder over the same logical values and compares produced bytes, digests and identifiers byte for byte."
do_2 = "Drive the harness from both the published vectors and randomly generated structures covering every activity type, payload shape and boundary length."
do_3 = "Compare rejection classification for malformed and non-canonical inputs across both implementations and fail on any classification divergence."
do_4 = "Record every divergence with the offending logical value, both byte strings and the first differing offset, so a failure is diagnosable without rerunning by hand."
do_5 = "Wire the harness into make agent-test-wire-parity and treat any divergence as build-breaking rather than a tracked issue."
verify_cmd = "make agent-test-wire-parity"
symbols = ["agent_wire_differential_harness"]
touches = ["agent/tools/wire-differential/src/main.rs","agent/tools/wire-differential/tests/parity.rs","Makefile"]
reqs = ["4.1","4.10","23.1"]

[task.3.6]
title = "Ship the codec fuzz targets"
status = "done"
wave = 2
requires = ["3.3"]
do_1 = "Add fuzz targets for the primitive decoder, the envelope decoder, the payload decoder, the receipt decoder and the proof decoder, each seeded from the protocol corpora."
do_2 = "Add a round-trip fuzz target that decodes, re-encodes and asserts byte equality, so a decoder that accepts a non-canonical form is caught rather than merely tolerated."
do_3 = "Add an allocation-bounded fuzz mode that fails on unbounded allocation or a hang rather than only on a crash."
do_4 = "Commit the seed corpora and a minimisation step so a discovered case is reproducible and small."
do_5 = "Wire the targets into make agent-fuzz with a short CI budget and a longer scheduled budget."
verify_cmd = "make agent-fuzz-wire"
symbols = ["fuzz_target_envelope_decode","fuzz_target_roundtrip"]
touches = ["agent/fuzz/fuzz_targets/envelope_decode.rs","agent/fuzz/fuzz_targets/roundtrip.rs","Makefile"]
reqs = ["4.6","23.7"]

[task.4]
section = "Wave 3 - Keys and Signing"
title = "Build layerx-crypto, key custody and disclosure-bound signing"
status = "pending"

[task.4.1]
title = "Implement Ed25519 and secp256k1 verification against core vectors"
status = "done"
wave = 3
requires = ["3.4"]
do_1 = "Implement Ed25519 verification for agent identity over domain-separated canonical bytes and secp256k1 verification for Paxeer-facing certificates, matching the core acceptance and rejection behaviour exactly."
do_2 = "Reject non-canonical and malleable signature encodings, including high-order and non-reduced scalar forms, with the same classification the core uses."
do_3 = "Validate against the protocol crypto vectors, including the negative vectors, and fail the build on any acceptance or rejection divergence."
do_4 = "Provide constant-time comparison for digests and secrets and avoid secret-dependent branching in this crate."
do_5 = "Add tests proving a signature valid on one network identifier or protocol version does not verify on another."
verify_cmd = "make agent-test-crypto-verify"
symbols = ["layerx_crypto::ed25519::verify","layerx_crypto::secp256k1::verify","layerx_crypto::ct::eq"]
touches = ["agent/crates/layerx-crypto/src/ed25519.rs","agent/crates/layerx-crypto/src/secp256k1.rs","agent/crates/layerx-crypto/tests/verify.rs"]
reqs = ["5.1","5.8","5.9"]

[task.4.2]
title = "Define the signer abstraction and the local signer"
status = "done"
wave = 3
requires = ["4.1"]
do_1 = "Define the signer interface such that the only input is a canonical byte string plus its disclosure, and the only output is a signature or a refusal, with no path that exports key material."
do_2 = "Implement the local in-process signer holding key material in memory that is zeroized on drop and excluded from every debug rendering."
do_3 = "Implement the operating-system keystore signer behind the identical interface so key location is a deployment choice."
do_4 = "Make the interface object-safe and asynchronous-friendly so a remote signer can implement it without changing callers."
do_5 = "Add tests proving key material never appears in a debug rendering, an error message or a panic payload."
verify_cmd = "make agent-test-crypto-signer"
symbols = ["layerx_crypto::signer::Signer","layerx_crypto::signer::LocalSigner","layerx_crypto::signer::KeystoreSigner"]
touches = ["agent/crates/layerx-crypto/src/signer.rs","agent/crates/layerx-crypto/src/local.rs","agent/crates/layerx-crypto/tests/signer.rs"]
reqs = ["5.2","5.5","12.6"]

[task.4.3]
title = "Implement disclosure-bound signing"
status = "done"
wave = 3
requires = ["4.2","3.2"]
do_1 = "Define the disclosure structure carrying activity type, actor, authority, every counterparty, every amount, the asset, the fee limit, the expiry and the idempotency key, derived by decoding the bytes rather than by copying the caller request."
do_2 = "Require every signer call to carry both the canonical bytes and the disclosure, and make signing opaque bytes without a disclosure impossible through the public API."
do_3 = "Re-encode the disclosure to canonical bytes before signing and refuse to sign if the result is not byte-identical to the bytes presented."
do_4 = "Return a refusal that names the mismatching field when the disclosure and the bytes disagree, so the failure is diagnosable rather than generic."
do_5 = "Add an adversarial test that presents a disclosure describing a small payment alongside bytes encoding a large one and asserts refusal."
verify_cmd = "make agent-test-crypto-disclosure"
symbols = ["layerx_crypto::disclosure::Disclosure","layerx_crypto::disclosure::bind","layerx_crypto::signer::sign_disclosed"]
touches = ["agent/crates/layerx-crypto/src/disclosure.rs","agent/crates/layerx-crypto/tests/disclosure.rs"]
reqs = ["5.3","5.4","12.2","12.4"]

[task.4.4]
title = "Implement the encrypted keystore and session-key issuance"
status = "done"
wave = 3
requires = ["4.2"]
do_1 = "Implement keystore encryption under an operator-supplied secret using an authenticated cipher and a memory-hard key derivation, with the identity and network bound into the authenticated data."
do_2 = "Refuse to load a keystore whose authenticated data does not match the expected identity or network, and return a typed error rather than a decryption failure alone."
do_3 = "Implement session-key issuance that requires an explicit expiry, an explicit permitted activity type set and an explicit revocation sequence, refusing to issue an unbounded session key."
do_4 = "Emit the protocol activity payload needed to register the session key in protocol state, so the delegated authority is the protocol authority and not a layer-local notion."
do_5 = "Add tests covering wrong secret, wrong network, wrong identity, tampered ciphertext and an attempt to issue a session key without bounds."
verify_cmd = "make agent-test-crypto-keystore"
symbols = ["layerx_crypto::keystore::Keystore","layerx_crypto::session::issue_session_key"]
touches = ["agent/crates/layerx-crypto/src/keystore.rs","agent/crates/layerx-crypto/src/session.rs","agent/crates/layerx-crypto/tests/keystore.rs"]
reqs = ["5.6","5.7","8.3"]

[task.4.5]
title = "Implement the remote signer transport and refusal semantics"
status = "done"
wave = 3
requires = ["4.3"]
do_1 = "Implement a remote signer client that transmits the canonical bytes and the disclosure, authenticates the signer endpoint mutually, and never receives key material."
do_2 = "Treat a refusal, a timeout, an unreachable signer and a malformed response as a refusal to sign, with distinct typed errors and no partially signed artefact left behind."
do_3 = "Forbid any fallback to a lower-assurance key or a cached signature, and prove the absence of such a path with a test that removes the remote signer mid-flight."
do_4 = "Verify the returned signature against the exact bytes before treating the signing as successful."
do_5 = "Add tests covering slow, hostile and flapping signers, asserting no duplicate signature is produced for one preparation."
verify_cmd = "make agent-test-crypto-remote"
symbols = ["layerx_crypto::remote::RemoteSigner","layerx_crypto::remote::SignRefusal"]
touches = ["agent/crates/layerx-crypto/src/remote.rs","agent/crates/layerx-crypto/tests/remote.rs"]
reqs = ["5.10","5.2","12.6"]

[task.4.6]
title = "Enforce secret hygiene as a build gate"
status = "done"
wave = 3
requires = ["4.4"]
do_1 = "Implement a check that fails the build when a type holding key material derives an unredacted debug format or a serialisation that would emit it."
do_2 = "Extend the check to logging, metrics, tracing and error construction sites so a secret cannot reach an output surface."
do_3 = "Add a runtime redaction wrapper for secret-bearing values whose rendering is a fixed placeholder, and require its use through the type system."
do_4 = "Add a test suite that exercises every error and log path with a loaded key and greps the captured output for key material."
do_5 = "Wire the gate into make agent-check so it runs with the other purity checks."
verify_cmd = "make agent-check-secrets"
symbols = ["agent_secret_hygiene_check","layerx_crypto::redact::Secret"]
touches = ["agent/tools/secret-check/src/main.rs","agent/crates/layerx-crypto/src/redact.rs","Makefile"]
reqs = ["5.5","20.4"]

[task.5]
section = "Wave 4 - Independent Verification"
title = "Build layerx-proof, verification from bytes alone"
status = "pending"

[task.5.1]
title = "Implement domain-separated hashing and merkle path verification"
status = "done"
wave = 4
requires = ["3.4"]
do_1 = "Implement merkle leaf and internal node hashing under distinct domain tags matching the protocol exactly, including the protocol rule for trees whose leaf count is not a power of two."
do_2 = "Implement path verification that recomputes the root from a leaf and a proof path and compares it against the claimed root."
do_3 = "Reject any proof that reinterprets an internal node as a leaf, carries a path length inconsistent with the committed tree, or claims an index outside the tree."
do_4 = "Validate against the protocol merkle vectors including the negative vectors and fail the build on divergence."
do_5 = "Add property tests over generated trees asserting that only the correct path verifies and every mutation fails."
verify_cmd = "make agent-test-proof-merkle"
symbols = ["layerx_proof::merkle::verify_path","layerx_proof::merkle::leaf_hash"]
touches = ["agent/crates/layerx-proof/src/merkle.rs","agent/crates/layerx-proof/tests/merkle.rs"]
reqs = ["6.2","6.9"]

[task.5.2]
title = "Verify signed receipts from bytes alone"
status = "done"
wave = 4
requires = ["5.1","4.1"]
do_1 = "Implement receipt verification that decodes the receipt, re-encodes it canonically, verifies the sequencer signature against the key authorised for that batch, and returns the sequencer-signed level on success."
do_2 = "Check internal consistency including that the single-leg before and after balances differ by exactly the amount, that the asset matches on both legs, and that the state roots chain as the receipt claims."
do_3 = "Return a typed verification failure naming the exact failing check rather than a generic invalid result."
do_4 = "Perform verification with no network access, no clock read and no database, proving it in a test that runs with those capabilities removed."
do_5 = "Add negative tests covering altered amount, altered recipient, re-signed receipt under a different key and mismatched roots."
verify_cmd = "make agent-test-proof-receipt"
symbols = ["layerx_proof::receipt::verify","layerx_proof::receipt::ReceiptCheck"]
touches = ["agent/crates/layerx-proof/src/receipt.rs","agent/crates/layerx-proof/tests/receipt.rs"]
reqs = ["6.1","6.7","6.8","1.10"]

[task.5.3]
title = "Verify activity inclusion and state inclusion proofs"
status = "done"
wave = 4
requires = ["5.1"]
do_1 = "Implement activity inclusion verification that recomputes activity_merkle_root from the leaf and path and matches it against the batch header commitment."
do_2 = "Implement state inclusion verification against a named resulting_state_root and verify that root against the batch header that claims it."
do_3 = "Verify the batch header signature against the sequencer identity authorised for that batch number, refusing a header signed by an unauthorised identity."
do_4 = "Raise the achieved level to batch-included and state-proven only when the corresponding check actually passed, never as a default."
do_5 = "Add negative tests covering truncated paths, swapped roots, a header for a different batch number and an unauthorised sequencer identity."
verify_cmd = "make agent-test-proof-inclusion"
symbols = ["layerx_proof::inclusion::verify_activity","layerx_proof::inclusion::verify_state"]
touches = ["agent/crates/layerx-proof/src/inclusion.rs","agent/crates/layerx-proof/tests/inclusion.rs"]
reqs = ["6.2","6.3","6.6"]

[task.5.4]
title = "Verify checkpoint certificates and guarantor thresholds"
status = "done"
wave = 4
requires = ["5.3"]
do_1 = "Implement certificate verification that checks each guarantor signature over the checkpoint identifier against the bonded guarantor set for that checkpoint."
do_2 = "Count only distinct signers, reject a duplicate signer counted twice, and report the achieved count and the required threshold rather than a bare boolean."
do_3 = "Raise the level to checkpoint-finalised only when the threshold is met, and to settlement-anchored only when the settlement reference is present and matches the registered checkpoint."
do_4 = "Return a typed failure identifying whether the shortfall was signature validity, signer membership, distinctness or threshold."
do_5 = "Add negative tests covering sub-threshold sets, duplicated signatures, a signer outside the bonded set and a certificate for a different checkpoint identifier."
verify_cmd = "make agent-test-proof-checkpoint"
symbols = ["layerx_proof::checkpoint::verify_certificate","layerx_proof::checkpoint::ThresholdReport"]
touches = ["agent/crates/layerx-proof/src/checkpoint.rs","agent/crates/layerx-proof/tests/checkpoint.rs"]
reqs = ["6.4","6.6","6.8"]

[task.5.5]
title = "Verify data-availability chunks and reassembled bytes"
status = "done"
wave = 4
requires = ["5.1"]
do_1 = "Implement chunk verification against the committed data_availability_root using the chunk inclusion proof and the protocol chunk hashing rule."
do_2 = "Implement reassembly verification that re-hashes the reassembled bytes and compares against the committed activity, receipt, event and oracle roots for the covered batches."
do_3 = "Classify a mismatch as an availability failure, retain the served bytes and the mismatching commitment as evidence, and name the failing check."
do_4 = "Report which of the five protocol availability classes were obtained and which were missing rather than reporting a single success flag."
do_5 = "Add negative tests covering altered chunks, reordered chunks, a chunk from a different batch and a withheld class."
verify_cmd = "make agent-test-proof-availability"
symbols = ["layerx_proof::availability::verify_chunk","layerx_proof::availability::verify_reassembled"]
touches = ["agent/crates/layerx-proof/src/availability.rs","agent/crates/layerx-proof/tests/availability.rs"]
reqs = ["6.5","15.2","15.3","15.4"]

[task.5.6]
title = "Define the verification level lattice and the negative corpora gate"
status = "done"
wave = 4
requires = ["5.2","5.4","5.5"]
do_1 = "Implement the level lattice so a level is produced only by the verification routine that established it and can never be raised by a caller."
do_2 = "Implement the evidence record that accompanies every level, naming the receipt, proof, header and certificate identifiers the level rests on."
do_3 = "Assemble the negative corpora covering altered values, re-signed receipts, truncated proofs, sub-threshold certificates, duplicated signers, swapped leaf and node domains, mismatched roots and withheld availability."
do_4 = "Assert that every negative case yields a typed verification failure and that none yields a partial success or a silently downgraded level."
do_5 = "Publish the crate as usable standalone, with an example that verifies a receipt from bytes on disk with no node, no daemon and no network."
verify_cmd = "make agent-test-proof-levels"
symbols = ["layerx_proof::level::achieved","layerx_proof::evidence::Evidence"]
touches = ["agent/crates/layerx-proof/src/level.rs","agent/crates/layerx-proof/src/evidence.rs","agent/crates/layerx-proof/tests/negative.rs","agent/crates/layerx-proof/examples/offline_verify.rs"]
reqs = ["6.6","6.8","6.9","6.10"]

[task.6]
section = "Wave 5 - The Node Boundary"
title = "Define and qualify the LayerX Node Interface"
status = "pending"

[task.6.1]
title = "Specify the node interface schema and its version rules"
status = "done"
wave = 5
requires = ["2.5"]
do_1 = "Author the node interface schema as a versioned artefact under source control declaring every request, response and stream message, with node info, submit, receipt lookup, account read, history range, batch header, checkpoint, proof bundle, availability fetch and event subscribe as the initial message set."
do_2 = "Declare that every payload carrying protocol data is opaque canonical LayerX bytes plus the proof material required to verify it, so the schema never restates a protocol structure in a second encoding."
do_3 = "Define the version rule as a major and minor pair where minor changes are additive only, and record golden encoding vectors for every message so a change to the encoding is visible as a diff."
do_4 = "Define the capability list a node advertises, so a node that cannot serve availability retrieval or historical proofs is describable rather than merely failing."
do_5 = "Document the schema in docs/node-boundary.md alongside the framing, transport and handshake rules and keep the document generated from or checked against the schema."
verify_cmd = "make agent-test-lni-schema"
symbols = ["lni_schema_v1","lni_golden_vectors"]
touches = ["agent/schema/lni/v1.kvx","agent/crates/layerx-client/src/lni/schema.rs","agent/crates/layerx-client/tests/lni_schema.rs","spec/layerx-agent-interface/docs/node-boundary.md"]
reqs = ["2.1","2.9","2.3"]

[task.6.2]
title = "Implement framing, transports and connection limits"
status = "done"
wave = 5
requires = ["6.1"]
do_1 = "Implement length-prefixed canonical framing with an explicit maximum message size, rejecting an over-long frame before allocating for it."
do_2 = "Implement the Unix domain socket transport as the default and the mutual-TLS TCP transport for remote deployment, with identical message semantics on both."
do_3 = "Implement connection limits, per-connection deadlines and stream multiplexing such that a slow read cannot starve submission delivery on the same connection."
do_4 = "Return typed transport errors distinguishing connection failure, deadline, frame violation and peer shutdown, and never map any of them onto a verification or core-rejection error."
do_5 = "Fuzz the framing decoder and add tests covering truncated frames, oversized frames, interleaved streams and abrupt peer loss."
verify_cmd = "make agent-test-lni-transport"
symbols = ["layerx_client::lni::framing","layerx_client::lni::transport::Uds","layerx_client::lni::transport::Tls"]
touches = ["agent/crates/layerx-client/src/lni/framing.rs","agent/crates/layerx-client/src/lni/transport.rs","agent/crates/layerx-client/tests/lni_transport.rs","agent/fuzz/fuzz_targets/lni_frame.rs"]
reqs = ["2.1","7.8","7.9"]

[task.6.3]
title = "Implement the handshake, capability intersection and refusal rules"
status = "done"
wave = 5
requires = ["6.2"]
do_1 = "Implement the handshake exchanging node interface version, protocol version, network identifier, node role, chain head, latest sealed batch, latest finalised checkpoint and the currently authorised sequencer public key."
do_2 = "Refuse the connection with an explicit incompatibility error naming both versions when the major node interface version differs from the built-against version, with no best-effort interpretation."
do_3 = "Refuse to operate against a network identifier or protocol version the layer was not configured for, and make that refusal a startup failure rather than a per-request one."
do_4 = "Compute the capability intersection, expose every unavailable capability through a capability report, and fail dependent requests as unavailable rather than emulating them."
do_5 = "Add tests covering a newer node, an older node, a wrong network, a missing capability and a node that changes its advertised sequencer key across reconnects."
verify_cmd = "make agent-test-lni-handshake"
symbols = ["layerx_client::lni::handshake","layerx_client::lni::Capabilities","layerx_client::lni::NodeInfo"]
touches = ["agent/crates/layerx-client/src/lni/handshake.rs","agent/crates/layerx-client/src/lni/capabilities.rs","agent/crates/layerx-client/tests/lni_handshake.rs"]
reqs = ["2.4","2.5","2.6","2.10"]

[task.6.4]
title = "Constrain the optional stable C ABI transport"
status = "done"
wave = 5
requires = ["6.3"]
do_1 = "Define the optional C ABI transport as an opaque-handle interface exchanging only canonical byte buffers and carrying its own ABI version, with the identical message set as the socket transport."
do_2 = "Forbid any shared struct layout across the ABI, and extend the boundary purity checker to fail the build if a repr C type mirrors an include/layerx structure outside the published ABI allowlist."
do_3 = "Implement version negotiation across the ABI with the same refusal rules as the socket handshake, so an ABI mismatch is a refusal rather than undefined behaviour."
do_4 = "Confine every unsafe block required by the ABI to one module listed in the unsafe allowlist, with its safety argument written next to it."
do_5 = "Run the boundary conformance suite over the ABI transport as well as the socket transport and assert identical observable behaviour."
verify_cmd = "make agent-test-lni-abi"
symbols = ["layerx_client::lni::abi::Handle","layerx_client::lni::abi::negotiate"]
touches = ["agent/crates/layerx-client/src/lni/abi.rs","agent/crates/layerx-client/tests/lni_abi.rs","agent/unsafe-allowlist.toml"]
reqs = ["2.3","23.9","2.5"]

[task.6.5]
title = "Build the boundary conformance suite against a real node"
status = "done"
wave = 5
requires = ["6.3"]
do_1 = "Build a harness that starts a real layerxd from this repository with a genesis manifest and runs the suite against it, with no in-process substitute for the node anywhere in the suite."
do_2 = "Cover every message in the schema including its error cases, its streaming behaviour, its pagination, its version negotiation and its capability reporting."
do_3 = "Assert that responses carrying protocol data are canonical bytes that re-hash to the identifiers they claim, so the suite tests the boundary rather than the client belief about it."
do_4 = "Cover node restart, node behind, node unreachable and node degraded, asserting the layer refusal behaviour in each case."
do_5 = "Wire the suite into make agent-test-boundary and mark it a release gate."
verify_cmd = "make agent-test-boundary"
symbols = ["agent_boundary_conformance_suite"]
touches = ["agent/tests/boundary/src/main.rs","agent/tests/boundary/cases.rs","Makefile"]
reqs = ["2.9","23.2","2.7"]

[task.6.6]
title = "Publish the capability gap report"
status = "done"
wave = 5
requires = ["6.5"]
do_1 = "Implement a report that enumerates every capability this specification requires, whether the connected node exposes it, and what the layer does when it is absent."
do_2 = "Fail a dependent request as unavailable when a capability is missing, and prove in test that no code path substitutes a locally reconstructed answer."
do_3 = "Record every gap discovered against the real node so the protocol feature can close it, without the agent layer working around it."
do_4 = "Expose the report through the daemon status surface and through the command line so an operator sees the same list the suite sees."
do_5 = "Include the report in the qualification output so a release states plainly which capabilities were unavailable."
verify_cmd = "make agent-test-capability-report"
symbols = ["layerx_client::lni::capability_report"]
touches = ["agent/crates/layerx-client/src/lni/report.rs","agent/tests/boundary/gaps.rs"]
reqs = ["2.10","2.6","23.10"]

[task.7]
section = "Wave 6 - The Boundary Client"
title = "Build layerx-client, the only crate that talks to the core"
status = "pending"

[task.7.1]
title = "Implement connection lifecycle and head tracking"
status = "done"
wave = 6
requires = ["6.3"]
do_1 = "Implement connection establishment, reconnection with bounded jittered backoff, and a connection state surface that distinguishes connected, degraded, incompatible and unreachable."
do_2 = "Track the chain head sequence, the latest sealed batch and the latest finalised checkpoint continuously, and expose them so every read can report what it is relative to."
do_3 = "Track the authorised sequencer key set and refuse to accept a receipt signature verified against a key the node did not advertise for that batch."
do_4 = "Enforce that this crate is the only one in the workspace that opens a boundary connection, and add the check to the boundary purity gate."
do_5 = "Add tests covering reconnect during a request, head regression, and a node that advertises an unexpected network mid-session."
verify_cmd = "make agent-test-client-connection"
symbols = ["Client","layerx_client::head::HeadTracker"]
touches = ["agent/crates/layerx-client/src/client.rs","agent/crates/layerx-client/src/head.rs","agent/crates/layerx-client/tests/connection.rs"]
reqs = ["7.1","2.4","14.6"]

[task.7.2]
title = "Implement submission with exact-byte transmission"
status = "done"
wave = 6
requires = ["7.1","3.2"]
do_1 = "Implement submit to transmit exactly the signed bytes it was given, with no re-encoding, normalisation, reordering or augmentation anywhere in the path."
do_2 = "Verify before transmission that the signature verifies over the exact bytes about to be sent, and fail rather than transmit otherwise."
do_3 = "Treat the acknowledgement as admission evidence only, returning a distinct type that cannot be confused with an executed outcome."
do_4 = "Classify an attempt that ends without a determinate outcome as unknown, and expose that classification rather than an error that reads as failure."
do_5 = "Add tests asserting byte equality between the signed bytes and the transmitted bytes under fragmentation, retry and reconnect."
verify_cmd = "make agent-test-client-submit"
symbols = ["layerx_client::submit::submit_signed","layerx_client::submit::Acknowledgement","layerx_client::submit::Unknown"]
touches = ["agent/crates/layerx-client/src/submit.rs","agent/crates/layerx-client/tests/submit.rs"]
reqs = ["7.2","7.3","7.5","1.6"]

[task.7.3]
title = "Implement receipt retrieval, verification and unknown resolution"
status = "done"
wave = 6
requires = ["7.2","5.2"]
do_1 = "Implement receipt lookup by activity identifier, idempotency key and global sequence, returning the exact core-produced bytes."
do_2 = "Verify every retrieved receipt through layerx-proof before returning it and attach the achieved verification level."
do_3 = "Implement unknown resolution that repeatedly looks up the receipt for an idempotency key under bounded jittered backoff and never resubmits as a way of resolving."
do_4 = "Resend byte-identical bytes under the original idempotency key when a resend is warranted, and treat the return of an existing receipt as the correct outcome."
do_5 = "Add tests covering a receipt that appears late, a receipt that never appears within the deadline, and a node that returns a receipt for a different activity."
verify_cmd = "make agent-test-client-receipt"
symbols = ["layerx_client::receipt::lookup","layerx_client::receipt::resolve_unknown"]
touches = ["agent/crates/layerx-client/src/receipt.rs","agent/crates/layerx-client/tests/receipt.rs"]
reqs = ["7.4","7.5","13.4","13.5"]

[task.7.4]
title = "Implement verified reads at a requested level"
status = "done"
wave = 6
requires = ["7.1","5.6"]
do_1 = "Implement balance, account, module state and history reads that take a requested verification level and fetch the proof material that level requires."
do_2 = "Verify the proof material locally before returning and attach the level actually achieved together with the head and checkpoint the value is relative to."
do_3 = "Refuse rather than downgrade when the requested level cannot be achieved, returning a typed error naming the missing evidence."
do_4 = "Implement history reads that preserve core bytes exactly and expose stable cursors with no gap and no repetition across pages."
do_5 = "Add tests proving a value is never returned at a level higher than its evidence and that a hostile node cannot raise the level by asserting it."
verify_cmd = "make agent-test-client-reads"
symbols = ["layerx_client::read::balance","layerx_client::read::history","layerx_client::read::Requested"]
touches = ["agent/crates/layerx-client/src/read.rs","agent/crates/layerx-client/tests/reads.rs"]
reqs = ["7.6","14.1","14.2","14.3"]

[task.7.5]
title = "Implement the ordered event stream and resumable cursors"
status = "done"
wave = 6
requires = ["7.1"]
do_1 = "Implement the event stream that delivers core-produced events in strict global sequence order with no synthesis, enrichment or reordering."
do_2 = "Expose a resumable cursor that survives disconnection and process restart and resumes without a gap or a duplicate at the seam."
do_3 = "Detect a sequence gap and surface it explicitly as a gap with the missing range rather than closing it silently."
do_4 = "Apply bounded buffering with explicit backpressure so a slow consumer cannot cause unbounded memory growth or event loss."
do_5 = "Add tests covering disconnect mid-stream, restart from cursor, an out-of-order node and a deliberately skipped sequence."
verify_cmd = "make agent-test-client-stream"
symbols = ["layerx_client::stream::subscribe","layerx_client::stream::Cursor","layerx_client::stream::Gap"]
touches = ["agent/crates/layerx-client/src/stream.rs","agent/crates/layerx-client/tests/stream.rs"]
reqs = ["7.7","16.1","16.4"]

[task.7.6]
title = "Implement availability retrieval and verification"
status = "done"
wave = 6
requires = ["7.1","5.5"]
do_1 = "Implement availability retrieval by checkpoint identifier, batch number, sequence range and activity identifier, streaming large retrievals rather than buffering them whole."
do_2 = "Verify each chunk and the reassembled bytes through layerx-proof before treating the retrieval as successful, and report which availability classes were obtained."
do_3 = "Support multiple providers, treat a provider serving non-verifying bytes as failed for that request, and never merge bytes from two providers into one unverified result."
do_4 = "Bound retrieval by explicit size, chunk-count and deadline limits and report partial completion honestly."
do_5 = "Add tests covering a withholding provider, a corrupting provider, a partial class set and a retrieval that exceeds its bound."
verify_cmd = "make agent-test-client-availability"
symbols = ["layerx_client::availability::fetch","layerx_client::availability::ProviderSet"]
touches = ["agent/crates/layerx-client/src/availability.rs","agent/crates/layerx-client/tests/availability.rs"]
reqs = ["15.1","15.2","15.5","15.7"]

[task.8]
section = "Wave 7 - The Agent Contract"
title = "Define layerx-agent-api, the agent-facing service contract"
status = "pending"

[task.8.1]
title = "Define the contract schema and its compatibility rules"
status = "done"
wave = 7
requires = ["7.4"]
do_1 = "Author the agent-api contract as a single versioned schema artefact from which the Rust types, the daemon surface and every generated SDK derive, and make it the source of truth for the client surface."
do_2 = "Define the compatibility rule permitting only additive change within a major version, and implement the CI gate that fails a breaking change without a major increment."
do_3 = "Commit golden schema snapshots and golden request and response vectors so any surface change is visible as a reviewable diff."
do_4 = "Represent every consensus-critical integer as an exact integer type in the schema and forbid a floating-point representation anywhere in it."
do_5 = "Generate the Rust contract types from the schema in the build and fail if the committed generated output differs from a fresh generation."
verify_cmd = "make agent-test-contract-schema"
symbols = ["agent_api_schema_v1","agent_api_compat_gate"]
touches = ["agent/schema/agent-api/v1.kvx","agent/crates/layerx-agent-api/src/generated.rs","agent/crates/layerx-agent-api/tests/schema.rs"]
reqs = ["18.1","18.7","18.5"]

[task.8.2]
title = "Define identity, session, capability and budget operations"
status = "done"
wave = 7
requires = ["8.1"]
do_1 = "Define agent registration, session open, refresh, close and list, each carrying tenant, agent DID, authority reference, permitted activity types, expiry and policy version."
do_2 = "Define capability creation, attenuation, listing and revocation with every dimension explicit and no dimension defaulting open."
do_3 = "Define budget creation, funding, listing, reconciliation status and revocation, distinguishing protocol-enforced budgets from daemon-enforced limits in the type itself."
do_4 = "Define the authority description returned with every response so a caller can see which protocol authority was actually used."
do_5 = "Add contract tests asserting that an operation cannot be expressed without its mandatory dimensions."
verify_cmd = "make agent-test-contract-identity"
symbols = ["layerx_agent_api::identity","layerx_agent_api::capability","layerx_agent_api::budget"]
touches = ["agent/schema/agent-api/identity.kvx","agent/crates/layerx-agent-api/src/identity.rs","agent/crates/layerx-agent-api/tests/identity.rs"]
reqs = ["8.2","9.1","10.2","18.10"]

[task.8.3]
title = "Define prepare, sign, submit and track operations"
status = "done"
wave = 7
requires = ["8.1"]
do_1 = "Define prepare to return the unsigned canonical bytes, the signing preimage, the structured disclosure and the preparation expiry."
do_2 = "Define submit to accept the signature and the preparation reference, and define track to return the submission state, its evidence and its verification level."
do_3 = "Define the submission state enumeration exactly as prepared, signed, queued, submitted, acknowledged, unknown, executed, failed and expired, with unknown a first-class terminal-pending state rather than an error."
do_4 = "Define the wait operation that takes a requested verification level and a deadline and returns the level actually reached."
do_5 = "Add contract tests asserting that no response shape allows an executed outcome to be reported without a receipt reference."
verify_cmd = "make agent-test-contract-write"
symbols = ["layerx_agent_api::prepare","layerx_agent_api::submit","layerx_agent_api::track","layerx_agent_api::SubmissionState"]
touches = ["agent/schema/agent-api/write.kvx","agent/crates/layerx-agent-api/src/write.rs","agent/crates/layerx-agent-api/tests/write.rs"]
reqs = ["12.1","12.2","13.2","13.9"]

[task.8.4]
title = "Define read, proof and availability operations"
status = "done"
wave = 7
requires = ["8.1"]
do_1 = "Define balance, account, module state, history, batch, checkpoint and proof-bundle reads, each taking a requested verification level and returning the achieved level with the value."
do_2 = "Define the availability retrieval operation with its class report, provider report and partial completion reporting."
do_3 = "Define the offline verification export that returns the receipts, proofs, certificates and headers needed to verify a stated fact set without the daemon."
do_4 = "Define the projection operations distinctly so an estimate can never be returned in a field that carries a verified value."
do_5 = "Add contract tests asserting every read response carries a verification level and a freshness reference."
verify_cmd = "make agent-test-contract-read"
symbols = ["layerx_agent_api::read","layerx_agent_api::proof","layerx_agent_api::availability","layerx_agent_api::export"]
touches = ["agent/schema/agent-api/read.kvx","agent/crates/layerx-agent-api/src/read.rs","agent/crates/layerx-agent-api/tests/read.rs"]
reqs = ["14.1","14.6","14.10","1.5"]

[task.8.5]
title = "Define subscription and streaming operations"
status = "done"
wave = 7
requires = ["8.1"]
do_1 = "Define subscription creation, listing, health, cursor acknowledgement, pause, resume and deletion, each scoped to tenant, agent and capability."
do_2 = "Define the filter vocabulary over agent, account, activity type, module, asset, counterparty and result class, and define filters as restrictions that cannot widen visibility."
do_3 = "Define the delivery envelope carrying the event bytes, the deduplication identifier, the cursor and the verification level of any receipt it references."
do_4 = "Define the gap notification and the truncation notification as first-class delivery events rather than out-of-band log lines."
do_5 = "Add contract tests asserting a subscription cannot be created without a scope and that a filter cannot name an object outside its tenant."
verify_cmd = "make agent-test-contract-stream"
symbols = ["layerx_agent_api::subscription","layerx_agent_api::Delivery","layerx_agent_api::GapNotice"]
touches = ["agent/schema/agent-api/stream.kvx","agent/crates/layerx-agent-api/src/stream.rs","agent/crates/layerx-agent-api/tests/stream.rs"]
reqs = ["16.2","16.3","16.4","16.6"]

[task.8.6]
title = "Define the error, idempotency and verification-level contract"
status = "done"
wave = 7
requires = ["8.2","8.3","8.4","8.5"]
do_1 = "Define the wire error shape carrying the layer error class, the protocol result code where one exists, the retriability classification, the request identifier and a stable machine-readable reason."
do_2 = "Define caller-supplied idempotency keys across every mutating operation, with the original result returned for a repeat and a conflict error for a repeat whose body differs."
do_3 = "Define the verification level as a contract-level enumeration so every SDK exposes the same lattice with the same ordering."
do_4 = "Define the unavailable-capability error distinctly from every other error so a client can tell a missing node capability from a refusal."
do_5 = "Add contract tests asserting the error taxonomy is total, that no operation lacks an idempotency path, and that no success shape omits its verification level."
verify_cmd = "make agent-test-contract-errors"
symbols = ["agent_api::error::ApiError","agent_api::idempotency::Key","agent_api::verify::Level"]
touches = ["agent/schema/agent-api/errors.kvx","agent/crates/layerx-agent-api/src/error.rs","agent/crates/layerx-agent-api/tests/errors.rs"]
reqs = ["13.6","18.4","2.6","1.4"]

[task.9]
section = "Wave 8 - Identity, Authority and Policy"
title = "Implement identity and sessions in layerx-agentd"
status = "pending"

[task.9.1]
title = "Implement the tenant-scoped store and its migrations"
status = "done"
wave = 8
requires = ["8.6"]
do_1 = "Implement the daemon local store with every object type carrying its tenant as part of its key, so a query without a tenant is unrepresentable in the storage access path."
do_2 = "Implement versioned schema migrations with a forward-only path, a refusal to start against a store from a newer version, and a migration test from every supported prior version."
do_3 = "Declare explicitly which stored data is local-only, being policy, cursors, audit, idempotency records and configuration, and which is a cache of core-produced bytes."
do_4 = "Implement durable write ordering so a crash cannot leave a signed submission recorded without its outbox entry or an outbox entry without its idempotency record."
do_5 = "Add a test that deletes the store, restarts against the same core, and asserts every protocol-derived answer is reconstructible while only declared local artefacts are lost."
verify_cmd = "make agent-test-agentd-store"
symbols = ["layerx_agentd::store::Store","layerx_agentd::store::TenantKey","layerx_agentd::store::migrate"]
touches = ["agent/crates/layerx-agentd/src/store/mod.rs","agent/crates/layerx-agentd/src/store/migrate.rs","agent/crates/layerx-agentd/tests/store.rs"]
reqs = ["21.1","22.6","1.9"]

[task.9.2]
title = "Bind agents to DIDs verified against the core"
status = "done"
wave = 8
requires = ["9.1","7.4"]
do_1 = "Implement agent registration that resolves the DID against current protocol state through the boundary and refuses registration for an unknown or frozen identity."
do_2 = "Record the identity binding with the head sequence it was verified at, and re-verify it before first use after a restart rather than trusting the stored copy."
do_3 = "Reject a registration that supplies identity facts as configuration instead of verifying them, and prove the absence of such a path in test."
do_4 = "Expose the identity record with its verification level and the protocol authority set it currently holds."
do_5 = "Add tests covering an unknown DID, a frozen identity, an identity that becomes frozen after registration and a core that is unavailable at registration time."
verify_cmd = "make agent-test-agentd-identity"
symbols = ["layerx_agentd::identity::register","layerx_agentd::identity::revalidate"]
touches = ["agent/crates/layerx-agentd/src/identity.rs","agent/crates/layerx-agentd/tests/identity.rs"]
reqs = ["8.1","8.10","1.2"]

[task.9.3]
title = "Implement the session lifecycle and token binding"
status = "done"
wave = 8
requires = ["9.2"]
do_1 = "Implement session open recording agent DID, tenant, presented authority, permitted activity types, expiry, opening client and policy version, refusing an open that omits any of them."
do_2 = "Issue session tokens bound to tenant, agent, scope and expiry, and treat a token strictly as a daemon authenticator that is never protocol authority."
do_3 = "Verify at open time that the presented protocol authority exists, is currently valid and is unrevoked, and refuse otherwise."
do_4 = "Support many concurrent sessions per agent and many agents per tenant with independent state, and prove closing one leaves others untouched."
do_5 = "Add tests covering an expired token, a token from another tenant, a token whose scope omits the operation and a session whose authority was never registered in protocol state."
verify_cmd = "make agent-test-agentd-session"
symbols = ["layerx_agentd::session::open","layerx_agentd::session::Token","layerx_agentd::session::close"]
touches = ["agent/crates/layerx-agentd/src/session.rs","agent/crates/layerx-agentd/tests/session.rs"]
reqs = ["8.2","8.6","8.7","8.8"]

[task.9.4]
title = "Re-evaluate protocol authority before every write"
status = "done"
wave = 8
requires = ["9.3"]
do_1 = "Re-resolve the underlying protocol authority against core state before every preparation and refuse when it has expired, rotated, been revoked or fallen out of scope."
do_2 = "Cache the authority resolution only within an explicit freshness bound and revalidate past it rather than trusting the session record."
do_3 = "Name the failing authority condition in the refusal so a caller can distinguish expiry from revocation from scope."
do_4 = "Report in every response which protocol authority was actually used for the activity."
do_5 = "Add tests covering revocation between session open and preparation, and rotation between preparation and submission."
verify_cmd = "make agent-test-agentd-authority"
symbols = ["layerx_agentd::authority::resolve","layerx_agentd::authority::require_valid"]
touches = ["agent/crates/layerx-agentd/src/authority.rs","agent/crates/layerx-agentd/tests/authority.rs"]
reqs = ["8.3","8.4","1.1"]

[task.9.5]
title = "Propagate rotation and revocation into sessions"
status = "done"
wave = 8
requires = ["9.4"]
do_1 = "Observe identity, key rotation, session key and capability grant revocation events from the event stream and apply them to session state immediately."
do_2 = "Invalidate every session whose authority no longer verifies, report the invalidation reason to affected clients, and cancel prepared but unsubmitted activities bound to it."
do_3 = "Leave activities the protocol already executed untouched and prove that no local invalidation is presented as reversing a protocol effect."
do_4 = "Persist invalidations so a restart does not resurrect a session whose authority is gone."
do_5 = "Add tests covering revocation arriving while a preparation is in flight and while a submission is in the unknown state."
verify_cmd = "make agent-test-agentd-revocation"
symbols = ["layerx_agentd::session::invalidate_on_revocation"]
touches = ["agent/crates/layerx-agentd/src/session_revocation.rs","agent/crates/layerx-agentd/tests/revocation.rs"]
reqs = ["8.5","8.9","9.5"]

[task.10]
title = "Implement capabilities and their attenuation"
status = "pending"

[task.10.1]
title = "Implement the capability model and its dimensions"
status = "done"
wave = 8
requires = ["9.3"]
do_1 = "Implement the capability record with permitted activity types, permitted counterparties, permitted assets, amount ceilings, rate ceilings, purpose constraints and expiry, rejecting a capability that leaves any dimension unspecified."
do_2 = "Implement evaluation that checks a prepared activity against every dimension and refuses on the first exceeded dimension, naming it."
do_3 = "Make evaluation deterministic from the recorded capability and the prepared activity alone, with no ambient input, and prove identical decisions for identical inputs."
do_4 = "Persist capabilities with their tenant scope and expiry and restore them on restart."
do_5 = "Add tests covering each dimension at, inside and outside its bound."
verify_cmd = "make agent-test-agentd-capability"
symbols = ["layerx_agentd::capability::Capability","layerx_agentd::capability::evaluate"]
touches = ["agent/crates/layerx-agentd/src/capability/mod.rs","agent/crates/layerx-agentd/tests/capability.rs"]
reqs = ["9.1","9.2","9.6"]

[task.10.2]
title = "Enforce narrowing against the underlying protocol authority"
status = "done"
wave = 8
requires = ["10.1"]
do_1 = "Resolve the underlying protocol authority at capability creation and reject any capability that would permit an activity type, asset, counterparty or amount that authority does not already permit."
do_2 = "Forbid presenting a capability to the core as authorisation, and prove in test that no submission path carries a capability identifier as protocol authority."
do_3 = "Record for every dimension whether it is protocol-enforced or daemon-enforced only."
do_4 = "Re-check narrowing when the underlying authority changes and disable a capability that would now be wider than its parent."
do_5 = "Add tests covering a capability created wider than its authority, and an authority narrowed after the capability was created."
verify_cmd = "make agent-test-agentd-narrowing"
symbols = ["layerx_agentd::capability::assert_narrowing","layerx_agentd::capability::Enforcement"]
touches = ["agent/crates/layerx-agentd/src/capability/narrowing.rs","agent/crates/layerx-agentd/tests/narrowing.rs"]
reqs = ["9.3","9.8","11.9"]

[task.10.3]
title = "Implement attenuation, derivation chains and revocation subtrees"
status = "done"
wave = 8
requires = ["10.2"]
do_1 = "Implement attenuation as the intersection of every dimension and forbid a derived capability from exceeding its parent in any dimension."
do_2 = "Record the derivation chain so any exercised capability is traceable to its root authority."
do_3 = "Implement revocation of a capability, its parent or the underlying authority as refusal of the entire subtree, applied to prepared but unsubmitted activities."
do_4 = "Persist chains and revocations and restore them consistently after restart."
do_5 = "Add tests covering deep chains, a revoked middle node and an attempt to widen at derivation."
verify_cmd = "make agent-test-agentd-attenuation"
symbols = ["layerx_agentd::capability::attenuate","layerx_agentd::capability::revoke_subtree"]
touches = ["agent/crates/layerx-agentd/src/capability/attenuate.rs","agent/crates/layerx-agentd/tests/attenuation.rs"]
reqs = ["9.4","9.5"]

[task.10.4]
title = "Account capability ceilings from verified receipts"
status = "done"
wave = 8
requires = ["10.3"]
do_1 = "Account consumption against a spending ceiling from verified receipts only, counting a failed activity as unconsumed."
do_2 = "Serialise the reservation decision so concurrent uses of one capability cannot exceed the ceiling in aggregate."
do_3 = "Hold a reservation while an outcome is unknown and release it only on a terminal state or a declared reservation expiry."
do_4 = "Rebuild consumption from persisted receipts on restart before permitting a write under that capability."
do_5 = "Add a concurrency test that drives many simultaneous uses of one ceiling and asserts the aggregate never exceeds it."
verify_cmd = "make agent-test-agentd-ceiling"
symbols = ["layerx_agentd::capability::consume","layerx_agentd::capability::Reservation"]
touches = ["agent/crates/layerx-agentd/src/capability/consume.rs","agent/crates/layerx-agentd/tests/ceiling.rs"]
reqs = ["9.7","10.5","10.6"]

[task.10.5]
title = "Report protocol-enforced versus daemon-enforced restrictions"
status = "done"
wave = 8
requires = ["10.4"]
do_1 = "Implement the capability report listing every restriction, its enforcement location and, for daemon-enforced restrictions, the plain statement that bypassing the daemon bypasses them."
do_2 = "Prefer expressing a restriction as a protocol capability grant, session key scope, budget or payer grant wherever the protocol offers an equivalent, and record the protocol object identifier when it does."
do_3 = "Expose the report through the contract, the command line and the audit trail with the same content."
do_4 = "Forbid any wording in the report or documentation that describes a daemon-enforced restriction as a protocol guarantee, and add a documentation check for it."
do_5 = "Add tests asserting the report is complete for every capability dimension."
verify_cmd = "make agent-test-agentd-enforcement-report"
symbols = ["layerx_agentd::capability::enforcement_report"]
touches = ["agent/crates/layerx-agentd/src/capability/report.rs","agent/crates/layerx-agentd/tests/enforcement_report.rs"]
reqs = ["9.9","9.10","10.2","18.10"]

[task.11]
title = "Implement budgets and spend control"
status = "pending"

[task.11.1]
title = "Create protocol budgets and grants through signed activities"
status = "done"
wave = 8
requires = ["10.2"]
do_1 = "Implement budget creation that offers to express the limit as a protocol budget or capability grant and creates it through an ordinary prepared, signed and submitted activity."
do_2 = "Return the resulting protocol object identifier and its receipt so the limit existence is evidence rather than a daemon record."
do_3 = "Label a limit that exists only in the daemon explicitly as daemon-enforced with its bypass statement."
do_4 = "Refuse to describe a daemon-enforced limit as equivalent to a protocol budget anywhere in the response."
do_5 = "Add tests covering creation, the failure of creation, and the state of the daemon record when creation fails."
verify_cmd = "make agent-test-agentd-budget-create"
symbols = ["layerx_agentd::budget::create_protocol_budget","layerx_agentd::budget::LocalLimit"]
touches = ["agent/crates/layerx-agentd/src/budget/create.rs","agent/crates/layerx-agentd/tests/budget_create.rs"]
reqs = ["10.1","10.2","1.1"]

[task.11.2]
title = "Reconcile local accounting against protocol budget state"
status = "done"
wave = 8
requires = ["11.1"]
do_1 = "Derive consumption from verified receipts and protocol budget state and treat the local figure strictly as a cache."
do_2 = "Take window boundaries and remaining allowance from protocol state rather than from a local timer, so the window view matches the deterministic protocol view."
do_3 = "Correct the local figure to the protocol figure whenever they differ and record the correction."
do_4 = "Expose reconciliation state including the last verified receipt applied, the protocol figure, the local figure and any divergence."
do_5 = "Add tests covering a rollover observed only through protocol state and a divergence introduced by a missed receipt."
verify_cmd = "make agent-test-agentd-budget-reconcile"
symbols = ["layerx_agentd::budget::reconcile","layerx_agentd::budget::ReconciliationState"]
touches = ["agent/crates/layerx-agentd/src/budget/reconcile.rs","agent/crates/layerx-agentd/tests/budget_reconcile.rs"]
reqs = ["10.3","10.7","10.9"]

[task.11.3]
title = "Implement serialised reservations and deterministic release"
status = "done"
wave = 8
requires = ["11.2"]
do_1 = "Implement reservation as a serialised decision per limit so concurrent or reordered requests cannot exceed the ceiling in aggregate."
do_2 = "Release a reservation deterministically on a terminal outcome or at a declared reservation expiry, and leave no reservation behind for a refused request."
do_3 = "Apply every applicable limit across tenant, agent, session, capability and counterparty simultaneously and refuse if any one is exceeded."
do_4 = "Name the limit, the ceiling, the consumed amount and the requested amount in every refusal."
do_5 = "Add a concurrency test asserting aggregate compliance and a test asserting no reservation leak across thousands of refused requests."
verify_cmd = "make agent-test-agentd-budget-reserve"
symbols = ["layerx_agentd::budget::reserve","layerx_agentd::budget::release"]
touches = ["agent/crates/layerx-agentd/src/budget/reserve.rs","agent/crates/layerx-agentd/tests/budget_reserve.rs"]
reqs = ["10.4","10.5","10.8"]

[task.11.4]
title = "Hold reservations across unknown outcomes and restarts"
status = "done"
wave = 8
requires = ["11.3"]
do_1 = "Keep a reservation held while the outcome of its activity is unknown and refuse to release it optimistically."
do_2 = "Persist reservations so a restart restores them before any write is accepted against that limit."
do_3 = "Rebuild spend accounting from persisted receipts and protocol state on restart and refuse writes against an unreconciled limit."
do_4 = "Expose held-but-unresolved amounts distinctly from consumed amounts so an operator can see why capacity is unavailable."
do_5 = "Add a fault-injection test that kills the daemon between submission and receipt and asserts the reservation survives and resolves correctly."
verify_cmd = "make agent-test-agentd-budget-unknown"
symbols = ["layerx_agentd::budget::hold_unknown","layerx_agentd::budget::rebuild"]
touches = ["agent/crates/layerx-agentd/src/budget/hold.rs","agent/crates/layerx-agentd/tests/budget_unknown.rs"]
reqs = ["10.6","10.10","13.4"]

[task.11.5]
title = "Report divergence rather than adopting the local figure"
status = "done"
wave = 8
requires = ["11.2"]
do_1 = "Raise an explicit alert when the local and protocol figures diverge and refuse to adopt the local figure silently."
do_2 = "Record the divergence in the audit trail with both figures, the last verified receipt and the head sequence."
do_3 = "Expose divergence through health so an operator sees it without reading logs."
do_4 = "Continue to enforce the more restrictive of the two figures while a divergence is open."
do_5 = "Add tests covering an injected divergence and its resolution once the missing receipt is verified."
verify_cmd = "make agent-test-agentd-budget-divergence"
symbols = ["layerx_agentd::budget::divergence_alert"]
touches = ["agent/crates/layerx-agentd/src/budget/divergence.rs","agent/crates/layerx-agentd/tests/budget_divergence.rs"]
reqs = ["10.9","20.8","1.3"]

[task.12]
title = "Implement the policy engine"
status = "pending"

[task.12.1]
title = "Implement deterministic deny-by-default evaluation"
status = "done"
wave = 8
requires = ["9.3"]
do_1 = "Implement evaluation over the request, the session, the capability, the reconciled budget state and the loaded policy version alone, denying by default when no rule permits."
do_2 = "Forbid consulting wall-clock convenience, random values, load or non-deterministic ordering, and prove determinism with a repeated-evaluation test over a recorded corpus."
do_3 = "Support constraints over activity type, counterparty, asset, amount, cumulative rate, purpose, capability, session, agent, tenant, time window and required approval, with exact-integer thresholds only."
do_4 = "Deny on evaluation failure, timeout or internal error, and prove there is no path that fails open."
do_5 = "Add tests covering an empty policy set, a conflicting rule pair and an evaluation that panics internally."
verify_cmd = "make agent-test-agentd-policy"
symbols = ["layerx_agentd::policy::evaluate","layerx_agentd::policy::Decision"]
touches = ["agent/crates/layerx-agentd/src/policy/eval.rs","agent/crates/layerx-agentd/tests/policy.rs"]
reqs = ["11.1","11.2","11.6","11.7"]

[task.12.2]
title = "Implement policy versioning, validation and activation"
status = "done"
wave = 8
requires = ["12.1"]
do_1 = "Version every policy set, validate it before activation, and reject an invalid set while continuing under the previous version."
do_2 = "Apply a new version only to requests received after activation and retain prior versions for audit reconstruction."
do_3 = "Record the active policy version in every decision, session and audit entry."
do_4 = "Fuzz the policy loader so a malformed policy source cannot panic or hang the daemon."
do_5 = "Add tests covering activation during in-flight requests and a rejected invalid set."
verify_cmd = "make agent-test-agentd-policy-version"
symbols = ["layerx_agentd::policy::activate","layerx_agentd::policy::validate"]
touches = ["agent/crates/layerx-agentd/src/policy/version.rs","agent/crates/layerx-agentd/tests/policy_version.rs","agent/fuzz/fuzz_targets/policy_loader.rs"]
reqs = ["11.5","11.3","23.7"]

[task.12.3]
title = "Implement dry-run and decision explanation"
status = "done"
wave = 8
requires = ["12.1"]
do_1 = "Implement dry-run evaluation returning the decision and its explanation without preparing, signing or submitting anything."
do_2 = "Return the matched rules, the deciding rule and the reason, and guarantee dry-run has no side effect beyond its audit entry."
do_3 = "State in every allow decision that policy is a local restriction and never protocol authorisation."
do_4 = "Make the explanation stable and machine-readable so it can be asserted in tests and diffed across policy versions."
do_5 = "Add tests asserting dry-run and live evaluation return identical decisions for identical inputs."
verify_cmd = "make agent-test-agentd-policy-dryrun"
symbols = ["layerx_agentd::policy::dry_run","layerx_agentd::policy::Explanation"]
touches = ["agent/crates/layerx-agentd/src/policy/dry_run.rs","agent/crates/layerx-agentd/tests/policy_dryrun.rs"]
reqs = ["11.4","11.3","11.9"]

[task.12.4]
title = "Implement approval holds and deterministic expiry"
status = "done"
wave = 8
requires = ["12.2"]
do_1 = "Implement the awaiting-approval state holding the request with its disclosure until an approver decides."
do_2 = "Expire an unapproved request deterministically at its declared window and never auto-approve on expiry."
do_3 = "Present the disclosure to the approver rather than the caller request, so approval covers what would actually be signed."
do_4 = "Record the approver identity, the decision and the disclosure digest in the audit trail."
do_5 = "Add tests covering approval after expiry, approval of an altered request and concurrent approvals."
verify_cmd = "make agent-test-agentd-approval"
symbols = ["layerx_agentd::policy::approval::hold","layerx_agentd::policy::approval::expire"]
touches = ["agent/crates/layerx-agentd/src/policy/approval.rs","agent/crates/layerx-agentd/tests/approval.rs"]
reqs = ["11.8","12.2","20.2"]

[task.12.5]
title = "Ship the policy test harness and the adversarial corpus"
status = "done"
wave = 8
requires = ["12.3","12.4"]
do_1 = "Ship a harness that evaluates a policy set against recorded request corpora and reports every decision, so a change can be verified before activation."
do_2 = "Assemble an adversarial corpus attempting to widen scope through crafted purposes, counterparties, unicode confusables, oversized fields and boundary amounts."
do_3 = "Assert deny-by-default holds across the corpus and that no crafted input produces an allow the policy did not intend."
do_4 = "Report corpus coverage across the constraint vocabulary and fail if a constraint is unexercised."
do_5 = "Wire the harness into make agent-test-policy-adversarial as a build gate."
verify_cmd = "make agent-test-policy-adversarial"
symbols = ["agent_policy_harness","agent_policy_adversarial_corpus"]
touches = ["agent/tools/policy-harness/src/main.rs","agent/tests/policy/adversarial.rs","Makefile"]
reqs = ["11.10","23.6","11.1"]

[task.13]
section = "Wave 9 - The Write Path and the Read Path"
title = "Implement activity preparation and signing"
status = "pending"

[task.13.1]
title = "Prepare complete unsigned canonical activities from core state"
status = "done"
wave = 9
requires = ["12.1","11.3"]
do_1 = "Implement preparation that constructs the complete unsigned canonical activity including actor, authority, account_sequence, timestamp_bound, idempotency_key, fee_limit, payload and payload_hash."
do_2 = "Take account_sequence and every other protocol-derived input from core state through the boundary and fail preparation when that state is unavailable rather than assuming a value."
do_3 = "Derive timestamp_bound and fee_limit from explicit caller input or explicit configured defaults and never widen a caller-supplied bound."
do_4 = "Return the exact canonical bytes and the signing preimage produced by layerx-wire, with no second encoding path anywhere in the daemon."
do_5 = "Add tests covering a stale sequence, an unavailable core, a widened bound attempt and a payload that exceeds a declared limit."
verify_cmd = "make agent-test-agentd-prepare"
symbols = ["layerx_agentd::prepare::prepare_activity","layerx_agentd::prepare::Prepared"]
touches = ["agent/crates/layerx-agentd/src/prepare/mod.rs","agent/crates/layerx-agentd/tests/prepare.rs"]
reqs = ["12.1","12.3","12.8","1.1"]

[task.13.2]
title = "Produce the structured disclosure and prove it matches the bytes"
status = "done"
wave = 9
requires = ["13.1","4.3"]
do_1 = "Produce the disclosure by decoding the prepared bytes rather than by copying the caller request, so the disclosure describes what would actually execute."
do_2 = "Include activity type, actor, authority, every counterparty, every amount, the asset, the fee limit, the expiry and the idempotency key."
do_3 = "Re-encode the disclosure and refuse preparation if the result is not byte-identical to the prepared bytes."
do_4 = "Return the disclosure with every preparation response and record its digest in the audit trail."
do_5 = "Add an adversarial test that mutates the prepared bytes after disclosure generation and asserts the mismatch is detected."
verify_cmd = "make agent-test-agentd-disclosure"
symbols = ["layerx_agentd::prepare::disclose","layerx_agentd::prepare::DisclosureDigest"]
touches = ["agent/crates/layerx-agentd/src/prepare/disclose.rs","agent/crates/layerx-agentd/tests/disclosure.rs"]
reqs = ["12.2","5.3","5.4"]

[task.13.3]
title = "Support external signing and session-key self-signing"
status = "done"
wave = 9
requires = ["13.2"]
do_1 = "Implement external signing as the default posture, in which preparation output leaves the daemon, a signature returns, and the daemon never holds an agent primary key."
do_2 = "Implement optional self-signing under an explicitly provisioned protocol session key, confined to the activity types, expiry and scope that key declares."
do_3 = "Record every self-signed activity distinctly in the audit trail and in the response so a reader can tell who signed."
do_4 = "Refuse self-signing for any activity type outside the provisioned session key scope even when policy would allow it."
do_5 = "Add tests covering an unprovisioned daemon asked to sign, a scope-exceeding self-sign attempt and an expired session key."
verify_cmd = "make agent-test-agentd-signing"
symbols = ["layerx_agentd::sign::external","layerx_agentd::sign::self_sign"]
touches = ["agent/crates/layerx-agentd/src/sign.rs","agent/crates/layerx-agentd/tests/signing.rs"]
reqs = ["12.6","12.7","5.7"]

[task.13.4]
title = "Verify signature over exact bytes before submission"
status = "done"
wave = 9
requires = ["13.3"]
do_1 = "Verify before submission that the signature verifies over the exact bytes about to be transmitted and refuse otherwise."
do_2 = "Detect any post-signature alteration through that verification and refuse submission, so a modified activity can never ride an earlier signature."
do_3 = "Carry the verified bytes through to the client submit path unchanged, with no re-encoding step between verification and transmission."
do_4 = "Record the activity identifier of the exact submitted bytes in the audit trail."
do_5 = "Add an adversarial test that alters amount, recipient and fee limit after signing and asserts refusal in each case."
verify_cmd = "make agent-test-agentd-signature-binding"
symbols = ["layerx_agentd::sign::verify_before_submit"]
touches = ["agent/crates/layerx-agentd/src/sign_verify.rs","agent/crates/layerx-agentd/tests/signature_binding.rs"]
reqs = ["12.4","12.5","7.2"]

[task.13.5]
title = "Expire prepared activities and release their reservations"
status = "done"
wave = 9
requires = ["13.1"]
do_1 = "Expire a prepared activity that is not submitted within its declared window and release every reservation it held."
do_2 = "Refuse to submit a prepared activity whose timestamp_bound has already passed relative to the core-reported batch time."
do_3 = "Retain signed bytes only for the retention needed to complete submission and receipt resolution, then discard them."
do_4 = "Redact payload contents in logs according to the configured redaction policy while keeping the activity identifier."
do_5 = "Add tests covering expiry during signing, expiry after signing and a retention sweep that must not delete an unresolved submission."
verify_cmd = "make agent-test-agentd-prepare-expiry"
symbols = ["layerx_agentd::prepare::expire","layerx_agentd::prepare::retention_sweep"]
touches = ["agent/crates/layerx-agentd/src/prepare/expiry.rs","agent/crates/layerx-agentd/tests/prepare_expiry.rs"]
reqs = ["12.9","12.10","10.5"]

[task.14]
title = "Implement submission, idempotency and receipt tracking"
status = "pending"

[task.14.1]
title = "Implement the durable outbox and the submission state machine"
status = "done"
wave = 9
requires = ["13.4"]
do_1 = "Record every submission in a durable outbox before transmission and retain it until a terminal state of executed, failed, expired or superseded."
do_2 = "Implement the state machine over prepared, signed, queued, submitted, acknowledged, unknown, executed, failed and expired, recording every transition with its cause."
do_3 = "Expose the current state and its evidence through the contract, with unknown reported as unknown rather than as an error."
do_4 = "Forbid any transition that reports success without a verified receipt reference and prove the absence of such a path in test."
do_5 = "Add tests driving every legal transition and asserting every illegal transition is rejected."
verify_cmd = "make agent-test-agentd-outbox"
symbols = ["layerx_agentd::outbox::Outbox","layerx_agentd::outbox::SubmissionState"]
touches = ["agent/crates/layerx-agentd/src/outbox/mod.rs","agent/crates/layerx-agentd/tests/outbox.rs"]
reqs = ["13.1","13.2","1.6"]

[task.14.2]
title = "Implement idempotency records and conflict detection"
status = "done"
wave = 9
requires = ["14.1"]
do_1 = "Implement caller-supplied idempotency keys across every mutating contract operation, storing the request digest and the original result."
do_2 = "Return the original result for a repeated key and a typed conflict error for a repeated key whose request body differs."
do_3 = "Carry the protocol idempotency_key through preparation and submission so one caller intent maps to exactly one protocol idempotency key."
do_4 = "Persist idempotency records durably with an explicit retention and prove that retention expiry cannot cause a duplicate economic effect within the protocol retention window."
do_5 = "Add a concurrency test issuing the same key from many callers simultaneously and asserting exactly one economic result."
verify_cmd = "make agent-test-agentd-idempotency"
symbols = ["layerx_agentd::idempotency::Store","layerx_agentd::idempotency::Conflict"]
touches = ["agent/crates/layerx-agentd/src/idempotency.rs","agent/crates/layerx-agentd/tests/idempotency.rs"]
reqs = ["13.6","13.3","23.5"]

[task.14.3]
title = "Implement unknown-state resolution by receipt lookup"
status = "done"
wave = 9
requires = ["14.1","7.3"]
do_1 = "Resolve an unknown submission only by looking up the receipt for its idempotency key, never by inferring the outcome from transport behaviour."
do_2 = "Apply bounded jittered backoff to resolution and expose the age and the attempt count of every unresolved submission."
do_3 = "Resend byte-identical bytes under the original idempotency key when a resend is warranted and treat an existing receipt as the outcome."
do_4 = "Keep every reservation and every consumed ceiling held while the state remains unknown."
do_5 = "Add fault-injection tests covering acknowledgement loss, response loss, node restart mid-flight and a receipt that appears minutes later."
verify_cmd = "make agent-test-agentd-unknown"
symbols = ["layerx_agentd::outbox::resolve_unknown","layerx_agentd::outbox::UnknownAge"]
touches = ["agent/crates/layerx-agentd/src/outbox/unknown.rs","agent/crates/layerx-agentd/tests/unknown.rs"]
reqs = ["13.4","1.6","10.6"]

[task.14.4]
title = "Verify, store and serve receipt bytes unchanged"
status = "done"
wave = 9
requires = ["14.3","5.2"]
do_1 = "Verify every retrieved receipt through layerx-proof before recording a terminal state and record the achieved verification level with it."
do_2 = "Store the exact receipt bytes and serve them unchanged by activity identifier, idempotency key and global sequence."
do_3 = "Record the exact protocol result code for a rejection, classify it as terminal or retriable from the protocol taxonomy, and never retry a terminal rejection."
do_4 = "Surface the result code unmodified to the caller with no local reinterpretation or substituted explanation."
do_5 = "Add tests asserting byte equality between stored and served receipts and asserting no retry occurs for each terminal code class."
verify_cmd = "make agent-test-agentd-receipts"
symbols = ["layerx_agentd::receipt::store","layerx_agentd::receipt::serve","layerx_agentd::receipt::classify"]
touches = ["agent/crates/layerx-agentd/src/receipt.rs","agent/crates/layerx-agentd/tests/receipts.rs"]
reqs = ["13.5","13.10","1.7"]

[task.14.5]
title = "Augment records with checkpoint proofs as finality advances"
status = "done"
wave = 9
requires = ["14.4","5.4"]
do_1 = "Follow checkpoint progress and augment stored submission records with the activity inclusion proof, the state inclusion proof, the checkpoint identifier, the guarantor certificate and the settlement reference."
do_2 = "Verify each augmentation before recording it and never alter a pre-checkpoint receipt field."
do_3 = "Raise the recorded verification level only to the level the verified evidence supports."
do_4 = "Allow a caller to wait for a declared level with an explicit deadline and return the level actually reached when the deadline elapses."
do_5 = "Add tests covering a checkpoint that never finalises, a certificate that arrives sub-threshold and a settlement reference that does not match."
verify_cmd = "make agent-test-agentd-finality"
symbols = ["layerx_agentd::finality::augment","layerx_agentd::finality::wait_for_level"]
touches = ["agent/crates/layerx-agentd/src/finality.rs","agent/crates/layerx-agentd/tests/finality.rs"]
reqs = ["13.8","13.9","6.4"]

[task.14.6]
title = "Recover the outbox on restart without duplication or loss"
status = "done"
wave = 9
requires = ["14.2","14.3"]
do_1 = "Recover the outbox on start, resume resolution of every non-terminal submission, and refuse writes until recovery completes."
do_2 = "Neither resubmit an activity already executed nor drop an activity that was signed and queued but never transmitted."
do_3 = "Reconstruct reservations, ceilings and budget accounting from persisted receipts before accepting a write."
do_4 = "Implement graceful shutdown that stops accepting work, durably records in-flight submissions and flushes audit entries."
do_5 = "Add a kill-and-restart test at every stage of the write path asserting exactly-once economic effect across the restart."
verify_cmd = "make agent-test-agentd-recovery"
symbols = ["layerx_agentd::outbox::recover","layerx_agentd::shutdown::graceful"]
touches = ["agent/crates/layerx-agentd/src/outbox/recover.rs","agent/crates/layerx-agentd/src/shutdown.rs","agent/crates/layerx-agentd/tests/recovery.rs"]
reqs = ["13.7","22.8","23.4"]

[task.15]
title = "Implement verified reads, proofs and availability in the daemon"
status = "pending"

[task.15.1]
title = "Serve balances at a requested verification level"
status = "done"
wave = 9
requires = ["7.4"]
do_1 = "Serve balances derived from core-produced state with the account, asset, global sequence and the batch or checkpoint the value is relative to."
do_2 = "Fetch and verify the proof material the requested level demands and return the level actually achieved."
do_3 = "Refuse rather than downgrade when the requested level cannot be achieved and name the missing evidence."
do_4 = "Return the current head, latest sealed batch and latest finalised checkpoint with every freshness-sensitive read."
do_5 = "Add tests asserting a value is never returned above its evidence and that a hostile node cannot raise the level by assertion."
verify_cmd = "make agent-test-agentd-balance"
symbols = ["layerx_agentd::read::balance","layerx_agentd::read::Freshness"]
touches = ["agent/crates/layerx-agentd/src/read/balance.rs","agent/crates/layerx-agentd/tests/balance.rs"]
reqs = ["14.1","14.2","14.6","1.3"]

[task.15.2]
title = "Serve ordered history with stable cursors"
status = "done"
wave = 9
requires = ["15.1"]
do_1 = "Serve activities, receipts and events as core-produced canonical bytes in strict global sequence order."
do_2 = "Implement stable cursors such that a resumed page continues exactly where the previous ended with no gap and no repetition."
do_3 = "Preserve served bytes exactly so a caller re-hashing them reproduces the committed roots for the covered batches."
do_4 = "Bound page size and total response size explicitly and report truncation as a cursor rather than as completeness."
do_5 = "Add tests covering concurrent writes during pagination, a restart mid-pagination and a cursor from a pruned range."
verify_cmd = "make agent-test-agentd-history"
symbols = ["layerx_agentd::read::history","layerx_agentd::read::Cursor"]
touches = ["agent/crates/layerx-agentd/src/read/history.rs","agent/crates/layerx-agentd/tests/history.rs"]
reqs = ["14.3","14.4","19.7"]

[task.15.3]
title = "Serve checkpoints, certificates and proof bundles"
status = "done"
wave = 9
requires = ["15.1","5.4"]
do_1 = "Serve checkpoint certificates with their header commitments, guarantor signature set, achieved count and threshold, and settlement reference where one exists."
do_2 = "Verify a certificate before returning it and refuse to return an unverified certificate as verified."
do_3 = "Serve proof bundles for activity inclusion and state inclusion against a named root, with the root provenance included."
do_4 = "Report the checkpoint as unavailable for verification purposes when its availability data cannot be obtained."
do_5 = "Add tests covering sub-threshold, duplicated-signer and mismatched-settlement certificates."
verify_cmd = "make agent-test-agentd-checkpoint"
symbols = ["layerx_agentd::read::checkpoint","layerx_agentd::read::proof_bundle"]
touches = ["agent/crates/layerx-agentd/src/read/checkpoint.rs","agent/crates/layerx-agentd/tests/checkpoint.rs"]
reqs = ["14.5","6.4","15.8"]

[task.15.4]
title = "Serve availability retrieval and report failures as evidence"
status = "done"
wave = 9
requires = ["7.6"]
do_1 = "Expose retrieval by checkpoint, batch, sequence range and activity identifier with streaming and explicit bounds."
do_2 = "Verify chunks and reassembled bytes before returning and report which of the five availability classes were obtained."
do_3 = "Classify a verification mismatch as an availability failure, retain the served bytes and the mismatching commitment, and record the failure with checkpoint, batch, chunk index and provider in the audit trail."
do_4 = "Emit retrieved history in a form an independent verifier can replay with no index or database, and document the ordering and framing."
do_5 = "Add tests covering withholding, corruption, partial classes and repeated failures from one provider."
verify_cmd = "make agent-test-agentd-availability"
symbols = ["layerx_agentd::read::availability","layerx_agentd::read::AvailabilityFailure"]
touches = ["agent/crates/layerx-agentd/src/read/availability.rs","agent/crates/layerx-agentd/tests/availability.rs"]
reqs = ["15.3","15.4","15.6","15.9"]

[task.15.5]
title = "Cache only with evidence and revalidate before serving stale"
status = "done"
wave = 9
requires = ["15.1"]
do_1 = "Cache core-produced bytes together with their verification evidence and never cache a decoded value without it."
do_2 = "Revalidate against the core before serving a value past its declared freshness bound and never serve above the evidence held."
do_3 = "Invalidate cached values on head movement, reorganisation-free sequence advance and checkpoint finalisation in a way that cannot raise a level."
do_4 = "Bound cache size and report hit and staleness metrics per tenant."
do_5 = "Add tests asserting a cached value cannot be served at a higher level than when it was cached and that a stale value is revalidated."
verify_cmd = "make agent-test-agentd-cache"
symbols = ["layerx_agentd::cache::EvidenceCache","layerx_agentd::cache::revalidate"]
touches = ["agent/crates/layerx-agentd/src/cache.rs","agent/crates/layerx-agentd/tests/cache.rs"]
reqs = ["14.7","1.3","22.4"]

[task.15.6]
title = "Implement the offline verification export"
status = "done"
wave = 9
requires = ["15.3","14.5"]
do_1 = "Implement an export that emits the receipts, proofs, certificates and headers needed for a third party to verify a stated fact set offline."
do_2 = "Include only core-produced bytes and their provenance, with no daemon-computed value presented as a fact."
do_3 = "Ship a verifier example that consumes the export with layerx-proof alone and confirms the facts without the daemon or a node."
do_4 = "Identify every contributing receipt for any derived aggregate included in the export so the aggregate is reconstructible."
do_5 = "Add a test that verifies an export on a machine with no network and no access to the daemon."
verify_cmd = "make agent-test-agentd-export"
symbols = ["layerx_agentd::export::build","layerx_proof::export::verify"]
touches = ["agent/crates/layerx-agentd/src/export.rs","agent/crates/layerx-proof/src/export.rs","agent/crates/layerx-agentd/tests/export.rs"]
reqs = ["14.10","14.9","1.10"]

[task.16]
title = "Implement streaming and durable subscriptions"
status = "pending"

[task.16.1]
title = "Ingest core events and preserve global order"
status = "done"
wave = 9
requires = ["7.5"]
do_1 = "Ingest events from the boundary stream and persist them in strict global sequence order with no synthesis, enrichment or reordering."
do_2 = "Attach to each stored event the receipt reference and verification level of any receipt it depends on."
do_3 = "Bound the ingestion buffer explicitly and apply backpressure to ingestion rather than dropping events."
do_4 = "Record the ingestion watermark durably so a restart resumes without re-ingesting or skipping."
do_5 = "Add tests covering ingestion restart, a node that repeats an event and a node that emits out of order."
verify_cmd = "make agent-test-agentd-ingest"
symbols = ["layerx_agentd::events::ingest","layerx_agentd::events::Watermark"]
touches = ["agent/crates/layerx-agentd/src/events/ingest.rs","agent/crates/layerx-agentd/tests/ingest.rs"]
reqs = ["16.1","1.5","19.5"]

[task.16.2]
title = "Implement the durable subscription store and cursors"
status = "done"
wave = 9
requires = ["16.1"]
do_1 = "Persist each subscription with its filter, start position, delivery target, owning tenant, agent and scope."
do_2 = "Restore and resume subscriptions after restart from the last acknowledged cursor."
do_3 = "Apply tenant and scope restrictions before any filter so a filter can never widen visibility."
do_4 = "Implement pause, resume, delete and cursor acknowledgement with durable effect."
do_5 = "Add tests covering restart mid-delivery, a filter naming another tenant object and an acknowledgement for a cursor that was never delivered."
verify_cmd = "make agent-test-agentd-subscription"
symbols = ["layerx_agentd::events::subscription::Store","layerx_agentd::events::subscription::Cursor"]
touches = ["agent/crates/layerx-agentd/src/events/subscription.rs","agent/crates/layerx-agentd/tests/subscription.rs"]
reqs = ["16.2","16.6","21.5"]

[task.16.3]
title = "Implement delivery semantics, deduplication identity and backfill seams"
status = "done"
wave = 9
requires = ["16.2"]
do_1 = "Deliver at least once with a monotonic cursor and a deduplication identifier derived from event identity, and document the consumer obligation to deduplicate."
do_2 = "Backfill from durable history for a subscription started at a historical position and transition to live delivery with no gap and no duplicate at the seam."
do_3 = "Make the backfill-to-live transition point observable to the consumer."
do_4 = "Apply bounded buffering with explicit backpressure and never drop an event silently to keep up."
do_5 = "Add tests covering the seam under load, a consumer that stalls at the seam and a subscription started before genesis of its filter subject."
verify_cmd = "make agent-test-agentd-delivery"
symbols = ["layerx_agentd::events::deliver","layerx_agentd::events::backfill"]
touches = ["agent/crates/layerx-agentd/src/events/deliver.rs","agent/crates/layerx-agentd/tests/delivery.rs"]
reqs = ["16.3","16.5","16.7"]

[task.16.4]
title = "Detect and surface gaps instead of closing them silently"
status = "done"
wave = 9
requires = ["16.3"]
do_1 = "Detect a global sequence gap and emit an explicit gap event naming the missing range."
do_2 = "Attempt backfill from the core for the missing range and report the outcome of that attempt to the consumer."
do_3 = "Refuse to continue delivering past a gap as though the stream were contiguous, and mark the subscription state accordingly."
do_4 = "Mark a subscription truncated rather than silently repaired when undelivered events fall outside the declared retention bound."
do_5 = "Add tests covering a skipped sequence, an unrecoverable gap and retention truncation during an outage."
verify_cmd = "make agent-test-agentd-gaps"
symbols = ["layerx_agentd::events::gap::detect","layerx_agentd::events::gap::Truncated"]
touches = ["agent/crates/layerx-agentd/src/events/gap.rs","agent/crates/layerx-agentd/tests/gaps.rs"]
reqs = ["16.4","16.9","1.5"]

[task.16.5]
title = "Implement outbound delivery, authentication and lag reporting"
status = "done"
wave = 9
requires = ["16.3"]
do_1 = "Implement delivery to external endpoints with mutual authentication and a signed or otherwise bound payload so a receiver can confirm origin."
do_2 = "Include the underlying receipt or event bytes required for the receiver to verify independently."
do_3 = "Retry failures with bounded jittered backoff without exceeding at-least-once semantics, and stop delivery immediately on revocation or deletion."
do_4 = "Expose subscription health including cursor position, lag in sequences, last delivery, failure count and last error."
do_5 = "Add tests covering an unreachable endpoint, a slow endpoint, revocation mid-flight and delivery after deletion."
verify_cmd = "make agent-test-agentd-webhook"
symbols = ["layerx_agentd::events::outbound::deliver","layerx_agentd::events::health"]
touches = ["agent/crates/layerx-agentd/src/events/outbound.rs","agent/crates/layerx-agentd/tests/webhook.rs"]
reqs = ["16.8","16.9","16.10"]

[task.17]
section = "Wave 10 - Limits, Tenancy and Evidence"
title = "Implement rate limits, quotas and backpressure"
status = "pending"

[task.17.1]
title = "Implement layered limits and typed refusals"
status = "done"
wave = 10
requires = ["14.1"]
do_1 = "Implement rate limits per tenant, per agent, per session, per capability and per operation class, applying every applicable limit."
do_2 = "Return a typed rate-limit error carrying the limit, the window, the remaining quota and a retry-after hint, and never queue indefinitely in place of refusing."
do_3 = "Enforce limits deterministically from recorded counters and the request, and document the consistency model plainly for multi-instance deployment."
do_4 = "Expose live configuration and utilisation per tenant so a refusal is diagnosable without reading logs."
do_5 = "Add tests covering each limit scope, a burst at the boundary and a multi-instance counter divergence scenario."
verify_cmd = "make agent-test-agentd-ratelimit"
symbols = ["layerx_agentd::limits::RateLimiter","layerx_agentd::limits::Refusal"]
touches = ["agent/crates/layerx-agentd/src/limits/rate.rs","agent/crates/layerx-agentd/tests/ratelimit.rs"]
reqs = ["19.1","19.2","19.3","19.10"]

[task.17.2]
title = "Implement boundary admission control and priority"
status = "done"
wave = 10
requires = ["17.1","7.2"]
do_1 = "Apply admission control to outbound boundary traffic so retries, backfills and subscription catch-up cannot saturate the node interface."
do_2 = "Prioritise submission delivery and receipt resolution over bulk reads under contention, and prove the priority holds under a synthetic bulk-read storm."
do_3 = "Propagate core backpressure and unavailability to callers as backpressure rather than absorbing it internally."
do_4 = "Bound every internal queue with explicit capacity and a defined overflow behaviour."
do_5 = "Add tests covering saturation, a slow node and a burst of reconnecting subscriptions."
verify_cmd = "make agent-test-agentd-admission"
symbols = ["layerx_agentd::limits::admission","layerx_agentd::limits::Priority"]
touches = ["agent/crates/layerx-agentd/src/limits/admission.rs","agent/crates/layerx-agentd/tests/admission.rs"]
reqs = ["19.4","19.5","7.8"]

[task.17.3]
title = "Bound request lifetime and cancel orphaned work"
status = "done"
wave = 10
requires = ["17.2"]
do_1 = "Apply an explicit deadline to every request and every boundary call within it."
do_2 = "Cancel downstream work when a caller disconnects, except work whose cancellation would orphan a submission."
do_3 = "Guarantee that no submission is left without an owner resolving its state, and prove it with a disconnect-mid-submission test."
do_4 = "Expose the population and age distribution of in-flight and unresolved work."
do_5 = "Add tests covering caller disconnect at every write stage and a deadline expiring during unknown resolution."
verify_cmd = "make agent-test-agentd-deadlines"
symbols = ["layerx_agentd::limits::deadline","layerx_agentd::limits::cancel"]
touches = ["agent/crates/layerx-agentd/src/limits/deadline.rs","agent/crates/layerx-agentd/tests/deadlines.rs"]
reqs = ["19.6","13.4","1.6"]

[task.17.4]
title = "Implement durable resource quotas and load shedding"
status = "done"
wave = 10
requires = ["17.1"]
do_1 = "Apply quotas to subscriptions, prepared activities, outbox entries, audit retention and stored receipts, refusing creation past a quota."
do_2 = "Detect a retry storm, a hot loop or a pathological client and shed load for that client specifically."
do_3 = "Never shed load for unrelated tenants to compensate and record every shedding decision."
do_4 = "Report quota utilisation per tenant through health and metrics."
do_5 = "Add tests covering quota exhaustion, a pathological client and the isolation of an unrelated tenant during shedding."
verify_cmd = "make agent-test-agentd-quota"
symbols = ["layerx_agentd::limits::Quota","layerx_agentd::limits::shed"]
touches = ["agent/crates/layerx-agentd/src/limits/quota.rs","agent/crates/layerx-agentd/tests/quota.rs"]
reqs = ["19.7","19.8","21.4"]

[task.17.5]
title = "Prove limits never cause a duplicate economic effect"
status = "done"
wave = 10
requires = ["17.4","14.2"]
do_1 = "Assert that a refused retry leaves the idempotency record and the outbox in a state that resolves to exactly one outcome."
do_2 = "Assert that shedding, quota refusal and deadline cancellation never release a reservation whose activity is unresolved."
do_3 = "Drive a combined storm of retries, shedding and restarts and assert exactly one receipt per intent."
do_4 = "Record any observed violation with the full submission history so it is diagnosable rather than flaky."
do_5 = "Wire the suite into make agent-test-limits-exactly-once as a build gate."
verify_cmd = "make agent-test-limits-exactly-once"
symbols = ["agent_limits_exactly_once_suite"]
touches = ["agent/tests/limits/exactly_once.rs","Makefile"]
reqs = ["19.9","23.5","13.3"]

[task.18]
title = "Implement tenant isolation"
status = "pending"

[task.18.1]
title = "Enforce tenancy in the storage access path"
status = "done"
wave = 10
requires = ["9.1"]
do_1 = "Scope every stored object to exactly one tenant in its key and make a query without a tenant unrepresentable rather than filtered at the edge."
do_2 = "Audit every storage call site to prove no path constructs a key without a tenant, enforced by a compile-time or checker-level gate."
do_3 = "Cover identities, sessions, capabilities, budgets, policies, prepared activities, outbox entries, receipts, subscriptions and audit entries."
do_4 = "Add a storage-level test that attempts a cross-tenant key construction and asserts it does not compile or is rejected."
do_5 = "Add a fuzz test over key construction asserting tenancy is always present and correctly delimited."
verify_cmd = "make agent-test-agentd-tenant-store"
symbols = ["layerx_agentd::store::TenantScoped","layerx_agentd::store::key"]
touches = ["agent/crates/layerx-agentd/src/store/tenant.rs","agent/crates/layerx-agentd/tests/tenant_store.rs"]
reqs = ["21.1","9.1"]

[task.18.2]
title = "Resolve tenancy from the authenticated principal only"
status = "done"
wave = 10
requires = ["18.1"]
do_1 = "Resolve the tenant from the authenticated principal and ignore any tenant identifier supplied in a request body or header as authoritative."
do_2 = "Refuse a request whose target object belongs to another tenant with an authorisation error that reveals nothing about the object existence."
do_3 = "Apply the same resolution to every surface including the contract, the SDKs, the MCP tools, subscriptions and exports."
do_4 = "Record the resolved tenant in every audit entry, metric and trace."
do_5 = "Add tests covering a spoofed tenant field, a cross-tenant object identifier and a token replayed across tenants."
verify_cmd = "make agent-test-agentd-tenant-resolve"
symbols = ["layerx_agentd::tenant::resolve","layerx_agentd::tenant::require_owner"]
touches = ["agent/crates/layerx-agentd/src/tenant.rs","agent/crates/layerx-agentd/tests/tenant_resolve.rs"]
reqs = ["21.2","21.10","8.7"]

[task.18.3]
title = "Isolate signers, limits and subscriptions per tenant"
status = "done"
wave = 10
requires = ["18.2","16.2"]
do_1 = "Scope every signer binding and key material to its tenant and forbid one tenant using another signer."
do_2 = "Apply limits, quotas and shedding per tenant so one tenant cannot consume another capacity, reserving capacity for submission delivery and receipt resolution across tenants."
do_3 = "Scope subscriptions, streams and MCP servers to a tenant and guarantee no filter, cursor, backfill or error can expose another tenant data or existence."
do_4 = "Support per-tenant configuration of policy, redaction, retention, verification defaults and approval requirements."
do_5 = "Add tests covering signer cross-use, capacity starvation and a cross-tenant subscription filter."
verify_cmd = "make agent-test-agentd-tenant-isolation"
symbols = ["layerx_agentd::tenant::SignerBinding","layerx_agentd::tenant::Config"]
touches = ["agent/crates/layerx-agentd/src/tenant/isolation.rs","agent/crates/layerx-agentd/tests/tenant_isolation.rs"]
reqs = ["21.3","21.4","21.5","21.7"]

[task.18.4]
title = "Remove tenancy leakage from errors, metrics and traces"
status = "done"
wave = 10
requires = ["18.3"]
do_1 = "Ensure an internal error message, trace or metric label carries no data belonging to another tenant, including raw identifiers from a failed cross-tenant lookup."
do_2 = "Keep metric cardinality bounded by forbidding high-cardinality identifiers such as activity identifiers in labels."
do_3 = "Normalise not-found and not-authorised responses so existence is not distinguishable across tenants."
do_4 = "Review timing-sensitive paths so a distinguishable timing signal does not reveal existence, and document the mitigations applied."
do_5 = "Add tests asserting response shape and timing indistinguishability for existing and non-existing cross-tenant objects."
verify_cmd = "make agent-test-agentd-tenant-leakage"
symbols = ["layerx_agentd::tenant::normalize_error"]
touches = ["agent/crates/layerx-agentd/src/tenant/errors.rs","agent/crates/layerx-agentd/tests/tenant_leakage.rs"]
reqs = ["21.6","21.8","20.6"]

[task.18.5]
title = "Ship the cross-tenant isolation suite and tenant deletion"
status = "done"
wave = 10
requires = ["18.4"]
do_1 = "Build a suite that attempts cross-tenant access through every public surface including the API, the SDKs, the MCP tools, subscriptions, exports and error paths."
do_2 = "Treat any leakage, including a distinguishable existence or timing signal, as a build-breaking defect rather than a tracked issue."
do_3 = "Implement tenant deletion that removes or irreversibly key-destroys tenant data per the configured retention policy and records the deletion in the audit trail."
do_4 = "Retain only what an explicitly configured legal retention requires and prove the retained set contains no protocol-derived value that could not be re-fetched."
do_5 = "Wire the suite into make agent-test-isolation as a release gate."
verify_cmd = "make agent-test-isolation"
symbols = ["agent_isolation_suite","layerx_agentd::tenant::delete"]
touches = ["agent/tests/isolation/src/main.rs","agent/crates/layerx-agentd/src/tenant/delete.rs","Makefile"]
reqs = ["21.8","21.9","23.6"]

[task.19]
title = "Implement the audit trail and observability"
status = "pending"

[task.19.1]
title = "Implement the append-only hash-chained audit log"
status = "done"
wave = 10
requires = ["12.3"]
do_1 = "Implement an append-only audit log with a hash chain over its entries so excision or reordering is detectable."
do_2 = "Ship a verifier tool that checks the chain and reports the first inconsistent entry."
do_3 = "Persist entries durably before the audited operation is allowed to proceed."
do_4 = "Refuse the operation when the audit entry cannot be written, because an unaudited mutation attempt is not permitted."
do_5 = "Add tests covering a tampered entry, a removed entry, a reordered segment and a write failure during audit."
verify_cmd = "make agent-test-agentd-audit-chain"
symbols = ["layerx_agentd::audit::Log","layerx_agentd::audit::verify_chain"]
touches = ["agent/crates/layerx-agentd/src/audit/log.rs","agent/tools/audit-verify/src/main.rs","agent/crates/layerx-agentd/tests/audit_chain.rs"]
reqs = ["20.3","20.9"]

[task.19.2]
title = "Record decision and mutation evidence on every path"
status = "done"
wave = 10
requires = ["19.1","14.4"]
do_1 = "Record authentication, session lifecycle, capability decisions, policy decisions, budget reservations, preparations, signature requests, submissions, terminal outcomes, subscription changes, configuration changes and administrative actions."
do_2 = "Carry tenant, agent, session, capability, policy version, request identifier, idempotency key, decision, reason, resulting activity identifier and verification level on every entry."
do_3 = "Make the trail sufficient to reconstruct why an activity was allowed, what exact bytes were submitted, which authority was used and what the core returned, using entries and stored receipts alone."
do_4 = "Assert reconstruction in a test that replays a recorded session from the audit trail alone."
do_5 = "Add coverage reporting over audited event classes and fail if a class is unaudited."
verify_cmd = "make agent-test-agentd-audit-coverage"
symbols = ["layerx_agentd::audit::record","layerx_agentd::audit::Entry"]
touches = ["agent/crates/layerx-agentd/src/audit/record.rs","agent/crates/layerx-agentd/tests/audit_coverage.rs"]
reqs = ["20.1","20.2","20.5"]

[task.19.3]
title = "Enforce redaction and secret hygiene across all outputs"
status = "done"
wave = 10
requires = ["19.2"]
do_1 = "Forbid private key material, session token values, unredacted secret configuration and out-of-retention payload contents from audit entries, logs, metrics, traces and error messages."
do_2 = "Implement per-tenant redaction configuration applied uniformly across every output surface."
do_3 = "Add an output-scanning test that drives every error and log path with loaded secrets and asserts no secret appears."
do_4 = "Fail the build when a new output surface bypasses the redaction wrapper."
do_5 = "Document the redaction model and its limits plainly rather than implying completeness it does not have."
verify_cmd = "make agent-test-agentd-redaction"
symbols = ["layerx_agentd::audit::redact","layerx_agentd::obs::redact"]
touches = ["agent/crates/layerx-agentd/src/audit/redact.rs","agent/crates/layerx-agentd/tests/redaction.rs"]
reqs = ["20.4","21.7","5.5"]

[task.19.4]
title = "Emit metrics, traces and health surfaces"
status = "done"
wave = 10
requires = ["19.2"]
do_1 = "Emit metrics for submission outcomes by result class, unknown-state population and age, verification levels achieved, boundary latency and error classes, policy and capability decisions, budget utilisation, subscription lag and rate-limit refusals."
do_2 = "Label metrics by tenant while keeping cardinality bounded and forbidding high-cardinality identifiers in labels."
do_3 = "Emit traces spanning caller, policy, preparation, signing, submission and receipt resolution with a request identifier shared by logs, traces and audit entries."
do_4 = "Expose health distinguishing liveness, readiness for writes, boundary connectivity, verification backlog, unknown backlog and degraded modes."
do_5 = "Add tests asserting readiness reports not-ready rather than accepting writes that cannot be delivered."
verify_cmd = "make agent-test-agentd-observability"
symbols = ["layerx_agentd::obs::metrics","layerx_agentd::obs::health"]
touches = ["agent/crates/layerx-agentd/src/obs/metrics.rs","agent/crates/layerx-agentd/src/obs/health.rs","agent/crates/layerx-agentd/tests/observability.rs"]
reqs = ["20.6","20.7","20.8"]

[task.19.5]
title = "Implement audit export with referenced receipts and proofs"
status = "done"
wave = 10
requires = ["19.2","15.6"]
do_1 = "Implement export of an audit slice by tenant, agent or time range together with the receipts and proofs it references."
do_2 = "Include the chain verification material so a reviewer can confirm the slice is intact and unexcised."
do_3 = "Keep the export within tenancy and apply the tenant redaction configuration."
do_4 = "Ship a reviewer example that verifies the protocol facts in an export independently with layerx-proof."
do_5 = "Add tests covering an export spanning a retention boundary and an export containing an availability failure record."
verify_cmd = "make agent-test-agentd-audit-export"
symbols = ["layerx_agentd::audit::export"]
touches = ["agent/crates/layerx-agentd/src/audit/export.rs","agent/crates/layerx-agentd/tests/audit_export.rs"]
reqs = ["20.10","21.9","14.10"]

[task.20]
title = "Implement configuration, degraded modes and operations"
status = "pending"

[task.20.1]
title = "Validate configuration fully and refuse unsafe startup"
status = "done"
wave = 10
requires = ["9.1"]
do_1 = "Load configuration from explicit files and environment with a documented precedence and validate it completely at startup."
do_2 = "Require explicit network identifier, node endpoint, expected protocol version, tenant set, policy source, signer configuration and verification defaults."
do_3 = "Treat an unset security-relevant setting as a startup failure rather than a permissive default, and enumerate those settings in one place."
do_4 = "Refuse to start on invalid, ambiguous or unsafe configuration with an error naming the setting and why it was rejected."
do_5 = "Add tests covering each missing security-relevant setting and an ambiguous precedence case."
verify_cmd = "make agent-test-agentd-config"
symbols = ["layerx_agentd::config::load","layerx_agentd::config::validate"]
touches = ["agent/crates/layerx-agentd/src/config.rs","agent/crates/layerx-agentd/tests/config.rs"]
reqs = ["22.1","22.2"]

[task.20.2]
title = "Gate writes on a successful boundary handshake"
status = "done"
wave = 10
requires = ["20.1","6.3"]
do_1 = "Perform the boundary handshake at startup and report node interface version, protocol version, network identifier, capability intersection and every missing capability."
do_2 = "Refuse writes until the handshake succeeds and report not-ready rather than queuing them."
do_3 = "Refuse to operate against a network identifier or protocol version the daemon was not configured for."
do_4 = "Re-run the handshake on reconnection and re-evaluate the capability intersection."
do_5 = "Add tests covering a node upgrade mid-session, a network mismatch and a capability that disappears on reconnect."
verify_cmd = "make agent-test-agentd-handshake-gate"
symbols = ["layerx_agentd::boot::handshake_gate"]
touches = ["agent/crates/layerx-agentd/src/boot.rs","agent/crates/layerx-agentd/tests/handshake_gate.rs"]
reqs = ["22.3","2.4","2.6"]

[task.20.3]
title = "Implement degraded read mode and honest staleness"
status = "done"
wave = 10
requires = ["20.2","15.5"]
do_1 = "Enter a degraded read mode when the core is unreachable, serving only values that can still be verified and marking their staleness with the head and checkpoint they are relative to."
do_2 = "Refuse preparation that needs live core state and refuse submission acknowledgement while degraded, while continuing to resolve unknown submissions when connectivity returns."
do_3 = "Propagate a core halted, emergency or data-unavailable mode into health and read responses and stop advertising finality that can no longer be verified."
do_4 = "Make every degraded state visible through health, metrics and the status surface rather than only in logs."
do_5 = "Add tests covering core loss mid-write, core loss mid-stream and a core that returns to a lower finality than previously advertised."
verify_cmd = "make agent-test-agentd-degraded"
symbols = ["layerx_agentd::degraded::Mode","layerx_agentd::degraded::enter"]
touches = ["agent/crates/layerx-agentd/src/degraded.rs","agent/crates/layerx-agentd/tests/degraded.rs"]
reqs = ["22.4","22.5","2.7"]

[task.20.4]
title = "Implement versioned store migration and graceful shutdown"
status = "done"
wave = 10
requires = ["20.1","14.6"]
do_1 = "Implement forward-only versioned migration of local stores with a refusal to start against a store from a newer version."
do_2 = "Forbid re-deriving any receipt, proof or protocol fact from anything other than the core during migration."
do_3 = "Implement graceful shutdown that stops accepting work, completes or durably records in-flight submissions, flushes audit entries and exits cleanly."
do_4 = "Publish the compatibility matrix of daemon version against node interface version against contract version against SDK version and verify the supported range in CI against a real node."
do_5 = "Add tests covering migration from each supported prior version and shutdown at every write stage."
verify_cmd = "make agent-test-agentd-migration"
symbols = ["layerx_agentd::store::migrate_forward","layerx_agentd::compat::matrix"]
touches = ["agent/crates/layerx-agentd/src/store/migrate_forward.rs","agent/crates/layerx-agentd/src/compat.rs","agent/crates/layerx-agentd/tests/migration.rs"]
reqs = ["22.6","22.7","22.8"]

[task.20.5]
title = "Implement the operator surface without protocol-mutating power"
status = "done"
wave = 10
requires = ["20.3","19.4"]
do_1 = "Implement operator commands to inspect and act on unknown submissions, stalled subscriptions, budget divergence and verification backlog."
do_2 = "Make every operator action audited and non-mutating with respect to protocol state."
do_3 = "Expose no administrative path that alters a recorded receipt, a verification level, an audit entry or a protocol-derived value, and prove the absence of such a path in test."
do_4 = "Require an operator action that would submit an activity to go through the identical prepare, policy, sign and submit path as any client."
do_5 = "Add adversarial tests attempting to mark an unknown submission executed, edit a receipt and raise a verification level through the operator surface."
verify_cmd = "make agent-test-agentd-operator"
symbols = ["layerx_agentd::admin::commands","layerx_agentd::admin::assert_non_mutating"]
touches = ["agent/crates/layerx-agentd/src/admin.rs","agent/crates/layerx-agentd/tests/operator.rs"]
reqs = ["22.9","22.10","1.2"]

[task.21]
section = "Wave 11 - Model and Application Surfaces"
title = "Build layerx-mcp, scoped model-facing tools"
status = "pending"

[task.21.1]
title = "Implement the MCP server bound to one tenant and scope"
status = "done"
wave = 11
requires = ["8.6","9.3"]
do_1 = "Implement an MCP server that binds at startup to exactly one tenant and one scope set derived from a session and capability created through the normal daemon path."
do_2 = "Expose no tool outside the bound scope and refuse to start when the scope cannot be established."
do_3 = "Declare for every tool the scope it requires, the mutations it can cause and the evidence it returns."
do_4 = "Route every operation through the daemon so policy, capability, budget, rate limit and audit apply identically, with no bypass path."
do_5 = "Add tests proving a tool outside scope is absent and that no tool reaches the boundary except through the daemon."
verify_cmd = "make agent-test-mcp-scope"
symbols = ["layerx_mcp::server::Server","layerx_mcp::server::ScopeBinding"]
touches = ["agent/crates/layerx-mcp/src/server.rs","agent/crates/layerx-mcp/tests/scope.rs"]
reqs = ["17.1","17.2","17.7"]

[task.21.2]
title = "Implement read tools that return verified values only"
status = "done"
wave = 11
requires = ["21.1","15.1"]
do_1 = "Implement read tools for balances, history, receipts, checkpoints, proofs and availability that return core-produced values with their verification level attached."
do_2 = "Forbid a read tool from returning a summary, estimate or inference presented as a protocol fact, and label any projection explicitly."
do_3 = "Bound tool result size and paginate large results with stable cursors, never truncating silently in a way that reads as completeness."
do_4 = "Return the freshness reference with every result so a model sees what the value is relative to."
do_5 = "Add tests asserting every read tool response carries a level and that truncation is always explicit."
verify_cmd = "make agent-test-mcp-read"
symbols = ["layerx_mcp::tools::read","layerx_mcp::tools::Pagination"]
touches = ["agent/crates/layerx-mcp/src/tools/read.rs","agent/crates/layerx-mcp/tests/read.rs"]
reqs = ["17.3","17.9","1.3"]

[task.21.3]
title = "Implement write tools over prepare, disclose, sign and submit"
status = "done"
wave = 11
requires = ["21.1","14.1"]
do_1 = "Implement write tools that follow the prepare, disclose, sign, submit and track path exactly as any other client."
do_2 = "Return the receipt or the honest non-terminal state, and never report success before a verified receipt exists."
do_3 = "Return a typed error naming the failing stage and the protocol result code where one exists, never prose a model could mistake for a completed action."
do_4 = "Record every tool invocation with its arguments digest, decision and outcome in the audit trail."
do_5 = "Add tests covering a refused policy, an unknown outcome, a terminal protocol rejection and a submission whose receipt arrives late."
verify_cmd = "make agent-test-mcp-write"
symbols = ["layerx_mcp::tools::write","layerx_mcp::tools::WriteOutcome"]
touches = ["agent/crates/layerx-mcp/src/tools/write.rs","agent/crates/layerx-mcp/tests/write.rs"]
reqs = ["17.4","17.8","1.6"]

[task.21.4]
title = "Implement approval thresholds and deterministic expiry"
status = "done"
wave = 11
requires = ["21.3","12.4"]
do_1 = "Require explicit approval before submission when a write tool would exceed a configured threshold."
do_2 = "Present the disclosure to the approver rather than the model request, so approval covers what would actually be signed."
do_3 = "Expire an unapproved request deterministically and never proceed on expiry."
do_4 = "Record approver identity, decision and disclosure digest in the audit trail."
do_5 = "Add tests covering threshold boundary amounts, approval of an altered request and expiry during approval."
verify_cmd = "make agent-test-mcp-approval"
symbols = ["layerx_mcp::approval::require","layerx_mcp::approval::expire"]
touches = ["agent/crates/layerx-mcp/src/approval.rs","agent/crates/layerx-mcp/tests/approval.rs"]
reqs = ["17.5","11.8","12.2"]

[task.21.5]
title = "Treat all tool input as untrusted and resist instruction injection"
status = "done"
wave = 11
requires = ["21.2","21.3"]
do_1 = "Validate every tool argument against the contract schema before use and reject anything outside it."
do_2 = "Ensure instructions embedded in arguments, resource content or tool results cannot widen scope, alter a capability, change an approval requirement or redirect a counterparty."
do_3 = "Keep every authority, scope and threshold decision on data the daemon holds, never on text the model supplied."
do_4 = "Build an injection corpus attempting each escalation through crafted text, unicode confusables, embedded directives and oversized arguments."
do_5 = "Wire the corpus into make agent-test-mcp-injection as a build gate and treat any escape as build-breaking."
verify_cmd = "make agent-test-mcp-injection"
symbols = ["layerx_mcp::validate::arguments","agent_mcp_injection_corpus"]
touches = ["agent/crates/layerx-mcp/src/validate.rs","agent/tests/mcp/injection.rs","Makefile"]
reqs = ["17.6","23.6","11.1"]

[task.21.6]
title = "Ship the read-only deployment mode"
status = "done"
wave = 11
requires = ["21.5"]
do_1 = "Implement a read-only deployment in which every write tool is absent from the tool list rather than merely refused."
do_2 = "Prove by test that no write path is reachable in read-only mode, including through pagination, resources and errors."
do_3 = "Make the mode visible in the server capability declaration so a client can see what it is connected to."
do_4 = "Document the two modes and the guarantee each provides without overstating either."
do_5 = "Add tests asserting the tool list difference and the absence of any write side effect in read-only mode."
verify_cmd = "make agent-test-mcp-readonly"
symbols = ["layerx_mcp::server::ReadOnly"]
touches = ["agent/crates/layerx-mcp/src/readonly.rs","agent/crates/layerx-mcp/tests/readonly.rs"]
reqs = ["17.10","17.1"]

[task.22]
title = "Build layerx-sdk and the generated TypeScript and Python SDKs"
status = "pending"

[task.22.1]
title = "Author the Rust SDK surface"
status = "pending"
wave = 11
requires = ["8.6","7.6"]
do_1 = "Implement the Rust SDK over layerx-client and the contract covering identity and sessions, capabilities and budgets, policy dry-run, preparation, signing integration, submission and tracking, verified reads, proof verification, availability retrieval and subscriptions."
do_2 = "Make the SDK usable both against a daemon and directly against a node with no daemon present."
do_3 = "Integrate external signers through an explicit interface and forbid any SDK path that exports key material."
do_4 = "Preserve verification levels, honest unknown state, idempotency handling and typed protocol result codes across the whole surface."
do_5 = "Add tests covering both deployment shapes and asserting identical guarantees in each."
verify_cmd = "make agent-test-sdk-rust"
symbols = ["layerx_sdk::Client","layerx_sdk::signer::ExternalSigner"]
touches = ["agent/crates/layerx-sdk/src/lib.rs","agent/crates/layerx-sdk/tests/sdk.rs"]
reqs = ["18.2","18.4","18.8","7.10"]

[task.22.2]
title = "Build the schema-to-SDK generator and the drift gate"
status = "pending"
wave = 11
requires = ["22.1"]
do_1 = "Implement the generator that emits SDK client code from the contract schema for each target language from one shared model."
do_2 = "Regenerate and diff generated output in CI and fail the build on drift or on a hand-edit to generated files."
do_3 = "Enforce that consensus-critical integers map to exact integer representations in every target and fail rather than lose precision."
do_4 = "Emit documentation from the schema that states which guarantees are protocol-enforced and which are daemon-enforced."
do_5 = "Add generator tests over the golden schema asserting stable, deterministic output."
verify_cmd = "make agent-test-sdk-generate"
symbols = ["agent_sdk_generator","agent_sdk_drift_gate"]
touches = ["agent/tools/sdk-gen/src/main.rs","agent/tools/sdk-gen/templates/","agent/tools/sdk-gen/tests/golden.rs"]
reqs = ["18.3","18.5","18.10"]

[task.22.3]
title = "Generate and package the TypeScript SDK"
status = "pending"
wave = 11
requires = ["22.2"]
do_1 = "Generate the TypeScript SDK from the schema with exact integer handling for every consensus-critical value."
do_2 = "Preserve verification levels, unknown submission state, idempotency handling and the typed error taxonomy in the generated surface."
do_3 = "Package it with types, an integration test suite against a running daemon and reproducible build metadata."
do_4 = "Ship runnable examples for payment with receipt verification, a 402-style settlement, budget-constrained delegated spending, a service lifecycle interaction and offline receipt verification."
do_5 = "Add a test asserting a hand edit to generated output fails the build."
verify_cmd = "make agent-test-sdk-ts"
symbols = ["layerx_sdk_ts_package"]
touches = ["agent/sdk/typescript/src/generated/","agent/sdk/typescript/test/integration.test.ts","agent/sdk/typescript/examples/"]
reqs = ["18.3","18.4","18.9"]

[task.22.4]
title = "Generate and package the Python SDK"
status = "pending"
wave = 11
requires = ["22.2"]
do_1 = "Generate the Python SDK from the schema with exact integer handling for every consensus-critical value."
do_2 = "Preserve verification levels, unknown submission state, idempotency handling and the typed error taxonomy in the generated surface."
do_3 = "Package it with type stubs, an integration test suite against a running daemon and reproducible build metadata."
do_4 = "Ship the same runnable example set as the other SDKs so behaviour can be compared directly."
do_5 = "Add a test asserting a hand edit to generated output fails the build."
verify_cmd = "make agent-test-sdk-py"
symbols = ["layerx_sdk_py_package"]
touches = ["agent/sdk/python/layerx_sdk/generated/","agent/sdk/python/tests/test_integration.py","agent/sdk/python/examples/"]
reqs = ["18.3","18.4","18.9"]

[task.22.5]
title = "Run the cross-SDK parity suite"
status = "pending"
wave = 11
requires = ["22.3","22.4"]
do_1 = "Build a parity suite that runs identical scenarios through the Rust, TypeScript and Python SDKs against the same daemon and the same node."
do_2 = "Assert identical observable behaviour including identical error taxonomy, identical verification levels and identical idempotency outcomes."
do_3 = "Cover the unknown submission path, a terminal rejection, a proven read, an availability failure and a subscription gap in every language."
do_4 = "Report any divergence with the scenario, the language and both observed behaviours."
do_5 = "Wire the suite into make agent-test-sdk-parity as a release gate."
verify_cmd = "make agent-test-sdk-parity"
symbols = ["agent_sdk_parity_suite"]
touches = ["agent/tests/parity/scenarios.kvx","agent/tests/parity/src/main.rs","Makefile"]
reqs = ["18.6","23.5","1.6"]

[task.22.6]
title = "Publish the compatibility matrix and guarantee documentation"
status = "pending"
wave = 11
requires = ["22.5"]
do_1 = "Publish the matrix of SDK version against contract version against node interface version against daemon version and verify the supported range in CI."
do_2 = "Document precisely which guarantees are protocol-enforced and which are daemon-enforced across every SDK."
do_3 = "Add a documentation check that fails when generated documentation describes a daemon-enforced restriction as a protocol guarantee."
do_4 = "Document the offline verification path so a counterparty can verify a receipt without any LayerX infrastructure."
do_5 = "Add a release check that the matrix, the examples and the documentation are regenerated with the SDKs."
verify_cmd = "make agent-test-sdk-compat"
symbols = ["agent_sdk_compat_matrix","agent_sdk_guarantee_doc_check"]
touches = ["agent/sdk/COMPATIBILITY.md","agent/tools/doc-check/src/main.rs","Makefile"]
reqs = ["18.7","18.10","9.10"]

[task.23]
section = "Wave 12 - Qualification"
title = "Qualify the interaction layer against the real core"
status = "pending"

[task.23.1]
title = "Run the differential wire conformance gate"
status = "pending"
wave = 12
requires = ["3.5","22.5"]
do_1 = "Run the differential harness over the published vectors and generated inputs across every activity type, payload shape and boundary length."
do_2 = "Compare produced bytes, digests, identifiers and rejection classifications against the C reference implementation."
do_3 = "Fail the build on any divergence and record the offending value, both byte strings and the first differing offset."
do_4 = "Report coverage over the structure and rejection taxonomies and fail on an unexercised class."
do_5 = "Include the result in the qualification report."
verify_cmd = "make agent-qualify-wire"
symbols = ["agent_qualify_wire_gate"]
touches = ["agent/tests/qualify/wire.rs","Makefile"]
reqs = ["23.1","4.10"]

[task.23.2]
title = "Run the boundary conformance suite against a real node"
status = "pending"
wave = 12
requires = ["6.5","20.2"]
do_1 = "Run the full boundary suite against a real layerxd started from this repository, with no in-process substitute anywhere in the suite."
do_2 = "Cover every message, error case, streaming behaviour, pagination path, version negotiation and capability report."
do_3 = "Cover node restart, node behind, node unreachable and node degraded, asserting the specified refusal behaviour in each case."
do_4 = "Record the capability gap report produced during the run."
do_5 = "Include the result and the gap report in the qualification report."
verify_cmd = "make agent-qualify-boundary"
symbols = ["agent_qualify_boundary_gate"]
touches = ["agent/tests/qualify/boundary.rs","Makefile"]
reqs = ["23.2","2.9","2.10"]

[task.23.3]
title = "Run the hostile-node no-fabrication suite"
status = "pending"
wave = 12
requires = ["5.6","15.3"]
do_1 = "Build a hostile node harness that returns altered balances, altered receipts, re-signed receipts, sub-threshold certificates, truncated proofs, reordered events and withheld availability data."
do_2 = "Assert the layer reports a verification failure or unavailability in every case and never surfaces the altered value as a result."
do_3 = "Assert that a hostile layer implementation that alters a reported value is caught by independent verification of the exported evidence."
do_4 = "Cover every read surface, the streaming surface, the MCP tools and all three SDKs."
do_5 = "Wire the suite into make agent-qualify-fabrication as a release gate."
verify_cmd = "make agent-qualify-fabrication"
symbols = ["agent_hostile_node_harness","agent_qualify_fabrication_gate"]
touches = ["agent/tests/qualify/hostile_node.rs","agent/tests/qualify/fabrication.rs","Makefile"]
reqs = ["23.3","1.10","1.3"]

[task.23.4]
title = "Run the fault-injection and exactly-once suite"
status = "pending"
wave = 12
requires = ["14.6","17.5"]
do_1 = "Inject connection loss at every stage of submission, process kill between signing and transmission, duplicate delivery, delayed acknowledgement, restart mid-resolution and clock disturbance."
do_2 = "Assert exactly-once economic effect and honest unknown reporting in every case."
do_3 = "Prove idempotency end to end through repeated retries, concurrent duplicates and post-restart resubmission, asserting exactly one receipt and one economic effect."
do_4 = "Assert no leaked reservation, no lost outbox entry and no submission left without an owner across every scenario."
do_5 = "Wire the suite into make agent-qualify-faults as a release gate."
verify_cmd = "make agent-qualify-faults"
symbols = ["agent_fault_injection_suite","agent_exactly_once_suite"]
touches = ["agent/tests/qualify/faults.rs","agent/tests/qualify/exactly_once.rs","Makefile"]
reqs = ["23.4","23.5","13.7"]

[task.23.5]
title = "Run the fuzz, sanitizer and unsafe-code gates"
status = "pending"
wave = 12
requires = ["3.6","21.5"]
do_1 = "Fuzz the wire codec, the node interface framing, the contract request surface and the policy loader with committed corpora and minimisation."
do_2 = "Treat a panic, a hang or an unbounded allocation as a defect rather than a finding to triage."
do_3 = "Run the suite under the address and thread sanitizers where the toolchain supports it and fail on any report."
do_4 = "Enforce the unsafe allowlist and fail the build on any unsafe block outside it."
do_5 = "Include corpus coverage and sanitizer results in the qualification report."
verify_cmd = "make agent-qualify-fuzz"
symbols = ["agent_qualify_fuzz_gate","agent_qualify_sanitizer_gate"]
touches = ["agent/tests/qualify/fuzz.rs","Makefile"]
reqs = ["23.7","23.9"]

[task.23.6]
title = "Run the soak and resource-stability test"
status = "pending"
wave = 12
requires = ["16.5","19.4"]
do_1 = "Sustain submission, streaming and verification load for the declared soak duration against a real node."
do_2 = "Assert no unbounded memory growth, no unbounded queue growth, no leaked reservations and no growing population of unresolved unknown submissions."
do_3 = "Assert subscription lag returns to baseline after induced stalls and that no event is lost across the soak."
do_4 = "Record resource curves and attach them to the qualification report."
do_5 = "Wire the soak into a scheduled CI job with the report as its artefact."
verify_cmd = "make agent-qualify-soak"
symbols = ["agent_qualify_soak"]
touches = ["agent/tests/qualify/soak.rs",".github/workflows/agent-soak.yml"]
reqs = ["23.8","19.5","16.9"]

[task.23.7]
title = "Publish the qualification report as the release gate"
status = "pending"
wave = 12
requires = ["23.1","23.2","23.3","23.4","23.5","23.6"]
do_1 = "Generate a qualification report stating which guarantees are protocol-enforced, which are daemon-enforced, which suites ran, what the results were and which node capabilities were unavailable."
do_2 = "Include the isolation, policy adversarial and MCP injection gate results alongside the qualification suites."
do_3 = "Make an unmet gate block the release rather than being waived silently, and record any exception with its explicit approval in the report."
do_4 = "Publish the report as a build artefact alongside the compatibility matrix."
do_5 = "Add a release check that refuses to tag a version whose report contains an unmet gate."
verify_cmd = "make agent-qualify"
symbols = ["agent_qualification_report","agent_release_gate"]
touches = ["agent/tools/qualify/src/main.rs","agent/QUALIFICATION.md","Makefile"]
reqs = ["23.10","22.7","18.10"]
