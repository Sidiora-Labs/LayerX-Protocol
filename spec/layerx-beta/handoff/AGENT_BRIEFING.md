# Shared briefing for wave 6 implementation agents (repo /root/Layerx-protocol, branch main)

Deadline context: a public beta is expected in ~48h. Be decisive; do not ask the user anything. Ship complete, real code.

## Workflow rules (from CLAUDE.md / spec/workflow.kvx — binding)
- Your task is `[task.6.N]` in spec/layerx-beta/spec.kvx. Read it plus [req.14] and the req/ac ids it lists. First set its `status = "in_progress"` (edit the kvx, commit that with the rest at the end).
- Phase separation: write the ENTIRE implementation first, then qualify (format, build, clippy, tests) and fix task-scoped failures. Never weaken an assertion, loosen a check, skip a test, or add a silent fallback.
- NO FAKES: no stub/mock/fake test doubles, no placeholder implementations. Tests run REAL layerxd (build/bin/layerxd, already built; also build/bin/layerx-genesis-build) or real paxd. agent/tests/boundary/node/layerxd_lni.c is a conformance test node — do NOT use it as the node under test.
- Anything unrelated/pre-existing/blocked: append an observation to spec/layerx-beta/qualification.kvx (follow the existing `[observation.*]` format there) with evidence; do not expand scope.
- Finish: run the task's verify_cmd exactly; only if it genuinely passes run `cg spec done 6.N` (no --force). If it cannot pass, leave in_progress and record why honestly in qualification.kvx.
- Publish: `git add <explicit paths you touched>` (NEVER `git add -A`; the tree has two unrelated pending changes: deleted CODEBASE_AUDIT_2026-08-28_REFRESH.md and modified spec/workflow.kvx — leave them alone), commit with a natural-sounding message describing the real change and the gates that ran, ending with
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01XzRoET32SoM2zB32XA4MaP`, then `git pull --rebase origin main` and `git push origin main`. Other agents push concurrently: on a platform/Cargo.lock conflict take upstream's (`git checkout --theirs platform/Cargo.lock` during rebase = upstream side), run `cd platform && cargo metadata --offline --format-version 1 >/dev/null` to re-add only your entries, confirm `cargo metadata --locked` passes, `git add platform/Cargo.lock`, `git rebase --continue`. On spec.kvx/qualification.kvx conflicts, keep both sides' blocks. Retry the push until it lands.
- Only edit files in your task's `touches` (plus spec.kvx status, qualification.kvx appends, and your crate's Cargo.toml/Cargo.lock entries). Do not touch other agents' directories.
- Stay in the foreground; no background waiters. Long commands: use timeouts up to 600000 ms.

## Hosted house style (copy from platform/hosted/faucet/src/main.rs, gateway, testnet — read them first)
- Blocking std::net::TcpListener + thread::spawn, hand-rolled HTTP/1.1 parser (bounded headers/body), NO tokio/hyper/axum (not in platform/Cargo.lock; do not add crates outside `[workspace.dependencies]` in platform/Cargo.toml; if you truly need a new dep add it there pinned `=x.y.z` and note it in the commit).
- rustls 0.23 server with DER cert + PKCS#8 DER key files; optional client-cert verification against a DER CA; min TLS 1.2. Outbound HTTPS via ureq/native-tls with `add_root_certificate(DER)`.
- Secrets: read from file paths given by env (`*_FILE`), trim CR/LF, 1..=4096 bytes, `Zeroizing<String>`; compare with `subtle` constant time.
- Responses: `Content-Type: application/json` exactly (no charset), `Cache-Control: no-store`, `Connection: close`, Content-Length, no chunked encoding, no duplicate headers. Refusal body `{"error":{"code":"snake_case","retry":"after"|"never","retry_after_seconds":N}}`. `/livez` and `/readyz` on every service.
- Workspace lints deny unsafe, clippy all+pedantic, unwrap/expect/float. Package `layerx-platform-<name>`, bin `layerx-<name>`, `[lints] workspace = true`. Dockerfile = copy platform/hosted/faucet/Dockerfile and change the crate/bin names (rust:1.91.1-alpine3.22 builder, alpine:3.22 runtime, uid/gid 4020, `cargo build --locked --release --manifest-path platform/Cargo.toml -p <crate>`).
- Manifests: pod securityContext runAsUser 4020, readOnlyRootFilesystem, drop ALL caps, `image: ghcr.io/sidiora-labs/<name>:0.1.0` at end of line, `imagePullPolicy: Always`, namespace layerx-testnet, pod label `layerx-plane: trusted-boundary`, container ports as the spec says. See platform/hosted/testnet/deployment.yaml and gateway/deployment.yaml for NetworkPolicy shape; the beta CA is `/O=LayerX beta/CN=LayerX beta internal CA` (P-256), certs issued by beta-cluster.sh `issue_cert` produce cert.der + key.der (PKCS#8) mounted via Secrets (see platform/hosted/tests/beta-cluster.sh secrets_apply, ~lines 404-518, and how gateway/testnet mount them).
- Format with `cargo fmt -p <crate>`; qualify with `cargo clippy --manifest-path platform/Cargo.toml -p <crate> --all-targets -- -D warnings` and `cargo test --manifest-path platform/Cargo.toml -p <crate>`.

## The real node (cmd/layerxd) — facts you need
- Binary build/bin/layerxd. CLI: `layerxd --check-config <cfg>`, `layerxd --serve <cfg>`, `layerxd --authority-replica <cfg>`. Config is an 8-line file: role, network_id, start_sequence, verify_workers, network_workers, projection_workers, checkpoint_workers, serial_execution — read cmd/layerxd/lxp_daemon_config.c for exact syntax, and cmd/layerxd/lni.env.example.
- Env (see lxp_daemon_config.c / lxp_daemon_process.c open_process ~2974-3277): LAYERX_NODE_PROGRAM_ADDRESS (must be 127.0.0.1) / _PORT / _BEARER_TOKEN; LAYERX_NODE_AUTHORITY_REPLICA_ADDRESS (127.0.0.1) / _PORT / _ID / _BEARER_TOKEN (>=32 bytes, distinct from program token); LAYERX_NODE_CHECKPOINT_DIRECTORY, LAYERX_NODE_SNAPSHOT, LAYERX_NODE_IDENTITIES, LAYERX_NODE_PROGRAM_FEED_LOG, _CANONICAL_LOG, _RECEIPT_AUTHORITY_LOG, _BATCH_LOG, _EVIDENCE_LOG, LAYERX_NODE_SEQUENCER_ID / _PUBLIC_KEY / _PRIVATE_KEY, _FIRST_BATCH/_LAST_BATCH, _HISTORY_DATABASE/_MIGRATIONS, LAYERX_NODE_LNI_SOCKET / _ALLOWED_UID / _ALLOWED_GID / _FRAME_BYTES(=1146902) / _DEADLINE_MS.
- Daemon program listener (plaintext loopback HTTP, GET only, bearer): `/v1/protocol/account-state/head`, `/v1/receipts/<64hex>/account-state`, `/v1/programs/account-state/changes?after_sequence=`, `/v1/programs/<64hex>/account-state?at=`, `/v1/batches/<64hex>/receipt-authority?receipt_digest=`; 401/200/404/503; error body `{"error":<int>}`. Source: cmd/layerxd/lxp_daemon_protocol.c, lxp_daemon_listener.c.
- Authority replica (`--authority-replica`, loopback HTTP): env LAYERX_AUTHORITY_REPLICA_LOG/_ID/SEQUENCER_ID/SEQUENCER_PUBLIC_KEY/FIRST_BATCH/LAST_BATCH/BEARER_TOKEN/ADDRESS/PORT; `POST /v1/receipt-authority/ingest` (LXAR1 framing, sequencer pushes) → 201 `{"authority_replica_id"}`; `GET /v1/batches/{batch_id}/receipt-authority?receipt_digest=<64hex>` → 200 `{"authority_replica_id","sequencer_public_key","batch_evidence":{"header_hex","header_signature","receipt_proof_hex"}}`. Source: cmd/layerxd/lxp_daemon_authority_replica.c, lxp_daemon_receipt_authority.c. Batch header has no batch_id/asset: derive batch_id via `layerx_wire::hash::execution_batch_id(previous_state_root, activity_id, global_sequence, batch_number)` exactly as agent/crates/layerx-agentd/src/protocol_evidence.rs:340-361; verify with `layerx_proof::inclusion::verify_receipt(receipt, proof, header, sig, SequencerAuthorization)` and `layerx_proof::AuthorizedBatch::new(...)`.
- Genesis: build/bin/layerx-genesis-build (cmd/layerx-genesis, include/layerx/lxp_genesis.h: lxp_genesis_account {account_id, asset_id, balance, locked, subaccount_kind, parent}); asset activities include LX_ASSET_SEND 0x00010005, LX_ASSET_GRANT_ISSUE 0x00010007 (include/layerx/lx_asset.h). How the existing tests bring up a real node end-to-end: grep the Makefile for `layerxd` targets, tests/ and agent/tests/boundary/src/production.rs (production-lni.env.example) for the env recipe, and cmd/layerxd/lni.env.example.
- LNI client: agent/crates/layerx-client (workspace dep `layerx-client` already added to platform/Cargo.toml). Unix socket only (Client::connect). Capabilities (src/lni/schema.rs:48-63): NodeInfo, Submit, AuthenticatedDurableSubmit, ReceiptLookup, AccountRead, HistoryRange, BatchHeader, Checkpoint, ProofBundle, AvailabilityFetch, EventSubscribe, HistoricalProofs, PreparationState, FinalityEvidenceRegister. There is NO simulate capability: answer such routes with a typed 503 `capability_unavailable` and record it in qualification.kvx (task 6.9 picks it up).
- Canonical activity encoding / signing: agent/crates/layerx-wire, layerx-crypto (ed25519), human/crates/layerx-intents; the CLI (platform/cli/src) and the emulator (platform/emulator/src/main.rs ~3470-3540) show how gateway-style routes map bodies; the gateway (platform/hosted/gateway/src/main.rs) shows exactly what the component/authority/identity clients parse — match those parsers byte-for-byte (they use deny_unknown_fields).
