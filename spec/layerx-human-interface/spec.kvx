# spec.kvx - LayerX Human Interface. SINGLE SOURCE OF TRUTH for this feature.
#
# requirements.md / design.md / tasks.md next to this file are GENERATED from it
# by spec/specgen. Never hand-edit those; edit this file and regenerate.
#
# Drive the work with:  cg spec next -> cg spec start <id> -> cg spec done <id>
# Provenance for every statement here: docs/00-source-brief.md
# Upstream normative sources: spec/layerx-protocol/spec.kvx (the protocol wins)
# and spec/layerx-agent-interface/spec.kvx (normative for the boundary and the
# agent layer this plane consumes).

[meta]
feature      = "layerx-human-interface"
title        = "LayerX Human Interface - The Human Control Plane"
kind         = "human-control-plane-specification"
version      = "1.0.0"
status       = "draft"
language     = "Rust services; TypeScript web application"
repo_root    = "human/"
upstream     = "spec/layerx-protocol (normative for protocol behaviour); spec/layerx-agent-interface (normative for the node boundary and the agent layer this plane consumes)"
intro        = "The LayerX Human Interface is the custody-aware control plane through which people hold LayerX accounts, fund them from Paxeer, create and govern managed agents, approve or veto agent spending, move money and see what happened. It comprises a KMS-backed custody service that holds human and managed-agent DID keys and signs only through the disclosure-bound signing path, a typed-intent compiler that is the only component permitted to construct protocol payload bytes for this plane, an additive approval-operations extension to the agent layer contract, a Paxeer custody-boundary client for deposits, withdrawal claims and emergency exits, a rebuildable public explorer index, and one web application with two native shells - a mobile experience and a desktop experience that share state and journey logic but ship device-specific layouts and components - split into a public explorer plane and an authenticated app plane. The surface exposes exactly five ideas: log in, add money, move money, manage agents, see what happened; every protocol mechanism is engine room. The plane inherits the agent layer's non-authority rule wholesale: it never invents protocol state, and the word Done is rendered only when a verified LayerX receipt or a Paxeer finality proof backs it. These requirements are normative; the design and the task plan beside them are compiled from this file."

# Decisions locked before implementation begins. These supersede any ambiguity
# in docs/00-source-brief.md. Where they touch protocol or agent-layer behaviour
# they defer to the upstream specs, which are normative for those layers.
[decision]
custody             = "Custodial by default. Human authentication is passkey-first. Human and managed-agent DID primary keys are generated and held inside layerx-human-service's KMS-backed keystore and never leave it, never reach the browser, and are never derivable from the login credential. Key export and self-custody are out of scope for v1."
signing             = "The custody service signs exclusively through the disclosure-bound signing path the agent layer defines: the exact canonical byte string plus a structured disclosure that must re-encode byte-identically, or the signer refuses. It acts as the remote signer role of layerx-crypto; layerx-agentd remains a non-custodian of primary keys."
intents_only        = "layerx-intents is the only component in human/ that constructs protocol payload bytes. The browser and the web application never assemble payloads, never see canonical bytes except as opaque evidence, and speak only human-api. A CI gate fails the build if any other human/ component depends on layerx-wire encoding entry points."
seams_first         = "The approval.* operation module is an additive agent-layer contract change within contract major 1, specified and built in the first wave of this feature so the agent-interface spec's scope and qualification gate stay clean. The typed-intent compiler lives in the human workspace and reaches the core through the agent layer's existing prepare, sign and submit pipeline - no second write path is added to the agent contract."
wallet_boundary     = "The Paxeer EVM wallet signs at the custody boundary only: the domain-separated binding statement, the deposit transaction, the withdrawal claim, the emergency exit claim. It opens only at those explicit signing moments - exactly once per required signature - is never a login method, and per protocol design its binding grants the EVM key no authority over LayerX balances."
vocabulary          = "Deposit and withdrawal name movements across the Paxeer custody boundary exclusively. Movements inside LayerX are fund, allocate, return or transfer, surfaced in the UI as one verb: Move money. The route resolver picks the protocol mechanism; the user never selects a transfer type."
done_is_receipt     = "No surface of this plane renders a success state for a money movement or a protocol mutation unless a locally verified LayerX receipt or a verified Paxeer finality proof backs it. Unknown outcomes render as still-checking, never as success or failure, and are resolved only by receipt lookup."
reclaim             = "A human takes money back from a managed agent only by defunding a protocol budget, by an agent-authorised transfer, or by drawing a receive under an explicit payer grant. There is no key-based sweep of an agent account, even though the custody service holds the agent's keys."
two_shells          = "One Next.js application serves a public /explorer plane and an authenticated /app plane, with two native shells: a mobile shell and a desktop shell that share state, data hooks and journey logic but render device-specific layouts and components. Shell selection is SSR-safe from viewport and pointer capability, never user-agent sniffing alone; tablets receive the desktop layout with touch-sized targets."
ui_rules            = "Client UI uses the exact styling and component API of the owner-supplied @layerx/ui package. Its borders, dividers, shadows, gradients, palette, radii, typography and interaction states are authoritative in both shells; application code composes the package instead of restyling its primitives, and CI enforces the package integration."
error_standard      = "Every screen ships its full state matrix - loading, empty, error, offline, degraded and, where money moves, still-checking - and every error state is a designed, user-friendly screen with actionable buttons such as Retry, Reload and Report. A blank screen, a raw stack trace or a dead-end error is a build defect, not a polish item."
emergency_exit_v1   = "The emergency exit ships in v1 as a minimal guided flow under Settings - it is a protocol safety property presented calmly, and it must remain usable when LayerX itself is degraded."
explorer_public     = "The public explorer is server-rendered, requires no authentication, shows protocol-public data only, and is served from layerx-explorer-index - a rebuildable projection fed exclusively through the node boundary, inheriting the agent layer's prohibition on reading node internals."
v1_scope            = "Version 1 ships onboarding, wallet binding, the managed-agent lifecycle, deposits, internal movements, withdrawals with claims, the emergency exit, the approval inbox, notifications, the public explorer, the activity view with exports, audit export and support chat. Escrow, streams, services and perps workspaces, team roles beyond the owner, an address book, localization beyond copy-catalog readiness and self-custody key export are post-v1 and out of scope."
human_api           = "human-api is a versioned HTTPS+JSON contract defined in human/schema/human-api. Within one major version changes are additive only. The TypeScript client used by the web application is generated from the schema; hand-editing generated output is a build failure."

[design]
include = "design.body.md"

[tasks]
heading          = "Implementation Plan: LayerX Human Interface"
overview_include = "tasks.overview.md"
notes_include    = "tasks.notes.md"

# --------------------------------------------------------------------------
# REQUIREMENTS
# --------------------------------------------------------------------------

[req.1]
title = "Non-Authority and Receipt-Backed Truth"
story = "As a person trusting this app with money, I want the human plane to be structurally incapable of telling me something the protocol did not do, so that what the screen says happened is what actually happened, every time."
ac_1 = "WHEN any component of the human plane effects a change of protocol state THE plane SHALL do so exclusively by compiling a typed intent into canonical module payload bytes through layerx-intents, preparing the activity through the agent layer's prepare operation, obtaining a disclosure-bound signature from the custody service over the exact prepared canonical bytes, and submitting them through the agent layer, and SHALL possess no other path by which protocol state can change."
ac_2 = "THE plane SHALL render a success or Done state for a money movement or protocol mutation only when a LayerX receipt verified through the agent layer's proof machinery, or a Paxeer finality proof verified by layerx-paxeer-client, backs that exact outcome, and the component that renders success SHALL be structurally unable to render it from any other input."
ac_3 = "WHEN the plane displays a balance, a movement outcome, an agent state, a budget figure or any other protocol fact THE plane SHALL display only values decoded from core-produced bytes with their verification status attached by the agent layer, SHALL never raise a verification level itself, and SHALL visibly mark any value it cannot verify to the required level."
ac_4 = "IF a submission's fate cannot be determined THEN THE plane SHALL present it as still checking, SHALL NOT present it as success or failure, SHALL resolve it only by receipt lookup under its idempotency key, and WHILE it remains unresolved THE plane SHALL disable every control that could duplicate the economic effect."
ac_5 = "IF the core refuses an activity THEN THE plane SHALL surface the refusal honestly, SHALL state in plain language whether any money left the account, and SHALL NOT retry into a different outcome, soften the result, or present a failed movement as pending because a retry is scheduled."
ac_6 = "THE plane SHALL maintain no ledger of balances or owed amounts that is not reconstructible from verified receipts alone, and WHEN the human-service store is deleted and the service restarted against the same core THE plane SHALL reconstruct every protocol-derived answer it serves, losing only declared local artefacts such as profile data, notification preferences, journey annotations and audit history."
ac_7 = "WHERE the plane shows a derived or prospective figure such as an estimated fee, an estimated arrival time or a spend projection THE plane SHALL label it an estimate, SHALL exclude it from any receipt-shaped presentation, and SHALL replace it with the receipt-backed actual once one exists."
ac_8 = "WHEN a read cannot be served fresh THE plane SHALL serve the last verified value marked with its age and the checkpoint or head it is relative to, and SHALL NOT silently present stale data as current."
ac_9 = "THE plane SHALL provide, for every claimed outcome, access to the underlying receipt or proof material sufficient for independent verification through the public explorer or exported evidence, and the qualification suite SHALL demonstrate that a tampered human-service altering a reported value is caught by independent verification of that evidence."
ac_10 = "THE plane SHALL treat the agent layer's contract as its only path to the core, SHALL NOT open the node database, read node log files, or link the node's internal structures, and CI SHALL fail the build when a dependency in human/ violates the boundary rules the agent layer defines."

[req.2]
title = "The Simplicity Contract"
story = "As a person with no crypto background, I want the app to expose only ideas I already understand, so that I can hold money, run agents and stay in control without learning a protocol."
ac_1 = "THE app SHALL expose exactly five top-level ideas - log in, add money, move money, manage agents, see what happened - and SHALL introduce no protocol concept as a navigation destination, decision point or required input anywhere on the default surfaces."
ac_2 = "WHEN a signed-in user initiates a common action - moving money, adding money, starting a withdrawal, creating an agent, acting on an approval - THE app SHALL complete the initiation in at most three decisions, where a decision is a screen requiring user choice or input, excluding authentication challenges."
ac_3 = "THE app SHALL keep a banned-vocabulary list in the copy catalog containing at least DID, session key, capability, nullifier, checkpoint, payload, canonical, idempotency, attestation and proof, SHALL enforce it with a copy lint that fails the build when a banned term appears on the authenticated plane's default surfaces - Technical details surfaces and the public explorer plane are exempt - and SHALL express every state in plain language instead."
ac_4 = "THE app SHALL never require a user to transcribe, retype or manually carry any identifier between screens; every identifier SHALL be copyable in one action, every reference SHALL be a link, and every flow that needs an identifier SHALL supply it itself."
ac_5 = "WHEN money moves between any two endpoints THE app SHALL resolve the protocol mechanism automatically through the route resolver, SHALL describe what will happen in plain language before commitment, and SHALL NOT ask the user to choose a transfer type, bridge, route or mechanism."
ac_6 = "WHERE an advanced control exists - verification detail, evidence export, audit, emergency exit - THE app SHALL reach it through progressive disclosure, and the advanced surface SHALL never interpose itself on the simple path."
ac_7 = "WHEN a custody-boundary journey needs the Paxeer wallet THE app SHALL open the wallet only at explicit signing moments, exactly once per signature the custody boundary requires - on a first journey with binding folded in, once for the binding statement and once for the custody signature - each with a plain-language statement of what the signature does, and SHALL never ask for a wallet signature at any other time."
ac_8 = "THE app SHALL present every protocol state through the normative status translation table in the design document, and a status string not in that table appearing on a default surface SHALL be a build failure."
ac_9 = "THE onboarding, deposit, move-money and create-agent journeys SHALL each be completable on a mobile device without documentation, and the qualification usability gate SHALL verify this with real participants against a declared success threshold."
ac_10 = "WHEN the user must wait THE app SHALL say what is happening and roughly how long it takes, SHALL state when it is safe to close the app, and SHALL never present a spinner without words on any journey surface."

[req.3]
title = "Human Identity and Onboarding"
story = "As a new user, I want creating an account to feel like any modern app - an email and a passkey - while the system stands up my protocol identity correctly underneath, so that I am protocol-safe without ever seeing the protocol."
ac_1 = "WHEN a person creates an account THE service SHALL establish a passkey-backed application identity as the primary credential, SHALL support a fallback credential without weakening the passkey path, and SHALL keep the application identity distinct from the LayerX DID and from any wallet."
ac_2 = "WHEN the application identity exists THE service SHALL generate the human's LayerX signing keys inside the custody keystore, SHALL register the human DID through a typed intent submitted via the agent layer, and SHALL treat the account as active only after the DID registration receipt verifies."
ac_3 = "WHEN the DID is registered THE service SHALL pre-register a recovery authority in the same onboarding journey through the protocol's recovery pre-registration mechanism - whose challenge delay applies when recovery is later exercised - and SHALL NOT allow the journey to complete silently without recovery in place."
ac_4 = "WHILE onboarding is in progress THE app SHALL show honest per-stage progress, SHALL present the account as ready only when ac_2 holds, and IF a stage fails THEN THE app SHALL preserve the journey, explain the failure plainly, and offer resumption without restarting completed stages."
ac_5 = "THE service SHALL make onboarding idempotent: repeating a stage after a crash, a timeout or a duplicate request SHALL converge on one application identity, one DID, one recovery registration and one set of receipts, with no orphaned protocol state."
ac_6 = "THE service SHALL store profile information such as display name and avatar as declared-local data, separated from protocol identity, editable and deletable without touching protocol state."
ac_7 = "THE service SHALL record, for the DID registration, the key activation, the recovery registration and any wallet binding, the verified receipt establishing when each became active, and SHALL surface that evidence under Technical details and in the audit export."
ac_8 = "WHEN a user signs in on a new device THE service SHALL require the passkey ceremony, SHALL record the device in the session inventory, and SHALL emit a security notification carrying an action that opens the device list."
ac_9 = "THE browser SHALL at no point receive, derive or cache LayerX private key material, and the qualification suite SHALL verify that the web application bundle and its runtime state contain no signing capability."
ac_10 = "IF the agent layer or the core is unavailable during onboarding THEN THE service SHALL complete the application-identity stage, SHALL queue the protocol stages with honest pending presentation, and SHALL NOT fabricate an active account."

[req.4]
title = "The Key Custody Service"
story = "As the owner of accounts and agents whose keys I never see, I want the custody service to guard signing power at least as carefully as a hardware wallet would, so that custody convenience never becomes custody exposure."
ac_1 = "THE service SHALL hold every human and managed-agent private key inside a KMS-backed keystore, SHALL keep key material out of process logs, error messages, metrics, traces, backups in plaintext and the browser, and SHALL zeroize in-memory key material on release."
ac_2 = "WHEN anything requests a signature THE service SHALL sign only through the disclosure-bound path: the exact canonical byte string together with a structured disclosure that re-encodes byte-identically to those bytes, and SHALL refuse to sign when the disclosure does not match, exactly as the agent layer's signer contract requires."
ac_3 = "WHERE an operation is designated step-up - acting on an approval, changing security settings, revealing a secret, initiating a withdrawal or exit, rebinding the payout wallet, archiving an agent - THE service SHALL require a fresh passkey ceremony bound to the specific operation, and SHALL reject a signature request for a step-up operation without one."
ac_4 = "WHEN a managed agent needs operating authority THE service SHALL provision a protocol session key to layerx-agentd with an explicit expiry, an explicit permitted activity set and a revocation path, SHALL never transfer a primary key, and SHALL propagate revocation promptly when the agent is paused or archived."
ac_5 = "THE service SHALL enforce per-principal isolation in the keystore so no request path can reach another human's keys or another human's agents' keys, and the isolation suite SHALL prove it adversarially."
ac_6 = "THE service SHALL append every signing decision - grant or refusal - to the audit log with the disclosure digest, the operation, the principal and the step-up evidence, and SHALL never write payload secrets or key material to the audit log."
ac_7 = "IF the KMS or keystore is unavailable THEN THE service SHALL refuse to sign with an honest error, SHALL NOT fall back to any lower-assurance key or cached material, and SHALL surface the degradation on its status surface."
ac_8 = "THE service SHALL support key rotation for human and agent DIDs through the protocol's rotation mechanism, SHALL present the challenge delay in plain time units, and SHALL keep the superseded key usable only for the window the protocol defines."
ac_9 = "THE service SHALL bound signing throughput per principal with typed refusals, so a compromised session cannot drain an account faster than limits allow, and the limits SHALL be declared configuration, not code constants."
ac_10 = "THE v1 service SHALL expose no key export path, and any future export SHALL be specified as its own feature; the qualification suite SHALL verify no API surface returns key material."

[req.5]
title = "Paxeer Wallet Binding"
story = "As a user with money on Paxeer, I want to link my EVM wallet once, safely, so that deposits and payouts flow to an address I control while my LayerX funds stay beyond the wallet's reach."
ac_1 = "WHEN a user links a Paxeer wallet THE plane SHALL obtain the EVM key's signature over the protocol's domain-separated binding statement naming the DID and network_id, SHALL submit the binding as a typed intent through the agent layer, and SHALL treat the binding as established only when its receipt verifies."
ac_2 = "THE plane SHALL treat the bound address as a payout and provenance association only, SHALL never accept an EVM signature as authorisation for any LayerX activity, and SHALL state on the binding surface, in one plain sentence, that the wallet cannot move LayerX funds."
ac_3 = "THE binding journey SHALL be deferred until first needed - the first deposit or withdrawal - SHALL open the wallet exactly once, and SHALL show the full address with a checksum-styled rendering, copy affordance and QR at every confirmation surface, never a truncation alone."
ac_4 = "WHEN a user replaces a bound wallet THE plane SHALL require step-up authentication, SHALL submit the rebinding through the same receipt-verified path, SHALL keep the old binding effective until the new receipt verifies, and SHALL emit a security notification with an action button."
ac_5 = "IF the wallet is unavailable, disconnects mid-journey or rejects the signature THEN THE plane SHALL show a designed state naming what happened and offering retry or cancel, and SHALL leave no partially-bound state behind."
ac_6 = "THE plane SHALL verify that the address that signed the binding statement is the address recorded in the receipt before presenting the binding as active, and a mismatch SHALL surface as a verification failure, never be silently accepted."
ac_7 = "WHERE no wallet is bound THE app SHALL present deposits and withdrawals as available-but-needs-linking, with the linking step folded into the journey rather than a separate errand."
ac_8 = "THE plane SHALL support at most one active bound payout wallet per human account in v1, SHALL display which address is bound wherever payouts are discussed, and SHALL record every past binding with its receipts in the audit trail."

[req.6]
title = "Approval Operations in the Agent Contract"
story = "As a human governing agents, I want approvals to be first-class operations of the agent layer contract, so that my app can list, inspect and decide holds with the same receipt-grade rigor as every other operation."
ac_1 = "THE agent-api contract SHALL gain an approval module - list, get, approve, reject - as an additive change within contract major 1, defined in the schema with golden vectors, and SHALL leave every existing declaration unchanged."
ac_2 = "WHEN a policy hold requires human approval THE daemon SHALL expose it through the approval module carrying the held activity's disclosure and the digest of its exact canonical bytes, and the disclosure SHALL re-encode byte-identically to the digest's preimage or the hold SHALL be reported as defective rather than approvable."
ac_3 = "WHEN an approval is granted THE daemon SHALL apply it to exactly the held digest - releasing exactly that prepared activity into the submission path - and SHALL treat any change to the underlying activity as voiding the hold, requiring a fresh preparation and a fresh hold."
ac_4 = "WHEN an approval is rejected THE daemon SHALL cancel the hold, release its reservations deterministically, and report the refusal to the requesting agent with a typed result, and rejection SHALL be final for that digest."
ac_5 = "THE daemon SHALL expire unactioned holds at their deterministic expiry exactly as the policy engine defines, SHALL release reservations on expiry, SHALL bound a hold's expiry by the held prepared activity's submission validity window so a granted approval is always releasable, and an expired hold SHALL NOT be approvable, with an approval attempt after expiry returning a typed expired result."
ac_6 = "THE approve and reject operations SHALL be idempotent under their idempotency keys: repeating a decision SHALL return the original outcome, and concurrent conflicting decisions SHALL resolve to exactly one winner with the loser receiving a typed conflict result."
ac_7 = "THE daemon SHALL emit approval lifecycle events - created, approved, rejected, expired - on the existing event stream so clients track holds without polling, and SHALL record each transition in the audit trail with its digest."
ac_8 = "THE generated TypeScript and Python SDKs and the Rust SDK SHALL gain the approval module from the same schema in the same build, the cross-SDK parity suite SHALL cover it, and the compatibility matrix SHALL record the minor version introducing it."
ac_9 = "THE approval surface SHALL be labelled a daemon-enforced restriction in every report, and nothing in it SHALL be described as protocol authority."

[req.7]
title = "Typed Intents and the Intent Compiler"
story = "As the operator of a plane whose users cannot inspect bytes, I want every payload built by one audited compiler from typed intents, so that what was described is provably what was signed."
ac_1 = "THE crate layerx-intents SHALL define a versioned typed-intent vocabulary covering, at v1: DID registration, key rotation, recovery registration, EVM payout binding, 402LXP send, 402LXP receive under a payer grant, payer grant registration, budget create, budget fund, budget defund, bridge deposit credit and bridge withdraw request, each intent carrying only domain-typed fields from layerx-types."
ac_2 = "WHEN an intent is compiled THE crate SHALL produce the canonical module payload bytes exclusively through layerx-types and layerx-wire, SHALL introduce no serialisation of its own, and SHALL reject at compile time any intent whose fields cannot produce a canonical payload."
ac_3 = "THE crate SHALL ship golden vectors for every intent kind proving intent-to-bytes stability across releases, and a vector change without a version bump SHALL fail the build."
ac_4 = "THE build SHALL run a differential gate proving that for every intent kind, compile then decode then disclosure re-encoding yields byte-identical payloads through layerx-wire, so the disclosure a human approves is provably the bytes signed."
ac_5 = "THE crate SHALL reject unknown intent versions and unknown intent kinds deterministically with typed errors, and SHALL preserve unknown-version inputs for diagnosis without executing them."
ac_6 = "THE crate SHALL accept amounts only as protocol integer types, SHALL perform no floating-point arithmetic, and SHALL enforce every protocol length and range bound at intent construction with typed errors."
ac_7 = "WHEN the service acts on an intent THE plane SHALL submit the compiled payload through the agent layer's existing prepare operation, SHALL verify field-for-field that the returned disclosure matches the originating intent before any signature is requested, and a mismatch SHALL abort the journey as a defect rather than being reconciled silently."
ac_8 = "THE crate SHALL ship fuzz targets for intent parsing and compilation, treating a panic, hang or unbounded allocation as a build-breaking defect."
ac_9 = "THE build SHALL fail if any component of human/ other than layerx-intents invokes payload-encoding entry points of layerx-wire, making the compiler the single payload authority for this plane."

[req.8]
title = "The Human API Contract"
story = "As the web application, I want one versioned contract that speaks in journeys and receipts rather than protocol structures, so that the browser can be simple, generated and honest."
ac_1 = "THE contract human-api SHALL be defined as a versioned schema under human/schema/human-api with explicit major and minor versions, SHALL admit only additive change within a major version, and SHALL publish golden request and response vectors for every operation."
ac_2 = "THE contract SHALL cover identity and sessions, wallet binding, journeys - deposit, withdraw, exit, move - agent lifecycle, approvals, activity and receipts, notifications, settings and support, and each operation SHALL be expressed in journey vocabulary, never raw protocol structures."
ac_3 = "THE TypeScript client used by the web application SHALL be generated from the schema with a drift gate that fails the build when generated output is stale or hand-edited, and the browser SHALL call the service only through that client."
ac_4 = "WHEN a mutation could move money or change protocol state THE contract SHALL require an idempotency key, and repeating the request SHALL return the original journey rather than creating a second effect."
ac_5 = "THE contract SHALL define a typed error model with stable machine codes, a copy-catalog key for the human message, the trace identifier, and a retriability classification, and no operation SHALL return an unstructured error."
ac_6 = "THE contract SHALL provide resumable streaming of journey progress, approval lifecycle and notification events with stable cursors, so both shells render live state without polling and recover from disconnection without gaps or duplicates."
ac_7 = "WHEN a response carries a protocol fact THE contract SHALL carry its verification status and, on request, the evidence reference sufficient to fetch receipt or proof material for export and independent verification."
ac_8 = "THE service SHALL enforce authenticated-principal scoping on every operation, SHALL bind sessions to the passkey identity with anti-forgery protections appropriate to a browser client, and SHALL rate-limit with typed refusals carrying retry timing."
ac_9 = "THE contract SHALL version its compatibility in a published matrix - schema version, service version, web app version - and the qualification suite SHALL run the generated client against the live schema to prove parity."

[req.9]
title = "Money Movement and the Route Resolver"
story = "As a user, I want to say who gets how much and have the system do the rest, so that moving money is one idea regardless of what the protocol does underneath."
ac_1 = "THE service SHALL reserve deposit and withdrawal for custody-boundary journeys and SHALL name internal movements fund, allocate, return or transfer in every API surface, log and copy string, surfacing them in the UI as Move money."
ac_2 = "WHEN a user requests a movement THE route resolver SHALL derive the protocol mechanism from source, destination and relationship - human to agent budget funding, human to agent send, agent to human return via budget defunding, an agent-authorised send or a receive under a payer grant, wallet to human deposit, human to wallet withdrawal - and SHALL compose multi-leg journeys automatically."
ac_3 = "WHEN a movement is reviewed THE app SHALL state in plain language what will happen, the amount, the fee estimate with its ceiling, the arrival expectation and any irreversibility, before the user commits."
ac_4 = "WHEN a movement executes THE service SHALL run each leg through intent compilation, disclosure-bound signing and agent-layer submission, SHALL verify the leg's receipt before starting the next leg, and SHALL present per-leg progress for multi-leg journeys."
ac_5 = "IF a leg lands in an unknown state THEN THE journey SHALL pause in still-checking, resolve only by receipt lookup, resume or conclude honestly, and never re-submit a leg whose fate is undetermined."
ac_6 = "THE service SHALL persist journeys durably so that a service restart resumes every in-flight journey without duplicating any leg, and the fault-injection suite SHALL prove exactly-once economic effect per leg across crashes at every stage."
ac_7 = "IF a movement is refused by policy, budget, capability or the protocol THEN THE app SHALL name which limit refused it in plain language and link the surface where the user can change that limit, when it is theirs to change."
ac_8 = "WHEN a movement completes THE app SHALL show the receipt-backed actual amounts and fees, and the journey record SHALL reference every leg receipt in order."
ac_9 = "THE resolver SHALL be a deterministic function of its declared inputs, property-tested so equivalent requests choose identical routes, and a route the resolver cannot serve SHALL produce a typed unavailable refusal, never a partial attempt."

[req.10]
title = "Deposits"
story = "As a user adding money, I want to sign once in my wallet and watch honest progress until the money is verifiably in LayerX, so that I never wonder where my funds are."
ac_1 = "WHEN a deposit starts THE app SHALL show the bound wallet and amount, open the wallet exactly once for the custody transaction signature, and from that point run the journey server-side with no further user action required."
ac_2 = "THE service SHALL track the custody transaction to Paxeer finality, obtain the finalised custody proof, submit bridge deposit credit through the agent layer as a typed intent, and treat the deposit as complete only when the LayerX credit receipt verifies."
ac_3 = "THE app SHALL present the deposit as a staged timeline - waiting for wallet, confirming on Paxeer, crediting, done - where each stage's completion is driven by its own evidence class and the final stage turns complete only on the verified credit receipt."
ac_4 = "WHILE a deposit is in flight THE app SHALL state that it is safe to close, SHALL continue the journey server-side, and SHALL notify the user on completion or failure with a deep link to the journey."
ac_5 = "IF the wallet rejects, the custody transaction fails, a reorg displaces it, finality stalls beyond the expected window, or the credit is refused THEN THE app SHALL show a designed per-stage failure state naming what happened, whether funds moved, and the next action - and a stalled stage SHALL read as delayed with a revised expectation, never as silent."
ac_6 = "THE service SHALL make deposits idempotent end to end: one custody transaction SHALL never produce more than one credit submission, and resubmission after a crash SHALL converge on one verified credit receipt."
ac_7 = "THE app SHALL NOT add the deposited amount to any balance presentation before the credit receipt verifies, and the pending amount SHALL be visible as in-flight, never merged."
ac_8 = "WHEN a deposit completes THE activity feed SHALL show one joined entry spanning wallet to credit with each stage's evidence reachable under Technical details."

[req.11]
title = "Withdrawals, Claims and Emergency Exit"
story = "As a user taking money out, I want the timeline and the waiting explained honestly, so that a checkpoint wait feels like a scheduled train, not a lost transfer."
ac_1 = "WHEN a withdrawal starts THE service SHALL first execute the protocol withdrawal debit through the agent layer and verify its receipt, and the app SHALL state before commitment that a committed withdrawal cannot be cancelled, only completed."
ac_2 = "THE app SHALL present the withdrawal as a staged timeline - processing, waiting for settlement, ready to claim, paid out - with an honest time expectation for the settlement stage derived from declared configuration, never a hardcoded guess."
ac_3 = "WHEN the checkpoint containing the debit is finalised THE service SHALL construct the claim, and the app SHALL present ready to claim with the wallet opening exactly once for the claim signature, with reminders if the claim remains unexercised."
ac_4 = "THE plane SHALL present the withdrawal as paid out only when the Paxeer payout transaction is verified final, and the joined journey record SHALL reference the debit receipt, the checkpoint proof and the payout evidence."
ac_5 = "IF a challenge window holds the payout THEN THE app SHALL say so plainly - why it is held, what is being checked and when to expect resolution - and IF the checkpoint is successfully challenged THEN THE app SHALL surface the protocol's cancellation of the payout with the funds' actual disposition, honestly."
ac_6 = "THE emergency exit SHALL be a guided flow under Settings framed as getting my money out, SHALL construct the exit against the last finalised checkpoint through layerx-paxeer-client, SHALL require typed confirmation of its irreversibility, SHALL remain operable when LayerX ordinary operation is degraded, and WHERE the protocol has not declared emergency-exit eligibility THE app SHALL state plainly that the exit is unavailable because the network is operating normally and SHALL offer the ordinary withdrawal instead."
ac_7 = "WHILE an exit is in flight THE app SHALL show staged progress with the same honesty rules as withdrawals, and completion SHALL be presented only on verified Paxeer finality of the exit."
ac_8 = "THE service SHALL make withdrawals, claims and exits idempotent and crash-resumable, with the fault-injection suite proving no double claim, no lost claimable state and no journey stranded without an owner."

[req.12]
title = "Managed Agent Lifecycle"
story = "As a person employing agents, I want creating, funding, limiting, pausing and retiring an agent to be everyday actions with receipts behind them, so that delegation never means losing control."
ac_1 = "WHEN a user creates an agent THE app SHALL ask at most name, purpose and monthly spend limit, and THE service SHALL orchestrate the rest - agent DID registration, custody key generation, recovery authority naming the human, session-key provisioning to layerx-agentd, capability narrowing and protocol budget creation with initial funding - each step receipt-verified."
ac_2 = "THE app SHALL present the agent as Active only when every orchestration step's receipt has verified, and WHILE creation is partial THE app SHALL show which steps completed honestly and offer resumption, never a phantom agent."
ac_3 = "WHEN a user pauses an agent THE service SHALL revoke or suspend the agent's operating authority - session keys and daemon-side permissions - promptly, THE app SHALL present pause as reversible with a plain consequence sentence, and resume SHALL restore authority through the same receipt-verified paths."
ac_4 = "WHEN a user changes an agent's spend limit THE service SHALL change the protocol budget through typed intents where the limit is protocol-backed, SHALL label any daemon-enforced remainder as app-enforced, and SHALL never describe a daemon restriction as a protocol guarantee."
ac_5 = "WHEN a user takes money back from an agent THE service SHALL use budget defunding, an agent-authorised transfer or a receive under an explicit payer grant only, exactly as the reclaim decision requires, and the app SHALL show the receipt-backed result."
ac_6 = "WHEN a user archives an agent THE app SHALL require the funds disposition first - returning remaining balances through legitimate reclaim paths - SHALL treat archive as destructive with a red confirm and typed name confirmation, and THE service SHALL revoke authority, retire session keys and retain the agent's history and receipts read-only."
ac_7 = "WHEN an agent's keys need rotation or recovery THE service SHALL run the protocol rotation or recovery flow under the human's recovery authority, present the challenge delay in plain time, and keep the agent's identity and history continuous across the rotation."
ac_8 = "THE app SHALL show per-agent spend against limit for the current period computed from verified receipts, with the reconciliation surfaced honestly if local accounting and protocol state diverge, adopting the protocol figure."
ac_9 = "THE service SHALL bound the blast radius of a compromised agent to its narrowed capability and budget, and the qualification suite SHALL demonstrate that a hostile agent cannot exceed either through any human-plane surface."

[req.13]
title = "The Approval Inbox"
story = "As the human in the loop, I want every request for my approval to show exactly what will happen if I say yes, so that my yes means precisely one thing."
ac_1 = "THE app SHALL present every pending hold from the approval module as an inbox item showing the agent, the counterparty, the exact amount, the reason it was held, a live expiry countdown, and the budget remaining after, computed from agent-layer verified budget reads and displayed with its verification status."
ac_2 = "WHEN an approval is opened THE app SHALL render the held activity's own facts - amount, counterparty, asset, fees and expiry - only from the disclosure that re-encodes to the held digest, never from a paraphrase or a separate data source, and SHALL source contextual figures such as budget remaining exclusively from verified reads labelled with their verification status."
ac_3 = "WHEN the user approves THE app SHALL require step-up authentication bound to the held digest, THE service SHALL grant the approval for exactly that digest through the approval module, and the app SHALL then track the released activity to its receipt like any movement."
ac_4 = "WHEN the user rejects THE service SHALL reject the hold immediately, and the app SHALL confirm that nothing moved."
ac_5 = "WHEN a hold expires unactioned THE app SHALL show it as expired with the statement that no money moved, and an expired item SHALL offer no approve control."
ac_6 = "WHEN two devices act on the same hold THE plane SHALL converge on exactly one outcome with the second decision receiving an honest already-decided presentation."
ac_7 = "WHEN a hold is created THE service SHALL notify the user with the agent and amount and a deep link to the inbox item, and the sidebar, tab and header surfaces SHALL carry a live count of holds awaiting the user."
ac_8 = "THE service SHALL record every approval decision in the audit trail with its digest, its step-up evidence and its outcome, exportable with the referenced receipts."

[req.14]
title = "Activity and Receipts"
story = "As a user, I want one place that tells me what happened in my words with the evidence one tap away, so that I never reconcile my own money across surfaces."
ac_1 = "THE app SHALL present one activity feed spanning deposits, withdrawals, internal movements, agent actions, approvals and security events, with custody-boundary journeys rendered as single joined entries spanning their stages."
ac_2 = "THE feed SHALL translate every state through the normative status translation table, SHALL render Done only under the receipt rule, and SHALL render a refusal with an explicit statement of whether money left the account."
ac_3 = "WHEN an entry is opened THE app SHALL show what happened in one plain sentence, the staged timeline where applicable, the receipt-backed amounts and fees, and a collapsed Technical details section containing identifiers, verification status, checkpoint linkage and the evidence references."
ac_4 = "THE feed SHALL group by month with per-group subtotals, SHALL filter by type, agent and date through deferred-apply controls, and SHALL echo an applied filter in the content so the active view is always self-describing."
ac_5 = "WHEN the user exports activity THE service SHALL produce a statement export and an evidence bundle containing the referenced receipts and proofs in their canonical bytes, verifiable independently of this plane."
ac_6 = "THE feed SHALL paginate with stable cursors, SHALL survive service restart without gaps, duplicates or reordering, and SHALL reflect a new verified receipt within the declared freshness bound."
ac_7 = "THE feed SHALL render amounts with sign, semantic color and direction word together, in tabular numerals, under the one amount format and one date format the design fixes."
ac_8 = "WHERE an entry's underlying receipt gains higher verification - batch inclusion, checkpoint finality - THE entry SHALL upgrade its shown verification status without changing its user-facing state except from Done to Done, finalised."

[req.15]
title = "The Public Explorer"
story = "As anyone - user or not - I want to look up a receipt, an account or a checkpoint and verify it from public data, so that trust in this plane never has to be taken on faith."
ac_1 = "THE explorer plane SHALL be public, server-rendered and deep-linkable, requiring no authentication, and SHALL show protocol-public data only, never profile data, notification content or any declared-local artefact."
ac_2 = "THE explorer SHALL serve checkpoint and batch browsing, receipt lookup by identifier, account activity views and an evidence verifier that accepts pasted receipt or proof bytes and reports the achieved verification level."
ac_3 = "THE explorer index SHALL be built by layerx-explorer-index exclusively through the node boundary and data-availability retrieval, SHALL be rebuildable from those sources alone, and SHALL never read node internals, inheriting the agent layer's boundary prohibitions."
ac_4 = "WHEN the explorer displays a protocol fact THE page SHALL show the verification level achieved for it, and the verifier SHALL perform its verification with the same proof machinery the plane uses, never a lookup of the index's own opinion."
ac_5 = "THE explorer SHALL state its own freshness - the head and latest finalised checkpoint it has indexed - on every page, and an index that is behind SHALL say so rather than presenting partial data as complete."
ac_6 = "THE app's Technical details surfaces SHALL link to the explorer for every receipt and checkpoint they reference, and those links SHALL resolve for a signed-out viewer."
ac_7 = "THE explorer SHALL meet the same design-system, accessibility and performance requirements as the app plane, rendered table-first for scanning."

[req.16]
title = "One Application, Two Native Shells"
story = "As a user on any device, I want the app to feel designed for the device in my hands - not a stretched phone app on my monitor, not a shrunken website on my phone - so that both feel native."
ac_1 = "THE application SHALL ship a mobile shell and a desktop shell that share state management, data access, journey logic and the copy catalog, while rendering device-specific layouts and device-specific components, and no journey logic SHALL be duplicated per shell."
ac_2 = "THE mobile shell SHALL provide bottom tab navigation, stacked screens, bottom sheets, full-screen one-decision-per-screen wizards and pinned thumb-reach primary actions; THE desktop shell SHALL provide persistent sidebar navigation, master-detail panes, centered modals, anchored popovers, split-pane wizards and a global command bar with keyboard shortcuts."
ac_3 = "THE application SHALL implement the pattern-pair mapping the design fixes - navigation, primary action, confirmation, detail, filters, money lists, wizards, search, code entry, notifications - such that every pattern has an explicit component in each shell, and a screen using a raw pattern outside the kit SHALL fail the UI gate."
ac_4 = "WHEN the server renders the first response THE application SHALL choose the shell SSR-safely from viewport and pointer capability hints, SHALL confirm on the client, and SHALL correct a mismatch without layout thrash or a flash of the wrong shell; user-agent strings SHALL never be the sole selector."
ac_5 = "THE application SHALL give tablets the desktop layout with touch-sized targets, and every interactive element in both shells SHALL meet the touch-target and pointer-affordance rules of its shell."
ac_6 = "THE two shells SHALL have feature parity over every v1 journey, and the qualification suite SHALL run every journey end to end in both shells."
ac_7 = "WHEN a deep link or notification link opens THE application SHALL resolve it to the equivalent screen in whichever shell is active, and links SHALL be shareable across shells without loss."
ac_8 = "THE desktop shell SHALL be fully keyboard-operable with visible focus order, Escape and overlay dismissal for layered surfaces, and focus trapping in modals; THE mobile shell SHALL honour safe areas, on-screen keyboard avoidance and swipe-dismissal conventions."

[req.17]
title = "The Design System"
story = "As a user, I want every screen to feel like one calm product, so that nothing visual competes with understanding my money."
ac_1 = "THE UI SHALL use the exact visual styling of the owner-supplied @layerx/ui package, including its border strokes, dividers, shadows, gradients, layered surfaces, radii and control treatments, in both the mobile and desktop shells."
ac_2 = "THE application SHALL consume @layerx/ui as its component primitive source, SHALL preserve the package's public component API and token stylesheet, and SHALL not replace package primitives with locally restyled raw equivalents."
ac_3 = "THE @layerx/ui token stylesheet SHALL be the authoritative source for the application palette, typography, radii, elevation and motion, and application-level styling SHALL compose those exported tokens without silently redefining the library's visual contract."
ac_4 = "THE UI SHALL use the package's semantic success, destructive, warning, primary and accent treatments, SHALL never use color as the sole carrier of meaning, and SHALL render money with sign, color and direction word together."
ac_5 = "THE UI SHALL use one fixed type scale, tabular numerals for all figures, one date format and one amount format everywhere, defined once in the design tokens and copy catalog."
ac_6 = "THE UI SHALL express every control state - hover, focus, active, disabled, loading - through the corresponding @layerx/ui component variant, with disabled states carrying their reason nearby."
ac_7 = "THE UI SHALL use the package-defined motion durations and easing, SHALL honour prefers-reduced-motion by disabling non-essential animation, and SHALL use the package's skeleton treatment for loading."
ac_8 = "THE component kit SHALL be the only source of UI primitives in both shells, and a screen composing raw elements where a kit component exists SHALL fail the UI gate."
ac_9 = "THE confirmation grammar SHALL distinguish reversible actions - neutral primary with consequence sentence - from destructive actions - red primary - from irreversible actions - red primary plus typed confirmation - and the kit SHALL make the wrong grammar unrepresentable."

[req.18]
title = "Nothing Fails Silently"
story = "As a user when something goes wrong, I want to be told what happened, what it means for my money and what I can do, so that failure never looks like a broken app or a lost balance."
ac_1 = "THE application SHALL ship, for every screen, the full state matrix - loading, empty, error, offline, degraded and, where money is in flight, still-checking - and the qualification suite SHALL enumerate every screen's states and fail on a missing one."
ac_2 = "THE application SHALL render every error state as a designed screen or section following the error anatomy: what happened in plain words, what it means for the user's money, action buttons - Retry where retriable, Reload where structural, Report always available - and a collapsed technical section with the machine code and trace identifier."
ac_3 = "THE application SHALL wrap every route in error boundaries rendering the same anatomy, and a user SHALL never see a blank screen, a raw stack trace or an unstyled failure."
ac_4 = "WHEN connectivity is lost THE app SHALL show an offline banner, per-screen offline states with retry, and honest queued presentation for actions awaiting connectivity, and SHALL never let an offline queue duplicate a money movement."
ac_5 = "WHEN data is stale THE app SHALL present the degraded state with the value's age and reference point, exactly as req 1 ac_8 requires, in amber staleness presentation."
ac_6 = "WHEN a form field is invalid THE app SHALL show the inline error at the field with how to fix it, keep the primary action disabled with the reason discoverable, and never fail a submission for a reason it could have shown inline."
ac_7 = "THE application SHALL give every hand-off to an external surface - the Paxeer wallet, an OS share or file surface - designed in-progress, failure and cancel states, and SHALL treat a blank interstitial during a hand-off as a defect."
ac_8 = "WHEN the user activates Report THE app SHALL submit the trace identifier, machine code and non-sensitive context with user consent, SHALL confirm the report, and support surfaces SHALL be able to retrieve the same trace the user saw."
ac_9 = "THE still-checking state SHALL lock every control that could duplicate the in-flight effect, state that the money is safe and that the user should not retry, and resolve automatically when the receipt lookup concludes."

[req.19]
title = "Notifications and Alerts"
story = "As a user who is not watching the app, I want to be told when something needs me or something landed, so that agents can work while I live my life."
ac_1 = "THE service SHALL notify on: an approval awaiting the user, money arriving, a journey completing or failing, a claim becoming ready, security events - new device, recovery initiated, key rotation, wallet rebinding - and service degradation, each with a deep link to the exact surface."
ac_2 = "THE service SHALL support push, email and in-app channels with per-event-class preferences nested under per-channel toggles and a notification detail level controlling how much financial detail payloads carry, and preference changes SHALL take effect immediately."
ac_3 = "THE service SHALL attach action buttons to security notifications - review devices, pause agent, open approval - that deep-link into the authenticated surface, and SHALL NOT allow security-critical classes including recovery initiation and wallet rebinding to be fully suppressed."
ac_4 = "THE in-app notification surface SHALL group by recency, distinguish read from unread, deduplicate repeated events, and on desktop appear as a bell popover with a full archive page."
ac_5 = "THE service SHALL keep notification payloads free of secrets, SHALL bound their financial detail by the user's notification detail level, and SHALL degrade lock-screen presentations gracefully to non-sensitive summaries."
ac_6 = "WHEN a notification's subject is already resolved THE tapped deep link SHALL land on the resolved state honestly - an expired approval reads expired, a completed journey reads done - never on a stale actionable presentation."
ac_7 = "THE approval count surfaced on tabs, sidebars and badges SHALL be consistent with the inbox within the declared freshness bound, and SHALL clear when no holds await the user."

[req.20]
title = "Accessibility, Performance and Internationalization"
story = "As any user on any device, connection or ability, I want the app to be fast, legible and operable, so that professional quality is a property, not a marketing claim."
ac_1 = "THE application SHALL meet WCAG 2.2 AA: text contrast at least 4.5 to 1, full keyboard operability, visible focus, focus trapping in layered surfaces, touch targets at least 44 pixels on mobile, and status changes announced through live regions."
ac_2 = "THE application SHALL label every amount, status and control for assistive technology such that a screen-reader user can complete every v1 journey, and the qualification suite SHALL include assistive-technology runs of the core journeys."
ac_3 = "THE application SHALL meet performance budgets at the 75th percentile - largest contentful paint at most 2.5 seconds, interaction to next paint at most 200 milliseconds, cumulative layout shift at most 0.1 - enforced in CI on representative pages and monitored in production with real-user metrics against the same budgets."
ac_4 = "THE application SHALL code-split by route with declared per-route script budgets, SHALL render the explorer plane server-side cacheable, and SHALL load data with skeletons that reserve layout so content arrival shifts nothing."
ac_5 = "THE application SHALL keep every user-facing string in the versioned copy catalog with ICU plural and selection support and no runtime string concatenation of sentence fragments, and the copy lint SHALL enforce catalog completeness, the banned-vocabulary list and the format rules."
ac_6 = "THE layouts SHALL tolerate 1.5 times text expansion without truncation or overlap, amounts SHALL render locale-aware with an explicit currency code, and dates SHALL use the single unambiguous format everywhere."
ac_7 = "THE application SHALL remain usable on slow connections: journey surfaces SHALL function at 3G-class latency with honest progress, and no journey SHALL require a large asset to proceed."

[req.21]
title = "Security UX, Sessions and Privacy"
story = "As the person the attacker would target, I want the app's everyday behaviours to be the safe ones, so that security is the path of least resistance."
ac_1 = "THE app SHALL require step-up authentication - a fresh passkey ceremony - for approvals, withdrawals and exits, security setting changes, secret reveals, wallet rebinding and agent archive, bound to the specific operation as req 4 ac_3 requires."
ac_2 = "THE app SHALL render every secret-class value - backup codes, recovery evidence, session tokens - masked by default, SHALL reveal it only after step-up and re-mask it on a timer, and SHALL keep it out of the clipboard except by an explicit user action on an explicitly copyable value."
ac_3 = "THE app SHALL provide a device and session inventory with per-session revoke, show each session's last activity, and revocation SHALL take effect promptly across shells."
ac_4 = "THE service SHALL emit the new-device notification of req 3 ac_8 and support sign-out-everywhere, which SHALL also invalidate refresh paths, not only active sessions."
ac_5 = "THE app SHALL provide a privacy mode masking balances and every derived figure - subtotals, spend bars, notification amounts - with one toggle, persisted per user, applying across both shells."
ac_6 = "THE plane SHALL emit telemetry containing no personal data, no balances, no addresses and no identifiers beyond pseudonymous ones, enforced by a redaction layer with a CI gate over emitted schemas."
ac_7 = "THE app plane SHALL enforce a strict content security policy, load no third-party scripts on authenticated surfaces, and pin its asset origins."
ac_8 = "THE 2FA surface SHALL offer authenticator-app methods with one-time backup codes issued at setup, SHALL present disable warnings with consequence copy, and SHALL follow the security-mutation step-up rules."
ac_9 = "THE app SHALL make session expiry honest: an expiring session warns before it expires where the platform allows, an expired session lands on re-authentication with the intended destination preserved, and no money control SHALL be operable on an expired session."

[req.22]
title = "Isolation, Audit and Observability"
story = "As an operator of a multi-tenant human plane, I want every principal's data isolated, every consequential action evidenced and every failure traceable, so that operating at 1M+ users is engineering, not archaeology."
ac_1 = "THE service SHALL scope every stored row, cache entry, journey, notification and audit record to its authenticated principal, SHALL map each human principal to its agent-layer tenancy so agent-layer isolation guarantees compose, and the adversarial isolation suite SHALL prove no cross-principal read or write path exists."
ac_2 = "THE service SHALL keep an append-only hash-chained audit log of authentication events, signing decisions, approvals, journey transitions, security changes and notification dispatches, with each entry referencing its evidence, and SHALL detect and refuse silent truncation or reordering."
ac_3 = "THE user SHALL be able to export their own audit trail with the referenced receipts and proofs as an evidence bundle verifiable independently of this plane."
ac_4 = "THE plane SHALL propagate a trace identifier from the browser through human-api to the agent layer and back, SHALL show it on every error surface, and SHALL make it the key support uses to retrieve the failure context the user saw."
ac_5 = "THE service SHALL instrument journey funnels - onboarding, deposit, move, create-agent, approval, withdrawal - with stage-level outcomes so drop-off and failure are measurable per stage without any personal data."
ac_6 = "THE service SHALL redact secrets, key material, personal data and financial values from logs, metrics and traces by construction with a gate over log and metric schemas, and a redaction failure SHALL be a build defect."
ac_7 = "THE service SHALL expose health and readiness surfaces distinguishing its own degradation from agent-layer and from Paxeer-side degradation, and the app SHALL translate each into the honest user-facing degraded states."
ac_8 = "THE service SHALL declare and enforce retention per store - journeys, notifications, audit, telemetry - with expiry that never deletes evidence still referenced by an exportable audit entry."

[req.23]
title = "Qualification and Release Gates"
story = "As the person accountable for shipping this to a million people, I want every promise in this specification demonstrated against real systems before release, so that the gate is evidence, not optimism."
ac_1 = "THE qualification suite SHALL run every v1 journey end to end - onboarding, wallet binding, deposit, move money, create agent, approval grant and reject, withdrawal with claim, emergency exit - against a real node, a real layerx-agentd and a Paxeer test network, with no in-process substitute standing in for any of the three."
ac_2 = "THE suite SHALL run a hostile-plane test in which a tampered human-service reports altered balances, altered outcomes and fabricated successes, and SHALL demonstrate that exported evidence and explorer verification catch every alteration, and that the untampered UI cannot render Done without a verifying receipt."
ac_3 = "THE suite SHALL run the fault-injection matrix over every money journey - crash, disconnect and duplicate delivery at each stage - proving exactly-once economic effect, honest still-checking presentation and correct resumption in every case."
ac_4 = "THE suite SHALL enumerate every screen in both shells and fail on a missing state-matrix entry, SHALL run visual regression across both shells at the canonical @layerx/ui reference viewports, and SHALL run the copy lint, component-library integrity gate and component-kit gate as build gates."
ac_5 = "THE suite SHALL run automated accessibility checks plus assistive-technology journey runs, and SHALL fail the build on a WCAG 2.2 AA violation on any v1 surface."
ac_6 = "THE suite SHALL enforce the performance budgets of req 20 ac_3 in CI and SHALL run a soak that holds long-lived journeys - a deposit awaiting finality, a withdrawal awaiting checkpoint, an unactioned approval - across service restarts, proving no journey is lost, duplicated or stranded."
ac_7 = "THE usability gate SHALL have real participants complete onboarding, deposit, move-money and create-agent on mobile without documentation, against a declared task-success threshold recorded in the report."
ac_8 = "THE isolation, custody, no-key-export and telemetry-redaction suites SHALL run as build gates, and any escape SHALL be a build-breaking defect rather than a tracked issue."
ac_9 = "THE release SHALL be gated on a qualification report stating which guarantees are protocol-enforced, which are agent-layer-enforced and which are human-plane-enforced, which suites ran with what results, and every unmet gate, and a release with an unmet gate SHALL be refusable by tooling, not reviewer discipline."

# --------------------------------------------------------------------------
# TASKS
# --------------------------------------------------------------------------

[task.1]
section = "Wave 1 - Foundations and the Approval Seam"
title = "Establish the human workspace and its engineering gates"
status = "pending"

[task.1.1]
title = "Create the human workspace and its build entry points"
status = "done"
wave = 1
do_1 = "Create human/ with the Cargo workspace members layerx-human-service, layerx-intents, layerx-paxeer-client and layerx-explorer-index, the Next.js TypeScript application at human/apps/web, and the contract directory human/schema/human-api."
do_2 = "Wire the Rust crates to the agent workspace crates layerx-types, layerx-wire, layerx-crypto, layerx-proof, layerx-client and layerx-sdk as path dependencies, consuming them without forking or re-implementing any of them."
do_3 = "Add the human-build, human-test, human-lint, human-check and human-e2e make targets to the repository Makefile, delegating to cargo and the web package manager inside human/, with none of them entering the C build or the agent build."
do_4 = "Prove the existing builds are untouched: make build and make agent-build succeed with the human workspace present, and no C or agent target gains a human dependency."
do_5 = "Add a workspace README whose first two paragraphs state the receipt-backed-truth rule and the five-ideas simplicity contract."
verify_cmd = "make human-build"
symbols = ["human_workspace_manifest","human_web_app_scaffold"]
touches = ["human/Cargo.toml","human/apps/web/package.json","human/README.md","Makefile"]
reqs = ["1.10","1.1"]

[task.1.2]
title = "Lock the dependency, boundary and supply-chain policy"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Apply the dependency policy to the Rust crates: deny unmaintained, yanked and vulnerable crates, require the license allowlist, forbid SQLite drivers and node-internal access exactly as the agent workspace boundary rules require, and fail the build rather than warn."
do_2 = "Apply the equivalent policy to the web application: a pinned lockfile, an audit gate on advisories, a license allowlist, and a dependency budget that flags any new runtime dependency for review."
do_3 = "Forbid ambient clock reads inside verification and journey-resolution paths through the lint set, mirroring the agent workspace rule."
do_4 = "Enable the strict lint sets for both ecosystems - clippy with deny on unwrap and expect outside tests for the crates, strict TypeScript with no implicit any and exhaustive switches for the app - and fail the build on warnings."
do_5 = "Wire all of it into make human-lint and CI so the policy is enforced on every change."
verify_cmd = "make human-lint"
symbols = ["human_dependency_policy","human_boundary_policy"]
touches = ["human/deny.toml","human/clippy.toml","human/apps/web/package.json",".github/workflows/human.yml"]
reqs = ["1.10","23.8"]

[task.1.3]
title = "Create the copy catalog and the copy lint"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Create the versioned copy catalog as the single home of every user-facing string, with ICU plural and selection support and per-key context notes for future localization."
do_2 = "Encode the banned-vocabulary list - DID, session key, capability, nullifier, checkpoint, payload, canonical, idempotency, attestation, proof - and enforce it with a lint that fails the build when a banned term appears in copy outside Technical details surfaces."
do_3 = "Encode the format rules as lint checks: the single date format, the single amount format with explicit sign and currency code, sentence case, and no exclamation marks in money-adjacent copy."
do_4 = "Forbid runtime string concatenation of sentence fragments by lint, so every sentence is a complete catalog entry."
do_5 = "Wire the copy lint into make human-lint and CI, and seed the catalog with the status translation table from the design as its first normative entries."
verify_cmd = "make human-lint-copy"
symbols = ["human_copy_catalog","human_copy_lint"]
touches = ["human/apps/web/copy/catalog.ts","human/tools/copy-lint/src/main.rs",".github/workflows/human.yml"]
reqs = ["2.3","20.5","2.8"]

[task.1.4]
title = "Integrate the LayerX component library and design system"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Extract the owner-supplied layerx-ui package source into the human web workspace without its showcase application, preserving the component source, public exports and exact token stylesheet."
do_2 = "Wire @layerx/ui, Tailwind CSS and the package stylesheet into the Next.js application with reproducible pinned dependencies and no parallel local primitive system."
do_3 = "Make the package tokens and styles authoritative, including their borders, dividers, gradients, shadows, radii, typography, semantic colors and platform-specific component treatments."
do_4 = "Build a component-library integrity gate that validates the required source inventory, public exports, stylesheet import, token contract and application dependency wiring."
do_5 = "Wire the integrity gate into make human-check-ui and CI, with fixtures proving missing exports, missing tokens and competing local primitives are caught and a clean integration passes."
verify_cmd = "make human-check-ui"
symbols = ["human_design_tokens","human_ui_rule_gates"]
touches = ["human/apps/web/src/design/tokens.ts","human/apps/web/packages/layerx-ui/src/styles/tokens.css","human/tools/ui-gate/src/main.rs",".github/workflows/human.yml"]
reqs = ["17.1","17.2","17.3"]

[task.1.5]
title = "Stand up the test, end-to-end and state-matrix harness"
status = "done"
wave = 1
requires = ["1.1"]
do_1 = "Establish the test layout for the crates - unit, integration, property and fault-injection suites - and for the app - component, journey and end-to-end suites - each mapped to a make target."
do_2 = "Stand up the browser end-to-end harness able to drive both shells against a real service, with device profiles for the mobile and desktop shells and trace capture on failure."
do_3 = "Stand up visual regression across both shells at the canonical @layerx/ui mobile and desktop reference viewports with a reviewed-baseline workflow."
do_4 = "Build the state-matrix registry: every screen declares its loading, empty, error, offline, degraded and still-checking states in a manifest the suite enumerates, failing on a missing declaration or an unrenderable state."
do_5 = "Add the CI workflow running build, lint, UI gates, unit and component suites on every change and the browser suites on merge, with the longer suites scheduled."
verify_cmd = "make human-test"
symbols = ["human_test_harness","human_state_matrix_registry"]
touches = ["human/apps/web/e2e/harness.ts","human/apps/web/e2e/state-matrix.ts",".github/workflows/human.yml","Makefile"]
reqs = ["23.4","18.1","16.6"]

[task.2]
title = "Extend the agent contract with approval operations"
status = "pending"

[task.2.1]
title = "Define the approval module schema"
status = "done"
wave = 1
do_1 = "Define approval.kvx in the agent-api schema with the list, get, approve and reject operations, the approval record carrying the held activity's structured disclosure, its canonical-bytes digest, its hold reason, its creation time and its deterministic expiry, and the lifecycle event types."
do_2 = "Include the module from v1.kvx as an additive minor-version change, leaving every existing declaration untouched, and record the minor bump in the schema compatibility notes."
do_3 = "Add golden request and response vectors for every approval operation and event."
do_4 = "Extend the schema conformance suite so the golden vectors and the additive-only rule are enforced for the new module."
do_5 = "Update the contract documentation to state that approvals are a daemon-enforced restriction and confer no protocol authority."
verify_cmd = "make agent-test"
symbols = ["agent_api_approval_module"]
touches = ["agent/schema/agent-api/approval.kvx","agent/schema/agent-api/v1.kvx","agent/schema/agent-api/golden/approval.kvx"]
reqs = ["6.1","6.9"]

[task.2.2]
title = "Implement approval operations in the daemon"
status = "done"
wave = 1
requires = ["2.1"]
do_1 = "Implement list, get, approve and reject in layerx-agentd over the existing policy hold store, scoped to the authenticated tenant like every other operation."
do_2 = "Carry the held activity's disclosure and digest on every approval record, verifying at read time that the disclosure re-encodes byte-identically to the digest's preimage and reporting a mismatch as a defective hold that cannot be approved."
do_3 = "Make approve release exactly the held prepared activity into the existing submission path, voiding the hold if the underlying activity changed in any way since the hold was created."
do_4 = "Make reject cancel the hold and release its reservations deterministically, reporting the refusal to the requesting agent with a typed result."
do_5 = "Return typed results for every outcome: granted, rejected, expired, defective, already-decided and conflict."
verify_cmd = "make agent-test-approvals"
symbols = ["layerx_agentd::approval::ApprovalService"]
touches = ["agent/crates/layerx-agentd/src/approval/mod.rs","agent/crates/layerx-agentd/tests/approval_ops.rs"]
reqs = ["6.2","6.3","6.4"]

[task.2.3]
title = "Enforce expiry, idempotency and concurrency semantics"
status = "done"
wave = 1
requires = ["2.2"]
do_1 = "Expire unactioned holds at exactly the deterministic expiry the policy engine computed, bounded by the held activity's submission validity window, releasing reservations on expiry, with an approval attempt after expiry returning the typed expired result."
do_2 = "Make approve and reject idempotent under their idempotency keys, with a repeated decision returning the original outcome."
do_3 = "Resolve concurrent conflicting decisions to exactly one winner, with the losing request receiving the typed conflict result naming the winning outcome."
do_4 = "Prove the semantics under crash-restart: a decision recorded before a crash survives it, and a hold expired during downtime is expired on recovery."
do_5 = "Cover all of it with concurrency and restart tests running against the real store."
verify_cmd = "make agent-test-approvals"
symbols = ["layerx_agentd::approval::ApprovalExpiry"]
touches = ["agent/crates/layerx-agentd/src/approval/expiry.rs","agent/crates/layerx-agentd/tests/approval_semantics.rs"]
reqs = ["6.5","6.6"]

[task.2.4]
title = "Emit approval lifecycle events and audit evidence"
status = "done"
wave = 1
requires = ["2.2"]
do_1 = "Emit created, approved, rejected and expired events on the existing ordered event stream with the approval's digest, so clients track holds without polling."
do_2 = "Record every lifecycle transition in the audit trail with the digest, the deciding principal where one exists, and the outcome."
do_3 = "Label the approval surface daemon-enforced in the restriction report and in every response that describes it."
do_4 = "Extend the streaming conformance tests to cover approval events, including resumption across a cursor gap."
do_5 = "Extend the audit export tests so approval decisions appear with their referenced evidence."
verify_cmd = "make agent-test-approvals"
symbols = ["layerx_agentd::approval::ApprovalEvents"]
touches = ["agent/crates/layerx-agentd/src/approval/events.rs","agent/crates/layerx-agentd/tests/approval_events.rs"]
reqs = ["6.7","6.9"]

[task.2.5]
title = "Regenerate the SDKs with the approval module"
status = "done"
wave = 1
requires = ["2.1","2.2"]
do_1 = "Regenerate the TypeScript and Python SDKs and extend the authored Rust SDK surface with the approval module from the same schema in the same build."
do_2 = "Extend the cross-SDK parity suite to cover every approval operation, event and error outcome."
do_3 = "Record the introducing minor version in the compatibility matrix and the guarantee documentation."
do_4 = "Prove the drift gate fails when generated output is stale by regenerating from a modified schema in a fixture."
do_5 = "Run the full agent qualification-relevant suites to prove the addition changed no existing behaviour."
verify_cmd = "make agent-test"
symbols = ["layerx_sdk::approval"]
touches = ["agent/crates/layerx-sdk/src/approval.rs","agent/sdk/typescript","agent/sdk/python","agent/COMPATIBILITY.md"]
reqs = ["6.8","6.1"]

[task.3]
section = "Wave 2 - Intents and the Human Contract"
title = "Build layerx-intents, the typed intent compiler"
status = "pending"

[task.3.1]
title = "Define the intent vocabulary and its versioning"
status = "done"
wave = 2
requires = ["1.1"]
do_1 = "Define the versioned intent types for v1: DID registration, key rotation, recovery registration, EVM payout binding, 402LXP send, 402LXP receive under a payer grant, payer grant registration, budget create, budget fund, budget defund, bridge deposit credit and bridge withdraw request."
do_2 = "Build every intent field from layerx-types domain types only - amounts as protocol integers, identifiers through their namespaces - so an invalid intent is unconstructible."
do_3 = "Give the vocabulary an explicit version with the rule that a change to any intent's compiled bytes requires a version bump."
do_4 = "Enforce protocol length and range bounds at intent construction with typed errors."
do_5 = "Document each intent kind with the journey it serves and the module payload it compiles to."
verify_cmd = "make human-test-intents"
symbols = ["layerx_intents::Intent","layerx_intents::IntentVersion"]
touches = ["human/crates/layerx-intents/src/lib.rs","human/crates/layerx-intents/src/vocabulary.rs"]
reqs = ["7.1","7.6"]

[task.3.2]
title = "Compile intents to canonical payload bytes"
status = "done"
wave = 2
requires = ["3.1"]
do_1 = "Implement compilation from each intent kind to its canonical module payload exclusively through layerx-types and layerx-wire, with no serialisation logic of its own."
do_2 = "Return the compiled payload with its payload hash computed through the wire crate's domain-tagged hashing, ready for the agent layer's prepare operation."
do_3 = "Reject at compile time any intent whose field combination cannot produce a canonical payload, with a typed error naming the field."
do_4 = "Property-test that compilation is deterministic: equal intents compile to identical bytes across processes and runs."
do_5 = "Add the intent-to-prepare integration test proving a compiled payload is accepted by the agent layer's prepare operation and produces a decodable disclosure."
verify_cmd = "make human-test-intents"
symbols = ["layerx_intents::compile"]
touches = ["human/crates/layerx-intents/src/compile.rs","human/crates/layerx-intents/tests/compile.rs"]
reqs = ["7.2","7.7"]

[task.3.3]
title = "Ship the golden vectors and the disclosure round-trip gate"
status = "done"
wave = 2
requires = ["3.2"]
do_1 = "Commit golden vectors for every intent kind: the intent, its compiled payload bytes and its payload hash, asserting byte-for-byte stability across releases."
do_2 = "Fail the build when a vector changes without an intent-version bump."
do_3 = "Build the differential gate proving compile, decode and disclosure re-encoding yield byte-identical payloads through layerx-wire for every vector and for generated intents."
do_4 = "Implement the disclosure-matches-intent checker that verifies field-for-field agreement between a returned disclosure and the originating intent, as the pre-signature gate journeys will use."
do_5 = "Wire both gates into make human-test-intents and CI."
verify_cmd = "make human-test-intents"
symbols = ["layerx_intents::golden","layerx_intents::DisclosureCheck"]
touches = ["human/crates/layerx-intents/vectors","human/crates/layerx-intents/src/disclosure.rs","human/crates/layerx-intents/tests/differential.rs"]
reqs = ["7.3","7.4","7.7"]

[task.3.4]
title = "Reject the unknown deterministically and fuzz the surface"
status = "done"
wave = 2
requires = ["3.2"]
do_1 = "Reject unknown intent versions and unknown intent kinds with typed errors, preserving the input for diagnosis without executing it."
do_2 = "Add fuzz targets for intent deserialisation and compilation with committed corpora, treating a panic, hang or unbounded allocation as a build-breaking defect."
do_3 = "Assert no floating-point type appears anywhere in the crate through the lint set."
do_4 = "Property-test bound enforcement: every over-long and out-of-range field is rejected at construction, never truncated."
do_5 = "Wire the fuzz targets into the scheduled fuzz job alongside the agent workspace targets."
verify_cmd = "make human-test-intents"
symbols = ["layerx_intents::fuzz_targets"]
touches = ["human/crates/layerx-intents/fuzz/fuzz_targets/intent.rs","human/crates/layerx-intents/src/reject.rs"]
reqs = ["7.5","7.8","7.6"]

[task.3.5]
title = "Enforce the single payload authority"
status = "done"
wave = 2
requires = ["3.2","1.2"]
do_1 = "Extend the human workspace dependency gate so no component of human/ other than layerx-intents may depend on layerx-wire's encoding entry points."
do_2 = "Add fixtures proving the gate catches a violating dependency in a service crate and in the web application's build."
do_3 = "Verify the web application bundle contains no payload-encoding code path by scanning the build output in CI."
do_4 = "Document in the workspace README that layerx-intents is the single payload authority for this plane and why."
do_5 = "Wire the gate into make human-check and CI."
verify_cmd = "make human-check"
symbols = ["human_payload_authority_gate"]
touches = ["human/tools/boundary-check/src/main.rs","human/tools/boundary-check/tests/fixtures.rs","Makefile"]
reqs = ["7.9","1.1"]

[task.4]
title = "Define human-api, the human-facing service contract"
status = "pending"

[task.4.1]
title = "Define the contract schema and its compatibility rules"
status = "done"
wave = 2
requires = ["1.1"]
do_1 = "Define the human-api schema under human/schema/human-api with explicit major and minor versions, the additive-only rule within a major, and the HTTPS+JSON encoding conventions."
do_2 = "Establish the golden request and response vector convention and the conformance suite that enforces it."
do_3 = "Define the journey vocabulary as the contract's spine - journeys with identifiers, stages, states and evidence references - so no operation speaks raw protocol structures."
do_4 = "Define the compatibility matrix artefact recording schema, service and web application versions."
do_5 = "Wire schema validation and the additive-only check into make human-check and CI."
verify_cmd = "make human-check"
symbols = ["human_api_schema"]
touches = ["human/schema/human-api/v1.kvx","human/schema/human-api/golden","human/tools/schema-check/src/main.rs"]
reqs = ["8.1","8.9"]

[task.4.2]
title = "Define identity, session and wallet-binding operations"
status = "done"
wave = 2
requires = ["4.1"]
do_1 = "Define account creation, passkey registration and assertion, session open, refresh, list and revoke, and the step-up challenge operations."
do_2 = "Define the onboarding journey operations: start, stage status and resume, with the account-active condition tied to receipt verification."
do_3 = "Define wallet binding: binding statement issuance, signed-binding submission, binding status and rebinding, each receipt-gated."
do_4 = "Define profile operations for declared-local data, separated from protocol identity."
do_5 = "Add golden vectors for every operation including the failure shapes."
verify_cmd = "make human-check"
symbols = ["human_api_identity_module"]
touches = ["human/schema/human-api/identity.kvx","human/schema/human-api/golden"]
reqs = ["8.2","3.1","5.1"]

[task.4.3]
title = "Define movement and custody journey operations"
status = "done"
wave = 2
requires = ["4.1"]
do_1 = "Define the movement operations - quote, review, commit, journey status - in movement vocabulary: fund, allocate, return, transfer, deposit, withdraw, exit."
do_2 = "Require an idempotency key on every operation that could move money, with the repeated-request rule returning the original journey."
do_3 = "Define deposit, withdrawal, claim and exit journey shapes with their stages and per-stage evidence classes."
do_4 = "Define the refusal shapes carrying which limit refused and where it can be changed."
do_5 = "Add golden vectors covering multi-leg journeys, still-checking and refusals."
verify_cmd = "make human-check"
symbols = ["human_api_movement_module"]
touches = ["human/schema/human-api/movement.kvx","human/schema/human-api/golden"]
reqs = ["8.2","8.4","9.1"]

[task.4.4]
title = "Define agent, approval, activity and notification operations"
status = "done"
wave = 2
requires = ["4.1","2.1"]
do_1 = "Define agent lifecycle operations: create with name, purpose and limit, creation-journey status, pause, resume, limit change, reclaim, rotate, recover and archive."
do_2 = "Define the approval inbox operations mirroring the agent layer's approval module: list, get with rendered disclosure, approve with step-up evidence, reject."
do_3 = "Define the activity feed operations: cursor-paginated feed, entry detail with evidence references, filters, statement and evidence-bundle export."
do_4 = "Define notification operations: inventory, read state, preferences per channel and event class, and the non-suppressible security classes."
do_5 = "Add golden vectors for every operation."
verify_cmd = "make human-check"
symbols = ["human_api_agent_module","human_api_activity_module"]
touches = ["human/schema/human-api/agents.kvx","human/schema/human-api/activity.kvx","human/schema/human-api/golden"]
reqs = ["8.2","13.1","14.1"]

[task.4.5]
title = "Define the error, streaming and verification contract"
status = "done"
wave = 2
requires = ["4.1"]
do_1 = "Define the typed error model: stable machine code, copy-catalog key, trace identifier and retriability class on every failure shape."
do_2 = "Define resumable streaming of journey progress, approval lifecycle and notifications with stable cursors and reconnection semantics."
do_3 = "Define verification-status carriage on every protocol fact and the evidence-reference shape for fetching receipt and proof material."
do_4 = "Define rate-limit refusals with retry timing."
do_5 = "Add golden vectors for the error, stream and evidence shapes."
verify_cmd = "make human-check"
symbols = ["human_api_error_model","human_api_stream_module"]
touches = ["human/schema/human-api/errors.kvx","human/schema/human-api/stream.kvx","human/schema/human-api/golden"]
reqs = ["8.5","8.6","8.7"]

[task.4.6]
title = "Generate the TypeScript client and its drift gate"
status = "pending"
wave = 2
requires = ["4.2","4.3","4.4","4.5"]
do_1 = "Build the generator emitting the TypeScript client from the schema, with types for every operation, journey, error and event."
do_2 = "Make the web application consume the generated client as its only service access path."
do_3 = "Build the drift gate failing the build when generated output is stale or hand-edited."
do_4 = "Run the generated client against the schema's golden vectors as a conformance suite."
do_5 = "Wire generation and the gate into make human-build and CI."
verify_cmd = "make human-build"
symbols = ["human_api_ts_client","human_api_drift_gate"]
touches = ["human/tools/api-gen/src/main.rs","human/apps/web/src/api/generated","Makefile"]
reqs = ["8.3","8.9"]

[task.5]
section = "Wave 3 - Custody and the Paxeer Boundary"
title = "Implement identity and custody in layerx-human-service"
status = "pending"

[task.5.1]
title = "Implement the principal-scoped store and its migrations"
status = "in_progress"
wave = 3
requires = ["4.1"]
do_1 = "Implement the service store with every table scoped by principal identifier and an access layer that makes an unscoped query unrepresentable."
do_2 = "Implement versioned migrations with forward-only application and a startup refusal on a version the binary does not know."
do_3 = "Map each human principal to its agent-layer tenancy so agent-layer isolation composes, storing the mapping as authenticated configuration."
do_4 = "Declare retention per store - journeys, notifications, audit, telemetry buffers - and enforce expiry that never deletes evidence referenced by an exportable audit entry."
do_5 = "Prove isolation adversarially with a suite attempting cross-principal reads and writes through every access path."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::store::PrincipalStore"]
touches = ["human/crates/layerx-human-service/src/store/mod.rs","human/crates/layerx-human-service/migrations","human/crates/layerx-human-service/tests/isolation.rs"]
reqs = ["22.1","22.8"]

[task.5.2]
title = "Implement passkey authentication, sessions and step-up"
status = "pending"
wave = 3
requires = ["5.1"]
do_1 = "Implement passkey registration and assertion as the primary credential with a fallback credential path that never weakens the passkey path."
do_2 = "Implement session issuance bound to the authenticated principal with anti-forgery protections for the browser client, expiry, refresh, per-session revocation including sign-out-everywhere, and per-principal request rate limiting with typed refusals carrying retry timing."
do_3 = "Implement step-up: a fresh passkey ceremony bound to a specific operation digest, with a validity window, required by the designated operation classes."
do_4 = "Record every device and session in the inventory with last-activity, and emit the new-device security notification."
do_5 = "Test session expiry honesty: expired sessions land on re-authentication with destination preserved, and no money operation succeeds on an expired session."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::auth::Passkeys","layerx_human_service::auth::StepUp"]
touches = ["human/crates/layerx-human-service/src/auth/mod.rs","human/crates/layerx-human-service/src/auth/stepup.rs","human/crates/layerx-human-service/tests/auth.rs"]
reqs = ["3.1","4.3","8.8","21.9"]

[task.5.3]
title = "Implement the KMS keystore and the custody signer"
status = "pending"
wave = 3
requires = ["5.1"]
do_1 = "Implement the KMS-backed keystore holding human and managed-agent keys with per-principal isolation, zeroization on release and no plaintext key material at rest outside the KMS boundary."
do_2 = "Implement the custody signer as a layerx-crypto remote signer: it signs only given the exact canonical bytes plus a disclosure that re-encodes byte-identically, refusing on any mismatch."
do_3 = "Gate designated operations on step-up evidence bound to the disclosure digest, refusing a signature without it."
do_4 = "Append every signing grant and refusal to the audit log with digest, operation, principal and step-up evidence, never key material or payload secrets."
do_5 = "Enforce per-principal signing throughput limits as declared configuration with typed refusals, and refuse honestly with no fallback when the KMS is unavailable."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::custody::Keystore","layerx_human_service::custody::CustodySigner"]
touches = ["human/crates/layerx-human-service/src/custody/mod.rs","human/crates/layerx-human-service/src/custody/signer.rs","human/crates/layerx-human-service/tests/custody.rs"]
reqs = ["4.1","4.2","4.10"]

[task.5.4]
title = "Implement onboarding: DID, recovery and activation"
status = "pending"
wave = 3
requires = ["5.3","3.3"]
do_1 = "Orchestrate onboarding as a durable journey: application identity, key generation in custody, DID registration through a typed intent, recovery registration, each stage receipt-verified before the next."
do_2 = "Treat the account as active only when the DID registration receipt verifies, exposing honest per-stage status until then."
do_3 = "Make every stage idempotent and resumable so crashes and duplicates converge on one identity, one DID, one recovery registration and one receipt set."
do_4 = "Queue protocol stages honestly when the agent layer or core is unavailable, never fabricating an active account."
do_5 = "Record the activation evidence - each stage's receipt - retrievably for Technical details and audit export."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::onboarding::OnboardingJourney"]
touches = ["human/crates/layerx-human-service/src/onboarding/mod.rs","human/crates/layerx-human-service/tests/onboarding.rs"]
reqs = ["3.2","3.3","3.5"]

[task.5.5]
title = "Provision agent session keys and propagate revocation"
status = "pending"
wave = 3
requires = ["5.3"]
do_1 = "Provision protocol session keys to layerx-agentd for managed agents with explicit expiry, permitted activity sets and revocation paths, never transferring a primary key."
do_2 = "Propagate pause and archive into prompt session-key revocation and daemon permission suspension."
do_3 = "Implement human and agent key rotation through the protocol mechanism, surfacing the challenge delay in plain time units."
do_4 = "Renew expiring session keys for active agents without human interruption, within the authority the human granted."
do_5 = "Test revocation latency and rotation continuity against a real agent layer."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::custody::SessionKeyProvisioner"]
touches = ["human/crates/layerx-human-service/src/custody/sessions.rs","human/crates/layerx-human-service/tests/session_keys.rs"]
reqs = ["4.4","4.8"]

[task.5.6]
title = "Implement the audit chain, redaction and trace propagation"
status = "pending"
wave = 3
requires = ["5.1"]
do_1 = "Implement the append-only hash-chained audit log covering authentication, signing decisions, approvals, journey transitions, security changes and notification dispatches, each entry referencing its evidence."
do_2 = "Detect and refuse silent truncation or reordering of the chain on read and on startup."
do_3 = "Implement the redaction layer so logs, metrics and traces cannot carry secrets, key material, personal data or financial values, with a CI gate over emitted schemas."
do_4 = "Propagate the trace identifier from human-api requests through the service into agent-layer calls and back onto error responses, and instrument the journey funnels - onboarding, deposit, move, create-agent, approval, withdrawal - with stage-level outcomes under the redaction rules."
do_5 = "Implement the user's own audit export with referenced receipts and proofs as an independently verifiable evidence bundle."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::audit::AuditChain","layerx_human_service::redaction"]
touches = ["human/crates/layerx-human-service/src/audit/mod.rs","human/crates/layerx-human-service/src/redaction.rs","human/crates/layerx-human-service/tests/audit.rs"]
reqs = ["22.2","22.4","22.5","22.6"]

[task.5.7]
title = "Implement the wallet-binding journey"
status = "pending"
wave = 3
requires = ["5.3","3.2"]
do_1 = "Issue the domain-separated binding statement naming the DID and network_id for the wallet to sign, and verify the returned EVM signature before anything is submitted."
do_2 = "Submit the binding as a typed intent through the agent layer and treat the binding as active only when its receipt verifies."
do_3 = "Verify the address that signed the statement matches the address recorded in the receipt before presenting the binding as active, surfacing a mismatch as a verification failure."
do_4 = "Implement rebinding under step-up, keeping the old binding effective until the new receipt verifies, with the security notification."
do_5 = "Enforce the single-active-binding invariant, record every binding with its receipts in the audit trail, and test binding and rebinding end to end against a real core."
verify_cmd = "make human-test-service"
symbols = ["layerx_human_service::binding::BindingJourney"]
touches = ["human/crates/layerx-human-service/src/binding/mod.rs","human/crates/layerx-human-service/tests/binding.rs"]
reqs = ["5.1","5.4","5.6","5.8"]

[task.6]
title = "Build layerx-paxeer-client, the custody-boundary client"
status = "pending"

[task.6.1]
title = "Implement Paxeer reads, finality tracking and reorg handling"
status = "in_progress"
wave = 3
requires = ["1.1"]
do_1 = "Implement the Paxeer read client for custody transactions, confirmations and finality status against configured endpoints."
do_2 = "Track a custody transaction from broadcast through the finality the bridge requires, exposing staged status with confirmation counts."
do_3 = "Detect reorgs that displace a tracked transaction and report displacement as a distinct honest state, never as silent restart or false failure."
do_4 = "Distinguish endpoint unavailability from chain-side delay in every reported state."
do_5 = "Test against a Paxeer test network with induced reorg and stall scenarios."
verify_cmd = "make human-test-paxeer"
symbols = ["layerx_paxeer_client::FinalityTracker"]
touches = ["human/crates/layerx-paxeer-client/src/lib.rs","human/crates/layerx-paxeer-client/src/finality.rs","human/crates/layerx-paxeer-client/tests/finality.rs"]
reqs = ["10.2","10.5"]

[task.6.2]
title = "Construct deposit proofs and the credit path"
status = "pending"
wave = 3
requires = ["6.1","3.2"]
do_1 = "Construct the finalised custody proof for a deposit in the form the bridge module requires."
do_2 = "Feed the proof into the bridge deposit-credit intent for compilation and agent-layer submission."
do_3 = "Guarantee one custody transaction can never produce more than one credit submission, across retries and restarts."
do_4 = "Surface each failure class - custody transaction failed, proof unavailable, credit refused - as a typed state for the journey engine."
do_5 = "Test the full path against a Paxeer test network and a real core: custody transaction to verified LayerX credit receipt."
verify_cmd = "make human-test-paxeer"
symbols = ["layerx_paxeer_client::DepositProof"]
touches = ["human/crates/layerx-paxeer-client/src/deposit.rs","human/crates/layerx-paxeer-client/tests/deposit.rs"]
reqs = ["10.2","10.6"]

[task.6.3]
title = "Construct withdrawal claims and verify payouts"
status = "pending"
wave = 3
requires = ["6.1"]
do_1 = "Construct the withdrawal claim from the committed LayerX debit and its checkpoint proof, in the form the Paxeer contracts require."
do_2 = "Track a submitted claim to Paxeer finality and verify the payout transaction before it is ever reported paid out."
do_3 = "Detect and report a challenge-window hold on the claimed checkpoint as its own honest state with the window's timing."
do_4 = "Surface payout cancellation after a successful challenge with the funds' actual disposition."
do_5 = "Test claim construction, payout verification and the hold path against the test network."
verify_cmd = "make human-test-paxeer"
symbols = ["layerx_paxeer_client::WithdrawalClaim"]
touches = ["human/crates/layerx-paxeer-client/src/withdraw.rs","human/crates/layerx-paxeer-client/tests/withdraw.rs"]
reqs = ["11.3","11.4"]

[task.6.4]
title = "Construct the emergency exit path"
status = "pending"
wave = 3
requires = ["6.1"]
do_1 = "Construct the emergency exit claim against the last finalised checkpoint from evidence retrievable without LayerX ordinary operation."
do_2 = "Keep the exit path operable when the core is degraded, depending only on published checkpoint data and Paxeer availability."
do_3 = "Track the exit to Paxeer finality with the same verification rigor as withdrawals."
do_4 = "Read emergency-exit eligibility from Paxeer contract state, and surface ineligible, failure and hold states typed for the journey engine."
do_5 = "Test the exit against the test network including a scenario with LayerX reads unavailable."
verify_cmd = "make human-test-paxeer"
symbols = ["layerx_paxeer_client::EmergencyExit"]
touches = ["human/crates/layerx-paxeer-client/src/exit.rs","human/crates/layerx-paxeer-client/tests/exit.rs"]
reqs = ["11.6","11.7"]

[task.6.5]
title = "Report degradation honestly across the boundary"
status = "pending"
wave = 3
requires = ["6.1"]
do_1 = "Expose a status surface distinguishing Paxeer endpoint failure, chain congestion, finality delay and contract-level refusal."
do_2 = "Attach expected-timing context from declared configuration to every delay state so journeys can present honest expectations."
do_3 = "Never collapse a delay into a failure or a failure into a delay; classify each observable case explicitly."
do_4 = "Feed the status surface into the service health endpoints so app-plane degraded states can name the custody side."
do_5 = "Test each classification with induced endpoint failure, stall and refusal scenarios."
verify_cmd = "make human-test-paxeer"
symbols = ["layerx_paxeer_client::BoundaryStatus"]
touches = ["human/crates/layerx-paxeer-client/src/status.rs","human/crates/layerx-paxeer-client/tests/status.rs"]
reqs = ["22.7","10.5"]

[task.7]
section = "Wave 4 - Journeys"
title = "Implement the money journeys"
status = "pending"

[task.7.1]
title = "Implement the route resolver and the movement vocabulary"
status = "pending"
wave = 4
requires = ["5.4","3.2"]
do_1 = "Implement the route resolver as a deterministic function from source, destination and relationship to the protocol mechanism: budget funding, send, budget defunding, receive under a payer grant, deposit, withdrawal."
do_2 = "Compose multi-leg journeys automatically where a movement requires them, with each leg's intent derived by the resolver."
do_3 = "Enforce the movement vocabulary in every API surface, log and copy string: deposit and withdrawal for the custody boundary only, fund, allocate, return and transfer inside LayerX."
do_4 = "Property-test resolver determinism and totality: equivalent requests choose identical routes, and an unservable route returns a typed unavailable refusal with no partial attempt."
do_5 = "Surface policy, budget, capability and protocol refusals with which limit refused and where it can be changed."
verify_cmd = "make human-test-journeys"
symbols = ["layerx_human_service::journeys::RouteResolver"]
touches = ["human/crates/layerx-human-service/src/journeys/resolver.rs","human/crates/layerx-human-service/tests/resolver.rs"]
reqs = ["9.1","9.2","9.9"]

[task.7.2]
title = "Implement the durable journey engine"
status = "pending"
wave = 4
requires = ["5.1","5.3"]
do_1 = "Implement the journey engine: durable state machines whose legs run intent compilation, disclosure verification, custody signing and agent-layer submission, verifying each leg's receipt before the next leg starts."
do_2 = "Persist every transition so a service restart resumes every in-flight journey without duplicating any leg, under the idempotency keys the contract requires."
do_3 = "Implement still-checking: an unknown leg pauses the journey, resolves only by receipt lookup, and never re-submits an undetermined leg."
do_4 = "Emit journey progress on the streaming surface and to the notification service at stage boundaries."
do_5 = "Prove exactly-once economic effect per leg with a fault-injection suite crashing at every stage against a real agent layer."
verify_cmd = "make human-test-journeys"
symbols = ["layerx_human_service::journeys::JourneyEngine"]
touches = ["human/crates/layerx-human-service/src/journeys/engine.rs","human/crates/layerx-human-service/tests/journey_faults.rs"]
reqs = ["9.4","9.5","9.6"]

[task.7.3]
title = "Implement the deposit journey"
status = "pending"
wave = 4
requires = ["7.2","6.2"]
do_1 = "Implement the deposit journey: binding check, wallet signature stage, custody finality tracking, proof construction, credit submission and credit-receipt verification, as one resumable journey."
do_2 = "Keep the in-flight amount out of every balance presentation until the credit receipt verifies, exposing it as in-flight."
do_3 = "Map each stage's failure to its typed state - wallet rejected, custody failed, reorg displaced, finality stalled, credit refused - with stall reported as delay carrying a revised expectation."
do_4 = "Notify on completion and failure with a deep link to the journey."
do_5 = "Test the full deposit end to end against the test network and a real core, including crash-resume at every stage."
verify_cmd = "make human-test-journeys"
symbols = ["layerx_human_service::journeys::DepositJourney"]
touches = ["human/crates/layerx-human-service/src/journeys/deposit.rs","human/crates/layerx-human-service/tests/deposit.rs"]
reqs = ["10.1","10.2","10.6"]

[task.7.4]
title = "Implement the withdrawal and claim journey"
status = "pending"
wave = 4
requires = ["7.2","6.3"]
do_1 = "Implement the withdrawal journey: receipt-verified debit first, checkpoint wait with configured expectation, claim readiness, wallet claim stage and payout verification."
do_2 = "State irreversibility at review: a committed withdrawal completes, it does not cancel."
do_3 = "Surface challenge-window holds and post-challenge cancellation honestly through the journey states."
do_4 = "Remind on unexercised claims and preserve claimable state across restarts indefinitely."
do_5 = "Prove no double claim and no lost claimable state under the fault-injection suite."
verify_cmd = "make human-test-journeys"
symbols = ["layerx_human_service::journeys::WithdrawalJourney"]
touches = ["human/crates/layerx-human-service/src/journeys/withdraw.rs","human/crates/layerx-human-service/tests/withdraw.rs"]
reqs = ["11.1","11.2","11.8"]

[task.7.5]
title = "Implement the emergency exit journey"
status = "pending"
wave = 4
requires = ["7.2","6.4"]
do_1 = "Implement the guided exit journey over the exit path: typed confirmation of irreversibility, claim construction against the last finalised checkpoint, staged progress to verified Paxeer finality."
do_2 = "Keep the journey operable when LayerX ordinary operation is degraded, sourcing evidence as the exit path requires."
do_3 = "Present completion only on verified finality of the exit."
do_4 = "Record the exit fully in the audit trail with its evidence."
do_5 = "Test the exit journey end to end including the degraded-core and ineligible-during-normal-operation scenarios."
verify_cmd = "make human-test-journeys"
symbols = ["layerx_human_service::journeys::ExitJourney"]
touches = ["human/crates/layerx-human-service/src/journeys/exit.rs","human/crates/layerx-human-service/tests/exit.rs"]
reqs = ["11.6","11.8"]

[task.7.6]
title = "Implement the move-money journey"
status = "pending"
wave = 4
requires = ["7.1","7.2"]
do_1 = "Implement the internal movement journey over the resolver and engine: quote with fee estimate and ceiling, plain-language review content, commit, per-leg progress and receipt-backed completion."
do_2 = "Return the receipt-backed actual amounts and fees at completion, replacing every estimate."
do_3 = "Reference every leg receipt in order on the journey record."
do_4 = "Surface refusals with the limit and its owning surface."
do_5 = "Test single-leg and multi-leg movements end to end including refusal, unknown and resume paths."
verify_cmd = "make human-test-journeys"
symbols = ["layerx_human_service::journeys::MoveJourney"]
touches = ["human/crates/layerx-human-service/src/journeys/move_money.rs","human/crates/layerx-human-service/tests/move_money.rs"]
reqs = ["9.3","9.7","9.8"]

[task.8]
title = "Implement the managed-agent journeys"
status = "pending"

[task.8.1]
title = "Orchestrate receipt-verified agent creation"
status = "pending"
wave = 4
requires = ["5.4","5.5"]
do_1 = "Implement the creation journey from name, purpose and monthly limit: agent DID registration, custody key generation, recovery authority naming the human, session-key provisioning, capability narrowing and budget creation with initial funding, each step receipt-verified."
do_2 = "Present Active only when every step's receipt has verified; expose honest partial status and resumption otherwise."
do_3 = "Make creation idempotent so a crash never yields a phantom agent or a duplicate."
do_4 = "Map purpose presets to declared capability templates as configuration, not code."
do_5 = "Test creation end to end against a real agent layer, including partial-failure resume at every step."
verify_cmd = "make human-test-agents"
symbols = ["layerx_human_service::agents::CreationJourney"]
touches = ["human/crates/layerx-human-service/src/agents/create.rs","human/crates/layerx-human-service/tests/agent_create.rs"]
reqs = ["12.1","12.2"]

[task.8.2]
title = "Implement pause, resume and limit changes"
status = "pending"
wave = 4
requires = ["8.1"]
do_1 = "Implement pause as prompt revocation or suspension of the agent's operating authority, and resume as its receipt-verified restoration."
do_2 = "Implement limit changes through budget intents where protocol-backed, labelling any daemon-enforced remainder app-enforced."
do_3 = "Never describe a daemon restriction as a protocol guarantee in any response or copy string."
do_4 = "Reflect pause, resume and limit changes in the agent's presented state within the declared freshness bound."
do_5 = "Test authority revocation latency and the enforcement labelling against a real agent layer."
verify_cmd = "make human-test-agents"
symbols = ["layerx_human_service::agents::AgentControls"]
touches = ["human/crates/layerx-human-service/src/agents/controls.rs","human/crates/layerx-human-service/tests/agent_controls.rs"]
reqs = ["12.3","12.4"]

[task.8.3]
title = "Implement the reclaim paths"
status = "pending"
wave = 4
requires = ["8.1","7.2"]
do_1 = "Implement taking money back exclusively through budget defunding, agent-authorised transfers and receives under explicit payer grants, as movement journeys with receipts."
do_2 = "Make any key-based sweep of an agent account structurally impossible through the custody service's operation set."
do_3 = "Present reclaim in the movement vocabulary as return."
do_4 = "Show the receipt-backed result on the agent and in activity."
do_5 = "Test reclaim through each mechanism against a real agent layer."
verify_cmd = "make human-test-agents"
symbols = ["layerx_human_service::agents::Reclaim"]
touches = ["human/crates/layerx-human-service/src/agents/reclaim.rs","human/crates/layerx-human-service/tests/reclaim.rs"]
reqs = ["12.5","9.1"]

[task.8.4]
title = "Implement agent rotation and recovery"
status = "pending"
wave = 4
requires = ["8.1"]
do_1 = "Implement agent key rotation and recovery under the human's recovery authority through the protocol mechanism, with the challenge delay in plain time."
do_2 = "Keep agent identity and history continuous across rotation."
do_3 = "Handle a competing rotation during an open challenge window exactly as the protocol does, surfaced honestly."
do_4 = "Notify the human of every rotation and recovery event as a security notification."
do_5 = "Test rotation and recovery against a real core including the challenge-delay path."
verify_cmd = "make human-test-agents"
symbols = ["layerx_human_service::agents::AgentRecovery"]
touches = ["human/crates/layerx-human-service/src/agents/recovery.rs","human/crates/layerx-human-service/tests/agent_recovery.rs"]
reqs = ["12.7","4.8"]

[task.8.5]
title = "Implement archive with funds disposition"
status = "pending"
wave = 4
requires = ["8.2","8.3"]
do_1 = "Implement archive as a destructive journey requiring funds disposition first through legitimate reclaim, then typed-name confirmation, then authority revocation and session-key retirement."
do_2 = "Retain the archived agent's history and receipts read-only."
do_3 = "Make archive irreversible in the service and honest about it in the contract shapes."
do_4 = "Emit the security notification and audit entries with evidence."
do_5 = "Test archive end to end including refusal while funds remain undisposed."
verify_cmd = "make human-test-agents"
symbols = ["layerx_human_service::agents::ArchiveJourney"]
touches = ["human/crates/layerx-human-service/src/agents/archive.rs","human/crates/layerx-human-service/tests/archive.rs"]
reqs = ["12.6","21.1"]

[task.8.6]
title = "Reconcile spend against limits from receipts"
status = "pending"
wave = 4
requires = ["8.1"]
do_1 = "Compute per-agent spend for the current period from verified receipts alone."
do_2 = "Reconcile local accounting against protocol budget state, adopting the protocol figure and surfacing divergence honestly."
do_3 = "Expose spend-versus-limit through the contract for both shells' agent surfaces."
do_4 = "Prove the blast-radius bound: a hostile agent cannot exceed its narrowed capability or budget through any human-plane surface."
do_5 = "Test reconciliation under concurrent agent activity against a real agent layer."
verify_cmd = "make human-test-agents"
symbols = ["layerx_human_service::agents::SpendReconciliation"]
touches = ["human/crates/layerx-human-service/src/agents/spend.rs","human/crates/layerx-human-service/tests/spend.rs"]
reqs = ["12.8","12.9"]

[task.9]
title = "Implement approvals and notifications"
status = "pending"

[task.9.1]
title = "Implement the approval inbox service"
status = "pending"
wave = 4
requires = ["2.5","5.2"]
do_1 = "Consume the agent layer's approval module to serve the inbox: pending holds with agent, counterparty, amount, budget-after, hold reason and expiry."
do_2 = "Track hold lifecycle through the approval event stream so the inbox and its counts stay live without polling."
do_3 = "Serve resolved holds honestly: approved with their released activity's tracking, rejected, and expired with the nothing-moved statement."
do_4 = "Keep inbox counts consistent with the inbox within the declared freshness bound."
do_5 = "Test the inbox against a real agent layer generating holds."
verify_cmd = "make human-test-approvals"
symbols = ["layerx_human_service::approvals::Inbox"]
touches = ["human/crates/layerx-human-service/src/approvals/mod.rs","human/crates/layerx-human-service/tests/approvals.rs"]
reqs = ["13.1","13.5","13.7"]

[task.9.2]
title = "Render disclosures bound to their digests"
status = "pending"
wave = 4
requires = ["9.1","3.3"]
do_1 = "Render the held activity's structured disclosure into plain-language approval content, with every rendered fact sourced only from the disclosure."
do_2 = "Verify before rendering that the disclosure re-encodes to the held digest, refusing to render an approvable presentation of a defective hold."
do_3 = "Cover every v1 activity class a hold can carry with a rendering template from the copy catalog."
do_4 = "Property-test that rendered amounts and counterparties equal the disclosure's fields exactly, and that budget figures equal the agent-layer verified budget read."
do_5 = "Show an unknown activity class as explicitly unrenderable and not approvable from this plane."
verify_cmd = "make human-test-approvals"
symbols = ["layerx_human_service::approvals::DisclosureRenderer"]
touches = ["human/crates/layerx-human-service/src/approvals/render.rs","human/crates/layerx-human-service/tests/render.rs"]
reqs = ["13.2","2.3"]

[task.9.3]
title = "Implement approve and reject with step-up"
status = "pending"
wave = 4
requires = ["9.2","5.3"]
do_1 = "Require step-up evidence bound to the held digest for approve, and grant through the approval module for exactly that digest."
do_2 = "Track the released activity to its receipt and surface it like any movement."
do_3 = "Implement reject with immediate confirmation that nothing moved."
do_4 = "Converge concurrent decisions from multiple devices on one outcome with the honest already-decided presentation for the second."
do_5 = "Audit every decision with digest, step-up evidence and outcome, exportable with receipts."
verify_cmd = "make human-test-approvals"
symbols = ["layerx_human_service::approvals::Decisions"]
touches = ["human/crates/layerx-human-service/src/approvals/decide.rs","human/crates/layerx-human-service/tests/decide.rs"]
reqs = ["13.3","13.4","13.6","13.8"]

[task.9.4]
title = "Implement the notification service"
status = "pending"
wave = 4
requires = ["5.1"]
do_1 = "Implement dispatch for the notification classes: approval waiting, money arrived, journey completed or failed, claim ready, security events and degradation notices, each carrying a deep link."
do_2 = "Implement push, email and in-app channels with per-event-class preferences nested under channel toggles, applied immediately."
do_3 = "Make security-critical classes - recovery initiation, wallet rebinding - non-suppressible."
do_4 = "Keep payloads free of secrets and within the user's notification detail level, degrading lock-screen content to non-sensitive summaries."
do_5 = "Deduplicate repeated events and record every dispatch in the audit trail."
verify_cmd = "make human-test-notify"
symbols = ["layerx_human_service::notify::Dispatcher"]
touches = ["human/crates/layerx-human-service/src/notify/mod.rs","human/crates/layerx-human-service/tests/notify.rs"]
reqs = ["19.1","19.2","19.3"]

[task.9.5]
title = "Implement deep links, counts and resolved-state landing"
status = "pending"
wave = 4
requires = ["9.4","9.1"]
do_1 = "Make every notification deep link resolve to the exact surface in whichever shell is active."
do_2 = "Land links whose subject is resolved on the resolved state honestly - expired approvals read expired, completed journeys read done."
do_3 = "Serve the in-app inventory grouped by recency with read state."
do_4 = "Serve badge counts consistent with the inbox and clear them when nothing waits."
do_5 = "Test link resolution across both shells and stale-link landing."
verify_cmd = "make human-test-notify"
symbols = ["layerx_human_service::notify::DeepLinks"]
touches = ["human/crates/layerx-human-service/src/notify/links.rs","human/crates/layerx-human-service/tests/links.rs"]
reqs = ["19.4","19.6","19.7"]

[task.10]
section = "Wave 5 - Evidence and Read Surfaces"
title = "Build the explorer index and the activity surface"
status = "pending"

[task.10.1]
title = "Build the rebuildable explorer index"
status = "pending"
wave = 5
requires = ["1.1"]
do_1 = "Implement layerx-explorer-index ingesting checkpoints, batches, receipts and events exclusively through the node boundary and availability retrieval."
do_2 = "Make the index rebuildable from those sources alone, proven by a delete-and-rebuild test that converges to identical query results."
do_3 = "Track and expose index freshness: the head and latest finalised checkpoint indexed."
do_4 = "Respect every agent-layer boundary prohibition, enforced by the workspace dependency gate."
do_5 = "Serve protocol-public data only, with no principal data in the index at all."
verify_cmd = "make human-test-explorer"
symbols = ["layerx_explorer_index::Indexer"]
touches = ["human/crates/layerx-explorer-index/src/lib.rs","human/crates/layerx-explorer-index/tests/rebuild.rs"]
reqs = ["15.3","15.5"]

[task.10.2]
title = "Assemble the unified activity feed"
status = "pending"
wave = 5
requires = ["7.2"]
do_1 = "Assemble one principal-scoped feed spanning deposits, withdrawals, movements, agent actions, approvals and security events, with custody journeys as single joined entries."
do_2 = "Translate every state through the status translation table with Done gated on the receipt rule."
do_3 = "Serve stable cursor pagination that survives restart without gaps, duplicates or reordering."
do_4 = "Reflect a new verified receipt within the declared freshness bound."
do_5 = "Serve deferred-apply filters by type, agent and date, echoing applied filters in the result."
verify_cmd = "make human-test-activity"
symbols = ["layerx_human_service::activity::Feed"]
touches = ["human/crates/layerx-human-service/src/activity/mod.rs","human/crates/layerx-human-service/tests/activity.rs"]
reqs = ["14.1","14.2","14.6"]

[task.10.3]
title = "Serve entry detail with evidence"
status = "pending"
wave = 5
requires = ["10.2"]
do_1 = "Serve entry detail: the one-sentence plain description, the staged timeline, receipt-backed amounts and fees, and the Technical details evidence references."
do_2 = "Upgrade shown verification status as receipts gain batch inclusion and checkpoint finality, changing the user-facing state only from Done to Done, finalised."
do_3 = "Reference the explorer for every receipt and checkpoint so Technical details links resolve signed-out."
do_4 = "State refusals with whether money left the account."
do_5 = "Test detail assembly for every journey class."
verify_cmd = "make human-test-activity"
symbols = ["layerx_human_service::activity::EntryDetail"]
touches = ["human/crates/layerx-human-service/src/activity/detail.rs","human/crates/layerx-human-service/tests/detail.rs"]
reqs = ["14.3","14.8"]

[task.10.4]
title = "Implement statement and evidence exports"
status = "pending"
wave = 5
requires = ["10.2","5.6"]
do_1 = "Implement the statement export over the feed's filters."
do_2 = "Implement the evidence bundle: referenced receipts and proofs in canonical bytes, verifiable independently of this plane."
do_3 = "Prove bundle verifiability with the agent layer's offline verification tooling in a test."
do_4 = "Serve the user's own audit export through the same bundle machinery."
do_5 = "Test exports for size bounds, principal scoping and redaction."
verify_cmd = "make human-test-activity"
symbols = ["layerx_human_service::activity::EvidenceExport"]
touches = ["human/crates/layerx-human-service/src/activity/export.rs","human/crates/layerx-human-service/tests/export.rs"]
reqs = ["14.5","22.3"]

[task.10.5]
title = "Serve explorer lookups and the verifier"
status = "pending"
wave = 5
requires = ["10.1"]
do_1 = "Serve checkpoint and batch browsing, receipt lookup by identifier and account activity views from the index."
do_2 = "Implement the evidence verifier: pasted receipt or proof bytes verified with the plane's proof machinery, reporting the achieved verification level, never the index's opinion."
do_3 = "Show the verification level achieved for every displayed protocol fact."
do_4 = "State index freshness on every response."
do_5 = "Test lookups and the verifier against a real core, including deliberately altered evidence."
verify_cmd = "make human-test-explorer"
symbols = ["layerx_explorer_index::Verifier"]
touches = ["human/crates/layerx-explorer-index/src/verify.rs","human/crates/layerx-explorer-index/tests/verify.rs"]
reqs = ["15.2","15.4","15.1"]

[task.11]
section = "Wave 6 - The Application Foundation"
title = "Build the application foundation: planes, shells and state machinery"
status = "pending"

[task.11.1]
title = "Build the two planes and SSR-safe shell selection"
status = "pending"
wave = 6
requires = ["1.4","4.6"]
do_1 = "Structure the application into the public /explorer plane and the authenticated /app plane with their routing, authentication boundary and layout roots, a strict content security policy with pinned asset origins, and no third-party scripts on authenticated surfaces, enforced by a CI check."
do_2 = "Implement shell selection from viewport and pointer capability hints at server render, confirmed on the client, correcting a mismatch without layout thrash or a flash of the wrong shell."
do_3 = "Never select on user-agent strings alone, and give tablets the desktop layout with touch-sized targets."
do_4 = "Make deep links resolve to the equivalent screen in whichever shell is active."
do_5 = "Test selection across device profiles, resize, and server-client capability mismatch."
verify_cmd = "make human-e2e-foundation"
symbols = ["human_web::shell::ShellSelector"]
touches = ["human/apps/web/src/shell/selector.ts","human/apps/web/src/app/layout.tsx","human/apps/web/e2e/shell.spec.ts"]
reqs = ["16.4","16.5","16.7","21.7"]

[task.11.2]
title = "Build the component kit in both shells"
status = "pending"
wave = 6
requires = ["11.1"]
do_1 = "Implement the pattern-pair kit: navigation, primary action, confirmation, detail, filters, money lists, wizards, search, code entry and notifications, each with an explicit mobile and desktop component sharing one logic layer."
do_2 = "Implement the money primitives: signed worded amounts in tabular numerals, label-over-value, status pills from the translation table, copyable identifiers, and the numbered fee-math disclosure that walks a charge from inputs to total."
do_3 = "Implement the confirmation grammar - reversible, destructive, irreversible with typed confirmation - such that the wrong grammar is unrepresentable through the kit."
do_4 = "Express every control state through fill and text-color shifts with disabled reasons nearby, honoring reduced motion."
do_5 = "Enforce kit-only composition through the UI gate, with fixtures proving a raw-element screen fails."
verify_cmd = "make human-check-ui"
symbols = ["human_web::kit"]
touches = ["human/apps/web/src/kit","human/apps/web/src/kit/confirm.tsx","human/apps/web/e2e/kit.spec.ts"]
reqs = ["16.3","17.8","17.9"]

[task.11.3]
title = "Build the state-matrix machinery and error surfaces"
status = "pending"
wave = 6
requires = ["11.2","1.5"]
do_1 = "Implement the per-screen state machinery: layout-reserving skeletons, empty states with a single CTA, the error anatomy with Retry, Reload and Report actions and the collapsed technical section, offline states with retry, degraded staleness presentation and the still-checking lock."
do_2 = "Wrap every route in error boundaries rendering the same anatomy, so no blank screen or raw stack trace can reach a user."
do_3 = "Implement Report: consented submission of trace identifier, machine code and non-sensitive context, with confirmation."
do_4 = "Implement the offline banner and queued-action honesty with no possibility of duplicate money movement from a queue."
do_5 = "Register every screen's states in the state-matrix registry as they are built, keeping the enumeration gate green."
verify_cmd = "make human-e2e-foundation"
symbols = ["human_web::states::StateMatrix","human_web::states::ErrorBoundary"]
touches = ["human/apps/web/src/states","human/apps/web/src/states/error.tsx","human/apps/web/e2e/states.spec.ts"]
reqs = ["18.1","18.2","18.3"]

[task.11.4]
title = "Build the accessibility and visual foundation"
status = "pending"
wave = 6
requires = ["11.2","1.4"]
do_1 = "Apply the authoritative @layerx/ui token stylesheet and component treatments across the complete kit without local primitive restyling."
do_2 = "Establish keyboard operability: visible focus order, focus trapping in layered surfaces, Escape and overlay dismissal on desktop, safe areas and keyboard avoidance on mobile."
do_3 = "Label every amount, status and control for assistive technology, announcing async status through live regions."
do_4 = "Enforce required contrast across the package's text and semantic token combinations through automated checks."
do_5 = "Wire automated accessibility checks into the browser suites, including 1.5x text-expansion layout checks and locale-aware amount rendering with explicit currency codes."
verify_cmd = "make human-e2e-foundation"
symbols = ["human_web::a11y"]
touches = ["human/apps/web/src/kit/a11y.ts","human/apps/web/e2e/a11y.spec.ts"]
reqs = ["20.1","20.2","20.6","17.3"]

[task.11.5]
title = "Build the performance machinery"
status = "pending"
wave = 6
requires = ["11.1"]
do_1 = "Code-split by route with declared per-route script budgets enforced in CI."
do_2 = "Render the explorer plane server-side cacheable."
do_3 = "Enforce the paint, interaction and layout-shift budgets in CI on representative pages."
do_4 = "Wire real-user monitoring against the same budgets with the redaction rules applied."
do_5 = "Prove journey surfaces function at 3G-class latency with honest progress."
verify_cmd = "make human-e2e-perf"
symbols = ["human_web::perf"]
touches = ["human/apps/web/src/perf/budgets.ts","human/apps/web/e2e/perf.spec.ts",".github/workflows/human.yml"]
reqs = ["20.3","20.4","20.7"]

[task.12]
section = "Wave 7 - The Application Surfaces"
title = "Build the application journeys"
status = "pending"

[task.12.1]
title = "Build onboarding and account activation"
status = "pending"
wave = 7
requires = ["11.3","5.4"]
do_1 = "Build account creation in both shells: email, passkey ceremony, honest activation progress, ready only on verified activation."
do_2 = "Build failure and resume presentation for every onboarding stage."
do_3 = "Keep the surfaces within three decisions and free of banned vocabulary."
do_4 = "Build the new-device sign-in flow with the security notification hook."
do_5 = "Run onboarding end to end in both shells against the real service."
verify_cmd = "make human-e2e-journeys"
symbols = ["human_web::journeys::Onboarding"]
touches = ["human/apps/web/src/journeys/onboarding","human/apps/web/e2e/onboarding.spec.ts"]
reqs = ["3.4","3.9","2.9"]

[task.12.2]
title = "Build home and the move-money wizard"
status = "pending"
wave = 7
requires = ["11.3","7.6"]
do_1 = "Build home in both shells: verified balance with freshness and privacy toggle, the primary actions Add money, Move money and Withdraw, agents summary, recent activity and the approval badge."
do_2 = "Build the move-money wizard - who, how much, review, result - as the mobile full-screen wizard and the desktop split pane with the live summary."
do_3 = "Render review from the journey quote in plain language with fee estimate and ceiling, and result only from journey state - Done receipt-backed, still-checking locked, refusal honest."
do_4 = "Keep initiation within three decisions with automatic routing."
do_5 = "Run move-money end to end in both shells including refusal and still-checking paths."
verify_cmd = "make human-e2e-journeys"
symbols = ["human_web::journeys::Home","human_web::journeys::MoveMoney"]
touches = ["human/apps/web/src/journeys/home","human/apps/web/src/journeys/move","human/apps/web/e2e/move.spec.ts"]
reqs = ["2.1","2.2","9.3"]

[task.12.3]
title = "Build the custody journey surfaces"
status = "pending"
wave = 7
requires = ["11.3","7.3","7.4","7.5"]
do_1 = "Build deposit: binding-folded start, single wallet hand-off with designed in-progress, failure and cancel states, the staged timeline, safe-to-close statement and receipt-gated completion."
do_2 = "Build withdrawal: irreversibility at review, the staged timeline with the configured settlement expectation, claim readiness with the single wallet hand-off, payout-verified completion."
do_3 = "Build challenge-window hold and delay presentations as honest states with expectations."
do_4 = "Build the guided emergency exit under Settings with typed confirmation and degraded-mode operability."
do_5 = "Run deposit and withdrawal end to end in both shells against the test network."
verify_cmd = "make human-e2e-journeys"
symbols = ["human_web::journeys::Deposit","human_web::journeys::Withdraw","human_web::journeys::Exit"]
touches = ["human/apps/web/src/journeys/deposit","human/apps/web/src/journeys/withdraw","human/apps/web/src/journeys/exit","human/apps/web/e2e/custody.spec.ts"]
reqs = ["10.3","11.2","18.7"]

[task.12.4]
title = "Build the agent surfaces"
status = "pending"
wave = 7
requires = ["11.3","8.2","8.5"]
do_1 = "Build agent creation - name, purpose, limit - with the honest creation-journey progress, in both shells."
do_2 = "Build the agent list and detail: the desktop master-detail and the mobile stacked screens, with spend-versus-limit from receipts."
do_3 = "Build the controls with the confirmation grammar: fund, return, change limit, pause reversible, archive destructive with disposition-first and typed confirmation."
do_4 = "Build rotation and recovery surfaces with the challenge delay in plain time."
do_5 = "Run create, pause, reclaim and archive end to end in both shells."
verify_cmd = "make human-e2e-journeys"
symbols = ["human_web::journeys::Agents"]
touches = ["human/apps/web/src/journeys/agents","human/apps/web/e2e/agents.spec.ts"]
reqs = ["12.1","12.6","2.2"]

[task.12.5]
title = "Build the approval and notification surfaces"
status = "pending"
wave = 7
requires = ["11.3","9.3","9.5"]
do_1 = "Build the approval inbox and detail in both shells - the mobile screen and the desktop split view - from the rendered disclosure only, with the expiry countdown."
do_2 = "Build approve with the step-up ceremony and reject with the nothing-moved confirmation, then track the released activity."
do_3 = "Build expired and already-decided presentations."
do_4 = "Build the notification surfaces: the mobile screen, the desktop bell popover with archive page, read state and preferences."
do_5 = "Run approval grant, reject, expiry and two-device convergence end to end in both shells."
verify_cmd = "make human-e2e-journeys"
symbols = ["human_web::journeys::Approvals","human_web::journeys::Notifications"]
touches = ["human/apps/web/src/journeys/approvals","human/apps/web/src/journeys/notifications","human/apps/web/e2e/approvals.spec.ts"]
reqs = ["13.1","13.3","19.4"]

[task.12.6]
title = "Build the activity surfaces"
status = "pending"
wave = 7
requires = ["11.3","10.3"]
do_1 = "Build the feed in both shells: the mobile stacked rows with month bands and subtotals, the desktop sortable table, with the deferred-apply filters echoed in content."
do_2 = "Build entry detail: the plain sentence, the timeline, receipt-backed figures and the collapsed Technical details with explorer links."
do_3 = "Build the statement and evidence exports."
do_4 = "Enforce the still-checking lock on every entry whose journey is unresolved."
do_5 = "Run the feed, detail and export end to end in both shells."
verify_cmd = "make human-e2e-journeys"
symbols = ["human_web::journeys::Activity"]
touches = ["human/apps/web/src/journeys/activity","human/apps/web/e2e/activity.spec.ts"]
reqs = ["14.3","14.4","18.9"]

[task.13]
title = "Build settings, security, support and the explorer plane"
status = "pending"

[task.13.1]
title = "Build settings and preferences"
status = "pending"
wave = 7
requires = ["11.2","9.4"]
do_1 = "Build the settings hub sectioned as profile, security, linked wallet, notifications, advanced and help, with current values on rows."
do_2 = "Build notification preferences: per-event classes under channel toggles, the notification detail level, and the non-suppressible security classes presented as such."
do_3 = "Build privacy mode masking balances and every derived figure across both shells, persisted per user."
do_4 = "Build profile editing over declared-local data."
do_5 = "Test preference application and privacy-mode cascade end to end."
verify_cmd = "make human-e2e-settings"
symbols = ["human_web::settings"]
touches = ["human/apps/web/src/settings","human/apps/web/e2e/settings.spec.ts"]
reqs = ["19.2","21.5"]

[task.13.2]
title = "Build the security center"
status = "pending"
wave = 7
requires = ["11.2","5.2"]
do_1 = "Build passkey management, the device and session inventory with per-session revoke and sign-out-everywhere."
do_2 = "Build the 2FA surface: authenticator methods, one-time backup codes at setup, disable warnings with consequence copy, all under step-up."
do_3 = "Build secret handling: masked by default, step-up reveal, timed re-mask, explicit copy only where declared copyable."
do_4 = "Build recovery status presentation with its receipts under Technical details."
do_5 = "Test every security mutation path for its step-up gate."
verify_cmd = "make human-e2e-settings"
symbols = ["human_web::security"]
touches = ["human/apps/web/src/settings/security","human/apps/web/e2e/security.spec.ts"]
reqs = ["21.1","21.2","21.3"]

[task.13.3]
title = "Build wallet binding management"
status = "pending"
wave = 7
requires = ["11.2","5.7"]
do_1 = "Build the binding surface: bound address with full checksum rendering, copy and QR, the one-sentence no-authority statement."
do_2 = "Build first-binding folded into the first custody journey and standalone rebinding under step-up."
do_3 = "Build the wallet hand-off states for binding: in-progress, rejected, disconnected, with retry and cancel."
do_4 = "Present binding as active only on its verified receipt."
do_5 = "Test binding and rebinding end to end with the security notification."
verify_cmd = "make human-e2e-settings"
symbols = ["human_web::wallet_binding"]
touches = ["human/apps/web/src/settings/wallet","human/apps/web/e2e/wallet.spec.ts"]
reqs = ["5.3","5.4","5.7"]

[task.13.4]
title = "Build the support surface"
status = "pending"
wave = 7
requires = ["11.3"]
do_1 = "Build support chat seeded with topic suggestion chips, as the mobile screen and the desktop docked panel."
do_2 = "Build its designed states: empty with suggestions, offline with retry, send-failure with inline retry, and the feedback loop."
do_3 = "Attach the trace identifier to support conversations started from an error's Report action."
do_4 = "Keep support reachable from every error surface."
do_5 = "Test the support states and the report-to-support path."
verify_cmd = "make human-e2e-settings"
symbols = ["human_web::support"]
touches = ["human/apps/web/src/support","human/apps/web/e2e/support.spec.ts"]
reqs = ["8.2","18.8","18.2"]

[task.13.5]
title = "Build the public explorer plane"
status = "pending"
wave = 7
requires = ["11.1","10.5"]
do_1 = "Build the server-rendered explorer pages: checkpoints, batches, receipt lookup, account views and the evidence verifier, table-first."
do_2 = "Show the verification level for every displayed fact and index freshness on every page."
do_3 = "Keep every page deep-linkable and functional signed-out, meeting the same design, accessibility and performance gates."
do_4 = "Link the app's Technical details surfaces to these pages."
do_5 = "Run the explorer against a real core end to end including the verifier with altered evidence."
verify_cmd = "make human-e2e-explorer"
symbols = ["human_web::explorer"]
touches = ["human/apps/web/src/explorer","human/apps/web/e2e/explorer.spec.ts"]
reqs = ["15.1","15.6","15.7"]

[task.14]
section = "Wave 8 - Qualification"
title = "Qualify the human plane end to end"
status = "pending"

[task.14.1]
title = "Run every journey against the real stack"
status = "pending"
wave = 8
requires = ["12.1","12.2","12.3","12.4","12.5","12.6","13.5"]
do_1 = "Run onboarding, wallet binding, deposit, move money, create agent, approval grant and reject, withdrawal with claim and emergency exit end to end against a real node, a real layerx-agentd and a Paxeer test network."
do_2 = "Run every journey in both shells."
do_3 = "Permit no in-process substitute for the node, the daemon or the test network anywhere in the suite."
do_4 = "Record per-journey evidence - receipts, proofs, timings - as suite artefacts."
do_5 = "Wire the suite into make human-qualify-journeys as a release gate."
verify_cmd = "make human-qualify-journeys"
symbols = ["human_qualify_journeys_gate"]
touches = ["human/tests/qualify/journeys.rs","human/apps/web/e2e/qualify","Makefile"]
reqs = ["23.1","16.6"]

[task.14.2]
title = "Run the hostile-plane no-fabrication suite"
status = "pending"
wave = 8
requires = ["10.4","10.5","12.6"]
do_1 = "Build a tampered human-service that reports altered balances, altered outcomes and fabricated successes."
do_2 = "Demonstrate exported evidence bundles and explorer verification catch every alteration."
do_3 = "Demonstrate the untampered application cannot render Done without a verifying receipt, by construction of the success-rendering path."
do_4 = "Cover balances, movement outcomes, agent states and approval outcomes."
do_5 = "Wire the suite into make human-qualify-fabrication as a release gate."
verify_cmd = "make human-qualify-fabrication"
symbols = ["human_hostile_plane_harness","human_qualify_fabrication_gate"]
touches = ["human/tests/qualify/hostile_plane.rs","Makefile"]
reqs = ["23.2","1.9"]

[task.14.3]
title = "Run the journey fault-injection matrix"
status = "pending"
wave = 8
requires = ["12.2","12.3"]
do_1 = "Inject crash, disconnect and duplicate delivery at every stage of every money journey, including between wallet signature and journey registration."
do_2 = "Assert exactly-once economic effect per leg, honest still-checking presentation and correct resumption in every case."
do_3 = "Assert no journey is lost, duplicated or stranded without an owner across restarts."
do_4 = "Cover the claim path: no double claim, no lost claimable state."
do_5 = "Wire the matrix into make human-qualify-faults as a release gate."
verify_cmd = "make human-qualify-faults"
symbols = ["human_qualify_faults_gate"]
touches = ["human/tests/qualify/faults.rs","Makefile"]
reqs = ["23.3","9.6"]

[task.14.4]
title = "Run the UI, copy and accessibility gates"
status = "pending"
wave = 8
requires = ["12.6","13.5"]
do_1 = "Enumerate every screen in both shells against the state-matrix registry and fail on any missing state."
do_2 = "Run visual regression across both shells at the canonical @layerx/ui reference viewports."
do_3 = "Run the copy lint, component-library integrity and component-kit gates as build gates."
do_4 = "Run automated accessibility checks on every surface plus assistive-technology runs of the core journeys, failing on any AA violation."
do_5 = "Wire all of it into make human-qualify-ui as a release gate."
verify_cmd = "make human-qualify-ui"
symbols = ["human_qualify_ui_gate"]
touches = ["human/apps/web/e2e/qualify/ui.spec.ts","Makefile"]
reqs = ["23.4","23.5"]

[task.14.5]
title = "Run the performance and soak gates"
status = "pending"
wave = 8
requires = ["11.5","14.1"]
do_1 = "Enforce the performance budgets in CI on the representative pages of both planes."
do_2 = "Run the soak: long-lived journeys - a deposit awaiting finality, a withdrawal awaiting checkpoint, an unactioned approval - held across service restarts."
do_3 = "Assert no journey lost, duplicated or stranded and no unbounded resource growth across the soak, then delete the human-service store, restart against the same core, and assert every protocol-derived answer - balances, journey outcomes, agent states, spend figures - reconstructs with only declared-local artefacts lost."
do_4 = "Record resource and latency curves as artefacts."
do_5 = "Wire the soak into a scheduled job and the budgets into make human-qualify-perf."
verify_cmd = "make human-qualify-perf"
symbols = ["human_qualify_perf_gate","human_qualify_soak"]
touches = ["human/tests/qualify/soak.rs",".github/workflows/human-soak.yml","Makefile"]
reqs = ["23.6","20.3","1.6"]

[task.14.6]
title = "Run the usability gate"
status = "pending"
wave = 8
requires = ["14.1"]
do_1 = "Define the protocol for the usability gate: participants, tasks - onboarding, deposit, move money, create agent - on mobile, without documentation, and the declared task-success threshold."
do_2 = "Run the sessions and record task success, time and abandonment per journey."
do_3 = "File every failure as a defect against the responsible requirement."
do_4 = "Repeat the gate after fixes until the threshold is met."
do_5 = "Record the protocol, results and threshold in the qualification report."
verify_cmd = "make human-qualify-usability"
symbols = ["human_qualify_usability_gate"]
touches = ["human/tests/qualify/usability.md","Makefile"]
reqs = ["23.7","2.9"]

[task.14.7]
title = "Run the custody gates and publish the release report"
status = "pending"
wave = 8
requires = ["14.1","14.2","14.3","14.4","14.5","14.6"]
do_1 = "Run the isolation, custody, no-key-export and telemetry-redaction suites as build gates."
do_2 = "Generate the qualification report stating which guarantees are protocol-enforced, agent-layer-enforced and human-plane-enforced, which suites ran with what results, and every unmet gate."
do_3 = "Make an unmet gate block the release through tooling, recording any exception with its explicit approval."
do_4 = "Publish the report as a build artefact alongside the human-api compatibility matrix."
do_5 = "Add the release check refusing to tag a version whose report contains an unmet gate."
verify_cmd = "make human-qualify"
symbols = ["human_qualification_report","human_release_gate"]
touches = ["human/tools/qualify/src/main.rs","human/QUALIFICATION.md","Makefile"]
reqs = ["23.8","23.9","22.1"]
