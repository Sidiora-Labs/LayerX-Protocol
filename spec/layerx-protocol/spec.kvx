# spec.kvx - LayerX Protocol. SINGLE SOURCE OF TRUTH for this feature.
#
# requirements.md / design.md / tasks.md next to this file are GENERATED from it
# by `cg spec render` (or spec/specgen). Never hand-edit those; edit this file.
#
# Drive the work with:  cg spec next -> cg spec start <id> -> cg spec done <id>
# Provenance for every statement here: docs/00-source-brief.md

[meta]
feature      = "layerx-protocol"
title        = "LayerX Protocol - Canonical Activity, Execution and Accounting Layer"
kind         = "protocol-specification"
version      = "1.0.0"
status       = "draft"
language     = "C17"
repo_root    = "."
reference    = "legacy Go implementation (external, read-only behavioural reference)"
intro        = "LayerX is the canonical activity, execution and accounting layer for autonomous agents. Paxeer provides custody, checkpoint finality, economic guarantees and dispute settlement, but never processes ordinary agent activity. An ordinary LayerX action never requires a Paxeer transaction: millions of activities collapse into one periodic checkpoint. Within LayerX, 402LXP is the single financial doorway. Every monetary effect in the system compiles into one or more authenticated balance transfers through one internal primitive, and no module may write a balance directly. These requirements are normative; the design and the task plan beside them are compiled from this file."

# Decisions locked before implementation begins. These supersede any ambiguity
# in docs/00-source-brief.md.
[decision]
language          = "C17 reference implementation. No C-plus-plus, C-sharp, Go, Rust or JavaScript in the protocol runtime."
repo_root         = "The implementation lives in this repository so spec and code share one Codify graph. This supersedes the brief proposal of a separate implementation directory."
legacy_reference  = "The external legacy Go implementation stays untouched and read-only. It is a behavioural reference, never translated file by file."
wire              = "Canonical binary protocol. The JSON and HTTP gateway is optional and never defines consensus behaviour."
sequencing        = "One active sequencer initially, with a bonded quorum of independent Paxeer guarantors that each fully replay every batch before attesting."
authority_of_log  = "The append-only activity log is the authority. SQLite indexes and materialised state are rebuildable projections."
modules           = "Payments, escrow, budgets, streams, services and perps are protocol modules over a minimal kernel. Crossverse is an oracle adapter outside the kernel."
paxeer_scope      = "Paxeer is used only for custody, checkpoint registration, guarantees, disputes and exits."
activity_scope_v1 = "Version 1 covers the COMPLETE agent work lifecycle, not only economically meaningful actions. Task commitments, tool execution attestations, deliveries, acceptances and disputes are first-class ordered and attested activities in the service module, carrying no direct monetary effect. Any value they imply still moves through 402LXP. This resolves the open scope question at the end of the brief."
guarantee_honesty = "Threshold guarantor attestation is NOT a validity proof. It is an economic guarantee backed by bonds and slashing for equivocation. Validity proofs can be added later without changing the activity protocol."

[design]
include = "design.body.md"

[tasks]
heading          = "Implementation Plan: LayerX Protocol"
overview_include = "tasks.overview.md"
notes_include    = "tasks.notes.md"

# --------------------------------------------------------------------------
# REQUIREMENTS
# --------------------------------------------------------------------------

[req.1]
title = "Division of Responsibility Between LayerX and Paxeer"
story = "As an autonomous agent operator, I want LayerX to own every ordinary agent activity while Paxeer owns only custody, checkpoint finality, economic guarantees and dispute settlement, so that my agents can act millions of times without ever originating, paying for or waiting on a Paxeer transaction."
ac_1 = "WHEN an agent submits any ordinary LayerX activity, including an identity or authority update, a 402LXP transfer, an escrow, budget or stream operation, a service lifecycle action, a perps order, or an oracle observation, THE protocol SHALL admit, order, execute, receipt and serve that activity entirely inside LayerX and SHALL NOT require, originate or await any Paxeer transaction as a precondition of its execution or of its receipt becoming available."
ac_2 = "WHEN a checkpoint epoch closes THE protocol SHALL collapse every activity accepted in that epoch, regardless of count, into exactly one Paxeer checkpoint registration carrying only the header commitments protocol_version, network_id, epoch, batch_number, first_sequence, last_sequence, previous_state_root, resulting_state_root, activity_merkle_root, receipt_merkle_root, event_merkle_root, data_availability_root, oracle_root, timestamp and sequencer_id, and SHALL NOT transmit individual activity envelopes, payloads or receipts to Paxeer."
ac_3 = "THE protocol SHALL confine every Paxeer contract input to a finalized checkpoint certificate, a membership or balance proof evaluated against a registered checkpoint state root, a withdrawal nullifier, guarantor signatures and bonds, challenge-window state, and emergency-exit eligibility, and SHALL define no other contract-visible input."
ac_4 = "IF a Paxeer contract interface would require the contract to interpret a perps order, a market or funding rule, a service agreement, a delivery or acceptance, an escrow or budget rule, or an ordinary agent-to-agent transfer, THEN THE protocol SHALL treat that interface as non-conformant and SHALL NOT define, deploy or depend on it."
ac_5 = "THE protocol SHALL assign to LayerX exclusive authority over agent identities and delegated authority, activity ordering, balances and payments, holds, escrow, budgets, subscriptions and streaming payments, service agreements, deliveries and attestations, trading, positions, funding and liquidation, deterministic state execution, receipts and inclusion proofs, data availability, replay and state reconstruction, sequencer operation, and fees and resource metering."
ac_6 = "THE protocol SHALL assign to Paxeer exclusive authority over asset custody, deposits and withdrawals, LayerX checkpoint registration, guarantor bonds, checkpoint attestations, slashing for conflicting attestations, emergency exits, dispute resolution, and final settlement between LayerX and external assets."
ac_7 = "WHEN a deposit is presented for credit inside LayerX THE protocol SHALL credit the agent only after the deposit is proven against finalized Paxeer custody state, SHALL perform the credit as a 402LXP transfer from the account system:paxeer-reserve to the agent main account, and SHALL reject any deposit credit that lacks such proof or that reuses a deposit identifier already credited."
ac_8 = "WHEN a withdrawal is requested THE protocol SHALL first execute a 402LXP transfer from the agent account to the account system:paxeer-withdrawals, SHALL make the funds payable on Paxeer only after a finalized checkpoint whose state root covers that transfer, and SHALL make the withdrawal payable at most once by consuming a unique withdrawal nullifier, so that a second presentation of the same withdrawal is rejected."
ac_9 = "IF Paxeer is unreachable, congested, or has registered no checkpoint for longer than the configured checkpoint interval, THEN THE protocol SHALL continue to admit, order, execute and receipt ordinary activities without interruption and SHALL suspend only checkpoint registration, withdrawal payout on Paxeer, and dispute or emergency-exit finalisation."
ac_10 = "WHERE the LayerX sequencer refuses service, censors an agent, or becomes unavailable, THE protocol SHALL allow that agent to recover its custodied balance directly on Paxeer through the emergency-exit path using a balance proof against the most recently finalized checkpoint state root, without any cooperation from the sequencer."

[req.2]
title = "Canonical Activity Envelope and Activity Receipt"
story = "As an autonomous agent operator, I want every action to be expressed as one fully specified, self-authenticating activity envelope and answered with a protocol-computed receipt, so that outcomes are verifiable evidence rather than values my client or any intermediary asserted."
ac_1 = "WHEN an activity is submitted THE protocol SHALL require the envelope to carry exactly the fields protocol_version, network_id, activity_type, actor_did, authority, account_sequence, timestamp_bound, idempotency_key, fee_limit, payload_hash, payload and signature, and SHALL reject with a malformed-envelope result code any submission that omits a field, repeats a field, or carries an undeclared field."
ac_2 = "IF protocol_version is unsupported by the executing transition version, or network_id does not equal the network identifier of the executing chain, THEN THE protocol SHALL reject the activity, SHALL NOT consume account_sequence, SHALL NOT charge a fee, and SHALL make an envelope valid on one network permanently invalid on every other network."
ac_3 = "IF payload_hash is not byte-equal to the domain-separated hash of the canonical binary encoding of payload, THEN THE protocol SHALL reject the activity with a payload-binding result code before evaluating authority, and SHALL NOT consume account_sequence, charge a fee, or apply any state effect."
ac_4 = "WHEN verifying an activity THE protocol SHALL verify signature over the domain-separated canonical binary encoding of every envelope field except signature itself, SHALL reject any activity whose signature does not verify under a key that the state machine currently binds to authority, and SHALL perform this verification on the canonical bytes rather than on any re-serialized or gateway representation."
ac_5 = "WHEN an activity is accepted THE protocol SHALL require account_sequence to equal exactly the next expected sequence of actor_did, SHALL consume that sequence exactly once, SHALL reject a lower sequence as a replay and a higher sequence as a gap, and SHALL NOT allow any two distinct accepted activities to consume the same account sequence."
ac_6 = "IF the batch timestamp under which an activity would execute lies outside the interval expressed by timestamp_bound, THEN THE protocol SHALL reject the activity as expired or not-yet-valid, and SHALL make that rejection identical on every node because the comparison uses only the batch-supplied timestamp."
ac_7 = "WHEN two activities from the same actor_did carry the same idempotency_key THE protocol SHALL produce at most one economic result, SHALL return the receipt of the first accepted activity in answer to the later submission, and SHALL NOT apply the state transition a second time even if the later submission differs in payload."
ac_8 = "IF the metered fee computed for an activity exceeds fee_limit, THEN THE protocol SHALL reject the activity without applying its effects, and in every accepted case THE protocol SHALL set fee_charged to the computed fee and SHALL guarantee fee_charged is never greater than fee_limit."
ac_9 = "WHEN an activity is executed THE protocol SHALL emit an ActivityReceipt carrying activity_id, global_sequence, previous_state_root, resulting_state_root, activity_root, result_code, effects, fee_charged, batch_id and sequencer_signature, SHALL compute every one of those values itself from the resulting state, and SHALL ignore and reject any client-supplied assertion of a result, an effect, a resulting balance or a state root."
ac_10 = "IF any consensus-critical field in an activity, payload, receipt or batch header is encoded as a floating-point value, a platform-dependent or endianness-dependent value, or a number that cannot be decoded to an exact fixed-width integer, THEN THE protocol SHALL reject that encoding with a decode error and SHALL never coerce, round or approximate it."

[req.3]
title = "Identity, Keys and Delegated Authority"
story = "As an autonomous agent operator, I want DID-native accounts with rotatable session keys, scoped capability grants and enforceable delegated limits evaluated inside the state machine, so that I can safely lend narrow spending and trading power to sub-agents and revoke it with protocol-level certainty."
ac_1 = "THE protocol SHALL use the agent DID as the native account identity, SHALL derive every account address from that DID under the namespaces agent:did:main, agent:did:budget:id, agent:did:escrow:id and agent:did:margin:position, and SHALL reject any activity whose actor_did is not a registered identity in current state."
ac_2 = "WHEN an identity is created THE protocol SHALL bind exactly one primary key and SHALL permit any number of session keys, each carrying an explicit expiration, an explicit set of permitted activity types, and an explicit revocation sequence, and SHALL treat a session key that omits any of those bounds as invalid."
ac_3 = "WHEN an activity presents a capability grant as its authority THE protocol SHALL require the grant to specify the authorized holder, the asset, the maximum single amount, the total or recurring allowance, the expiration, the permitted purpose, the optional service or invoice identifier, and the revocation sequence, and SHALL reject the activity if the requested action exceeds any one of those bounds."
ac_4 = "WHILE a delegated spending or trading limit is in force THE protocol SHALL accumulate consumption against that limit inside state at execution time, SHALL reject the first activity that would carry cumulative consumption past the limit, and SHALL NOT permit concurrent or reordered submissions to exceed the limit in aggregate."
ac_5 = "WHEN a revocation of a session key or capability grant is executed at a given global sequence THE protocol SHALL reject every subsequent activity authorized by that key or grant, SHALL apply the revocation to activities already submitted but not yet executed, and SHALL NOT retroactively invalidate activities already executed before that sequence."
ac_6 = "IF the batch timestamp is at or after the expiration of the presented session key, capability grant, payer grant or allowance, THEN THE protocol SHALL reject the activity with an expired-authority result code, evaluated solely against the batch-supplied timestamp."
ac_7 = "IF the presented authority does not explicitly name the activity_type being executed, or names a broader scope than the module declares for that activity type, THEN THE protocol SHALL reject the activity with an out-of-scope-authority result code, and THE protocol SHALL never treat possession of a key as implying authority over an unnamed action."
ac_8 = "WHEN a primary key rotation or account recovery is requested THE protocol SHALL require authorisation from the current primary key or from a pre-registered recovery authority, SHALL apply the change only after the configured challenge delay measured in batch timestamps has elapsed, SHALL invalidate the superseded key for every activity executed at or after the effective sequence, and SHALL reject a competing rotation submitted during an open challenge delay."
ac_9 = "WHERE an agent binds an optional EVM payout address THE protocol SHALL require a signature from the EVM key over a domain-separated binding statement naming the DID and network_id, SHALL record the binding in state, and SHALL grant that address no authority to move, debit or authorise any LayerX balance."
ac_10 = "THE protocol SHALL evaluate every authorisation decision inside the deterministic state machine as part of the transition, SHALL NOT consult HTTP session state, gateway authentication, in-memory challenges or any transport-layer credential during execution, and SHALL produce the identical authorisation result code on every node for the identical activity even when the submitting gateway accepted the request."

[req.4]
title = "Activity Kernel and the Module Boundary"
story = "As an autonomous agent operator, I want a minimal kernel that understands only universal concepts, with payments, markets and service coordination built as isolated versioned modules above it, so that new business capability can ship without ever changing the law that governs balances and ordering."
ac_1 = "THE protocol SHALL restrict the kernel to identities, accounts, assets, authority, nonces and sequences, fees, state transitions, events, receipts, checkpoints and modules, and SHALL define no payment product, market rule, service workflow or oracle semantic inside the kernel."
ac_2 = "THE protocol SHALL implement payments, escrow, budgets, streams, service coordination, perpetual markets, governance and Paxeer bridging as the registered modules asset, escrow, budget, stream, service, perps, governance and bridge, each built on the kernel and none of them privileged above the kernel rules."
ac_3 = "WHEN a module is registered, whether at genesis or by a governance activity, THE protocol SHALL record its module identifier, its version and the exhaustive list of activity types it declares, and SHALL reject with an unknown-activity result code any activity whose activity_type is not declared by a currently registered module."
ac_4 = "THE protocol SHALL confine every module to writing only state under its own module namespace, SHALL require every monetary effect a module produces to be expressed as one or more authenticated 402LXP transfers applied by the kernel transfer primitive, and SHALL make a direct balance write by any module impossible rather than merely discouraged."
ac_5 = "THE protocol SHALL treat Crossverse strictly as an oracle and data adapter consumed by the perps module, SHALL admit external observations only as signed oracle activities that are ordered like any other activity, SHALL make the exact accepted oracle payload part of replayable history and part of oracle_root, and SHALL NOT permit the kernel or any module to contact Crossverse or any external data source during transition evaluation."
ac_6 = "WHEN a module version changes by governance activity THE protocol SHALL apply the new version only from a declared epoch boundary onward, SHALL record in each receipt the module version under which the activity executed, and SHALL replay each historical activity under its recorded version so that replay of old history is unaffected by later upgrades."
ac_7 = "WHEN a single logical operation spans multiple modules or multiple balance legs THE protocol SHALL execute it as one atomic transfer set with a single authorisation context, a single execution sequence and a single receipt, and SHALL roll back every leg and every module write if any leg violates an invariant."
ac_8 = "IF a module transition returns a failure result code or violates an invariant, THEN THE protocol SHALL discard all state writes attempted by that activity, SHALL leave every other module unchanged, SHALL still consume account_sequence and charge the metered fee for an otherwise well-formed and authorised activity, and SHALL emit a receipt bearing the failure result code."
ac_9 = "THE protocol SHALL express governance parameter changes and emergency controls as ordinary governance-module activities subject to the same envelope, authority, ordering, fee and receipt rules as any other activity, and SHALL provide no operator path that mutates state outside the ordered activity log."
ac_10 = "WHEN an activity is accepted THE protocol SHALL emit its events only through the kernel event stream, SHALL commit those events in event_merkle_root for the batch, and SHALL provide no module-private or process-local channel by which an effect can become externally observable without appearing in that commitment."

[req.5]
title = "Deterministic Execution"
story = "As an autonomous agent operator, I want any node on any machine to reproduce byte-identical state from the identical activity history, so that I can independently verify every balance and receipt rather than trusting the sequencer."
ac_1 = "WHEN two nodes execute the identical genesis manifest and the identical ordered activity history THE protocol SHALL produce byte-identical state roots, receipts, events and batch headers on every operating system, CPU architecture, endianness, compiler and optimisation level."
ac_2 = "THE protocol SHALL perform every consensus-critical computation in integer-only checked fixed-width arithmetic, including the 128-bit and 256-bit widths required by perps and fee arithmetic, and SHALL treat the presence of any floating-point operation in a transition path as a conformance failure."
ac_3 = "IF any arithmetic operation inside a transition would overflow, underflow, or divide by zero, THEN THE protocol SHALL fail that activity with an explicit arithmetic result code and roll back the entire activity, and SHALL NOT silently wrap, saturate, truncate or invoke undefined behaviour."
ac_4 = "WHEN a transition iterates a map, a set, or any collection of accounts, orders, positions, grants or effects THE protocol SHALL iterate in canonical byte-lexicographic key order, and SHALL NOT allow iteration order to depend on hash seeds, pointer addresses, memory layout, allocation order or insertion order."
ac_5 = "WHEN a computation requires rounding THE protocol SHALL apply the single specified rounding rule for that computation identically on every node, SHALL never let rounding create or destroy value, and SHALL guarantee that for every asset the sum of debits equals the sum of credits in every executed transfer set."
ac_6 = "WHEN a batch is executed or replayed THE protocol SHALL select the transition function version recorded for that batch rather than the newest available version, and SHALL make replay of the entire history from genesis reproduce every historical state root exactly after any subsequent protocol upgrade."
ac_7 = "THE protocol SHALL supply time to execution only as the batch header timestamp, SHALL require batch timestamps to be monotonically non-decreasing, and SHALL NOT read a wall clock, a monotonic clock, a timezone, a locale, or any scheduler timing during transition evaluation."
ac_8 = "THE protocol SHALL NOT read operating-system state during transition evaluation, including environment variables, filesystem enumeration, process or thread identifiers, uninitialised memory, or non-deterministic entropy sources, SHALL NOT perform any external network or HTTP call during transition evaluation, and SHALL derive any required unpredictability solely from values already committed in state."
ac_9 = "THE protocol SHALL apply state transitions from exactly one deterministic writer processing activities in strict global_sequence order, SHALL confine worker threads to signature verification, networking, indexing and other non-consensus work, and SHALL reduce the results of any parallel work into the single writer in a fixed deterministic order so that thread scheduling cannot alter outcomes."
ac_10 = "IF a node crashes at any write boundary, THEN on restart THE protocol SHALL resume from the last durably committed global_sequence with no partially applied activity and no duplicated effect, and SHALL reproduce from that point the identical resulting_state_root that an uninterrupted node produced."

[req.6]
title = "Canonical Binary Wire Format"
story = "As an autonomous agent operator, I want exactly one legal byte encoding for every logical protocol value, so that signatures, identifiers and commitments are unambiguous and no gateway or alternate representation can change what the protocol agreed to."
ac_1 = "THE protocol SHALL define exactly one valid byte encoding for every logical value using a canonical CBOR profile or a purpose-built canonical binary codec, and SHALL guarantee that re-encoding any successfully decoded value reproduces the original bytes exactly."
ac_2 = "IF received bytes contain a non-minimal integer encoding, an indefinite-length item, a duplicate map key, map keys not in canonical order, unused padding, or trailing bytes after the terminal item, THEN THE protocol SHALL reject the message with a non-canonical-encoding result code before evaluating authority, and SHALL NOT consume a sequence or charge a fee."
ac_3 = "THE protocol SHALL declare an explicit maximum length for every variable-length element, including the maximum activity size, payload size, DID length, authority chain depth, transfer-set leg count, effect count and batch activity count, and SHALL reject any message exceeding a declared limit rather than allocating to accommodate it."
ac_4 = "WHEN computing any hash THE protocol SHALL prefix the input with a distinct domain tag per purpose, covering at least activity_id, payload_hash, signature preimages, authorization_hash, context_hash, merkle leaves, merkle internal nodes, batch headers and checkpoint certificates, and SHALL guarantee that identical bytes hashed under two different domains yield different digests."
ac_5 = "WHEN constructing any merkle tree THE protocol SHALL hash leaves and internal nodes under different domain tags, SHALL specify a single deterministic rule for trees whose leaf count is not a power of two, and SHALL reject any proof that relies on reinterpreting an internal node as a leaf."
ac_6 = "IF a message carries an unknown protocol_version, an unknown activity_type, or an unknown field identifier in a consensus-critical structure, THEN THE protocol SHALL reject it deterministically with an unsupported-version or unknown-field result code, and SHALL NOT skip, ignore or best-effort interpret the unrecognised content."
ac_7 = "WHERE an optional JSON or HTTP gateway is deployed THE protocol SHALL treat it purely as a translation and transport surface that converts to canonical bytes before signature verification, SHALL decide consensus solely on those canonical bytes, and SHALL reject any submission whose JSON representation does not translate to exactly the bytes the signature covers."
ac_8 = "THE protocol SHALL verify Ed25519 signatures for agent identity and secp256k1 signatures for Paxeer-facing certificates over the domain-separated canonical byte string of the signed structure, SHALL reject signatures verified over any re-serialized, JSON or textual rendering, and SHALL reject non-canonical signature encodings such as high-order or malleable scalar forms."
ac_9 = "THE protocol SHALL guarantee that no two distinct byte strings decode to the same logical activity, SHALL define activity_id as the domain-separated hash of the canonical activity bytes, and SHALL therefore make any alteration of the encoding produce a different activity_id rather than a second valid form of the same activity."
ac_10 = "WHEN an independent implementation is run against the published conformance vectors THE protocol SHALL require it to reproduce byte-identical encodings, byte-identical digests and merkle roots, and identical rejection result codes for every malformed and non-canonical vector, in order to be considered conformant."

[req.7]
title = "402LXP as the Single Financial Gateway"
story = "As an autonomous agent operator, I want every monetary effect in LayerX to compile into one or more authenticated 402LXP balance transfers applied by a single balance-mutation primitive, so that there is exactly one financial doorway, one execution law and one auditable balance-transition model for the entire protocol."
ac_1 = "WHEN any protocol module (asset, escrow, budget, stream, service, perps, governance or bridge) produces a monetary effect THE protocol SHALL express that effect as one or more authenticated 402LXP transfers and SHALL apply them exclusively through the single internal balance-mutation primitive, in its single-transfer or atomic transfer-set form."
ac_2 = "IF a state transition attempts to change any account balance by any path other than the single internal balance-mutation primitive THEN THE protocol SHALL abort the transition with result code ERR_BALANCE_BYPASS, SHALL leave every balance unchanged, and SHALL NOT emit a success receipt."
ac_3 = "WHEN a node persists state THE protocol SHALL treat the append-only activity log and the 402LXP transfers recorded in it as the sole authority for balances, and SHALL forbid any direct database balance update, so that a SQLite index rebuilt purely by replaying the log reproduces every balance byte-for-byte."
ac_4 = "WHEN a payment-shaped action occurs THE protocol SHALL compile it into these exact legs: agent payment as payer main to payee main; service purchase as buyer main to provider main; refund as merchant main or escrow subaccount to buyer main; protocol fee as actor account to system fees."
ac_5 = "WHEN a custody-shaped action occurs THE protocol SHALL compile it into these exact legs: open escrow as owner main to owner escrow subaccount; capture escrow as owner escrow subaccount to provider main; release escrow as owner escrow subaccount to owner main; create budget as owner main to owner budget subaccount; spend budget as owner budget subaccount to recipient; stream payment as payer stream account to recipient."
ac_6 = "WHEN a Paxeer-facing action occurs THE protocol SHALL compile it into these exact legs: deposit as system Paxeer reserve mirror to agent main; withdrawal as agent main to system Paxeer withdrawals."
ac_7 = "WHEN a perps-module action occurs THE protocol SHALL compile it into these exact legs: perps margin as agent main to agent margin subaccount for the position; release margin as agent margin subaccount to agent main; trading loss as position margin subaccount to system liquidity for the market; trading profit as system liquidity for the market to agent main; funding payment as long funding account to short funding account; liquidation fee as position margin subaccount to liquidator main or system insurance; insurance payout as system insurance to the deficit account."
ac_8 = "WHERE a module holds non-monetary state such as an order, a service agreement, a task commitment, a tool execution attestation, a delivery, an acceptance, a dispute or a position, THE protocol SHALL allow that state to be created, ordered and attested with no direct monetary effect, and SHALL require any value movement it implies to execute as a separate authenticated 402LXP transfer."
ac_9 = "IF an activity payload carries a client-supplied resulting balance, new balance, or balance delta for any account THEN THE protocol SHALL reject the activity with result code ERR_CLIENT_SUPPLIED_BALANCE, because the protocol alone computes resulting balances."
ac_10 = "WHERE a new protocol module is introduced by a versioned transition function, THE protocol SHALL grant it monetary capability only as a protocol-module capability that authorises 402LXP transfers, and SHALL NOT expose any additional balance-mutation entry point, so that the count of balance-mutation primitives remains exactly one across all protocol versions."

[req.8]
title = "The SEND Operation"
story = "As an autonomous agent operator, I want SEND to push value from an account I control under an explicit, bounded and replay-proof authorisation, so that every debit of my agent is provably authorised, exactly-once, and impossible to forge, expire past, or overflow."
ac_1 = "WHEN a SEND activity is submitted THE protocol SHALL require the canonically encoded fields from, to, asset, amount, sequence, idempotency_key, expires_at, context_hash, conditions and authorization, and SHALL reject with result code ERR_MALFORMED_SEND any activity that omits a field, adds an unknown field, or is not canonically encoded."
ac_2 = "WHEN the protocol evaluates the authorization field THE protocol SHALL accept exactly one of six authorisation kinds, namely an owner direct signature, a session key, a delegated capability, a budget allowance, an escrow authority, or a protocol-module capability, and SHALL reject any other authorisation kind with result code ERR_UNKNOWN_AUTHORITY_KIND."
ac_3 = "IF the presented authorization does not control the from account, or is expired, revoked, key-rotated out, outside its declared spending or trading scope, or bound to a different network_id or protocol_version THEN THE protocol SHALL reject the SEND with result code ERR_UNAUTHORIZED_DEBIT and SHALL change no balance and consume no sequence."
ac_4 = "WHEN the protocol validates sequence THE protocol SHALL require it to be exactly equal to the next expected sequence of the account that the authorization controls, SHALL reject any lower or higher value with result code ERR_SEQUENCE_MISMATCH, and on success SHALL consume that sequence exactly once so that a byte-identical resubmission is rejected as a replay."
ac_5 = "IF the deterministic timestamp supplied by the batch containing the SEND is strictly greater than expires_at THEN THE protocol SHALL reject the SEND with result code ERR_EXPIRED, SHALL NOT apply it in any later batch, and SHALL NOT consume the sequence."
ac_6 = "IF amount is zero or not strictly greater than zero, or if the available balance of the from account in the named asset is strictly less than amount, THEN THE protocol SHALL reject the SEND with result code ERR_INVALID_AMOUNT or ERR_INSUFFICIENT_BALANCE respectively, and SHALL leave the from and to balances exactly as they were."
ac_7 = "IF the asset held by the from account and the asset credited to the to account are not the identical asset identifier, or the named asset is unknown to the protocol, THEN THE protocol SHALL reject the SEND with result code ERR_ASSET_MISMATCH."
ac_8 = "WHEN the protocol computes the debit and the credit THE protocol SHALL use checked fixed-width integer arithmetic with no floating-point values, SHALL reject with result code ERR_ARITHMETIC_OVERFLOW rather than wrapping if any subtraction would go below zero or any addition would exceed the asset maximum, and SHALL guarantee that the sum of all balance changes produced by the SEND is exactly zero for the named asset."
ac_9 = "WHEN the authorization is verified THE protocol SHALL require context_hash and conditions to be covered by the signed authorisation bytes, SHALL reject with result code ERR_CONTEXT_MISMATCH if the submitted context_hash differs from the one that was signed, and SHALL reject with result code ERR_CONDITION_UNMET if any declared condition does not hold at the deterministic execution timestamp."
ac_10 = "WHEN two SEND activities carry the same idempotency_key for the same authorising account THE protocol SHALL produce at most one economic result, SHALL return the receipt of the first accepted execution to every subsequent submission, and SHALL NOT apply a second transfer even if the later submission differs in amount, recipient or fee_limit."

[req.9]
title = "The RECEIVE Operation and Payer Grants"
story = "As an autonomous agent operator, I want a service to be able to pull payment from my account only inside a grant I previously signed, so that a recipient can collect what I owe without ever being able to debit an arbitrary account or exceed the limit, purpose, window or lifetime I authorised."
ac_1 = "WHEN a RECEIVE activity is submitted THE protocol SHALL require the canonically encoded fields from, to, asset, amount, grant_id, receiver_sequence, idempotency_key, context_hash, receiver_authorization and payer_grant, and SHALL reject with result code ERR_MALFORMED_RECEIVE any activity that omits a field or is not canonically encoded."
ac_2 = "IF a RECEIVE names a from account for which no valid, unexpired, unrevoked payer grant signed by an authority controlling that account and naming the submitting recipient exists THEN THE protocol SHALL reject with result code ERR_NO_PAYER_GRANT, so that a recipient can never debit an arbitrary account."
ac_3 = "WHEN the protocol validates a payer grant THE protocol SHALL require it to specify the authorised recipient, the asset, the maximum amount, whether the allowance is total or recurring together with the recurring window length, the expiration, the permitted purpose, an optional service or invoice identifier, and the revocation sequence, and SHALL reject with result code ERR_MALFORMED_GRANT any grant missing a required element."
ac_4 = "IF the submitting recipient is not the authorised recipient named in the grant, or the RECEIVE asset differs from the grant asset, or amount exceeds the grant maximum amount, or the from and to accounts do not match the grant, THEN THE protocol SHALL reject the RECEIVE with result code ERR_GRANT_SCOPE_VIOLATION and SHALL debit nothing."
ac_5 = "WHILE a grant declares a total allowance THE protocol SHALL track the cumulative amount already drawn under that grant_id and SHALL reject with result code ERR_GRANT_EXHAUSTED any RECEIVE whose amount would make the cumulative drawn total exceed the allowance, including the case where several RECEIVE activities are ordered in the same batch."
ac_6 = "WHILE a grant declares a recurring allowance THE protocol SHALL reset the remaining allowance to the full recurring amount at the deterministic start of each window, SHALL NOT carry unused allowance forward into the next window, and SHALL reject with result code ERR_GRANT_EXHAUSTED any RECEIVE exceeding the remaining allowance of the current window."
ac_7 = "IF the deterministic timestamp of the batch containing the RECEIVE is strictly greater than the grant expiration THEN THE protocol SHALL reject the RECEIVE with result code ERR_GRANT_EXPIRED, and SHALL treat any remaining allowance as permanently unusable."
ac_8 = "WHEN the payer submits a revocation whose revocation sequence is greater than or equal to the revocation sequence of a grant THE protocol SHALL make that grant unusable from the global sequence of the revocation onward, SHALL reject every later-ordered RECEIVE against it with result code ERR_GRANT_REVOKED, and SHALL leave already-executed transfers ordered before the revocation intact."
ac_9 = "IF the RECEIVE context_hash does not commit to the permitted purpose of the grant, or the grant names a service or invoice identifier and the RECEIVE does not reference that identifier, or that invoice identifier has already been settled, THEN THE protocol SHALL reject with result code ERR_PURPOSE_MISMATCH or ERR_INVOICE_ALREADY_SETTLED and SHALL move no value."
ac_10 = "WHEN a RECEIVE passes all grant checks THE protocol SHALL execute the identical internal transfer function used by SEND under the identical invariant checks, SHALL consume receiver_sequence exactly once, SHALL enforce that idempotency_key produces at most one economic result, and SHALL emit a receipt that differs from a SEND receipt only in the operation field and the authorisation evidence."

[req.10]
title = "Accounts and Subaccounts"
story = "As an autonomous agent operator, I want every locked, escrowed, budgeted and margined unit to sit in a real named account rather than a hidden balance column, so that I can trace every unit of value through one ledger and reconcile it against Paxeer custody."
ac_1 = "WHEN the protocol immobilises value for escrow, a budget, a stream, position margin, fees, liquidity, insurance or a Paxeer flow THE protocol SHALL represent that value as the balance of a real account reached by a 402LXP transfer, and SHALL NOT represent it as a reserved, held, locked or pending field attached to another account."
ac_2 = "WHEN the protocol derives an account name THE protocol SHALL accept exactly the namespace agent:<did>:main, agent:<did>:budget:<id>, agent:<did>:escrow:<id>, agent:<did>:margin:<position>, system:liquidity:<market>, system:insurance, system:fees, system:paxeer-reserve and system:paxeer-withdrawals, and SHALL reject with result code ERR_UNKNOWN_ACCOUNT_NAMESPACE any transfer leg naming an account outside that namespace."
ac_3 = "WHEN an account identifier is derived from its canonical namespace string THE protocol SHALL use a domain-separated commitment over the canonical binary encoding of that string, SHALL produce the identical identifier on every node, architecture and protocol implementation, and SHALL keep that identifier stable for the lifetime of the account."
ac_4 = "IF two distinct canonical namespace strings would derive the same account identifier, or an activity presents an account identifier that does not match the derivation of its declared namespace string, THEN THE protocol SHALL reject the activity with result code ERR_ACCOUNT_ID_MISMATCH."
ac_5 = "WHEN a transfer credits a namespace-valid account that does not yet exist THE protocol SHALL create it deterministically at that global sequence with a zero opening balance before applying the credit, and SHALL record its creation in the activity log so that replay recreates it identically."
ac_6 = "IF a request would close or delete an account whose balance in any asset is non-zero, or that is still referenced by an open escrow, budget, stream or position, THEN THE protocol SHALL reject the closure with result code ERR_ACCOUNT_NOT_EMPTY."
ac_7 = "WHEN an agent opens a perps position THE protocol SHALL physically move the margin as a 402LXP transfer from agent:<did>:main to agent:<did>:margin:<position>, SHALL treat the balance of that margin subaccount as the collateral of record for the position, and SHALL NOT satisfy the margin requirement by setting a reserved-margin field on the main account."
ac_8 = "WHEN a position is closed, settled or liquidated THE protocol SHALL drain the margin subaccount to zero through explicit 402LXP legs to the counterpart accounts, and SHALL reject any position closure that would leave residual value stranded in the margin subaccount."
ac_9 = "IF a debit is attempted from an agent budget, escrow, margin or stream subaccount using only an owner direct signature or a session key THEN THE protocol SHALL reject it with result code ERR_UNAUTHORIZED_DEBIT, because those subaccounts SHALL be debited only under the escrow authority, budget allowance or protocol-module capability that owns them."
ac_10 = "IF a debit is attempted against system:liquidity:<market>, system:insurance, system:fees, system:paxeer-reserve or system:paxeer-withdrawals without the protocol-module capability designated for that system account THEN THE protocol SHALL reject it with result code ERR_UNAUTHORIZED_DEBIT, and system accounts SHALL be created only by the genesis manifest or a governance activity."

[req.11]
title = "Atomic Transfer Sets and Conservation"
story = "As an autonomous agent operator, I want complex multi-leg operations such as liquidation to commit entirely or not at all under one authorisation and one receipt, so that no partially settled state, no invented value and no destroyed value can ever be observed on the ledger."
ac_1 = "WHEN a transfer set containing more than one leg is executed THE protocol SHALL either apply every leg or apply no leg, and SHALL NOT expose any intermediate state in which a proper subset of the legs has been applied to any reader, index, event stream or receipt."
ac_2 = "WHEN a transfer set is submitted THE protocol SHALL bind it to exactly one authorisation context, SHALL consume exactly one execution sequence for the authorising account, and SHALL emit exactly one receipt for the whole set rather than one receipt per leg."
ac_3 = "WHEN a position is liquidated THE protocol SHALL execute a single atomic transfer set whose legs are, in this order, position margin subaccount to system liquidity for the market, then position margin subaccount to the liquidation fee account, then system insurance to system liquidity for the market to cover any deficit, then remaining position margin subaccount to agent main."
ac_4 = "IF any leg of a transfer set violates any protocol invariant, including insufficient balance, asset mismatch, arithmetic overflow, unknown account namespace or missing authority, THEN THE protocol SHALL roll the entire set back, SHALL report the zero-based index and result code of the first failing leg, and SHALL NOT emit a success receipt for any leg."
ac_5 = "WHEN a transfer set is applied THE protocol SHALL enforce for every asset that the sum of debits equals the sum of credits within the set, and SHALL reject with result code ERR_CONSERVATION_VIOLATION any set whose net supply change for any asset is non-zero."
ac_6 = "WHEN a Paxeer deposit is credited THE protocol SHALL move value from system:paxeer-reserve to the agent main account as an ordinary conserving transfer set, so that the total LayerX supply of the asset is unchanged and remains equal to the value attested in Paxeer custody."
ac_7 = "WHEN a withdrawal is initiated THE protocol SHALL move value from the agent main account to system:paxeer-withdrawals as an ordinary conserving transfer set, and SHALL NOT reduce total LayerX supply until a finalized Paxeer settlement reference retires the withdrawal against system:paxeer-reserve."
ac_8 = "IF any module other than the bridge module operating under a finalized Paxeer proof attempts a transfer set that increases or decreases the total supply of an asset THEN THE protocol SHALL reject it with result code ERR_SUPPLY_MUTATION, because ordinary modules SHALL never mint or burn."
ac_9 = "WHEN a transfer set is executed THE protocol SHALL apply its legs in the exact declared order using checked integer arithmetic, and IF applying a leg in that order would drive any account balance below zero THEN THE protocol SHALL fail the entire set rather than reordering, netting or deferring legs."
ac_10 = "IF a node crashes at any write boundary while a transfer set is being applied THEN THE protocol SHALL, on restart and replay of the append-only activity log, converge to a state in which that set is either fully applied or entirely absent, with a state root identical to that of a node that never crashed."

[req.12]
title = "402LXP Receipts and Proofs"
story = "As an autonomous agent operator, I want every successful 402LXP operation to yield one durable receipt carrying protocol-computed before and after balances plus checkpoint proofs, so that any third party can verify the payment independently without trusting the sequencer or the service that was paid."
ac_1 = "WHEN a 402LXP operation succeeds THE protocol SHALL return a 402LXPReceipt containing protocol_version, transaction_id, operation, global_sequence, asset, amount, from, from_balance_before, from_balance_after, from_sequence, to, to_balance_before, to_balance_after, transfer_set_root, authorization_hash, context_hash, previous_state_root, resulting_state_root, batch_id, timestamp and sequencer_signature."
ac_2 = "WHEN the receipt records from_balance_before, from_balance_after, to_balance_before and to_balance_after THE protocol SHALL compute those values itself from the state it applied, and SHALL ignore or reject any client-submitted balance value, so that the before and after balances are evidence rather than client input."
ac_3 = "WHEN a single-leg operation is receipted THE protocol SHALL guarantee that from_balance_after equals from_balance_before minus amount and that to_balance_after equals to_balance_before plus amount for the named asset, and WHEN a multi-leg set is receipted THE protocol SHALL set transfer_set_root to a commitment over the canonical encoding of every leg in execution order."
ac_4 = "WHEN a receipt is issued THE protocol SHALL sign the canonical binary encoding of the receipt with the sequencer identity named in the batch header, and SHALL make that signature verifiable against the published sequencer key without any further interaction with the node."
ac_5 = "WHEN a receipt is issued THE protocol SHALL have already durably appended the corresponding activity and receipt to the append-only activity log, so that after any crash the same receipt is reproduced by replay and no acknowledged receipt is ever lost or altered."
ac_6 = "WHEN a caller queries by transaction_id, by idempotency_key or by global_sequence THE protocol SHALL return the identical receipt bytes that were originally issued, for as long as the corresponding history is retained."
ac_7 = "WHEN the batch containing a receipt is included in a checkpoint accepted by Paxeer THE protocol SHALL augment the retrievable receipt with activity_inclusion_proof, state_inclusion_proof, checkpoint_id, guarantor_certificate and paxeer_settlement_reference, without altering any pre-checkpoint field."
ac_8 = "WHEN a third party holds an augmented receipt THE protocol SHALL allow it to be verified independently by checking the sequencer signature, recomputing the activity merkle root from activity_inclusion_proof, verifying state_inclusion_proof against resulting_state_root, verifying that guarantor_certificate carries at least the configured guarantor threshold of valid bonded signatures over checkpoint_id, and matching paxeer_settlement_reference against the checkpoint registered on Paxeer."
ac_9 = "WHEN two submissions share an idempotency_key THE protocol SHALL return byte-identical receipt content for both, and SHALL maintain exactly one durable receipt per successful transfer or transfer set."
ac_10 = "IF an operation fails for any reason THEN THE protocol SHALL emit a failure result carrying the result code and the unchanged balances, SHALL NOT emit a signed 402LXPReceipt for it, and SHALL NOT include any balance change from it in any state root."

[req.13]
title = "Core Protocol Invariants"
story = "As an autonomous agent operator, I want the fourteen financial invariants of LayerX enforced at runtime on every state transition, so that a bug, a malicious module or a hostile activity aborts the transition instead of silently corrupting balances, receipts or checkpoints."
ac_1 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every monetary mutation is a 402LXP transfer, and SHALL abort with ERR_INVARIANT_1 any transition that changes a balance outside a 402LXP transfer."
ac_2 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every debit carries explicit authority resolvable to an owner signature, session key, delegated capability, budget allowance, escrow authority or protocol-module capability that controls the debited account, and SHALL abort with ERR_INVARIANT_2 otherwise."
ac_3 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every RECEIVE is backed by a valid, unexpired, unrevoked and unexhausted payer grant naming the submitting recipient, and SHALL abort with ERR_INVARIANT_3 otherwise."
ac_4 = "WHILE any state transition is evaluated THE protocol SHALL enforce that no account balance in any asset becomes negative at any point, including intermediate points inside a transfer set, and SHALL abort with ERR_INVARIANT_4 otherwise."
ac_5 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every normal transfer conserves supply so that for each asset the sum of debits equals the sum of credits, and SHALL abort with ERR_INVARIANT_5 otherwise."
ac_6 = "WHILE any state transition is evaluated THE protocol SHALL enforce that transfer sets are completely atomic, committing all legs or none, and SHALL abort with ERR_INVARIANT_6 rather than persisting a partially applied set."
ac_7 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every successful transfer or transfer set yields exactly one durable receipt, and SHALL abort with ERR_INVARIANT_7 if zero receipts or more than one receipt would be produced."
ac_8 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every idempotency key produces at most one economic result, and SHALL abort with ERR_INVARIANT_8 rather than applying a second economic effect under a key already settled."
ac_9 = "WHILE any state transition is evaluated THE protocol SHALL enforce that every account sequence is consumed exactly once, and SHALL abort with ERR_INVARIANT_9 on any reuse, gap or out-of-order consumption of an account sequence."
ac_10 = "WHILE any state transition is evaluated THE protocol SHALL enforce that no module writes balances directly and that all balance writes pass through the single balance-mutation primitive, and SHALL abort with ERR_INVARIANT_10 otherwise."
ac_11 = "WHILE any state transition is evaluated THE protocol SHALL enforce that external oracle data, including Crossverse prices, cannot directly alter a balance and may only be consumed as a signed oracle activity whose effects execute as authenticated 402LXP transfers, and SHALL abort with ERR_INVARIANT_11 otherwise."
ac_12 = "WHILE any state transition is evaluated THE protocol SHALL enforce that a Paxeer deposit credits a LayerX account only against a finalized custody proof, and SHALL abort with ERR_INVARIANT_12 on any deposit credit lacking such a proof."
ac_13 = "WHILE any state transition is evaluated THE protocol SHALL enforce that a Paxeer withdrawal pays at most once by consuming a unique withdrawal nullifier, and SHALL abort with ERR_INVARIANT_13 on any attempt to settle an already-consumed nullifier or to pay a withdrawal twice."
ac_14 = "WHILE history is replayed on any machine or architecture THE protocol SHALL enforce that replaying the same activity history produces identical balances, identical receipts and identical state roots, and SHALL abort with ERR_INVARIANT_14 at the first divergent global sequence rather than continuing."
ac_15 = "WHEN any activity, batch, transfer, transfer set, checkpoint application or replay step is executed THE protocol SHALL evaluate all fourteen invariants at runtime for that step, and SHALL NOT allow any build, configuration, governance parameter or emergency mode to disable an invariant check."
ac_16 = "IF any invariant check fails THEN THE protocol SHALL abort the entire state transition atomically leaving the previous state root intact, SHALL emit a violation record naming the invariant identifier, the offending global sequence and the offending account, SHALL NOT emit a success receipt, and SHALL halt the node rather than producing or attesting a divergent state root."

[req.14]
title = "Asset Module"
story = "As an autonomous agent operator, I want a fully reserved asset registry whose balances change only through authenticated 402LXP transfers, so that every unit I hold on LayerX is traceable, integer-exact and always reconcilable against Paxeer custody."
ac_1 = "WHEN an asset registration activity is accepted THE protocol SHALL record the asset id, symbol, decimals and Paxeer custody reference in the asset registry, and IF an asset with the same asset id already exists THEN THE protocol SHALL reject the activity with result code ASSET_ALREADY_REGISTERED and make no state change other than sequence and fee consumption."
ac_2 = "WHERE an amount appears in any accepted activity payload THE protocol SHALL interpret it as an unsigned fixed-width integer count of the smallest indivisible unit of the named asset, and SHALL reject with result code INVALID_AMOUNT any amount encoded as a decimal fraction, an exponent form or a floating point value, so that decimals are display metadata only and never participate in arithmetic."
ac_3 = "WHERE any module of the protocol produces a monetary effect THE protocol SHALL express that effect exclusively as one or more authenticated 402LXP transfers applied by the single balance mutation primitive, and SHALL reject with result code DIRECT_BALANCE_WRITE_FORBIDDEN any accepted activity or module effect that attempts to assign a balance value directly."
ac_4 = "IF a transfer leg would reduce a source account balance below zero THEN THE protocol SHALL reject the entire transfer or transfer set with result code INSUFFICIENT_FUNDS and SHALL leave every balance in the set exactly as it was before evaluation."
ac_5 = "IF crediting a destination account would exceed the maximum representable fixed-width balance THEN THE protocol SHALL reject the entire transfer or transfer set with result code BALANCE_OVERFLOW and SHALL never wrap, saturate or truncate the value."
ac_6 = "IF the asset of the source account differs from the asset of the destination account, or the transfer amount is zero, THEN THE protocol SHALL reject the transfer with result code ASSET_MISMATCH or INVALID_AMOUNT respectively and SHALL apply no leg of the enclosing transfer set."
ac_7 = "WHEN a deposit activity is processed THE protocol SHALL credit the named agent account only by executing a 402LXP transfer from the system Paxeer reserve mirror account to the agent main account, and only after verifying a finalised Paxeer custody proof bound to a finalised checkpoint; IF the proof is absent, unverifiable or references a checkpoint that is not yet finalised THEN THE protocol SHALL reject the deposit with result code DEPOSIT_PROOF_NOT_FINAL and credit nothing."
ac_8 = "IF a deposit activity presents a custody proof whose custody reference or deposit nullifier has already been consumed THEN THE protocol SHALL reject it with result code DEPOSIT_ALREADY_CREDITED, so that a replayed or resubmitted deposit proof produces at most one economic result for a given asset and custody reference."
ac_9 = "WHEN a withdrawal activity is accepted THE protocol SHALL debit the agent by executing a 402LXP transfer from the agent main account into the system Paxeer withdrawal account and SHALL record a unique withdrawal nullifier in the resulting state; IF a settlement or a further withdrawal activity presents an already recorded nullifier THEN THE protocol SHALL reject it with result code WITHDRAWAL_ALREADY_SETTLED so that no withdrawal can pay twice."
ac_10 = "WHILE a batch is being executed THE protocol SHALL enforce for every asset that the sum of debits equals the sum of credits across all legs, and that total circulating supply, defined as the sum of all agent, module and system account balances excluding the Paxeer reserve mirror, never exceeds the custody amount attested by the most recent finalised Paxeer proof less the amount already settled out of the Paxeer withdrawal account; IF reconciliation fails THEN THE protocol SHALL reject the offending activity and SHALL NOT produce a batch whose resulting state root violates full reserve accounting."

[req.15]
title = "Escrow Module"
story = "As an autonomous agent operator, I want holds that physically move funds into an escrow subaccount with deterministic capture, release, expiry and dispute behaviour, so that counterparties can rely on funds being reserved and I can prove that escrowed value was never spendable from my main account."
ac_1 = "WHEN a hold creation activity is accepted THE protocol SHALL execute a 402LXP transfer of the exact hold amount from the owner main account to the escrow subaccount named for that hold identifier, and SHALL record the beneficiary, asset, amount, expiry timestamp and dispute window in escrow state; the hold SHALL have no monetary effect other than that transfer."
ac_2 = "WHILE a hold is active THE protocol SHALL keep the held funds in the escrow subaccount, so that any transfer attempting to source those funds from the owner main account is rejected with result code INSUFFICIENT_FUNDS, and SHALL reject with result code UNAUTHORIZED_ESCROW_SPEND any transfer out of the escrow subaccount that is not a capture, a release, an expiry release or a dispute resolution produced by the escrow module."
ac_3 = "WHEN an authorised capture activity for the full remaining hold amount is accepted THE protocol SHALL execute a 402LXP transfer of that amount from the escrow subaccount to the beneficiary account, set the escrow subaccount balance to zero and mark the hold closed."
ac_4 = "WHEN an authorised capture activity names an amount smaller than the remaining hold amount THE protocol SHALL execute a 402LXP transfer of exactly that amount from the escrow subaccount to the beneficiary account, reduce the remaining hold amount by exactly that amount and keep the hold active for the remainder."
ac_5 = "IF a capture amount exceeds the remaining hold amount, or the hold identifier is unknown, or the capturing authority is not the recorded beneficiary or an authority delegated by the owner THEN THE protocol SHALL reject the capture with result code CAPTURE_EXCEEDS_HOLD or UNAUTHORIZED_CAPTURE and SHALL move no funds."
ac_6 = "WHEN an authorised release activity is accepted THE protocol SHALL execute a 402LXP transfer of the entire remaining escrow subaccount balance back to the owner main account, leave the escrow subaccount at zero and mark the hold closed."
ac_7 = "WHEN the batch timestamp reaches or passes the recorded hold expiry and the hold is neither closed nor disputed THE protocol SHALL release the entire remaining escrow subaccount balance to the owner main account deterministically during that batch, and SHALL thereafter reject any capture against that hold with result code HOLD_EXPIRED."
ac_8 = "WHEN a dispute activity is accepted for an active hold and the batch timestamp is within the recorded dispute window THE protocol SHALL mark the hold disputed and SHALL reject subsequent capture, release and expiry release with result code HOLD_DISPUTED until a resolution is accepted; IF a dispute is raised after the dispute window has closed THEN THE protocol SHALL reject it with result code DISPUTE_WINDOW_CLOSED."
ac_9 = "WHEN a dispute resolution from the authority recorded for that hold is accepted THE protocol SHALL apply it as one atomic 402LXP transfer set whose legs split the escrow subaccount balance between the beneficiary and the owner main account, SHALL require that the sum of the legs equals the escrow subaccount balance exactly, SHALL leave the escrow subaccount at zero, and IF any leg violates an invariant THEN THE protocol SHALL roll back the whole set and leave the hold disputed."
ac_10 = "IF a capture, release or resolution activity is resubmitted with an idempotency key that has already produced an economic result THEN THE protocol SHALL move no additional funds and SHALL return the receipt of the original execution, so that every escrow idempotency key yields at most one monetary effect."

[req.16]
title = "Budget Module"
story = "As an autonomous agent operator, I want funded budget subaccounts with recurring allowances and delegated spenders, so that another agent can spend on my behalf inside limits I control and can revoke, while every spend is still an authenticated 402LXP transfer."
ac_1 = "WHEN a budget creation activity is accepted THE protocol SHALL fund the budget by executing a 402LXP transfer of the requested funding amount from the owner main account to the budget subaccount named for that budget identifier, and SHALL record the asset, per-period limit, period length in protocol time units, period start timestamp, rollover policy, delegate set, expiry and revocation sequence."
ac_2 = "WHEN a delegate submits a spend whose amount is within both the remaining period allowance and the budget subaccount balance THE protocol SHALL execute a 402LXP transfer of that amount from the budget subaccount to the named recipient and SHALL reduce the remaining period allowance by exactly that amount."
ac_3 = "IF a spend amount exceeds the remaining period allowance THEN THE protocol SHALL reject it with result code BUDGET_ALLOWANCE_EXCEEDED and move no funds, even when the budget subaccount holds enough balance to cover the amount."
ac_4 = "IF a spend amount is within the remaining period allowance but exceeds the budget subaccount balance THEN THE protocol SHALL reject it with result code INSUFFICIENT_BUDGET_FUNDS, so that allowance accounting alone never creates spendable value."
ac_5 = "IF the submitting identity is not in the recorded delegate set, or its capability has expired, or its capability scope does not cover the asset, recipient or amount THEN THE protocol SHALL reject the spend with result code UNAUTHORIZED_DELEGATE and SHALL make no monetary effect."
ac_6 = "WHEN the batch timestamp crosses one or more period boundaries THE protocol SHALL roll the budget over deterministically at the first activity in that batch which observes the boundary, advancing the period start by whole period lengths, and SHALL reset the remaining period allowance to the configured per-period limit; WHERE the rollover policy is carry-forward THE protocol SHALL add the unspent allowance to the new period up to the configured carry cap and discard the excess, and WHERE the policy is discard THE protocol SHALL drop all unspent allowance."
ac_7 = "WHEN the owner submits a defunding activity THE protocol SHALL execute a 402LXP transfer of the requested amount from the budget subaccount back to the owner main account and SHALL reduce the remaining period allowance so that it never exceeds the resulting budget subaccount balance."
ac_8 = "WHEN the owner submits a budget revocation THE protocol SHALL, in the same activity, execute a 402LXP transfer of the entire remaining budget subaccount balance to the owner main account, mark the budget revoked, and SHALL reject every later spend against that budget with result code BUDGET_REVOKED."
ac_9 = "IF a revocation or capability update carries a revocation sequence that is not strictly greater than the recorded revocation sequence THEN THE protocol SHALL reject it with result code STALE_REVOCATION, so that a replayed authorisation can never reinstate a revoked delegate."
ac_10 = "WHERE a budget authorises a recipient-initiated pull THE protocol SHALL require a signed payer grant naming the authorised recipient, asset, maximum amount, allowance kind, expiry, permitted purpose and revocation sequence, and SHALL reject with result code GRANT_SCOPE_VIOLATION any pull exceeding the grant, presented after expiry, or presented against a superseded revocation sequence."

[req.17]
title = "Stream Module"
story = "As an autonomous agent operator, I want time-based and metered payments that accrue from batch timestamps and settle through 402LXP transfers, so that continuous payment is exactly reproducible on replay and never depends on any node wall-clock."
ac_1 = "WHEN a stream open activity is accepted THE protocol SHALL execute a 402LXP transfer of the funding amount from the payer main account to the stream subaccount named for that stream identifier, and SHALL record the recipient, asset, rate, rate unit, start timestamp, optional end timestamp and total cap; opening a stream SHALL have no monetary effect other than that transfer."
ac_2 = "WHERE accrual for any stream is computed THE protocol SHALL derive elapsed time solely from batch header timestamps and derive metered quantity solely from accepted usage activities, and SHALL NEVER read operating system time, node wall-clock or any value outside the replayable activity history, so that replaying identical history yields identical accrued amounts."
ac_3 = "WHEN a settlement activity for a time-based stream is accepted THE protocol SHALL compute the newly accrued amount as rate multiplied by elapsed protocol time units using checked fixed-width integer arithmetic with rounding down to the smallest indivisible unit of the asset, SHALL subtract the amount already settled, and SHALL execute a 402LXP transfer of the difference from the stream subaccount to the recipient."
ac_4 = "IF a stream computation would produce a negative elapsed interval, or the batch timestamp presented is earlier than the timestamp of the last settlement for that stream, THEN THE protocol SHALL reject the settlement with result code NON_MONOTONIC_TIME and SHALL move no funds; IF the accrual product would exceed the maximum representable fixed-width value THEN THE protocol SHALL reject with result code ACCRUAL_OVERFLOW rather than wrapping."
ac_5 = "WHEN a usage attestation for a metered stream is accepted THE protocol SHALL accrue the metered units multiplied by the recorded unit price with the same rounding rule, and IF the cumulative meter reading in the attestation is lower than the last recorded cumulative reading, or the attestation is not signed by a meter authority recorded for the stream, THEN THE protocol SHALL reject it with result code METER_REGRESSION or UNAUTHORIZED_METER and accrue nothing."
ac_6 = "IF the accrued unsettled amount exceeds the stream subaccount balance THEN THE protocol SHALL settle only up to the available balance, SHALL mark the stream underfunded, SHALL suspend further accrual from that settlement timestamp, and SHALL NOT create debt or allow any account to become negative; WHEN a top-up transfer refunds the stream subaccount THE protocol SHALL resume accrual from the timestamp of the top-up activity."
ac_7 = "WHEN a pause activity from an authorised party is accepted THE protocol SHALL settle accrual up to the batch timestamp of the pause, SHALL accrue nothing for the paused interval, and WHEN a resume activity is accepted THE protocol SHALL accrue again only from the batch timestamp of the resume."
ac_8 = "WHEN a cancellation activity is accepted THE protocol SHALL perform final settlement as one atomic 402LXP transfer set that pays the accrued unsettled amount to the recipient and returns the entire remaining stream subaccount balance to the payer main account, leaving the stream subaccount at zero and the stream closed; IF any leg violates an invariant THEN THE protocol SHALL roll back the whole set and leave the stream open."
ac_9 = "IF a settlement, pause, resume or cancellation activity is submitted against a stream that is already closed, or is resubmitted with an idempotency key that has already produced an economic result, THEN THE protocol SHALL move no additional funds and SHALL respond with result code STREAM_CLOSED or return the receipt of the original execution."
ac_10 = "WHERE a stream reaches its recorded end timestamp or total cap THE protocol SHALL stop accrual at exactly that bound, SHALL cap the lifetime paid amount at the recorded total cap, and SHALL express every payment it has made only as 402LXP transfers out of the stream subaccount."

[req.18]
title = "Service Module and the Agent Work Lifecycle"
story = "As an autonomous agent operator, I want the complete lifecycle of agent work recorded as first-class ordered and attested activities that carry no direct monetary effect, so that offers, commitments, tool executions, deliveries and disputes are independently replayable evidence while any value they imply still moves through 402LXP transfers."
ac_1 = "WHERE any service activity implies a value movement THE protocol SHALL execute that movement only as one or more authenticated 402LXP transfers, typically through an escrow hold, and SHALL reject with result code MODULE_MAY_NOT_WRITE_BALANCE any service activity that attempts to encode a balance mutation of its own; offer, accept, commit, attest, deliver, accept-delivery, reject and dispute SHALL each have no direct monetary effect."
ac_2 = "WHEN an offer activity is accepted THE protocol SHALL assign it a global sequence, record the offering agent, asset, price, terms hash, deliverable specification hash, delivery deadline, acceptance window, dispute window and offer expiry, and SHALL include it in the activity merkle root of its batch."
ac_3 = "WHEN an acceptance activity references an open offer THE protocol SHALL bind it to the offer activity id and require an exact match of the recorded terms hash, and IF the offer has expired, has already been accepted, has been withdrawn, or the presented terms hash differs THEN THE protocol SHALL reject the acceptance with result code OFFER_UNAVAILABLE or TERMS_MISMATCH."
ac_4 = "WHEN a work commitment activity is accepted THE protocol SHALL record the committing provider identity, the agreement id, the deadline expressed as a protocol timestamp bound, and the identifier of any escrow hold the parties reference, and SHALL NOT itself move funds even when an escrow hold is named."
ac_5 = "WHEN a tool execution attestation is accepted THE protocol SHALL require a tool identifier, an input commitment hash, an output commitment hash, execution start and end timestamp bounds, and a signature from a key with attestation authority for the committing agent, SHALL order the attestation in the activity log and include its exact payload in the activity merkle root; IF the signature is invalid, the attesting key is revoked or expired, or a required commitment hash is absent THEN THE protocol SHALL reject the attestation with result code INVALID_ATTESTATION."
ac_6 = "WHEN a delivery activity is accepted THE protocol SHALL record every deliverable hash and its data-availability reference, and IF a deliverable hash does not match the deliverable specification hash committed in the accepted offer, or the batch timestamp is later than the recorded delivery deadline, THEN THE protocol SHALL reject the delivery with result code DELIVERABLE_MISMATCH or DELIVERY_DEADLINE_PASSED."
ac_7 = "WHEN the buyer submits an acceptance of delivery within the acceptance window THE protocol SHALL mark the agreement accepted and closed for delivery purposes, and SHALL require that any resulting payment be executed as a separate 402LXP escrow capture or transfer rather than as an effect of the acceptance activity itself."
ac_8 = "IF the acceptance window elapses with neither an acceptance nor a rejection recorded THEN THE protocol SHALL apply the default outcome recorded in the accepted offer terms deterministically during the first batch whose timestamp crosses the window end, and SHALL apply it identically on every replay of that history."
ac_9 = "WHEN the buyer submits a rejection within the acceptance window THE protocol SHALL record the rejection with its reason code and the deliverable hashes it contests, and SHALL hold the agreement in a disputable state until the recorded dispute window closes."
ac_10 = "WHEN a dispute activity is raised by a party to the agreement while the batch timestamp is inside the recorded dispute window THE protocol SHALL order it and record the evidence hashes it carries, and IF the raiser is not a party to the agreement or the batch timestamp is past the dispute window end THEN THE protocol SHALL reject it with result code UNAUTHORIZED_DISPUTANT or DISPUTE_WINDOW_CLOSED."

[req.19]
title = "Perps Module and the Oracle Adapter"
story = "As an autonomous agent operator, I want perpetual markets whose margin lives in real subaccounts and whose prices, funding and liquidations are deterministic integer computations over signed oracle activities, so that outcomes are reproducible byte for byte and bad market data halts a market instead of being guessed."
ac_1 = "WHEN a market creation activity is accepted THE protocol SHALL record the market id, quote asset, contract size, tick size, initial and maintenance margin ratios, funding interval, maximum oracle staleness, price bounds and permitted oracle key set, all as integer parameters, and SHALL reject a duplicate market id with result code MARKET_ALREADY_EXISTS."
ac_2 = "WHEN an order activity is accepted THE protocol SHALL match it against the resting book deterministically in canonical global sequence order under fixed price and sequence priority, and IF the order would require more initial margin than the agent can fund THEN THE protocol SHALL reject it with result code INSUFFICIENT_MARGIN before making any change to the book or to any balance."
ac_3 = "WHEN a position is opened or its margin is increased THE protocol SHALL move the initial or additional margin by a 402LXP transfer from the agent main account to the margin subaccount named for that position, and WHEN margin is released THE protocol SHALL move it back by a 402LXP transfer from the margin subaccount to the agent main account, so that margin is always a real account balance and never a reserved balance column."
ac_4 = "WHERE any price, notional, profit, loss, funding or liquidation value is computed THE protocol SHALL use checked fixed-width integer arithmetic with a single fixed rounding rule that rounds against the trader and in favour of the protocol, SHALL produce identical results across machines and architectures for identical history, and IF any intermediate computation would overflow THEN THE protocol SHALL reject the activity with result code ARITHMETIC_OVERFLOW rather than wrapping, saturating or truncating."
ac_5 = "WHEN an oracle observation is accepted THE protocol SHALL require a signature from a key in the market permitted oracle key set, SHALL order the observation as a signed oracle activity, SHALL commit its exact payload bytes into the replayable history and the batch oracle root, and SHALL apply no balance change as a result of the observation itself."
ac_6 = "IF an oracle observation is older than the configured maximum staleness at the batch timestamp, carries an observation sequence that is not strictly greater than the last accepted one for that market, falls outside the configured price bounds, or exceeds the configured deviation limit from the last accepted price THEN THE protocol SHALL reject the observation and halt the affected market rather than substituting, extrapolating or reusing a last-known price."
ac_7 = "IF no acceptable oracle observation exists for a market within the required freshness window THEN THE protocol SHALL halt that market, and WHILE a market is halted THE protocol SHALL reject new orders, position increases and liquidations for that market with result code MARKET_HALTED, SHALL suspend funding accrual for that market, SHALL permit only order cancellation and margin additions, and SHALL resume the market only when a fresh in-bound observation from a permitted oracle key is accepted."
ac_8 = "WHEN the funding interval elapses according to batch timestamps THE protocol SHALL settle funding as 402LXP transfers between the long and short funding accounts of that market using the deterministic integer funding rate, and SHALL require that the sum of debits equals the sum of credits for the funding settlement so that funding never mints or burns value."
ac_9 = "WHEN a position margin ratio falls below the maintenance margin ratio at the most recently accepted in-bound oracle price THE protocol SHALL liquidate the position as one atomic 402LXP transfer set whose legs move loss from the margin subaccount to the market liquidity pool, the liquidation fee from the margin subaccount to the liquidator and insurance accounts, any deficit cover from the insurance pool to the market liquidity pool, and any remaining margin from the margin subaccount to the agent main account; IF any leg violates an invariant THEN THE protocol SHALL roll back the entire set and leave the position open and unchanged."
ac_10 = "IF a liquidation loss exceeds the available position margin and the insurance pool balance THEN THE protocol SHALL record an explicit deficit against the insurance account, SHALL NOT mint value, SHALL NOT allow any account balance to become negative, and SHALL still express every monetary effect of the perps module and its oracle adapter exclusively as authenticated 402LXP transfers."

[req.20]
title = "Governance Module and Emergency Controls"
story = "As an autonomous agent operator, I want protocol parameters, activation delays, staged rollout and emergency pause to be explicit versioned protocol state changed only by ordered governance activities, so that operators can respond to incidents without any authority ever rewriting settled history or moving value outside the 402LXP transfer law."
ac_1 = "WHEN the protocol executes any activity THE protocol SHALL evaluate it against an explicit versioned parameter set identified by a parameter_version, and the resulting receipt SHALL identify the parameter_version that was in force for that execution."
ac_2 = "WHEN a governance parameter change activity signed by the governance authority is accepted THE protocol SHALL record it as an ordered activity carrying target_module, parameter_key, proposed_value, activation_epoch and rollout_scope, and SHALL keep the current value in force until the activation epoch is reached."
ac_3 = "IF a parameter change activity names an activation_epoch earlier than the current epoch plus the configured minimum activation delay, or names an unknown target_module or parameter_key, or a proposed_value outside the declared bounds for that key THEN THE protocol SHALL reject the activity with a governance validation error and SHALL make no state change."
ac_4 = "WHEN the first batch whose epoch equals a pending activation_epoch is sealed THE protocol SHALL apply the new parameter value atomically at that deterministic boundary and SHALL increment parameter_version, so every replica and guarantor replaying the same history computes the identical resulting_state_root."
ac_5 = "WHERE a parameter change declares a staged rollout THE protocol SHALL apply the new value only to the enumerated stage cohort, identified by module, market or account set, and SHALL leave every other cohort on the prior value until a later stage activation activity is itself accepted and activated."
ac_6 = "WHEN an emergency pause activity is accepted THE protocol SHALL apply the pause at the declared scope of a single module, a single market or the whole network, and SHALL thereafter reject every submitted activity inside that scope with a paused-scope error while leaving all activities outside that scope fully executable."
ac_7 = "WHILE any scope is paused or the network is in emergency mode THE protocol SHALL continue to order, seal, distribute and checkpoint batches, and SHALL continue to serve receipts, inclusion proofs and balance proofs against the last finalised checkpoint so that withdrawal and emergency-exit paths remain usable."
ac_8 = "WHEN emergency mode is entered or exited THE protocol SHALL emit an ordered governance activity recording the trigger, the scope, the entry epoch and the declared exit conditions, and exit SHALL require an explicit signed governance activity rather than elapsed wall-clock time or any implicit background timer."
ac_9 = "IF a governance activity attempts to write a balance, mint or burn supply, alter an issued receipt, rewrite a sealed batch, substitute a state root, or reassign history already covered by a finalised checkpoint THEN THE protocol SHALL reject that activity, because every monetary effect SHALL compile into one or more authenticated 402LXP transfers and settled history is immutable."
ac_10 = "IF a parameter change would alter the outcome of activities already included in a sealed batch THEN THE protocol SHALL reject the retroactive application and SHALL preserve the original versioned transition function for those sequence numbers, so historical replay reproduces the original results and roots exactly."

[req.21]
title = "Activity Batches and the Sequencer"
story = "As an autonomous agent operator, I want the sequencer to seal signed batches with contiguous global sequence numbers and deterministic timestamps that execution reads instead of the system clock, so that every node reconstructs identical state and no activity is lost, duplicated or reordered across crashes and sequencer changes."
ac_1 = "WHEN the sequencer seals a batch THE protocol SHALL produce a BatchHeader containing exactly protocol_version, network_id, epoch, batch_number, first_sequence, last_sequence, previous_state_root, resulting_state_root, activity_merkle_root, receipt_merkle_root, event_merkle_root, data_availability_root, oracle_root, timestamp and sequencer_id, serialised in the canonical binary encoding with no optional, platform-dependent or floating-point fields."
ac_2 = "WHEN a batch is sealed THE protocol SHALL assign contiguous global sequence numbers such that first_sequence equals the previous batch last_sequence plus one, every included activity occupies exactly one global sequence number, and no sequence number is skipped, duplicated or reused across the entire chain history."
ac_3 = "IF a presented batch has a previous_state_root that does not equal the resulting_state_root of batch_number minus one, or a first_sequence that is not contiguous with the prior batch, or a last_sequence less than first_sequence THEN THE protocol SHALL reject the batch, SHALL NOT advance state and SHALL NOT emit receipts for it."
ac_4 = "WHEN executing any activity THE protocol SHALL derive every time-dependent decision, including timestamp_bound checks, grant and authorisation expiry, escrow timeouts, stream accrual, funding intervals and governance activation, solely from the sealed batch timestamp, and SHALL NEVER read the operating-system clock or any external service during state transition evaluation."
ac_5 = "IF a sealed batch timestamp is not strictly greater than the timestamp of the preceding batch, or exceeds the preceding batch timestamp by more than the configured maximum forward drift bound THEN THE protocol SHALL reject the batch as non-deterministic and SHALL NOT treat it as checkpoint-eligible."
ac_6 = "WHEN a batch is sealed THE protocol SHALL sign the canonical serialisation of the complete BatchHeader with the key bound to sequencer_id, and every replica and guarantor SHALL reject a batch whose sequencer signature is absent, malformed, over a different byte string, or produced by a sequencer_id that governance has not authorised for that batch_number."
ac_7 = "WHEN a batch has been sealed and signed THE protocol SHALL distribute the complete batch body, comprising activities, receipts, events, oracle inputs, state-diff material and recovery metadata, to LayerX replicas, and the batch SHALL NOT become checkpoint-eligible until acknowledgements from the configured replica threshold are recorded."
ac_8 = "IF the sequencer signs two different sealed batches carrying the same batch_number, or two different activities at the same global sequence number THEN THE protocol SHALL treat this as sequencer equivocation, SHALL reject the conflicting batch observed later, SHALL halt checkpoint eligibility for that height, and SHALL publish both signed headers as slashable evidence."
ac_9 = "WHEN the sequencer restarts after a crash THE protocol SHALL recover from the append-only activity log at the last durable write boundary, SHALL discard any partially written trailing record, SHALL rebuild every SQLite index projection from that log, and SHALL resume at the next unused global sequence number without re-executing, duplicating or skipping any already-sealed activity."
ac_10 = "IF the active sequencer becomes unavailable THEN THE protocol SHALL stop accepting new activities rather than order them out of band, SHALL keep all sealed batches and finalised checkpoints readable and independently replayable, and SHALL require an explicit governance sequencer-handover activity naming the new sequencer_id and the first batch_number it may seal before any other party seals a batch."

[req.22]
title = "Paxeer Guarantors and Checkpoint Finality"
story = "As an autonomous agent operator, I want a bonded quorum of independent Paxeer guarantors to fully re-download, re-verify, re-execute and re-hash every batch before attesting, so that checkpoint finality carries a real economic guarantee that is described honestly and can later be strengthened with validity proofs."
ac_1 = "WHEN a batch becomes checkpoint-eligible THE protocol SHALL require every guarantor to download the complete batch body, including all activities, receipts, events, oracle inputs, state-diff material and recovery metadata, before performing any other attestation step."
ac_2 = "WHEN a guarantor evaluates a batch THE protocol SHALL require it to verify every actor signature, every session key, capability grant and delegated authority proof, every oracle signature, and the sequencer signature, and the guarantor SHALL withhold attestation if any single signature or authority check fails."
ac_3 = "WHEN a guarantor evaluates a batch THE protocol SHALL require it to independently replay every state transition from previous_state_root using the versioned transition function, the sealed batch timestamp and the parameter_version in force, and to recompute resulting_state_root, activity_merkle_root, receipt_merkle_root, event_merkle_root, oracle_root and data_availability_root without trusting any sequencer-supplied result."
ac_4 = "IF any recomputed root, result_code, fee_charged, effect list or resulting balance differs from the sequencer-published batch by even one byte THEN THE guarantor SHALL withhold its signature and SHALL publish a dissent record naming the batch_number and the first divergent global_sequence."
ac_5 = "WHEN a guarantor signs a checkpoint THE protocol SHALL require that it has already durably stored the availability data it is attesting to, so a guarantor signature is simultaneously a correctness attestation and a possession attestation and neither can be given without the other."
ac_6 = "WHEN attestations over a byte-identical checkpoint certificate from at least the configured guarantor threshold are registered on Paxeer THE protocol SHALL treat that checkpoint as finalised, and receipts covered by it SHALL become augmentable with activity_inclusion_proof, state_inclusion_proof, checkpoint_id, guarantor_certificate and paxeer_settlement_reference."
ac_7 = "IF fewer than the configured threshold of valid attestations are registered before the configured checkpoint deadline THEN THE protocol SHALL leave the checkpoint unfinalised, SHALL settle no withdrawal, deposit credit or dispute against it, and SHALL retain the previous finalised checkpoint as the authoritative settlement anchor."
ac_8 = "WHERE a guarantor bond on Paxeer is below the configured minimum, or the guarantor is under an unresolved slashing action, or has been removed by governance THE protocol SHALL exclude that guarantor signature from the threshold count even if the signature itself is cryptographically valid."
ac_9 = "IF a guarantor signs two conflicting attestations for the same batch height or checkpoint height THEN THE protocol SHALL accept either signed pair submitted by any party as sufficient equivocation evidence on Paxeer, SHALL slash the offending guarantor bond and SHALL remove that guarantor from the active attesting set."
ac_10 = "WHERE the protocol states its security guarantee THE protocol SHALL declare that threshold attestation is a bonded economic guarantee and is NOT a validity proof, and the checkpoint certificate format SHALL reserve space for an optional validity proof so a later protocol version can add one without changing the activity envelope, the receipt format or the batch header semantics."

[req.23]
title = "Data Availability"
story = "As an autonomous agent operator, I want a checkpoint to be finalisable only when guarantors attest possession of the complete underlying data, and I want to retrieve that data and replay it myself to the same roots, so that a state root can never be finalised over history that nobody can produce."
ac_1 = "WHEN a checkpoint is proposed THE protocol SHALL treat a state root alone as insufficient and SHALL require the checkpoint certificate to commit, via data_availability_root, to the complete activity batch, the receipts, the oracle inputs, the state-diff material and the recovery metadata."
ac_2 = "WHEN a guarantor signs a checkpoint THE protocol SHALL require an explicit possession attestation covering all five availability classes named in the certificate, and SHALL reject as invalid any attestation that covers fewer classes or a different data_availability_root."
ac_3 = "WHEN any party requests finalised history by checkpoint_id, batch_number, global_sequence range or activity_id THE protocol SHALL serve the canonical binary bytes over the published retrieval interface, such that re-hashing the served bytes reproduces the committed activity_merkle_root, receipt_merkle_root, event_merkle_root, oracle_root and data_availability_root exactly."
ac_4 = "WHEN an agent replays retrieved history from genesis or from any finalised checkpoint THE protocol SHALL produce byte-identical receipts and state roots to those in the checkpoint certificate, across differing machines, operating systems and CPU architectures, with no index database required as an input."
ac_5 = "WHEN an availability challenge naming a batch_number and a chunk index is issued THE protocol SHALL require the challenged guarantor to return that chunk together with an inclusion proof against the committed data_availability_root within the configured response window."
ac_6 = "IF a bonded guarantor fails to answer a valid availability challenge within the response window THEN THE protocol SHALL record the failure on Paxeer, SHALL make it slashable under the configured availability policy, and SHALL exclude that guarantor from subsequent availability and finality thresholds until it is restored by governance."
ac_7 = "IF the data behind an unfinalised checkpoint becomes unavailable THEN THE protocol SHALL refuse to finalise that checkpoint, SHALL permit no withdrawal, deposit credit or dispute settlement against it, and SHALL keep the last finalised checkpoint as the settlement anchor."
ac_8 = "IF the data behind an already finalised checkpoint becomes unavailable THEN THE protocol SHALL keep that checkpoint valid for emergency exit, SHALL enter emergency data mode, SHALL halt further checkpoint finalisation, and SHALL resume only when availability is restored or governance reconstitutes the guarantor set."
ac_9 = "WHEN availability data is served THE protocol SHALL include the recovery metadata needed to rebuild every SQLite index projection and materialised view from the append-only activity log alone, so that all indexes are treated as rebuildable projections and never as protocol authority."
ac_10 = "IF served bytes do not hash to the committed roots, or an inclusion proof does not verify against data_availability_root THEN THE protocol SHALL treat the response as an availability failure rather than a satisfied request, and SHALL publish the mismatching signed commitment together with the served bytes as evidence."

[req.24]
title = "Bridge Module, Settlement, Withdrawals and Emergency Exits"
story = "As an autonomous agent operator, I want Paxeer contracts to hold custody while understanding almost no LayerX business logic, verifying only finalised certificates, proofs, nullifiers, signatures and challenge windows, so that deposits cannot be credited without proof, withdrawals cannot pay twice, and I can always exit against the last finalised checkpoint even if the sequencer never returns."
ac_1 = "WHEN a Paxeer custody contract processes any LayerX claim THE protocol SHALL require it to verify only a finalised checkpoint certificate, membership or balance proofs against that certificate state root, withdrawal nullifiers, guarantor signatures against the bonded active set, the challenge window state and emergency-exit eligibility, and the contract SHALL NOT interpret perps orders, service agreements, escrow rules or ordinary transfers."
ac_2 = "WHEN assets are deposited into Paxeer custody THE protocol SHALL credit the depositing agent on LayerX only through a bridge deposit activity carrying the finalised Paxeer deposit proof, executed as an authenticated 402LXP transfer from the Paxeer reserve mirror account to the agent main account, and SHALL reject any credit path that does not present that proof."
ac_3 = "IF a deposit proof is replayed, references a different network_id or protocol_version, references a Paxeer transaction not yet finalised, or carries a deposit identifier already consumed THEN THE protocol SHALL reject the bridge deposit activity and SHALL credit no balance a second time."
ac_4 = "WHEN an agent initiates a withdrawal THE protocol SHALL first execute an authenticated 402LXP transfer from the agent account to the Paxeer withdrawals account, so the LayerX debit is committed and checkpointed before any Paxeer payout becomes claimable."
ac_5 = "WHEN a withdrawal is claimed on Paxeer THE protocol SHALL require a membership or balance proof of the withdrawal record against the state root of a finalised checkpoint together with guarantor signatures meeting the configured threshold, and SHALL record a withdrawal nullifier derived deterministically from the withdrawal identity."
ac_6 = "IF a withdrawal claim presents a nullifier that has already been recorded THEN THE protocol SHALL reject the claim and pay nothing, so that no withdrawal can be paid twice through resubmission, an alternative inclusion proof, a later checkpoint, or a reorganisation of the underlying settlement chain."
ac_7 = "WHILE the configured challenge window for a checkpoint remains open THE protocol SHALL hold payouts proved against that checkpoint, and IF a fraud, availability or equivocation challenge against that checkpoint succeeds THEN THE protocol SHALL cancel the pending payouts bound to it and SHALL slash the guarantors that attested to it."
ac_8 = "IF no new checkpoint is finalised within the configured liveness bound, or the network enters emergency mode THEN THE protocol SHALL declare emergency-exit eligibility and SHALL allow any agent to exit directly against the last finalised checkpoint using only that certificate, its state root, a balance proof and the recorded guarantor signatures."
ac_9 = "WHEN an emergency exit is executed THE protocol SHALL require no cooperation from the sequencer, no newly sealed LayerX batch and no ongoing guarantor liveness, so the exit remains executable against the last finalised checkpoint even if the sequencer is permanently gone."
ac_10 = "WHEN any withdrawal or emergency exit settles THE protocol SHALL enforce that Paxeer custody for each asset remains greater than or equal to the sum of LayerX balances for that asset plus outstanding claims, and SHALL reject any payout that would break full reserve reconciliation or make any account negative."

[req.25]
title = "HTTP 402 Payment Gateway"
story = "As an autonomous agent operator, I want a standard HTTP 402 payment handshake that compiles into ordinary 402LXP transfers and produces a receipt my counterparty can verify offline, so that paid machine-to-machine services settle without either side trusting the other or trusting the gateway itself."
ac_1 = "WHEN an agent requests a paid resource and presents no valid proof of payment THE protocol SHALL define the service response as HTTP status 402 carrying an LXP payment requirement whose fields are exactly network_id, recipient, asset, amount, invoice_id, purpose_hash, expiry, acceptable_conditions and service_signature."
ac_2 = "IF a payment requirement omits any of those nine fields, encodes amount as anything other than a non-negative integer in the asset base unit, names a network_id other than the one the agent is transacting on, or carries a service_signature that does not verify against the declared recipient service key THEN THE protocol SHALL define the requirement as invalid and SHALL NOT treat any payment made against it as satisfying the invoice."
ac_3 = "WHEN the SEND settlement model is used THE protocol SHALL require the agent to submit a 402LXP SEND whose to equals the requirement recipient, whose asset and amount equal the requirement asset and amount, whose context_hash equals the requirement purpose_hash, whose expires_at is not later than the requirement expiry, and whose conditions are a subset of acceptable_conditions, and SHALL execute the authenticated transfer and return one signed 402LXP receipt that the agent presents to the service to obtain the resource."
ac_4 = "WHEN the RECEIVE settlement model is used THE protocol SHALL require the service to submit a 402LXP RECEIVE accompanied by a signed payer grant that names the authorized recipient, the asset, the maximum amount, the total or recurring allowance, the expiration, the permitted purpose, the service or invoice identifier and the revocation sequence, and SHALL debit the payer only within those bounds and only for that invoice_id."
ac_5 = "IF a RECEIVE is submitted by an account other than the grant authorized recipient, for an amount above the grant maximum or remaining allowance, after the grant expiration, for a purpose other than the permitted purpose, or against a grant whose revocation sequence has been consumed THEN THE protocol SHALL reject the operation with a deterministic result code, SHALL leave every balance unchanged, and SHALL never allow a recipient to debit an account for which no valid payer grant exists."
ac_6 = "WHEN the same invoice_id and idempotency_key pair is settled more than once, whether by repeated SEND, repeated RECEIVE, or one of each THE protocol SHALL produce at most one economic result and SHALL return the original receipt for every subsequent attempt, so that a retried or replayed HTTP 402 exchange can never move value twice."
ac_7 = "IF settlement is attempted after the requirement expiry, or after the batch timestamp exceeds the SEND expires_at THEN THE protocol SHALL reject the transfer with an expiry result code and SHALL NOT credit the recipient."
ac_8 = "WHERE a service verifies payment offline THE protocol SHALL make a receipt independently verifiable from the receipt bytes alone using only the canonical encoding rules, the sequencer public key and, once checkpointed, the guarantor certificate and inclusion proofs, without any call back to LayerX, and the service SHALL be able to confirm that the receipt asset, amount, recipient, network_id and context_hash match the requirement it issued."
ac_9 = "WHERE the JSON over HTTP gateway is deployed THE protocol SHALL treat it strictly as a convenience surface: consensus behaviour SHALL be defined solely by the canonical binary encoding and the deterministic state machine, and disabling, changing or removing the gateway SHALL NOT alter any accepted outcome, receipt field, result code or state root."
ac_10 = "IF the HTTP 402 gateway is unreachable, returns malformed JSON, or reports an outcome that contradicts the canonical record THEN THE protocol SHALL treat the canonical binary transport and the signed receipt as authoritative, and an agent SHALL be able to complete the identical payment by submitting the same 402LXP SEND directly over the canonical transport."

[req.26]
title = "Fees and Resource Metering"
story = "As an autonomous agent operator, I want fees computed by a deterministic integer function against a ceiling I declare in advance and charged only as an ordinary authenticated transfer, so that my costs are predictable, replay reproduces them exactly, and no module can quietly deduct value outside the single financial doorway."
ac_1 = "WHEN an activity is executed THE protocol SHALL compute its fee from a versioned integer-only function of activity type, canonical encoded byte length, metered execution units and metered storage units, using only checked fixed-width arithmetic and one fixed rounding rule that rounds any fractional base unit upward, and SHALL produce a bit-identical fee on every machine, compiler and architecture."
ac_2 = "WHEN an activity envelope is submitted THE protocol SHALL require a fee_limit field expressed as a non-negative integer in the fee asset base unit, and SHALL reject as malformed any envelope lacking fee_limit or encoding it as a floating point or ambiguous numeric value."
ac_3 = "IF the computed fee exceeds the declared fee_limit THEN THE protocol SHALL reject the activity with the deterministic result code for fee limit exceeded, SHALL apply no module state effect, and SHALL charge no more than the fee_limit."
ac_4 = "WHEN a fee is charged THE protocol SHALL charge it as a 402LXP transfer from the actor main account to the system fee treasury account, SHALL record the amount in the receipt fee_charged field, and SHALL NOT reduce any balance by any mechanism other than that transfer."
ac_5 = "WHEN an activity is admitted THE protocol SHALL require that the actor spendable balance in the fee asset is at least fee_limit at admission time, and SHALL charge the computed fee before applying any module effect so that the fee transfer can never fail because of the activity own spending."
ac_6 = "WHERE an activity fails THE protocol SHALL apply exactly one rejection policy: an activity that fails admission, meaning malformed encoding, wrong protocol_version or network_id, invalid or unauthorized signature, non-next account_sequence, elapsed timestamp_bound, duplicate idempotency_key, or spendable balance below fee_limit, SHALL NOT be assigned a global sequence, SHALL NOT consume the account sequence and SHALL NOT be charged any fee; an activity that is admitted and ordered but then fails execution SHALL consume its account sequence exactly once, SHALL be charged the computed fee up to fee_limit, and SHALL produce a durable failure receipt carrying its result code and zero module effects."
ac_7 = "WHILE an activity executes THE protocol SHALL meter execution units and net state storage bytes, SHALL abort the activity with the deterministic resource exhaustion result code when either metered quantity exceeds its governance parameter ceiling, and SHALL price storage growth at the storage rate parameter in force for that batch."
ac_8 = "IF any step of fee computation, metering accumulation or fee treasury credit would overflow or underflow its fixed-width integer type THEN THE protocol SHALL reject the activity with the arithmetic overflow result code and SHALL NOT wrap, saturate or truncate."
ac_9 = "WHEN a governance activity changes a fee or metering parameter THE protocol SHALL bind the new value to an explicitly named activation epoch, and replay of any batch SHALL use the parameter values in force at that batch epoch so that historical fees remain reproducible."
ac_10 = "WHEN a batch is replayed from the append-only activity log THE protocol SHALL reproduce every fee_charged value, every fee treasury balance and every resulting state root exactly, and SHALL preserve per-asset conservation such that the sum of fee debits equals the sum of fee treasury credits."

[req.27]
title = "Genesis Manifest, Migration and Shadow Replay"
story = "As an autonomous agent operator, I want the new protocol to begin from an explicit reconciled genesis manifest and prove itself against the legacy system before cutover, so that no balance, hold, position or obligation is silently inherited, invented or lost in the transition."
ac_1 = "WHEN a LayerX network is instantiated THE protocol SHALL require an explicit signed genesis manifest declaring protocol_version, network_id, genesis timestamp, the full parameter set, the initial guarantor set with bonds, the complete imported account and module state, and the genesis state root, and SHALL refuse to start if any existing database, index or log is presented in place of that manifest."
ac_2 = "WHEN a genesis import is prepared THE protocol SHALL require the manifest to account separately and explicitly for USDX balances, vault reserves, open holds, queued withdrawals, liquidity pools, insurance pools, open perps positions, pending orders, funding state, DID to EVM bindings, and outstanding receipts with their anchored roots, each as a distinct enumerated section with its own item count and per-asset totals."
ac_3 = "IF any imported value fails to reconcile against Paxeer custody records, or if for any asset the sum of imported account balances, subaccount balances, holds, margin and pool balances is not exactly equal to the Paxeer attested custody amount for that asset THEN THE protocol SHALL reject the entire manifest, SHALL import nothing, and SHALL NOT permit a partial or best-effort import."
ac_4 = "WHEN the manifest is accepted THE protocol SHALL materialise every imported locked value as a real account or subaccount rather than a hidden balance column, SHALL carry outstanding receipts and previously anchored roots forward as immutable historical commitments that are verifiable but not re-executable, and SHALL register the genesis state root on Paxeer as the first checkpoint before any activity is accepted."
ac_5 = "WHILE shadow replay is in progress THE legacy LayerX SHALL remain the sole authority and SHALL be operated read-only with respect to new protocol effects, and the C implementation SHALL consume the identical accepted activity stream without being able to alter legacy state or custody."
ac_6 = "IF shadow replay produces any divergence from the legacy system in accepted or rejected classification, ordering, resulting balance, fee charged, result code or any receipt field THEN THE protocol SHALL block promotion to authority, and SHALL record the divergence together with the activity identifier, the expected value and the produced value."
ac_7 = "WHERE the legacy implementation is used as a behavioural reference THE protocol SHALL NOT adopt PostgreSQL table structures as the protocol definition and SHALL NOT adopt HTTP endpoints as the canonical wire protocol: state layout and wire format SHALL be defined only by the canonical binary encoding and the versioned transition functions."
ac_8 = "WHERE authority and external data are concerned THE protocol SHALL NOT carry over in-memory authentication challenges as authority state, since all authority SHALL derive from ordered signed activities recorded in the append-only log, and SHALL NOT carry over direct Crossverse access inside execution, since external observations SHALL enter only as signed oracle activities whose exact payload becomes part of replayable history."
ac_9 = "WHERE settlement and events are concerned THE protocol SHALL NOT carry over development or test settlement fallbacks, and SHALL fail closed when a settlement path is unavailable rather than simulating success, and SHALL NOT carry over process-local server-sent-event streams as the authoritative event mechanism, since the authoritative events SHALL be those committed under the batch event merkle root."
ac_10 = "WHERE timing is concerned THE protocol SHALL NOT carry over implicit background timers, cron intervals or wall-clock reads as consensus behaviour: every time-dependent transition, including expiry, funding, streaming accrual and hold timeout, SHALL be driven only by the deterministic timestamp supplied in the batch header, so that replay at any later date yields the identical outcome."

[req.28]
title = "Qualification and Conformance"
story = "As an autonomous agent operator, I want an explicit and exhaustive qualification gate that the implementation must pass before it is called ready, so that determinism, safety, custody reconciliation and interoperability are demonstrated by evidence rather than asserted."
ac_1 = "WHEN the same activity history is replayed on different machines, operating systems, compilers, optimisation levels and CPU architectures including at least one 64-bit and one non-x86 target THE protocol SHALL produce byte-identical activity encodings, receipts, batch headers and state roots, and the qualification SHALL fail on any single differing byte."
ac_2 = "WHEN a corpus of at least ten million activities spanning every activity type is replayed THE protocol SHALL produce a deterministic terminal state root that matches the independently recorded root, and SHALL produce identical intermediate roots at every batch boundary."
ac_3 = "WHEN fault injection aborts the process at every distinct durable write boundary in the append-only log, index and checkpoint store THE protocol SHALL, on restart, recover to a consistent state that either fully contains or fully excludes each activity, SHALL never expose a partially applied transfer set, and SHALL resume the global sequence without gap, duplication or reordering."
ac_4 = "WHEN network partition, replica isolation, message reordering, message loss and prolonged sequencer unavailability are simulated THE protocol SHALL never finalise conflicting state for the same sequence range, SHALL halt rather than fork when the active sequencer is lost, and SHALL resume from the last checkpointed root once connectivity is restored."
ac_5 = "WHEN malformed, truncated, over-long, deeply nested, non-canonically encoded and randomly mutated activity bytes are fuzzed against the decoder and the state machine THE protocol SHALL reject every non-canonical input with a deterministic result code, SHALL NOT crash, hang, read out of bounds or allocate unbounded memory, and SHALL leave state unchanged."
ac_6 = "WHEN signature fields are fuzzed, including malleated, truncated, all-zero, wrong-curve, wrong-domain-separator and replayed-from-another-network signatures THE protocol SHALL reject every one of them, SHALL accept no activity whose signature does not verify over the exact canonical bytes under the correct domain separation, and SHALL treat a signature valid on another network_id as invalid."
ac_7 = "WHEN the arithmetic layer is qualified THE protocol SHALL demonstrate by exhaustive boundary testing and machine-checked proof over the 128-bit and 256-bit routines that every addition, subtraction, multiplication and division either returns an exact checked result or a deterministic overflow error, and that every rounding site rounds in the single specified direction with no floating point anywhere in consensus-critical code."
ac_8 = "WHEN guarantors are made to disagree, including a guarantor that replays to a different root, one that refuses to sign, and one that signs two conflicting checkpoints for the same batch number THE protocol SHALL withhold finality below the signing threshold, SHALL produce a portable equivocation proof for the double signer, and SHALL make that proof sufficient for slashing on Paxeer without any trusted intermediary."
ac_9 = "WHEN data unavailability is simulated by withholding the activity batch, receipts, oracle inputs, state-diff material or recovery metadata from guarantors THE protocol SHALL leave the affected checkpoint unfinalizable, SHALL NOT allow an availability attestation to be signed for data a guarantor does not hold, and SHALL allow agents to detect the unavailability from public information."
ac_10 = "WHEN emergency exit is exercised end to end on Paxeer against a live custody deployment THE protocol SHALL allow an agent to withdraw its proven balance using only a finalized checkpoint certificate, a membership or balance proof against its state root and an unspent withdrawal nullifier, with the LayerX sequencer offline and uncooperative for the entire test."
ac_11 = "WHEN full reserve reconciliation is run THE protocol SHALL show for every asset that the sum of all agent accounts, subaccounts, escrow, budget, margin, liquidity, insurance, fee treasury and withdrawal accounts equals the Paxeer custody balance exactly, with zero tolerance, and SHALL show that every historical batch preserved the invariant that debits equal credits."
ac_12 = "WHEN shadow comparison against the existing LayerX is run over a continuous production activity stream THE protocol SHALL reproduce the legacy accepted outcomes for every activity in the comparison window, and any divergence SHALL block qualification until it is explained and either fixed or recorded as an intentional specified difference."
ac_13 = "WHEN a canary is run with real agents and real value under a bounded exposure limit THE protocol SHALL operate for the declared canary period with no reserve discrepancy, no unrecoverable crash, no missed checkpoint deadline and no receipt that fails independent verification, before general availability is declared."
ac_14 = "WHEN the C API conformance suite is executed by an independent party THE protocol SHALL satisfy every documented contract of the public headers, including return codes, ownership and lifetime rules, thread-safety declarations, and the rule that the single balance mutation primitive is the only path that writes a balance, with any direct balance write detected and failed."
ac_15 = "WHEN the wire-protocol conformance suite is executed by an independently written implementation THE protocol SHALL accept every valid canonical vector, reject every listed non-canonical and adversarial vector, and produce the exact expected encodings, identifiers, merkle roots and receipt bytes for every published test vector."
ac_16 = "WHEN the release build is qualified THE protocol SHALL build and pass the entire test, fuzz and replay corpus clean under address, undefined-behaviour, thread and memory sanitizers and under a leak checker, with zero reported errors, zero leaked allocations and zero data races, and SHALL fail qualification if any sanitizer is disabled or any finding is suppressed without a recorded justification."

[req.29]
title = "Paxeer Contract Systems, Deployment and Migration Safety"
story = "As a custodian and protocol operator, I want the Paxeer settlement contracts to have a complete reusable safety substrate, deterministic deployment manifest and fail-closed migration system, so that LayerX can custody institutional-scale value without relying on thin one-off contracts or an undocumented upgrade ceremony."
ac_1 = "THE contract system SHALL provide checked arithmetic, canonical constants and fixed-width protocol types for every value crossing the LayerX to Paxeer boundary, SHALL reject narrowing conversions that lose information, and SHALL define no duplicate constant with a divergent value."
ac_2 = "THE contract system SHALL provide bounded byte readers, canonical encoding, domain-separated hashing, cryptographic primitives and index-aware Merkle proof verification, and SHALL reject malformed lengths, out-of-range reads, invalid proof depths and non-canonical encodings without returning a plausible value."
ac_3 = "WHEN a contract performs CALL, STATICCALL or value transfer THE contract system SHALL bound copied return data, preserve an exact success or failure classification, reject calls to an account without deployed code where code is required, and prevent unbounded revert data from exhausting the caller."
ac_4 = "WHEN an entry point requires a direct codeless origin THE contract system SHALL use an EIP-7702-aware caller rule rather than extcodesize alone, SHALL distinguish msg.sender from tx.origin explicitly, and SHALL test constructor, contract, delegated-account and direct-origin cases."
ac_5 = "THE contract system SHALL make delegate-call assumptions explicit: implementation-only functions SHALL reject delegated execution, proxy-only functions SHALL reject direct execution, and every non-upgradeable LayerX settlement contract SHALL expose an unambiguous rejection of UUPS upgrade entry points rather than accidentally appearing upgradeable."
ac_6 = "THE contract system SHALL centralise namespaced persistent storage slots and transient execution context, SHALL reject a zero or reserved slot identifier, SHALL clear transient authority at the end of the call, and SHALL provide a persistent fallback when the target Paxeer EVM revision does not support transient storage."
ac_7 = "THE deployment configuration SHALL bind chain_id, protocol_version, release semantic version, governance timelock, emergency council, genesis state root, challenge and liveness parameters, asset definitions and enabled feature bits into one canonical static-config hash, and every deployed contract SHALL be validated against that hash."
ac_8 = "WHEN a LayerX settlement suite is deployed THE system SHALL derive deterministic CREATE2 addresses from a versioned blueprint hash, SHALL publish the exhaustive predeploy and preinstall manifest, SHALL validate runtime code hashes and constructor-bound immutable configuration at every address, and SHALL reject an occupied address whose code hash differs."
ac_9 = "THE contracts manager SHALL stage a migration as an immutable commitment to source release, target release, static-config hash, ordered operation root, earliest execution time and expiry, SHALL validate the entire operation set before the first mutation, SHALL execute operations in their committed order, and SHALL make a partial or replayed migration impossible."
ac_10 = "WHEN a migration is proposed or executed THE validator SHALL enforce strictly increasing semantic versions, compatible protocol and storage-layout versions, exact target and selector allowlists, code-hash expectations before and after each operation, bounded call value and calldata, timelock maturity and expiry, and SHALL reject an emergency path that can install arbitrary code or bypass the manager."
ac_11 = "THE contract system SHALL define a canonical cross-domain message envelope and message type enumeration binding source chain, destination chain, source sender, destination target, nonce, value, gas limit and payload hash, and SHALL make every message or signature permanently invalid on a different chain, manager or protocol version."
ac_12 = "WHEN token amounts cross decimal domains THE contract system SHALL convert only through checked powers of ten, SHALL expose the exact remainder for down-scaling, SHALL never round a custody credit upward, and SHALL reject unsupported decimals or a non-zero remainder where exact conversion is required."
ac_13 = "THE compatibility layer SHALL fail closed on foreign chain assumptions: Arbitrum address aliasing and precompiles, OP Stack predeploy addresses, gas-token hooks and any other absent facility SHALL be represented as unsupported feature checks rather than silently called or copied into Paxeer logic."
ac_14 = "THE manager container SHALL enumerate every critical LayerX contract, its role, expected interface identifier, runtime code hash, configuration hash and release version, SHALL reject duplicate roles or addresses, and SHALL provide one machine-verifiable system manifest for deployment, monitoring and incident response."
ac_15 = "WHEN contract qualification runs THE suite SHALL include unit, fuzz, invariant and negative-path tests for every primitive, deployment collision, wrong code hash, storage-layout mismatch, semantic-version downgrade, migration replay, partial execution, malicious return data, decimal dust, cross-chain replay, direct implementation call and delegated non-upgradeable call, and SHALL fail on any untested external state-changing function."
ac_16 = "THE production contract set SHALL use custom errors with stable selectors, explicit event commitments for every governance or custody transition, CEI and reentrancy protection on every external-value path, checksummed deterministic build settings, and no selfdestruct, unrestricted delegatecall, tx.origin authorization, arbitrary target execution or operator-only balance mutation."

# --------------------------------------------------------------------------
# TASKS
# --------------------------------------------------------------------------

[task.1]
section = "Wave 0 — Foundation"
title = "Scaffold the repository, the C17 build system and the protocol constant layer"
status = "pending"

[task.1.1]
title = "Create the source tree and the strict C17 build"
status = "done"
wave = 0
do_1 = "Create the directory tree from the design source tree: include/layerx, src/protocol, src/codec, src/crypto, src/state, src/storage, src/network, src/sequencer, src/replica, src/guarantor, src/paxeer, src/modules, cmd, contracts, migrations, tests, fuzz and tools."
do_2 = "Write a root Makefile compiling every translation unit with -std=c17 -pedantic -Werror -Wall -Wextra -Wconversion -Wshadow -Wvla -fno-strict-aliasing -ffp-contract=off, and linking no C++, Go, Rust or JavaScript object into the runtime."
do_3 = "Add a make build target producing build/liblayerx.a with per-area object directories, plus make clean removing build entirely."
do_4 = "Add include/layerx/lxp_build.h and src/protocol/lxp_build_info.c exposing lxp_build_info with compiler id, target triple, optimisation level and revision, none of which may ever be read during transition evaluation."
do_5 = "Prove reproducibility by building twice into different output directories and comparing the archive byte for byte."
verify_cmd = "make build"
symbols = ["lxp_build_info","lxp_build_target_triple","lxp_build_compiler_id"]
touches = ["Makefile","include/layerx/lxp_build.h","src/protocol/lxp_build_info.c"]
reqs = ["5.1","28.1","28.16"]

[task.1.2]
title = "Define the shared result and error code enumeration"
status = "done"
wave = 0
requires = ["1.1"]
do_1 = "Add include/layerx/lxp_result.h defining lxp_result as int32_t with LXP_OK equal to zero and every failure code negative."
do_2 = "Partition the negative space by domain exactly as the design fixes it: codec -1 to -99, envelope -100 to -199, identity and authority -200 to -299, sequencing -300 to -399, ledger -400 to -499, arithmetic -500 to -599, fees and metering -600 to -699, module semantics -700 to -799, batch and checkpoint -800 to -899, storage and recovery -900 to -999, fatal below -1000."
do_3 = "Implement lxp_result_name and lxp_result_domain in src/protocol/lxp_result.c as total non-allocating functions, plus lxp_result_is_fatal for the codes that halt the node instead of propagating."
do_4 = "Test that every declared code is unique, sits inside its declared domain range and has a name, so renumbering breaks the build rather than silently changing a receipt field."
do_5 = "Document in the header that a result code is part of the receipt and therefore part of consensus, so renumbering is a protocol version change and never a refactor."
verify_cmd = "make test-result"
symbols = ["lxp_result","lxp_result_name","lxp_result_domain","lxp_result_is_fatal"]
touches = ["include/layerx/lxp_result.h","src/protocol/lxp_result.c","tests/protocol/lxp_test_result.c"]
reqs = ["2.9","5.3","26.6","13.16"]

[task.1.3]
title = "Fix the protocol version, network id, length limits and domain tags"
status = "done"
wave = 0
requires = ["1.1","1.2"]
do_1 = "Add include/layerx/lxp_protocol.h declaring the protocol version as a uint16 and the network id as a uint32 supplied by node configuration and never inferred."
do_2 = "Declare every variable-length limit as a named constant: maximum activity bytes, payload bytes, DID length, authority chain depth, transfer set leg count, effect count and batch activity count."
do_3 = "Declare the domain tag table with one distinct tag per hashing purpose, covering activity id, payload hash, signature preimage, authority hash, context hash, merkle leaf, merkle internal node, batch header, receipt and checkpoint certificate."
do_4 = "Implement lxp_protocol_version_supported and lxp_network_id_matches so an envelope valid on one network is permanently invalid on every other network."
do_5 = "Test that no two domain tags are equal or a prefix of one another, and that every declared limit is non-zero and fits its wire type."
verify_cmd = "make test-protocol"
symbols = ["lxp_protocol_version_supported","lxp_network_id_matches","lxp_domain_tag","lxp_domain_tag_id"]
touches = ["include/layerx/lxp_protocol.h","src/protocol/lxp_protocol.c","tests/protocol/lxp_test_protocol.c"]
reqs = ["2.2","6.3","6.4","6.6"]

[task.1.4]
title = "Implement the deterministic arena allocator"
status = "done"
wave = 0
requires = ["1.1","1.2"]
do_1 = "Add include/layerx/lxp_arena.h and src/protocol/lxp_arena.c providing bump-pointer allocation from a caller-supplied fixed buffer with explicit alignment and no malloc on any execution path."
do_2 = "Implement lxp_arena_init, lxp_arena_alloc, lxp_arena_mark and lxp_arena_reset, returning the arena-exhausted result code rather than growing when a request exceeds the buffer."
do_3 = "Guarantee no observable address dependence: never hash, order or branch on a returned pointer, and zero every reset region so reused memory is never read as uninitialised state."
do_4 = "Test that two arenas over different base addresses produce identical allocation sequences and identical byte contents."
do_5 = "Run the arena suite under AddressSanitizer with redzone poisoning so a single-byte overrun fails the run."
verify_cmd = "make test-arena"
symbols = ["lxp_arena","lxp_arena_init","lxp_arena_alloc","lxp_arena_mark","lxp_arena_reset"]
touches = ["include/layerx/lxp_arena.h","src/protocol/lxp_arena.c","tests/protocol/lxp_test_arena.c"]
reqs = ["5.8","28.5","28.16"]

[task.1.5]
title = "Build the zero-dependency C test harness and the CI entry point"
status = "done"
wave = 0
requires = ["1.2"]
do_1 = "Add tests/lxp_test_harness.h and tests/lxp_test_harness.c providing lxp_test_register, lxp_test_run_all and assertion helpers for integers, byte buffers and result codes, with no external test framework."
do_2 = "Print expected and produced bytes in hex with file and line on failure, and return a non-zero process exit status so CI fails on any assertion."
do_3 = "Add a make test target that builds and runs every registered suite, and a make list-tests target that prints the registry."
do_4 = "Land one no-op suite so make test passes on an otherwise empty tree and CI has a green baseline to regress from."
do_5 = "Add tools/ci/run-tests.sh running make test for each build variant and aggregating exit codes without swallowing a failure."
verify_cmd = "make test"
symbols = ["lxp_test_register","lxp_test_run_all","lxp_test_assert_bytes_eq","lxp_test_assert_result"]
touches = ["tests/lxp_test_harness.h","tests/lxp_test_harness.c","tools/ci/run-tests.sh","Makefile"]
reqs = ["28.16","28.1","5.1"]

[task.1.6]
title = "Add the sanitizer build variants and the determinism CI gates"
status = "done"
wave = 0
requires = ["1.3","1.5"]
do_1 = "Add asan, ubsan and tsan variants through tools/build/sanitizers.mk with the sanitize flags, frame pointers retained and halt-on-error set, exposed as make build-asan, make build-ubsan and make build-tsan."
do_2 = "Add make test-sanitizers running the full suite under each variant and failing on any sanitizer report, any leaked allocation and any data race."
do_3 = "Add tools/ci/no-float-scan.sh rejecting float, double, long double and the math header under src/protocol, src/codec, src/state, src/storage and src/modules, and scanning consensus objects for floating-point instructions."
do_4 = "Add tools/ci/symbol-allowlist.sh rejecting time, clock_gettime, gettimeofday, rand, random, getenv, getpid, gethostname and stat in execution translation units."
do_5 = "Wire both scanners and make test-sanitizers into make ci so a violation fails the build instead of producing a warning to triage later."
verify_cmd = "make ci"
symbols = ["lxp_sanitizer_smoke","lxp_ci_gate_report"]
touches = ["tools/build/sanitizers.mk","tools/ci/no-float-scan.sh","tools/ci/symbol-allowlist.sh","tests/protocol/lxp_test_sanitizer_smoke.c","Makefile"]
reqs = ["5.2","5.8","28.7","28.16"]

[task.2]
section = "Waves 1-2 — Deterministic Primitives, State and Storage"
title = "Implement the canonical binary codec"
status = "pending"

[task.2.1]
title = "Implement the canonical primitive encoder and decoder"
status = "done"
wave = 1
requires = ["1.1","1.2","1.4"]
do_1 = "Add include/layerx/lxp_codec.h and src/codec/lxp_codec.c with a writer over a caller arena and a reader over a borrowed byte span, both total and non-allocating."
do_2 = "Encode every integer fixed-width big-endian with no varint and therefore no length-of-length ambiguity: u8, u16, u32, u64, u128 as 16 bytes and i32 as 4 bytes in twos-complement form."
do_3 = "Reject any short read, any negative remaining length and any read past the span end with the truncated result code, never reading out of bounds."
do_4 = "Guarantee re-encoding a decoded primitive reproduces the original bytes exactly, asserted over the boundary set zero, one, max minus one and max for every width."
do_5 = "Forbid presence bitmaps and optional fields: absence is an explicit zero or an explicit tagged none, so there is no presence encoding to disagree about."
verify_cmd = "make test-codec"
symbols = ["lxp_codec_writer","lxp_codec_reader","lxp_codec_write_u64","lxp_codec_read_u64","lxp_codec_write_u128","lxp_codec_read_u128"]
touches = ["include/layerx/lxp_codec.h","src/codec/lxp_codec.c","tests/codec/lxp_test_codec_primitives.c"]
reqs = ["6.1","6.2","2.10"]

[task.2.2]
title = "Encode byte strings, tagged unions and ordered sequences under declared limits"
status = "done"
wave = 1
requires = ["1.3","2.1"]
do_1 = "Encode byte strings and text as a u32 big-endian length followed by raw bytes, with text required to be UTF-8 and NFC-normalised at admission."
do_2 = "Encode every collection as a sequence with a declared sort key and reject an unsorted or duplicate-keyed sequence with the unsorted result code rather than normalising it, because maps and sets do not exist on the wire."
do_3 = "Encode tagged unions as a one-byte tag plus tag body and reject an unknown tag deterministically instead of skipping the body."
do_4 = "Enforce every length limit from the protocol header at decode time before allocating, so an over-long element is rejected rather than accommodated."
do_5 = "Test that iteration order is byte-lexicographic and independent of insertion order, hash seed, pointer value and allocation order."
verify_cmd = "make test-codec-limits"
symbols = ["lxp_codec_write_bytes","lxp_codec_read_bytes","lxp_codec_write_seq","lxp_codec_seq_check_sorted","lxp_codec_write_tag"]
touches = ["src/codec/lxp_codec.c","include/layerx/lxp_codec.h","tests/codec/lxp_test_codec_composite.c"]
reqs = ["6.2","6.3","5.4"]

[task.2.3]
title = "Enforce version tagging, unknown fields and trailing byte rejection"
status = "done"
wave = 1
requires = ["2.1","2.2"]
do_1 = "Prefix every consensus-critical structure with its protocol version and structure tag, and reject an unknown version or tag with the unsupported-version result code."
do_2 = "Reject an unknown field identifier in a consensus-critical structure rather than skipping, ignoring or best-effort interpreting it."
do_3 = "Implement lxp_codec_finish returning the trailing-bytes result code whenever the decoder consumed fewer than all input bytes."
do_4 = "Reject non-minimal integer encodings, indefinite-length items, duplicate keys, unused padding and any second byte string that decodes to an already-representable logical value."
do_5 = "Assert that a decode rejection happens before authority evaluation, so it consumes no account sequence and charges no fee."
verify_cmd = "make test-codec-version"
symbols = ["lxp_codec_write_struct_header","lxp_codec_read_struct_header","lxp_codec_finish","lxp_codec_reject_unknown_field"]
touches = ["src/codec/lxp_codec.c","include/layerx/lxp_codec.h","tests/codec/lxp_test_codec_strict.c"]
reqs = ["6.2","6.6","6.9","2.10"]

[task.2.4]
title = "Publish the round-trip and rejection conformance corpora"
status = "done"
wave = 1
requires = ["2.3"]
do_1 = "Add tests/vectors/codec holding a valid corpus of canonical byte strings with their expected decoded values and expected digests."
do_2 = "Add an adversarial corpus covering non-minimal integers, unsorted sequences, duplicate keys, over-long lengths, truncation, trailing bytes and unknown tags, each carrying its expected result code."
do_3 = "Implement lxp_codec_vector_run walking both corpora and asserting encode, decode, re-encode equality and the exact expected result code for every case."
do_4 = "Declare these corpora the published wire conformance vectors that an independently written implementation must reproduce byte for byte."
do_5 = "Fail the suite when any vector lacks an expected result code, so a vector cannot be added without declaring its outcome."
verify_cmd = "make test-codec-vectors"
symbols = ["lxp_codec_vector_run","lxp_codec_vector_load","lxp_codec_vector_case"]
touches = ["tests/codec/lxp_test_codec_vectors.c","tests/vectors/codec/valid.lxv","tests/vectors/codec/adversarial.lxv"]
reqs = ["6.1","6.10","28.5","28.15"]

[task.2.5]
title = "Ship the codec fuzz targets"
status = "done"
wave = 1
requires = ["2.3"]
do_1 = "Add fuzz/lxp_fuzz_codec.c with a decode totality target asserting the decoder never crashes, hangs, reads out of bounds or allocates unbounded memory on arbitrary bytes."
do_2 = "Add a round-trip target asserting that encode of decode reproduces the input exactly for every input the decoder accepts."
do_3 = "Seed the corpus from the valid and adversarial vectors and keep every discovered crasher as a permanent regression input."
do_4 = "Add make fuzz-codec-smoke running a bounded iteration count under AddressSanitizer and UndefinedBehaviorSanitizer for CI."
do_5 = "Fail the target when a decoded value re-encodes to different bytes, because that is a second valid form of one logical value."
verify_cmd = "make fuzz-codec-smoke"
symbols = ["lxp_fuzz_codec_decode","lxp_fuzz_codec_roundtrip"]
touches = ["fuzz/lxp_fuzz_codec.c","fuzz/corpus/codec/seed.bin","Makefile"]
reqs = ["6.1","6.2","28.5"]

[task.3]
title = "Implement signatures, domain-separated hashing and Merkle commitments"
status = "pending"

[task.3.1]
title = "Implement domain-separated hashing"
status = "done"
wave = 1
requires = ["1.1","1.3"]
do_1 = "Add src/crypto/lxp_hash.c implementing SHA-256 in portable C17 with no assembly dependency and a streaming init, update and final interface."
do_2 = "Implement lxp_hash_domain prefixing every input with the distinct tag for its purpose, so identical bytes hashed under two purposes yield different digests."
do_3 = "Provide the purpose helpers for activity id, payload hash, signature preimage, authority hash, context hash and account id derivation, each bound to exactly one tag."
do_4 = "Test against the published SHA-256 vectors plus a cross-domain separation test asserting no two purposes collide on the same input."
do_5 = "Assert digests are byte-identical across optimisation levels and on a big-endian target build."
verify_cmd = "make test-crypto-hash"
symbols = ["lxp_hash_init","lxp_hash_update","lxp_hash_final","lxp_hash_domain","lxp_hash_sha256"]
touches = ["src/crypto/lxp_hash.c","include/layerx/lxp_hash.h","tests/crypto/lxp_test_hash.c"]
reqs = ["6.4","6.9","10.3"]

[task.3.2]
title = "Implement Ed25519 verification for agent identities"
status = "done"
wave = 1
requires = ["1.4","3.1"]
do_1 = "Add src/crypto/lxp_ed25519.c verifying in constant time over the domain-separated canonical byte string of the signed structure and never over a re-serialized, JSON or textual rendering."
do_2 = "Reject non-canonical encodings: non-canonical scalar forms, small-order and non-canonical public keys, and every malleable variant."
do_3 = "Implement lxp_ed25519_verify_batch for the verification worker pool, returning one boolean per input and never partial credit for a batch containing a failure."
do_4 = "Test against the published Ed25519 vectors plus the malleability edge cases, and assert a signature valid under one network id fails under another."
do_5 = "Add fuzz/lxp_fuzz_signature.c mutating signature bytes, keys and preimages and asserting every mutation is rejected without a crash."
verify_cmd = "make test-crypto-ed25519"
symbols = ["lxp_ed25519_verify","lxp_ed25519_verify_batch","lxp_ed25519_pubkey_is_canonical"]
touches = ["src/crypto/lxp_ed25519.c","include/layerx/lxp_crypto.h","tests/crypto/lxp_test_ed25519.c","fuzz/lxp_fuzz_signature.c"]
reqs = ["2.4","6.8","28.6"]

[task.3.3]
title = "Implement secp256k1 verification for Paxeer-facing certificates"
status = "done"
wave = 1
requires = ["3.1"]
do_1 = "Add src/crypto/lxp_secp256k1.c verifying ECDSA over the domain-separated canonical encoding of the certificate structure."
do_2 = "Reject high-S malleable forms, non-minimal encodings, wrong-curve points and any recovery identifier outside its declared range."
do_3 = "Provide address recovery solely for the optional EVM payout binding statement, and grant a recovered address no authority to move, debit or authorise any LayerX balance."
do_4 = "Test against the published secp256k1 vectors plus a low-S normalisation set and a wrong-domain-separator rejection set."
do_5 = "Keep secp256k1 out of the agent activity path entirely, so an Ed25519 agent signature can never be satisfied by a secp256k1 key."
verify_cmd = "make test-crypto-secp256k1"
symbols = ["lxp_secp256k1_verify","lxp_secp256k1_sig_is_low_s","lxp_secp256k1_recover_address"]
touches = ["src/crypto/lxp_secp256k1.c","include/layerx/lxp_crypto.h","tests/crypto/lxp_test_secp256k1.c"]
reqs = ["3.9","6.8","28.6"]

[task.3.4]
title = "Implement the Merkle tree for activity, receipt, event and state roots"
status = "done"
wave = 1
requires = ["2.2","3.1"]
do_1 = "Add src/crypto/lxp_merkle.c hashing leaves under one domain tag and internal nodes under a different one, so an internal node can never be reinterpreted as a leaf."
do_2 = "Specify and implement a single deterministic rule for leaf counts that are not a power of two, and reject any tree built under a different rule."
do_3 = "Implement lxp_merkle_root over a sorted leaf sequence, refusing to build from an unsorted or duplicate-keyed input."
do_4 = "Provide the builders the batch header needs for activity, receipt, event and oracle leaves so all four share exactly one implementation."
do_5 = "Test that the empty tree, the single-leaf tree and an odd-leaf-count tree each have exactly one specified root, byte-identical across architectures."
verify_cmd = "make test-merkle"
symbols = ["lxp_merkle_root","lxp_merkle_build","lxp_merkle_leaf_hash","lxp_merkle_node_hash"]
touches = ["src/crypto/lxp_merkle.c","include/layerx/lxp_merkle.h","tests/crypto/lxp_test_merkle.c"]
reqs = ["2.9","6.4","6.5"]

[task.3.5]
title = "Generate and verify Merkle inclusion proofs"
status = "done"
wave = 1
requires = ["3.4"]
do_1 = "Implement lxp_merkle_proof_generate emitting the sibling path and the leaf index in canonical encoding under a declared maximum depth."
do_2 = "Implement lxp_merkle_proof_verify recomputing the root from the leaf and path and comparing it in constant time against the expected root."
do_3 = "Reject a proof that reinterprets an internal node as a leaf, that exceeds the declared depth, or whose index disagrees with the path length."
do_4 = "Test that every leaf of trees sized one through one thousand yields a verifying proof, and that every single-bit mutation of a proof fails."
do_5 = "Expose the verifier so a third party can check a receipt inclusion proof against the committed activity root from the receipt bytes alone."
verify_cmd = "make test-merkle-proof"
symbols = ["lxp_merkle_proof","lxp_merkle_proof_generate","lxp_merkle_proof_verify"]
touches = ["src/crypto/lxp_merkle.c","include/layerx/lxp_merkle.h","tests/crypto/lxp_test_merkle_proof.c"]
reqs = ["6.5","12.8","23.3"]

[task.3.6]
title = "Provide constant-time comparison and key hygiene"
status = "done"
wave = 1
requires = ["3.2","3.5"]
do_1 = "Implement lxp_ct_memcmp with no data-dependent branch or memory access and lxp_secure_zero that the compiler may not elide."
do_2 = "Route every digest, signature, key and nullifier comparison in consensus code through lxp_ct_memcmp and add a CI check rejecting plain memcmp on those types."
do_3 = "Zero every stack buffer holding key or preimage material before return."
do_4 = "Add a timing-variance test asserting comparison cost is independent of the position of the first differing byte."
do_5 = "Run the whole crypto suite under the address, undefined-behaviour and thread sanitizers with zero findings."
verify_cmd = "make test-crypto-ct"
symbols = ["lxp_ct_memcmp","lxp_ct_is_zero","lxp_secure_zero"]
touches = ["src/crypto/lxp_ct.c","include/layerx/lxp_crypto.h","tests/crypto/lxp_test_ct.c","tools/ci/symbol-allowlist.sh"]
reqs = ["6.8","28.6","28.16"]

[task.4]
title = "Implement checked fixed-width integer arithmetic"
status = "pending"

[task.4.1]
title = "Implement the 128-bit unsigned type with checked addition and subtraction"
status = "done"
wave = 1
requires = ["1.1","1.2"]
do_1 = "Add include/layerx/lxp_u128.h defining lxp_u128 as a struct of hi and lo uint64 words, so bare plus, minus and multiply do not compile on an amount."
do_2 = "Implement lxp_u128_add returning the overflow result code and lxp_u128_sub returning the underflow result code, never wrapping, saturating or truncating."
do_3 = "Implement lxp_u128_cmp, lxp_u128_is_zero and the 16-byte big-endian conversions the codec uses for amounts and fee limits."
do_4 = "Prove correctness by exhaustive boundary tests at zero, one, max minus one, max and every carry and borrow boundary of the low word."
do_5 = "Assert under UndefinedBehaviorSanitizer that no signed overflow and no undefined shift exists anywhere in the amount path."
verify_cmd = "make test-arith-u128"
symbols = ["lxp_u128","lxp_u128_add","lxp_u128_sub","lxp_u128_cmp","lxp_u128_is_zero"]
touches = ["include/layerx/lxp_u128.h","src/protocol/lxp_u128.c","tests/arith/lxp_test_u128.c"]
reqs = ["5.2","5.3","28.7"]

[task.4.2]
title = "Implement the 256-bit widening multiply and floor division"
status = "done"
wave = 1
requires = ["4.1"]
do_1 = "Define lxp_u256 as four uint64 words and implement lxp_u128_mul widening to 256 bits, so the multiply-then-divide shape of every ratio computation never loses precision to an intermediate overflow."
do_2 = "Implement lxp_u256_div_floor returning quotient and remainder with the divide-by-zero result code, and lxp_u256_add for the conservation accumulators."
do_3 = "Specify that integer division floors and that every residue is explicitly returned to the caller rather than dropped, so dust is never implicit."
do_4 = "Return the overflow result code when a 256-bit quotient does not fit the requested 128-bit output, rather than truncating it."
do_5 = "Test the full carry chain across all four words and the divisor boundaries one, two to the sixty-fourth power and max."
verify_cmd = "make test-arith-u256"
symbols = ["lxp_u256","lxp_u128_mul","lxp_u256_add","lxp_u256_div_floor"]
touches = ["include/layerx/lxp_u128.h","src/protocol/lxp_u256.c","tests/arith/lxp_test_u256.c"]
reqs = ["5.2","5.3","28.7"]

[task.4.3]
title = "Implement signed magnitude arithmetic and the fixed rounding rules"
status = "done"
wave = 1
requires = ["4.2"]
do_1 = "Define lxp_i128 as a sign-magnitude struct so negativity is explicit, while balances stay unsigned and negativity therefore stays unrepresentable."
do_2 = "Implement lxp_i128_add and lxp_i128_sub with checked magnitude arithmetic and a canonical zero that is never a negative zero."
do_3 = "Express every ratio as uint32 basis points where ten thousand is exactly unity, with lxp_u128_mul_bps_floor and lxp_u128_mul_bps_ceil as the only ratio entry points."
do_4 = "Implement lxp_u128_mul_div_floor as the multiply-then-divide primitive that every price, fee, funding and accrual computation must call."
do_5 = "Fix the rounding direction per site so fees round up and payouts round down, and test that rounding never creates or destroys a unit of any asset."
verify_cmd = "make test-arith-rounding"
symbols = ["lxp_i128","lxp_i128_add","lxp_u128_mul_bps_floor","lxp_u128_mul_bps_ceil","lxp_u128_mul_div_floor"]
touches = ["include/layerx/lxp_u128.h","src/protocol/lxp_i128.c","tests/arith/lxp_test_rounding.c"]
reqs = ["5.5","19.4","26.1","28.7"]

[task.4.4]
title = "Prove the arithmetic layer against a reference implementation"
status = "done"
wave = 1
requires = ["4.3"]
do_1 = "Add tests/arith/lxp_arith_reference.c implementing slow decimal-string arithmetic that is linked only into tests and never into the runtime."
do_2 = "Generate operand pairs from a fixed seed so every property run is reproducible, and compare every add, subtract, multiply, divide and basis-point operation against the reference."
do_3 = "Assert that each operation returns either an exact checked result or a deterministic overflow, underflow or divide-by-zero code, and never a wrapped or saturated value."
do_4 = "Add exhaustive boundary sweeps around every word boundary in addition to the randomised corpus."
do_5 = "Add fuzz/lxp_fuzz_arith.c driving operands from fuzzer bytes and asserting the same property under the sanitizers."
verify_cmd = "make test-arith-property"
symbols = ["lxp_arith_reference_apply","lxp_test_arith_property","lxp_fuzz_arith"]
touches = ["tests/arith/lxp_test_arith_property.c","tests/arith/lxp_arith_reference.c","fuzz/lxp_fuzz_arith.c"]
reqs = ["5.2","5.3","28.7"]

[task.4.5]
title = "Enforce the no-floating-point rule across the arithmetic surface"
status = "done"
wave = 1
requires = ["4.4"]
do_1 = "Extend tools/ci/no-float-scan.sh to fail on any float, double, long double, floating literal or math header include under the consensus directories."
do_2 = "Scan the compiled consensus objects for floating-point and floating SIMD instructions and fail the build if any appear."
do_3 = "Add a compile-fail test asserting that assigning a double to an amount, or applying bare plus to two lxp_u128 values, does not compile."
do_4 = "Document in the arithmetic header that every amount call site must be checked and that an unchecked call site is a conformance failure, not a style issue."
do_5 = "Wire the scan into make ci so the rule is enforced continuously rather than at review time."
verify_cmd = "make test-arith-nofloat"
symbols = ["lxp_arith_nofloat_gate","lxp_test_arith_compile_fail"]
touches = ["tools/ci/no-float-scan.sh","tests/arith/lxp_test_nofloat.c","include/layerx/lxp_u128.h","Makefile"]
reqs = ["5.2","26.8","28.7"]

[task.5]
title = "Implement the append-only activity log, SQLite projections and crash recovery"
status = "pending"

[task.5.1]
title = "Implement the append-only log record framing and segment files"
status = "done"
wave = 2
requires = ["1.2","2.2","4.1"]
do_1 = "Add src/storage/lxp_log.c defining lxp_log_record_header with the log magic, record kind, a reserved field that must be zero, global sequence, body length, body CRC32C and the previous record offset."
do_2 = "Support exactly the record kinds activity, receipt, batch header, checkpoint, state diff, oracle and genesis, rejecting an unknown kind rather than skipping it."
do_3 = "Pre-allocate fixed-size segment files named by zero-padded sequence so an append never extends a file and never hits an out-of-space failure mid-record."
do_4 = "Implement lxp_log_append writing canonical body bytes only, never rewriting a record, so compaction can only create snapshots alongside history."
do_5 = "Test that a written record reads back byte-identical and that a corrupted body CRC is detected rather than accepted."
verify_cmd = "make test-log"
symbols = ["lxp_log_record_header","lxp_log_open","lxp_log_append","lxp_log_segment_create","lxp_log_crc32c"]
touches = ["src/storage/lxp_log.c","include/layerx/lxp_storage.h","tests/storage/lxp_test_log.c"]
reqs = ["7.3","21.9","23.9"]

[task.5.2]
title = "Enforce the fsync and write-boundary discipline"
status = "done"
wave = 2
requires = ["5.1"]
do_1 = "Implement the per-activity write order: append the encoded activity and sync, execute on the writer, then append the receipt, effect list and state diff and sync again."
do_2 = "Define durable head as the highest global sequence holding both an activity record and its receipt, since an activity without a receipt never had durable effects."
do_3 = "Append the sealed batch header with its own sync before publishing anything derived from it."
do_4 = "Implement lxp_log_sync and lxp_log_durable_head so no acknowledged receipt can be lost or altered by a crash occurring after acknowledgement."
do_5 = "Add a fault-injection harness that aborts the process at each distinct durable write boundary and asserts the boundary invariant at that point."
verify_cmd = "make test-log-durability"
symbols = ["lxp_log_sync","lxp_log_durable_head","lxp_log_write_boundary","lxp_log_fault_point"]
touches = ["src/storage/lxp_log.c","include/layerx/lxp_storage.h","tests/storage/lxp_test_log_durability.c"]
reqs = ["5.10","12.5","28.3"]

[task.5.3]
title = "Implement crash recovery and partial tail truncation"
status = "done"
wave = 2
requires = ["5.2"]
do_1 = "Implement lxp_log_recover scanning the last segment forward, validating magic and CRC and truncating at the first invalid or short record, treating a partial tail as a crash artifact and never as a fork."
do_2 = "Load the newest snapshot at or before durable head, replay forward, recompute state roots and compare them against the recorded receipts."
do_3 = "Halt with the replay divergence fatal code on any mismatch, because the binary and the history disagree and continuing would serve state the node cannot justify."
do_4 = "Resume at the next unused global sequence with no re-execution, duplication or skipping of any already sealed activity."
do_5 = "Test recovery from an abort at every write boundary, asserting each activity is either fully contained or fully excluded and no partially applied transfer set is ever observable."
verify_cmd = "make test-recovery"
symbols = ["lxp_log_recover","lxp_log_scan_tail","lxp_log_truncate_partial","lxp_log_resume_sequence"]
touches = ["src/storage/lxp_log_recover.c","include/layerx/lxp_storage.h","tests/storage/lxp_test_recovery.c"]
reqs = ["5.10","11.10","21.9","28.3"]

[task.5.4]
title = "Define the rebuildable SQLite projection schema"
status = "done"
wave = 2
requires = ["5.1"]
do_1 = "Add migrations/0001_projection.sql creating the balance view, receipt lookup by activity id and by idempotency key, module secondary indexes and the agent query tables."
do_2 = "Write the projection watermark inside the same SQLite transaction that applies a projected record batch, so the watermark can never disagree with the tables."
do_3 = "Open the database in WAL mode from a single projection thread and forbid every other thread from writing it."
do_4 = "Add a CI check asserting that no translation unit under src/protocol, src/state, src/storage consensus paths or src/modules links any sqlite3 symbol, since no consensus code may read a projection."
do_5 = "Make a projection failure mark the projection stale and nothing more, never blocking execution and never sitting on the consensus path."
verify_cmd = "make test-projection"
symbols = ["lxp_projection_open","lxp_projection_apply","lxp_projection_watermark","lxp_projection_mark_stale"]
touches = ["migrations/0001_projection.sql","src/storage/lxp_projection.c","tests/storage/lxp_test_projection.c","tools/ci/symbol-allowlist.sh"]
reqs = ["7.3","21.9","23.9"]

[task.5.5]
title = "Rebuild every index projection from the log alone"
status = "done"
wave = 2
requires = ["5.3","5.4"]
do_1 = "Implement lxp_projection_rebuild dropping every table and replaying the append-only log from genesis with no other input."
do_2 = "Assert that a rebuilt database reproduces every balance byte for byte, because the log and the 402LXP transfers recorded in it are the sole authority for balances."
do_3 = "Detect a projection watermark ahead of durable head and drop and rebuild rather than attempting to reconcile forward."
do_4 = "Emit the recovery metadata a rebuild needs as part of the served availability data, so a third party can rebuild without the sequencer."
do_5 = "Test that deleting the database entirely and rebuilding yields an identical content digest for every projected table."
verify_cmd = "make test-rebuild"
symbols = ["lxp_projection_rebuild","lxp_projection_drop_all","lxp_log_replay_range"]
touches = ["src/storage/lxp_projection.c","tests/storage/lxp_test_rebuild.c","tools/log_inspect.c"]
reqs = ["5.1","7.3","13.14","23.9"]

[task.5.6]
title = "Implement the single deterministic state writer and its journal"
status = "done"
wave = 2
requires = ["5.2"]
do_1 = "Add src/state/lxp_journal.c providing one journal per activity, opened before dispatch and either committed in full or rolled back in full."
do_2 = "Guarantee that no partial-commit path exists, so no reader, index, event stream or receipt can observe a proper subset of a transfer set."
do_3 = "Bind mutation to exactly one executor thread and treat a mutating call from any other thread as a fatal invariant violation that halts the node."
do_4 = "Process activities in strict global sequence order and reduce every parallel verification result into the writer in a fixed deterministic order, so scheduling cannot alter outcomes."
do_5 = "Test under ThreadSanitizer with the worker pool at zero and at maximum threads and assert byte-identical output in both configurations."
verify_cmd = "make test-journal"
symbols = ["lxp_state_journal","lxp_state_journal_open","lxp_state_journal_commit","lxp_state_journal_rollback","lxp_state_writer_assert_owner"]
touches = ["src/state/lxp_journal.c","include/layerx/lxp_state.h","tests/state/lxp_test_journal.c"]
reqs = ["5.9","11.1","11.10","13.6"]

[task.6]
title = "Implement the activity envelope, admission and validation pipeline"
status = "pending"

[task.6.1]
title = "Define the Activity envelope and its canonical encoding"
status = "done"
wave = 2
requires = ["1.3","2.3","3.1"]
do_1 = "Add include/layerx/lxp_activity.h carrying exactly protocol_version, network_id, activity_type, actor_did, authority, account_sequence, timestamp_bound, idempotency_key, fee_limit, payload_hash, payload and signature, and no other field."
do_2 = "Encode activity_type as u32 whose high sixteen bits are the module id and low sixteen the type ordinal, and encode fee_limit as a 16-byte big-endian unsigned amount."
do_3 = "Implement lxp_activity_encode and lxp_activity_decode over the canonical codec, rejecting an omitted, repeated or undeclared field with the malformed envelope code."
do_4 = "Implement lxp_activity_id as the domain-separated hash of the canonical activity bytes, so altering the encoding yields a different identifier rather than a second valid form of one activity."
do_5 = "Add fuzz/lxp_fuzz_activity.c asserting decode totality and encode-of-decode equality on arbitrary bytes."
verify_cmd = "make test-activity-codec"
symbols = ["lxp_activity","lxp_activity_encode","lxp_activity_decode","lxp_activity_id","lxp_fuzz_activity"]
touches = ["include/layerx/lxp_activity.h","src/protocol/lxp_activity.c","tests/protocol/lxp_test_activity_codec.c","fuzz/lxp_fuzz_activity.c"]
reqs = ["2.1","2.10","6.1","6.9"]

[task.6.2]
title = "Bind the payload hash and check the envelope preconditions"
status = "done"
wave = 2
requires = ["3.1","6.1"]
do_1 = "Implement lxp_activity_verify_payload_hash comparing payload_hash byte for byte against the domain-separated hash of the canonical payload encoding, checked before the payload is parsed."
do_2 = "Reject an unsupported protocol_version or a network_id differing from the node network identifier, consuming no account sequence, charging no fee and applying no effect."
do_3 = "Implement lxp_activity_signing_preimage producing the domain-separated canonical encoding of every envelope field except the signature itself."
do_4 = "Fix the check order so that the failure code is deterministic and not merely the failure, and record that order in the header as normative."
do_5 = "Test that a payload-binding failure is reported before any authority evaluation and leaves state untouched."
verify_cmd = "make test-envelope"
symbols = ["lxp_activity_check_envelope","lxp_activity_verify_payload_hash","lxp_activity_signing_preimage"]
touches = ["src/protocol/lxp_activity.c","include/layerx/lxp_activity.h","tests/protocol/lxp_test_envelope.c"]
reqs = ["2.2","2.3","2.4","6.4"]

[task.6.3]
title = "Offload signature verification to the worker pool"
status = "done"
wave = 2
requires = ["3.2","6.2"]
do_1 = "Add src/network/lxp_verify_pool.c creating a fixed pool of verification workers that never touch consensus state."
do_2 = "Implement lxp_verify_pool_submit and lxp_verify_pool_join so each worker yields one boolean attached to its activity rather than applying anything out of band."
do_3 = "Preserve the sequencer admission order regardless of the order in which verification finishes, so a verification race can never change execution order."
do_4 = "Support a pool size of zero for fully serial execution and assert byte-identical output against the parallel configuration."
do_5 = "Run the pool suite under ThreadSanitizer with zero data races reported."
verify_cmd = "make test-verify-pool"
symbols = ["lxp_verify_pool","lxp_verify_pool_create","lxp_verify_pool_submit","lxp_verify_pool_join"]
touches = ["src/network/lxp_verify_pool.c","include/layerx/lxp_verify_pool.h","tests/network/lxp_test_verify_pool.c"]
reqs = ["2.4","5.9","28.16"]

[task.6.4]
title = "Check timestamp bounds and define the admission result codes"
status = "done"
wave = 2
requires = ["6.2"]
do_1 = "Implement lxp_activity_check_timestamp_bound comparing the batch-supplied timestamp against not_before and not_after, with the window width bounded by the declared maximum."
do_2 = "Read time only from the batch header and never from a wall clock, a monotonic clock, a timezone or a locale, so the same rejection occurs on every node."
do_3 = "Implement lxp_admit_activity performing the fixed admission sequence and returning one lxp_admission_result carrying the result code and whether a sequence would be consumed."
do_4 = "Apply the single rejection policy: an activity failing admission is assigned no global sequence, consumes no account sequence and is charged no fee."
do_5 = "Test the expired, not-yet-valid and window-too-wide cases against a fixed batch timestamp, with no clock call anywhere on the path."
verify_cmd = "make test-admission"
symbols = ["lxp_admit_activity","lxp_admission_result","lxp_activity_check_timestamp_bound"]
touches = ["src/sequencer/lxp_admission.c","include/layerx/lxp_admission.h","tests/sequencer/lxp_test_admission.c"]
reqs = ["2.6","5.7","21.4","26.6"]

[task.6.5]
title = "Implement the idempotency key registry"
status = "done"
wave = 2
requires = ["5.6","6.4"]
do_1 = "Store the pair of actor did and idempotency key in the state tree rather than in a process cache, so the registry survives restart and is part of replay."
do_2 = "Implement lxp_idempotency_lookup short-circuiting a repeat submission to the stored receipt with the idempotent replay code and zero new effects."
do_3 = "Implement lxp_idempotency_record writing the key at the same point the account sequence is consumed, on both the success and the failure path."
do_4 = "Return byte-identical receipt content to both submissions even when the later one differs in payload, amount, recipient or fee limit."
do_5 = "Test that two activities sharing a key produce at most one economic result and exactly one durable receipt."
verify_cmd = "make test-idempotency"
symbols = ["lxp_idempotency_lookup","lxp_idempotency_record","lxp_idempotency_key_state"]
touches = ["src/state/lxp_idempotency.c","include/layerx/lxp_state.h","tests/state/lxp_test_idempotency.c"]
reqs = ["2.7","8.10","12.9","13.8"]

[task.6.6]
title = "Compute the metered fee and enforce the declared fee limit"
status = "done"
wave = 2
requires = ["4.3","6.4"]
do_1 = "Implement lxp_fee_compute as a versioned integer-only function of activity type, canonical encoded byte length, metered execution units and metered storage units."
do_2 = "Round any fractional base unit upward through the basis-point ceiling helper so the fee is bit-identical on every machine, compiler and architecture."
do_3 = "Reject the activity with the fee limit code when the computed fee exceeds fee_limit, applying no module effect and charging no more than the limit."
do_4 = "Require at admission that the actor spendable balance in the fee asset is at least fee_limit, so the fee charge can never fail because of the activity own spending."
do_5 = "Record in the header that the charge itself later executes as an authenticated 402LXP transfer from the actor main account to the system fees account, never as a direct balance write."
verify_cmd = "make test-fee-gate"
symbols = ["lxp_fee_compute","lxp_fee_limit_check","lxp_fee_meter","lxp_fee_params"]
touches = ["src/protocol/lxp_fee.c","include/layerx/lxp_fee.h","tests/protocol/lxp_test_fee_gate.c"]
reqs = ["2.8","7.1","26.1","26.3","26.5"]

[task.7]
section = "Waves 3-4 — Identity, Authority and the Activity Kernel"
title = "Implement identity, keys, capabilities and delegated authority"
status = "pending"

[task.7.1]
title = "Implement DID identities, account derivation and sequence discipline"
status = "done"
wave = 3
requires = ["3.1","5.6","6.1"]
do_1 = "Add src/state/lxp_identity.c with lxp_identity carrying status, primary key, pending key, rotation timestamps, next sequence, revocation sequence, recovery root, recovery threshold and the optional EVM payout binding."
do_2 = "Derive the DID identifier as a domain-separated 32-byte commitment over the canonical encoding of the DID string, identical on every node and architecture and stable for the account lifetime."
do_3 = "Derive account identifiers for the namespaces agent main, agent budget, agent escrow and agent margin, and reject with the unknown namespace code any account outside the declared set."
do_4 = "Require account_sequence to equal next_sequence exactly, rejecting a lower value as a replay and a higher value as a gap, and never queueing a gap."
do_5 = "Consume the sequence exactly once by incrementing it on both the success and the failure path, so a failed activity cannot be replayed under the same sequence."
do_6 = "Reject any activity whose actor did does not resolve to an identity that is active or recovering and not frozen."
verify_cmd = "make test-identity"
symbols = ["lxp_identity","lxp_identity_register","lxp_did_id_derive","lxp_account_id_derive","lxp_identity_consume_sequence"]
touches = ["src/state/lxp_identity.c","include/layerx/lxp_identity.h","tests/state/lxp_test_identity.c"]
reqs = ["2.5","3.1","10.2","10.3","13.9"]

[task.7.2]
title = "Encode session keys and capability grants"
status = "done"
wave = 3
requires = ["2.3","7.1"]
do_1 = "Define lxp_authority_grant with grant id, grantor, grantee, kind, key, scope, not_before, not_after, grantor revocation sequence, revoked flag and grantor signature."
do_2 = "Define lxp_authority_scope with module mask, activity type ordinal range, asset, maximum per activity, maximum total, spent total, period length, maximum per period, spent this period, period start and purpose hash."
do_3 = "Require every session key to carry an explicit expiration, an explicit set of permitted activity types and an explicit revocation sequence, treating a key missing any bound as invalid."
do_4 = "Compute grant_id as the content hash of the canonical grant encoding so a grant is content-addressed and cannot be silently amended in place."
do_5 = "Reject a capability grant omitting the authorised holder, the asset, the maximum single amount, the total or recurring allowance, the expiration, the permitted purpose or the revocation sequence."
verify_cmd = "make test-grants"
symbols = ["lxp_authority_grant","lxp_authority_scope","lxp_grant_encode","lxp_grant_id_compute","lxp_session_key_bind"]
touches = ["src/state/lxp_authority.c","include/layerx/lxp_authority.h","tests/state/lxp_test_grants.c"]
reqs = ["3.2","3.3","6.1","9.3"]

[task.7.3]
title = "Implement the authority resolver inside the state machine"
status = "done"
wave = 3
requires = ["3.2","7.2"]
do_1 = "Implement lxp_authority_resolve accepting exactly the six kinds owner direct signature, session key, delegated capability, budget allowance, escrow authority and protocol-module capability, and rejecting any other kind."
do_2 = "Produce lxp_authority_resolved carrying actor, the principal whose funds may move, kind, verified key, scope pointer and authority hash."
do_3 = "Compute the authority hash as a domain-separated commitment over kind, grant id and verified key, and record it in every receipt so every debit is attributable to one named authority artifact."
do_4 = "Reject an authority that does not explicitly name the activity type being executed, or that names a broader scope than the module declares for that type."
do_5 = "Evaluate every authorisation decision inside the deterministic transition, consulting no HTTP session state, no gateway authentication, no in-memory challenge and no transport credential."
do_6 = "Test that the identical activity yields the identical authorisation result code on every node even when the submitting gateway accepted the request."
verify_cmd = "make test-authority-resolve"
symbols = ["lxp_authority_resolve","lxp_authority_resolved","lxp_authority_hash","lxp_authority_check_scope"]
touches = ["src/state/lxp_authority.c","include/layerx/lxp_authority.h","tests/state/lxp_test_authority_resolve.c"]
reqs = ["3.7","3.10","8.2","13.2"]

[task.7.4]
title = "Enforce delegated spending and trading limits"
status = "done"
wave = 3
requires = ["4.3","7.3"]
do_1 = "Implement lxp_authority_charge_allowance accumulating consumption against the grant inside state at execution time, never in a cache and never at admission only."
do_2 = "Reject the first activity that would carry cumulative consumption past the per-activity, total or per-period limit, so concurrent or reordered submissions cannot exceed a limit in aggregate."
do_3 = "Implement lxp_authority_period_roll advancing the period start by whole period lengths at the first activity that observes the boundary and resetting the remaining allowance deterministically."
do_4 = "Never carry unused recurring allowance forward, so an unused window is permanently unusable rather than accumulating into the next one."
do_5 = "Use only checked fixed-width arithmetic for allowance accumulation, rejecting with the overflow code rather than wrapping."
do_6 = "Test that a total allowance is exhausted at exactly the declared unit and never one unit past it, including several draws ordered inside a single batch."
verify_cmd = "make test-allowance"
symbols = ["lxp_authority_charge_allowance","lxp_authority_period_roll","lxp_authority_spend_check"]
touches = ["src/state/lxp_authority.c","include/layerx/lxp_authority.h","tests/state/lxp_test_allowance.c"]
reqs = ["3.4","9.5","9.6","13.2"]

[task.7.5]
title = "Implement revocation, expiry and monotone amendment"
status = "done"
wave = 3
requires = ["7.3"]
do_1 = "Implement lxp_authority_revoke marking a grant unusable from the global sequence of the revocation onward, applying to activities already submitted but not yet executed."
do_2 = "Implement bulk revocation by bumping the identity revocation sequence, invalidating every grant referencing the superseded value in one activity without enumerating them."
do_3 = "Reject a revocation or capability update whose revocation sequence is not strictly greater than the recorded one, so a replayed authorisation can never reinstate a revoked delegate."
do_4 = "Implement lxp_authority_amend as narrowing only, permitting lower caps, earlier expiry and fewer modules, and requiring a fresh grant to widen anything."
do_5 = "Reject an expired authority using only the batch-supplied timestamp, and never retroactively invalidate an activity already executed before the revocation sequence."
verify_cmd = "make test-revocation"
symbols = ["lxp_authority_revoke","lxp_authority_amend","lxp_authority_is_live","lxp_identity_bump_revocation_sequence"]
touches = ["src/state/lxp_authority.c","include/layerx/lxp_authority.h","tests/state/lxp_test_revocation.c"]
reqs = ["3.5","3.6","9.8","16.9"]

[task.7.6]
title = "Implement key rotation, recovery and the optional EVM payout binding"
status = "done"
wave = 3
requires = ["3.3","7.1"]
do_1 = "Implement two-phase rotation: lxp_identity_rotate_announce records the pending key and effective timestamp, and lxp_identity_rotate_commit promotes it only after the configured challenge delay measured in batch timestamps."
do_2 = "Verify both the old and the new key inside the rotation window so an in-flight activity signed by the superseded key is not orphaned, and treat a lapsed rotation as a no-op rather than a lockout."
do_3 = "Reject a competing rotation submitted during an open challenge delay and invalidate the superseded key for every activity executed at or after the effective sequence."
do_4 = "Implement k-of-n recovery where lxp_identity_recover_begin opens a challenge window in which the existing primary key can veto, and lxp_identity_recover_commit applies only after that window elapses."
do_5 = "Implement lxp_identity_bind_evm_payout requiring a secp256k1 signature over a domain-separated binding statement naming the DID and network id, and granting the bound address no authority to move, debit or authorise any balance."
do_6 = "Test that retirement is refused unless every balance in every asset is zero, so no value is stranded outside the reachable account namespace."
verify_cmd = "make test-rotation"
symbols = ["lxp_identity_rotate_announce","lxp_identity_rotate_commit","lxp_identity_recover_begin","lxp_identity_recover_commit","lxp_identity_bind_evm_payout"]
touches = ["src/state/lxp_identity.c","include/layerx/lxp_identity.h","tests/state/lxp_test_rotation.c"]
reqs = ["3.8","3.9","5.7","10.6"]

[task.8]
title = "Implement the activity kernel and module dispatch"
status = "pending"

[task.8.1]
title = "Implement the kernel state handle and the module registration table"
status = "done"
wave = 4
requires = ["5.6","6.4","7.3"]
do_1 = "Add src/protocol/lxp_kernel.c defining the kernel handle over the state tree, the journal, the parameter set and the registered module table."
do_2 = "Define lxp_module_iface with module id, ABI version, name and the genesis, decode, validate, execute, epoch begin, epoch end, state root and release entry points."
do_3 = "Restrict the kernel to identities, accounts, assets, authority, nonces and sequences, fees, state transitions, events, receipts, checkpoints and modules, defining no payment product, market rule, service workflow or oracle semantic."
do_4 = "Implement lxp_kernel_register_module recording the module identifier, its version and the exhaustive list of activity types it declares, whether at genesis or by a governance activity."
do_5 = "Reject with the unknown activity code any activity whose type is not declared by a currently registered and epoch-enabled module."
do_6 = "Reserve module ids one through eight for asset, escrow, budget, stream, service, perps, governance and bridge, none of them privileged above the kernel rules."
verify_cmd = "make test-kernel"
symbols = ["lxp_kernel","lxp_kernel_create","lxp_kernel_register_module","lxp_module_iface","lxp_module_ctx"]
touches = ["src/protocol/lxp_kernel.c","include/layerx/lxp_kernel.h","include/layerx/lxp_module.h","tests/protocol/lxp_test_kernel.c"]
reqs = ["1.5","4.1","4.2","4.3"]

[task.8.2]
title = "Implement the module context capability surface"
status = "done"
wave = 4
requires = ["8.1"]
do_1 = "Provide namespaced state access through lxp_ctx_kv_get, lxp_ctx_kv_put, lxp_ctx_kv_del and lxp_ctx_kv_iter, with the kernel prefixing every key by module id so a module physically cannot address another module subtree."
do_2 = "Make lxp_ctx_emit_transfer_set the only monetary capability a module has, so every monetary effect compiles into one or more authenticated 402LXP transfers applied by the single kernel primitive."
do_3 = "Expose the deterministic environment as batch timestamp, epoch, global sequence, parameter read, gas charge and arena allocation, and nothing else."
do_4 = "Define no now function, no random function, no HTTP function and no set-balance function, so determinism and the no-direct-balance-write invariant are enforced by what does not exist."
do_5 = "Add a CI check asserting that no module translation unit links the ledger apply internals and that the account balance field is unreachable from the module ABI."
do_6 = "Test that a module attempting a balance write outside the transfer primitive aborts the transition with the balance bypass code, leaves every balance unchanged and emits no success receipt."
verify_cmd = "make test-module-ctx"
symbols = ["lxp_ctx_kv_get","lxp_ctx_kv_put","lxp_ctx_kv_iter","lxp_ctx_emit_transfer_set","lxp_ctx_batch_timestamp_ms"]
touches = ["src/protocol/lxp_module_ctx.c","include/layerx/lxp_module.h","tests/protocol/lxp_test_module_ctx.c","tools/ci/symbol-allowlist.sh"]
reqs = ["4.4","7.1","7.2","7.10","13.1","13.10"]

[task.8.3]
title = "Implement versioned transition dispatch in fixed order"
status = "done"
wave = 4
requires = ["6.5","6.6","8.2"]
do_1 = "Implement lxp_kernel_execute_activity in fixed order: decode, envelope and module enablement checks, identity and authority resolution with signature verification, exact sequence equality, timestamp bound, idempotency short-circuit, fee computation against the limit, journal open, validate then execute, commit or roll back, then fee charge and sequence consumption and idempotency record and receipt on both paths, then state root chaining."
do_2 = "Select the transition version recorded for the batch rather than the newest available version, so replay of old history is unaffected by later upgrades."
do_3 = "Record in each receipt the module version under which the activity executed, and apply a new module version only from a declared epoch boundary onward."
do_4 = "On a module failure discard every state write that activity attempted, leave every other module unchanged, and still consume the account sequence and charge the metered fee for a well-formed authorised activity."
do_5 = "Forbid a validate implementation from mutating state and assert that property against the test module the suite registers."
do_6 = "Test that a failure receipt carries the failure result code, zero module effects and correctly chained state roots."
verify_cmd = "make test-dispatch"
symbols = ["lxp_kernel_execute_activity","lxp_kernel_dispatch","lxp_module_version_for_epoch","lxp_kernel_step_order"]
touches = ["src/protocol/lxp_kernel.c","include/layerx/lxp_kernel.h","tests/protocol/lxp_test_dispatch.c"]
reqs = ["4.6","4.8","5.6","26.6"]

[task.8.4]
title = "Implement the event and receipt emission path"
status = "done"
wave = 4
requires = ["3.4","8.3"]
do_1 = "Implement lxp_effect_buffer collecting module effects sorted by module id and effect ordinal, and require every monetary effect to be a transfer effect carrying a transfer set root."
do_2 = "Trap on any other monetary effect kind, because such an effect is a kernel bug rather than a module error."
do_3 = "Emit events only through lxp_ctx_emit_event and commit them under the batch event merkle root, providing no module-private or process-local channel by which an effect could become observable without appearing in that commitment."
do_4 = "Build the ActivityReceipt with activity id, global sequence, previous state root, resulting state root, activity root, result code, effects, fee charged, batch id and sequencer signature, every value computed by the protocol from the resulting state."
do_5 = "Sign the receipt over the domain-separated canonical encoding of the receipt without its own signature, verifiable against the published sequencer key with no further interaction with the node."
do_6 = "Reject any client-supplied assertion of a result, an effect, a resulting balance or a state root."
verify_cmd = "make test-receipts"
symbols = ["lxp_effect_buffer","lxp_ctx_emit_event","lxp_receipt_build","lxp_receipt_sign","lxp_receipt_encode"]
touches = ["src/protocol/lxp_receipt.c","include/layerx/lxp_receipt.h","tests/protocol/lxp_test_receipts.c"]
reqs = ["2.9","4.10","12.1","12.4"]

[task.8.5]
title = "Compute and chain the state root"
status = "done"
wave = 4
requires = ["3.4","5.6","8.4"]
do_1 = "Implement lxp_state_root as a pure function of state composing each module subtree root under the kernel domain tags into a single root."
do_2 = "Iterate every account, grant, order, position and effect collection in canonical byte-lexicographic key order, never in insertion, allocation, hash-seed or pointer order."
do_3 = "Recompute each asset total units against the account tree at every seal and return the supply mismatch fatal code on divergence rather than continuing."
do_4 = "Chain previous state root to resulting state root per activity and not only per batch, so the receipt at a global sequence names the resulting root of the preceding sequence."
do_5 = "Test that two nodes with different allocation patterns and optimisation levels produce byte-identical roots for the identical history."
verify_cmd = "make test-state-root"
symbols = ["lxp_state_root","lxp_state_subtree_root","lxp_state_root_chain","lxp_state_supply_check"]
touches = ["src/state/lxp_state_root.c","include/layerx/lxp_state.h","tests/state/lxp_test_state_root.c"]
reqs = ["5.1","5.4","6.5","13.5"]

[task.8.6]
title = "Enforce the execution guard and prove golden replay"
status = "done"
wave = 4
requires = ["8.5"]
do_1 = "Extend the link-time symbol allowlist to forbid time, clock_gettime, gettimeofday, rand, random, getenv, getpid, gethostname, stat and every socket or HTTP entry point in execution translation units."
do_2 = "Implement lxp_determinism_guard_check so a transition path reaching a forbidden call halts the node rather than producing a state root it cannot justify."
do_3 = "Add tests/replay holding a golden activity history together with its recorded per-batch state roots, receipts and event roots."
do_4 = "Implement lxp_kernel_replay executing the golden history from genesis with no index database as an input, asserting byte-identical roots at every batch boundary."
do_5 = "Run the golden replay at two optimisation levels and on a second CPU architecture in CI, failing on a single differing byte with the replay divergence fatal code."
do_6 = "Assert that reducing the worker pool to zero threads changes no output byte, which is the operational definition that the threading model is not part of consensus."
verify_cmd = "make test-replay-golden"
symbols = ["lxp_determinism_guard_check","lxp_kernel_replay","lxp_replay_golden_run","lxp_replay_compare_roots"]
touches = ["src/protocol/lxp_determinism_guard.c","tests/replay/lxp_test_golden_replay.c","tests/replay/golden/history.lxl","tools/ci/symbol-allowlist.sh"]
reqs = ["1.1","5.7","5.8","13.14","13.15","28.1"]

[task.9]
section = "Wave 5 — 402LXP Financial Kernel"
title = "Build the 402LXP ledger core as the single balance-mutation primitive"
status = "pending"

[task.9.1]
title = "Derive the account and subaccount namespace deterministically"
status = "done"
wave = 5
requires = ["8.1"]
do_1 = "Define the canonical account string grammar in include/layerx/lxp_ledger.h as ASCII, lowercase, colon-delimited with no empty segments, admitting exactly agent:did:main, agent:did:budget:id, agent:did:escrow:id, agent:did:stream:id, agent:did:margin:position, system:liquidity:market, system:funding:market:long, system:funding:market:short, system:insurance, system:fees, system:paxeer-reserve and system:paxeer-withdrawals."
do_2 = "Implement lx_account_id_from_string as the domain-separated commitment over the tag LX:ACCOUNT:v1 followed by a big-endian u32 length and the canonical string bytes, so the identifier is byte-identical on every node, architecture and implementation and stable for the account lifetime."
do_3 = "Implement lx_account_name_parse and lx_account_kind_of to classify a name into enum lx_account_kind, and reject any transfer leg naming an account outside the namespace with ERR_UNKNOWN_ACCOUNT_NAMESPACE."
do_4 = "Implement lx_account_open so that a credit to a namespace-valid account that does not yet exist creates it deterministically at that global sequence with a zero opening balance before the credit is applied, and records the creation in the append-only activity log so replay recreates it identically."
do_5 = "Reject any presented account identifier that does not equal the derivation of its declared namespace string with ERR_ACCOUNT_ID_MISMATCH, and reject closure of an account whose balance is non-zero with ERR_ACCOUNT_NOT_EMPTY."
do_6 = "Add fixed conformance vectors covering every namespace form, a derivation mismatch, an unknown namespace and a lazily created credit destination, asserting exact identifier bytes and exact result codes."
verify_cmd = "make test-ledger-accounts"
symbols = ["lx_account_id_from_string","lx_account_name_parse","lx_account_kind_of","lx_account_open","lx_account_lookup"]
touches = ["include/layerx/lxp_ledger.h","src/ledger/lx_account_id.c","src/ledger/lx_account_registry.c","tests/ledger/test_account_id.c"]
reqs = ["10.2","10.3","10.4","10.5","10.6","10.10"]

[task.9.2]
title = "Implement lxp_apply_transfer as the one balance-mutation primitive"
status = "done"
wave = 5
requires = ["9.1"]
do_1 = "Implement lxp_apply_transfer in src/ledger/lxp_apply.c as the one-leg case of the set form, and make that translation unit the only place in the tree that ever assigns struct lx_account.balance."
do_2 = "Implement lxp_precondition_check in the fixed order leg-count bound, non-zero amount, asset registered and unpaused, source and destination asset equality, neither endpoint frozen, authority covers every debited account, actor sequence exact, idempotency key unseen, batch timestamp within expiry, net debit within balance, credit addition overflow-free, per-asset conservation, so the failure code is deterministic and not merely the failure."
do_3 = "Route every amount computation through the checked helpers lx_u128_sub and lx_u128_add so an underflow returns LX_ERR_UNDERFLOW and an overflow returns LX_ERR_OVERFLOW instead of wrapping, saturating or truncating."
do_4 = "Capture from_balance_before, from_balance_after, to_balance_before and to_balance_after from the state actually mutated, and reject any payload carrying a client-asserted resulting balance or balance delta with ERR_CLIENT_SUPPLIED_BALANCE."
do_5 = "Add unit tests for zero amount, asset mismatch, unknown asset, frozen endpoint, insufficient balance, maximum-representable credit and an exact-boundary transfer, asserting the exact result code and unchanged balances for every rejection."
verify_cmd = "make test-ledger-transfer"
symbols = ["lxp_apply_transfer","lxp_precondition_check","lxp_balance_apply_leg","lxp_state_balance_get"]
touches = ["src/ledger/lxp_apply.c","include/layerx/lxp_transfer.h","include/layerx/lxp_ledger.h","tests/ledger/test_apply_transfer.c"]
reqs = ["7.1","7.9","8.6","8.7","8.8","13.4"]

[task.9.3]
title = "Implement atomic transfer sets with full rollback and per-asset conservation"
status = "done"
wave = 5
requires = ["9.2"]
do_1 = "Implement lxp_apply_transfer_set to open exactly one state journal, apply the legs in their declared order, and either commit every leg or roll the whole set back before any reader, index, event stream or receipt can observe an intermediate state."
do_2 = "Bind each set to exactly one authorisation context and one execution sequence for the authorising account, and emit exactly one receipt for the whole set rather than one receipt per leg."
do_3 = "Implement lxp_conservation_check accumulating debits and credits per asset in 256-bit accumulators, returning LX_ERR_CONSERVATION when the net supply change for any asset is non-zero, and rejecting any non-bridge set that would increase or decrease total supply."
do_4 = "Evaluate the non-negativity precondition on net per-account movement across the whole set so a set may legitimately credit an account before debiting it, and fail the entire set rather than reordering, netting or deferring legs."
do_5 = "Report the zero-based index and result code of the first failing leg in the failure result, emit no success receipt for any leg, and omit computed-zero legs before authorisation so a set never contains a zero-amount leg."
do_6 = "Implement lxp_transfer_set_root as a merkle commitment over the canonically encoded legs in execution order under distinct leaf and node domain tags, and test the four-leg liquidation template, a failing final leg, a supply-mutating set and a crash injected between legs."
verify_cmd = "make test-ledger-set"
symbols = ["lxp_apply_transfer_set","lxp_journal_open","lxp_journal_commit","lxp_journal_rollback","lxp_conservation_check","lxp_transfer_set_root"]
touches = ["src/ledger/lxp_apply.c","src/ledger/lxp_journal.c","include/layerx/lxp_transfer.h","tests/ledger/test_transfer_set.c"]
reqs = ["4.7","11.1","11.2","11.4","11.5","11.9","13.6"]

[task.9.4]
title = "Implement the SEND operation over the shared internal transfer function"
status = "done"
wave = 5
requires = ["9.3"]
do_1 = "Decode struct lxp_send with exactly the fields from, to, asset, amount, sequence, idempotency_key, expires_at, context_hash, conditions and authorization, rejecting an omitted field, an unknown field or a non-canonical encoding with ERR_MALFORMED_SEND."
do_2 = "Resolve the authorization to exactly one of the six admitted kinds, namely owner direct signature, session key, delegated capability, budget allowance, escrow authority or protocol-module capability, rejecting anything else with ERR_UNKNOWN_AUTHORITY_KIND and rejecting an authority that does not control the source account with ERR_UNAUTHORIZED_DEBIT."
do_3 = "Require sequence to equal exactly the next expected sequence of the account the authorization controls, consume it exactly once on success, and reject a lower or higher value with ERR_SEQUENCE_MISMATCH."
do_4 = "Evaluate expires_at and every declared condition against the batch-supplied timestamp only, returning ERR_EXPIRED or ERR_CONDITION_UNMET, and reject a submitted context_hash that differs from the one covered by the signed authorisation bytes with ERR_CONTEXT_MISMATCH."
do_5 = "Compile the accepted SEND into a one-leg transfer set with reason LXP_REASON_PAYMENT and hand it to lxp_apply_transfer_set, so SEND owns no balance-mutation code of its own."
do_6 = "Add a fuzz target over the SEND payload plus tests proving a byte-identical resubmission is rejected as a replay and a repeated idempotency key returns the first receipt with zero new economic effect."
verify_cmd = "make test-ledger-send"
symbols = ["lxp_send_decode","lxp_send_validate","lxp_send_execute","lxp_send_build_transfer_set"]
touches = ["src/ledger/lxp_send.c","include/layerx/lxp_ledger.h","tests/ledger/test_send.c","fuzz/fuzz_lxp_send.c"]
reqs = ["7.1","8.1","8.2","8.3","8.4","8.5","8.9","13.2"]

[task.9.5]
title = "Implement RECEIVE with signed payer grant storage and enforcement"
status = "done"
wave = 5
requires = ["9.4"]
do_1 = "Decode struct lxp_receive with exactly the fields from, to, asset, amount, grant_id, receiver_sequence, idempotency_key, context_hash, receiver_authorization and payer_grant, rejecting an omission or non-canonical encoding with ERR_MALFORMED_RECEIVE."
do_2 = "Implement lxp_verify_payer_grant to check the grantor signature over the canonical grant bytes and require the authorized recipient, asset, per-draw maximum, total or recurring allowance with its window length, expiration, permitted purpose, optional service or invoice identifier and revocation sequence, rejecting an incomplete grant with ERR_MALFORMED_GRANT and a missing grant with ERR_NO_PAYER_GRANT."
do_3 = "Persist grant draw state in the state tree through lxp_grant_store_put and lxp_grant_draw_record so drawn_total and drawn_this_period are protocol-maintained and monotone, rejecting a draw past a total allowance or past the current window allowance with ERR_GRANT_EXHAUSTED even when several RECEIVE activities are ordered in the same batch."
do_4 = "Reset the recurring allowance at the deterministic start of each window computed from the batch timestamp, carry no unused allowance forward, and reject a grant past its expiration with ERR_GRANT_EXPIRED and a grant superseded by lxp_grant_revoke with ERR_GRANT_REVOKED from the revocation global sequence onward."
do_5 = "Reject a recipient that is not the named authorized recipient, an asset or account pair that does not match the grant, a purpose that the context_hash does not commit to, and an already settled invoice identifier, with ERR_GRANT_SCOPE_VIOLATION, ERR_PURPOSE_MISMATCH or ERR_INVOICE_ALREADY_SETTLED, moving no value."
do_6 = "Compile the accepted RECEIVE into the identical internal transfer set that SEND builds, so the emitted receipt differs from a SEND receipt only in the operation field and the authorisation evidence, and test that a recipient can never debit an account for which no grant exists."
verify_cmd = "make test-ledger-receive"
symbols = ["lxp_receive_execute","lxp_verify_payer_grant","lxp_grant_store_put","lxp_grant_draw_record","lxp_grant_revoke"]
touches = ["src/ledger/lxp_receive.c","src/ledger/lxp_grant_store.c","include/layerx/lxp_ledger.h","tests/ledger/test_receive.c"]
reqs = ["7.1","9.1","9.2","9.3","9.4","9.5","9.7","9.9","9.10","13.3"]

[task.9.6]
title = "Emit the 402LXPReceipt and lock the ledger as the only balance writer"
status = "done"
wave = 5
requires = ["9.5"]
do_1 = "Implement lxp_receipt_build populating protocol_version, transaction_id, operation, global_sequence, asset, amount, from, from_balance_before, from_balance_after, from_sequence, to, to_balance_before, to_balance_after, transfer_set_root, authorization_hash, context_hash, previous_state_root, resulting_state_root, batch_id and timestamp entirely from the state the ledger just mutated."
do_2 = "Guarantee for a single-leg operation that from_balance_after equals from_balance_before minus amount and to_balance_after equals to_balance_before plus amount, and for a multi-leg set that transfer_set_root commits to every leg in execution order."
do_3 = "Sign the canonical binary encoding of the receipt under the sequencer identity named in the batch header with lxp_receipt_sign, implement lxp_receipt_verify against the published sequencer key with no further node interaction, and append the receipt durably before it is returned so replay reproduces it exactly."
do_4 = "Return byte-identical receipt content for two submissions sharing an idempotency key, keep exactly one durable receipt per successful transfer or transfer set, and emit no signed receipt at all for a failed operation, only a failure result carrying the result code and unchanged balances."
do_5 = "Add tools/lxp_check_sole_writer.sh asserting that struct lx_account.balance is assigned in exactly one translation unit, src/ledger/lxp_apply.c, that no object under src/modules links the internal apply symbols, and that no balance-write entry point is exposed on the module context."
do_6 = "Wire lxp_balance_writer_guard and the sole-writer script into the test-ledger-receipt target so any bypass fails verification with ERR_BALANCE_BYPASS rather than being merely discouraged."
verify_cmd = "make test-ledger-receipt"
symbols = ["lxp_receipt_verify","lxp_balance_writer_guard"]
touches = ["src/ledger/lxp_receipt.c","include/layerx/lxp_receipt.h","tools/lxp_check_sole_writer.sh","tests/ledger/test_receipt.c"]
reqs = ["7.2","12.1","12.2","12.3","12.4","12.10","13.7","13.10"]

[task.10]
section = "Waves 6-9 — Native Protocol Modules"
title = "Build the asset module over the ledger kernel"
status = "pending"

[task.10.1]
title = "Implement the asset registry with integer decimals"
status = "done"
wave = 6
requires = ["9.1"]
do_1 = "Register the asset module as module id 1 through the kernel module interface, declaring exactly the activity types asset.register, asset.pause, asset.unpause, asset.account_open, asset.send, asset.receive, asset.grant_issue and asset.grant_revoke."
do_2 = "Implement lx_asset_register recording asset_id, symbol, decimals, custody kind and the Paxeer custody reference, rejecting a duplicate asset id with ASSET_ALREADY_REGISTERED while still consuming the sequence and charging the metered fee."
do_3 = "Treat decimals strictly as display metadata: every amount is an unsigned fixed-width integer count of the smallest indivisible unit, and any amount encoded as a decimal fraction, an exponent form or a floating point value is rejected with INVALID_AMOUNT."
do_4 = "Implement lx_asset_pause and lx_asset_unpause so a paused asset fails the ledger precondition for every leg naming it, and expose lx_asset_lookup for the ledger to resolve asset existence and paused state."
do_5 = "Add tests for duplicate registration, a fractional amount, a paused-asset transfer and canonical round-trip of the registry record."
verify_cmd = "make test-asset-registry"
symbols = ["lx_asset_module_iface","lx_asset_register","lx_asset_lookup","lx_asset_pause","lx_asset_unpause"]
touches = ["src/modules/asset/lx_asset_registry.c","include/layerx/lx_asset.h","tests/modules/test_asset_registry.c"]
reqs = ["14.1","14.2","4.2","4.3"]

[task.10.2]
title = "Implement balance queries and the asset module state root"
status = "done"
wave = 6
requires = ["10.1"]
do_1 = "Implement lx_asset_balance_get as a read-only query over the balance tree keyed by account_id and asset_id, returning the exact fixed-width integer balance with no rounding or aggregation."
do_2 = "Implement lx_asset_account_open as the module-facing wrapper over the kernel account creation path so locked value is always a real account balance and never a reserved, held, locked or pending field on another account."
do_3 = "Maintain per-asset total_units as the sum of every account balance and implement lx_asset_total_units so reserve reconciliation has an authoritative figure."
do_4 = "Implement lx_asset_state_root as a pure function of module state, iterating accounts in canonical byte-lexicographic key order with no dependence on hash seeds, pointer values, allocation order or insertion order."
do_5 = "Add tests proving the state root is byte-identical across two differently ordered insertion histories that reach the same logical state."
verify_cmd = "make test-asset-balance"
symbols = ["lx_asset_balance_get","lx_asset_account_open","lx_asset_total_units","lx_asset_state_root"]
touches = ["src/modules/asset/lx_asset_state.c","include/layerx/lx_asset.h","tests/modules/test_asset_state.c"]
reqs = ["10.1","10.5","5.4","14.10"]

[task.10.3]
title = "Route asset transfers exclusively through the ledger kernel"
status = "done"
wave = 6
requires = ["10.2","9.4"]
do_1 = "Implement lx_asset_send_execute compiling asset.send into a single leg from agent main to agent main with reason LXP_REASON_PAYMENT, emitted only through the module transfer-set capability."
do_2 = "Implement lx_asset_receive_execute compiling asset.receive into a single leg from the grant payer account to the recipient main account, reusing the payer grant verification of the ledger rather than duplicating it."
do_3 = "Implement lx_asset_validate as a read-only admissibility check that mutates no state, so validation and execution stay separable and validation can never move value."
do_4 = "Reject with DIRECT_BALANCE_WRITE_FORBIDDEN any accepted activity or module effect that attempts to assign a balance value directly, and prove in test that the asset module holds the registry but no privilege over the ledger."
do_5 = "Add tests for insufficient funds, destination overflow, asset mismatch and a zero amount, asserting INSUFFICIENT_FUNDS, BALANCE_OVERFLOW, ASSET_MISMATCH and INVALID_AMOUNT with every balance left exactly as it was."
verify_cmd = "make test-asset-transfer"
symbols = ["lx_asset_send_execute","lx_asset_receive_execute","lx_asset_validate"]
touches = ["src/modules/asset/lx_asset_transfer.c","include/layerx/lx_asset.h","tests/modules/test_asset_transfer.c"]
reqs = ["7.1","7.4","13.1","14.3","14.4","14.5","14.6"]

[task.10.4]
title = "Credit deposits only against a finalised Paxeer custody proof"
status = "done"
wave = 6
requires = ["10.3"]
do_1 = "Implement lx_asset_deposit_credit to execute a single leg from system:paxeer-reserve to the agent main account with reason LXP_REASON_DEPOSIT, so a deposit moves units the reserve mirror already holds and never mints."
do_2 = "Implement lx_bridge_verify_deposit to fail closed unless the activity carries a finalised Paxeer custody proof bound to a registered checkpoint state root and matching the executing network_id and protocol_version, rejecting otherwise with DEPOSIT_PROOF_NOT_FINAL."
do_3 = "Implement lx_deposit_nullifier_consume so a custody reference or deposit identifier already credited is rejected with DEPOSIT_ALREADY_CREDITED and a replayed proof produces at most one economic result."
do_4 = "Assert at runtime that a deposit credit lacking a finalised custody proof aborts the transition under invariant 12 rather than crediting anything."
do_5 = "Add tests for an unfinalised checkpoint reference, a replayed proof, a wrong-network proof and a valid credit, asserting exact result codes and conserved total supply in every case."
verify_cmd = "make test-asset-deposit"
symbols = ["lx_asset_deposit_credit","lx_bridge_verify_deposit","lx_deposit_nullifier_consume"]
touches = ["src/modules/asset/lx_asset_custody.c","include/layerx/lx_asset.h","tests/modules/test_asset_deposit.c"]
reqs = ["1.7","7.6","11.6","13.12","14.7","14.8"]

[task.10.5]
title = "Debit withdrawals into the Paxeer withdrawal account under a unique nullifier"
status = "done"
wave = 6
requires = ["10.3"]
do_1 = "Implement lx_asset_withdraw_request to execute a single leg from the agent main account to system:paxeer-withdrawals with reason LXP_REASON_WITHDRAWAL, so the LayerX debit is committed and checkpointable before any Paxeer payout becomes claimable."
do_2 = "Implement lx_withdrawal_nullifier as the domain-separated commitment over network_id, withdrawal_id, account, asset, amount and checkpoint_id, and record it in resulting state at request time."
do_3 = "Implement lx_asset_nullifier_seen so a settlement or a further withdrawal presenting an already recorded nullifier is rejected with WITHDRAWAL_ALREADY_SETTLED and no withdrawal can pay twice."
do_4 = "Keep total LayerX supply unchanged at request time, retiring value against system:paxeer-reserve only when a finalised settlement reference is presented, so the withdrawal path never burns."
do_5 = "Add tests for a duplicate nullifier, a withdrawal exceeding balance and a request-then-settle sequence, asserting conservation across every step."
verify_cmd = "make test-asset-withdraw"
symbols = ["lx_asset_withdraw_request","lx_withdrawal_nullifier","lx_asset_nullifier_seen"]
touches = ["src/modules/asset/lx_asset_custody.c","include/layerx/lx_asset.h","tests/modules/test_asset_withdraw.c"]
reqs = ["1.8","7.6","11.7","13.13","14.9"]

[task.10.6]
title = "Report full reserve reconciliation per asset"
status = "done"
wave = 6
requires = ["10.4","10.5"]
do_1 = "Implement lx_asset_reserve_reconcile computing, per asset, the sum of all agent, module and system account balances excluding the reserve mirror, and comparing it against the custody amount attested by the most recent finalised Paxeer proof less the amount already settled out of the withdrawal account."
do_2 = "Implement lx_asset_supply_check at every batch seal, recomputing each asset total_units against the account tree and returning LX_FATAL_SUPPLY_MISMATCH on divergence rather than sealing a root that violates full reserve accounting."
do_3 = "Implement lx_asset_reserve_report emitting a canonical per-asset report with zero tolerance, enumerating agent, escrow, budget, stream, margin, liquidity, insurance, fee treasury and withdrawal balances separately."
do_4 = "Reject the offending activity rather than producing a batch whose resulting state root breaks reconciliation, and prove in test that a synthetic over-credit is caught at seal time."
do_5 = "Add a replay test that runs the report at every batch boundary of a mixed deposit, transfer and withdrawal history and asserts exact equality throughout."
verify_cmd = "make test-asset-reserve"
symbols = ["lx_asset_reserve_reconcile","lx_asset_supply_check","lx_asset_reserve_report"]
touches = ["src/modules/asset/lx_asset_reserve.c","include/layerx/lx_asset.h","tests/modules/test_asset_reserve.c"]
reqs = ["14.10","11.6","11.8","28.11"]

[task.11]
title = "Build the escrow module with holds, capture, timeout and disputes"
status = "pending"

[task.11.1]
title = "Create holds by moving funds into an escrow subaccount"
status = "done"
wave = 7
requires = ["10.3"]
do_1 = "Register the escrow module as module id 2 declaring escrow.open, escrow.capture, escrow.partial_capture, escrow.release, escrow.timeout, escrow.dispute_open and escrow.dispute_resolve."
do_2 = "Implement lx_escrow_open_execute to emit exactly one leg from agent owner main to agent owner escrow subaccount with reason LXP_REASON_ESCROW_LOCK, and no other monetary effect."
do_3 = "Record beneficiary, arbiter, asset, locked amount, captured amount, state, expiry timestamp, dispute window, terms hash and agreement reference through lx_escrow_state_put, all under the escrow module namespace."
do_4 = "Model the hold lifecycle as the explicit states Open, PartiallyCaptured, Captured, Released, Disputed, Resolved and TimedOut, rejecting any transition not defined for the current state with LX_ERR_ESCROW_STATE."
do_5 = "Add tests proving the opening transfer is the only effect, that the escrow subaccount is created with a zero opening balance before the credit, and that the recorded amount equals the transferred amount exactly."
verify_cmd = "make test-escrow-open"
symbols = ["lx_escrow_module_iface","lx_escrow_open_execute","lx_escrow_state_put","lx_escrow_lookup"]
touches = ["src/modules/escrow/lx_escrow_open.c","include/layerx/lx_escrow.h","tests/modules/test_escrow_open.c"]
reqs = ["15.1","7.5","10.1","13.1"]

[task.11.2]
title = "Implement capture and partial capture out of the escrow subaccount"
status = "done"
wave = 7
requires = ["11.1"]
do_1 = "Implement lx_escrow_capture_execute for the full remaining amount as one leg from the escrow subaccount to the beneficiary account with reason LXP_REASON_ESCROW_CAPTURE, setting the subaccount to zero and marking the hold closed."
do_2 = "Implement lx_escrow_partial_capture_execute to transfer exactly the named smaller amount to the beneficiary, reduce the remaining hold amount by exactly that amount, and keep the hold active for the remainder."
do_3 = "Implement lx_escrow_remaining as the authoritative remaining figure derived from the escrow subaccount balance, so allowance bookkeeping can never diverge from real funds."
do_4 = "Reject a capture exceeding the remaining amount with CAPTURE_EXCEEDS_HOLD, an unknown hold identifier, and a capturing authority that is neither the recorded beneficiary nor an authority delegated by the owner with UNAUTHORIZED_CAPTURE, moving no funds in any of those cases."
do_5 = "Return the original receipt with zero additional funds moved when a capture is resubmitted under an idempotency key that already produced an economic result."
verify_cmd = "make test-escrow-capture"
symbols = ["lx_escrow_capture_execute","lx_escrow_partial_capture_execute","lx_escrow_remaining"]
touches = ["src/modules/escrow/lx_escrow_capture.c","include/layerx/lx_escrow.h","tests/modules/test_escrow_capture.c"]
reqs = ["15.3","15.4","15.5","15.10","7.5"]

[task.11.3]
title = "Implement release and deterministic timeout from batch timestamps"
status = "done"
wave = 7
requires = ["11.1"]
do_1 = "Implement lx_escrow_release_execute to return the entire remaining escrow subaccount balance to the owner main account with reason LXP_REASON_ESCROW_RELEASE, leaving the subaccount at zero and the hold closed."
do_2 = "Implement lx_escrow_timeout_execute so that when the batch timestamp reaches or passes the recorded expiry and the hold is neither closed nor disputed, the remaining balance is released to the owner deterministically during that batch."
do_3 = "Evaluate expiry solely against the sealed batch timestamp through the module context, never a wall clock, a timer or a background job, and drive lazy evaluation from lx_escrow_epoch_begin plus the first activity that observes the boundary."
do_4 = "Reject any capture attempted against a timed-out hold with HOLD_EXPIRED, and reject any transfer out of the escrow subaccount that is not a capture, release, expiry release or dispute resolution produced by this module with UNAUTHORIZED_ESCROW_SPEND."
do_5 = "Add a replay test that runs the identical history under two different real elapsed durations and asserts byte-identical roots, proving timeout depends only on batch time."
verify_cmd = "make test-escrow-timeout"
symbols = ["lx_escrow_release_execute","lx_escrow_timeout_execute","lx_escrow_epoch_begin"]
touches = ["src/modules/escrow/lx_escrow_timeout.c","include/layerx/lx_escrow.h","tests/modules/test_escrow_timeout.c"]
reqs = ["15.6","15.7","21.4","27.10","7.5"]

[task.11.4]
title = "Implement dispute entry and atomic dispute resolution"
status = "done"
wave = 7
requires = ["11.2","11.3"]
do_1 = "Implement lx_escrow_dispute_open_execute to mark an active hold disputed when the batch timestamp is inside the recorded dispute window, emitting zero transfer legs, and reject a dispute raised after the window with DISPUTE_WINDOW_CLOSED."
do_2 = "Reject subsequent capture, release and expiry release while a hold is disputed with HOLD_DISPUTED until a resolution is accepted."
do_3 = "Implement lx_escrow_dispute_resolve_execute as one atomic transfer set of at most two legs splitting the escrow subaccount balance between beneficiary and owner, requiring that the sum of the legs equals the subaccount balance exactly and leaves it at zero."
do_4 = "Implement lx_escrow_split_bps using integer basis points with floor division and assign the residue explicitly to the owner main account, so dust is never dropped and never implicit."
do_5 = "Roll the whole set back and leave the hold disputed if any leg violates an invariant, and add a test proving no partial split is ever observable."
verify_cmd = "make test-escrow-dispute"
symbols = ["lx_escrow_dispute_open_execute","lx_escrow_dispute_resolve_execute","lx_escrow_split_bps"]
touches = ["src/modules/escrow/lx_escrow_dispute.c","include/layerx/lx_escrow.h","tests/modules/test_escrow_dispute.c"]
reqs = ["15.8","15.9","11.1","11.5","13.6"]

[task.11.5]
title = "Prove held funds are unspendable from the owner main account"
status = "done"
wave = 7
requires = ["11.4"]
do_1 = "Implement lx_escrow_authority_check so the escrow subaccount is debitable only under the escrow authority owned by this module, and an attempted debit under an owner direct signature or a session key is rejected with ERR_UNAUTHORIZED_DEBIT."
do_2 = "Add an adversarial test that opens a hold for the full main balance and then attempts an ordinary transfer of the same amount from the main account, asserting INSUFFICIENT_FUNDS because the funds physically left."
do_3 = "Implement lx_escrow_invariant_check asserting that a terminal hold with a non-zero escrow subaccount balance is an invariant violation that halts the node rather than being logged and skipped."
do_4 = "Add a property test over randomised open, partial capture, release, timeout and dispute sequences asserting that locked amount always equals the escrow subaccount balance and that conservation holds at every step."
do_5 = "Extend the reserve report so every escrow subaccount balance appears as a distinct enumerated line rather than being folded into the owner main balance."
verify_cmd = "make test-escrow-invariants"
symbols = ["lx_escrow_authority_check","lx_escrow_invariant_check"]
touches = ["src/modules/escrow/lx_escrow_invariants.c","include/layerx/lx_escrow.h","tests/modules/test_escrow_invariants.c"]
reqs = ["15.2","10.9","10.1","13.2","13.4"]

[task.12]
title = "Build the budget module with recurring allowances and delegated spending"
status = "pending"

[task.12.1]
title = "Create and fund budget subaccounts"
status = "done"
wave = 7
requires = ["10.3"]
do_1 = "Register the budget module as module id 3 declaring budget.create, budget.fund, budget.amend, budget.delegate_add, budget.delegate_remove, budget.spend and budget.close."
do_2 = "Implement lx_budget_create_execute and lx_budget_fund_execute to emit exactly one leg from agent owner main to agent owner budget subaccount with reason LXP_REASON_BUDGET_FUND."
do_3 = "Record asset, per-period limit, period length in protocol time units, period start timestamp, rollover policy with its carry cap, delegate set, purpose hash, expiry and revocation sequence through lx_budget_state_put."
do_4 = "Reject a budget whose period length, per-period limit or expiry is absent or out of declared bounds, and reject a duplicate budget identifier, before any funding leg is constructed."
do_5 = "Add tests proving the funding transfer is the only monetary effect and that the budget subaccount balance, not the recorded limit, is the source of spendable value."
verify_cmd = "make test-budget-create"
symbols = ["lx_budget_module_iface","lx_budget_create_execute","lx_budget_fund_execute","lx_budget_state_put"]
touches = ["src/modules/budget/lx_budget_create.c","include/layerx/lx_budget.h","tests/modules/test_budget_create.c"]
reqs = ["16.1","7.5","10.1","13.1"]

[task.12.2]
title = "Compute recurring period windows from batch timestamps"
status = "done"
wave = 7
requires = ["12.1"]
do_1 = "Implement lx_budget_periods_elapsed as integer division of the batch timestamp minus period start by the period length, with no timer, no cron and no wall-clock read anywhere in the path."
do_2 = "Implement lx_budget_rollover to advance the period start by whole period lengths and reset the remaining allowance to the configured per-period limit at the first activity in a batch that observes the boundary."
do_3 = "Implement the carry-forward policy by adding unspent allowance into the new period up to the configured carry cap and discarding the excess, and the discard policy by dropping all unspent allowance."
do_4 = "Drive lazy rollover from both lx_budget_epoch_begin and the spend path so a budget untouched for many periods rolls forward identically however it is next observed."
do_5 = "Add a replay test crossing several period boundaries in one batch and assert byte-identical resulting state roots on repeat execution."
verify_cmd = "make test-budget-period"
symbols = ["lx_budget_periods_elapsed","lx_budget_rollover","lx_budget_epoch_begin"]
touches = ["src/modules/budget/lx_budget_period.c","include/layerx/lx_budget.h","tests/modules/test_budget_period.c"]
reqs = ["16.6","21.4","27.10","5.7"]

[task.12.3]
title = "Implement allowance accounting and budget spends"
status = "done"
wave = 7
requires = ["12.2"]
do_1 = "Implement lx_budget_spend_execute to emit exactly one leg from the budget subaccount to the named recipient main account with reason LXP_REASON_BUDGET_SPEND."
do_2 = "Implement lx_budget_allowance_debit to reduce the remaining period allowance by exactly the spent amount at execution time, so concurrent or reordered submissions can never exceed the limit in aggregate."
do_3 = "Reject a spend above the remaining period allowance with BUDGET_ALLOWANCE_EXCEEDED even when the subaccount holds enough balance, and reject a spend within the allowance but above the subaccount balance with INSUFFICIENT_BUDGET_FUNDS, so allowance accounting alone never creates spendable value."
do_4 = "Implement lx_budget_remaining as the minimum of the remaining period allowance and the subaccount balance, and expose it to the read path so callers see the truly spendable figure."
do_5 = "Add tests for exhaustion at the exact limit, a spend straddling a period boundary and a spend against a defunded budget."
verify_cmd = "make test-budget-spend"
symbols = ["lx_budget_spend_execute","lx_budget_allowance_debit","lx_budget_remaining"]
touches = ["src/modules/budget/lx_budget_spend.c","include/layerx/lx_budget.h","tests/modules/test_budget_spend.c"]
reqs = ["16.2","16.3","16.4","7.5","13.1"]

[task.12.4]
title = "Authorise delegated spenders and recipient-initiated pulls"
status = "done"
wave = 7
requires = ["12.3"]
do_1 = "Implement lx_budget_delegate_add_execute and lx_budget_delegate_remove_execute to maintain the delegate set in canonical byte-lexicographic order with no monetary effect of their own."
do_2 = "Implement lx_budget_authorize_delegate to reject a submitter absent from the delegate set, or whose capability has expired, or whose scope does not cover the asset, recipient or amount, with UNAUTHORIZED_DELEGATE and no monetary effect."
do_3 = "Accumulate delegated consumption inside state at execution time so the first activity that would carry cumulative consumption past a delegated limit is rejected."
do_4 = "Support recipient-initiated pulls only under a signed payer grant naming the authorised recipient, asset, maximum amount, allowance kind, expiry, permitted purpose and revocation sequence, rejecting anything beyond the grant with GRANT_SCOPE_VIOLATION."
do_5 = "Add tests for a revoked delegate, an expired capability, an out-of-scope recipient and a pull exceeding the grant maximum."
verify_cmd = "make test-budget-delegate"
symbols = ["lx_budget_delegate_add_execute","lx_budget_delegate_remove_execute","lx_budget_authorize_delegate"]
touches = ["src/modules/budget/lx_budget_delegate.c","include/layerx/lx_budget.h","tests/modules/test_budget_delegate.c"]
reqs = ["16.5","16.10","3.3","3.4","9.3"]

[task.12.5]
title = "Implement defunding, revocation and budget closure"
status = "done"
wave = 7
requires = ["12.3"]
do_1 = "Implement lx_budget_defund_execute to move the requested amount from the budget subaccount back to the owner main account and reduce the remaining period allowance so it never exceeds the resulting subaccount balance."
do_2 = "Implement lx_budget_revoke_execute to move the entire remaining subaccount balance to the owner main account in the same activity, mark the budget revoked, and reject every later spend with BUDGET_REVOKED."
do_3 = "Reject a revocation or capability update whose revocation sequence is not strictly greater than the recorded one with STALE_REVOCATION, so a replayed authorisation can never reinstate a revoked delegate."
do_4 = "Implement lx_budget_close_execute to drain the subaccount to zero, omitting the leg entirely when the balance is already zero rather than emitting a zero-amount leg."
do_5 = "Add tests proving a revoked budget leaves no stranded value and that a spend ordered before the revocation stays intact while one ordered after is rejected."
verify_cmd = "make test-budget-revoke"
symbols = ["lx_budget_defund_execute","lx_budget_revoke_execute","lx_budget_close_execute"]
touches = ["src/modules/budget/lx_budget_close.c","include/layerx/lx_budget.h","tests/modules/test_budget_close.c"]
reqs = ["16.7","16.8","16.9","7.5","13.2"]

[task.13]
title = "Build the stream module for time-based and metered payments"
status = "pending"

[task.13.1]
title = "Open and top up streams into a stream subaccount"
status = "done"
wave = 7
requires = ["10.3"]
do_1 = "Register the stream module as module id 4 declaring stream.open, stream.top_up, stream.meter, stream.settle, stream.pause, stream.resume and stream.close."
do_2 = "Implement lx_stream_open_execute and lx_stream_top_up_execute to emit exactly one leg from agent payer main to agent payer stream subaccount with reason LXP_REASON_STREAM_FUND, and no other monetary effect."
do_3 = "Record recipient, asset, mode as TIME or METERED, rate per second or price per unit, rate unit, start timestamp, optional end timestamp, total cap, drawn total and remainder carry through lx_stream_state_put."
do_4 = "Register the meter authority key set for a metered stream at open time so later usage attestations have a recorded authority to verify against."
do_5 = "Add tests proving the funding transfer is the only effect at open and that a top up resumes a previously underfunded stream from the top-up timestamp."
verify_cmd = "make test-stream-open"
symbols = ["lx_stream_module_iface","lx_stream_open_execute","lx_stream_top_up_execute","lx_stream_state_put"]
touches = ["src/modules/stream/lx_stream_open.c","include/layerx/lx_stream.h","tests/modules/test_stream_open.c"]
reqs = ["17.1","7.5","10.1","13.1"]

[task.13.2]
title = "Implement deterministic integer accrual from batch timestamps"
status = "done"
wave = 7
requires = ["13.1"]
do_1 = "Implement lx_stream_elapsed_ms deriving elapsed protocol time solely from sealed batch header timestamps, never an operating system clock or any value outside replayable history."
do_2 = "Implement lx_stream_accrue multiplying rate by elapsed time into 256 bits, adding the carried residue, floor-dividing to the smallest indivisible unit and writing the new residue back through lx_stream_carry_apply, so nothing is rounded away and the payer is never over-drawn by a rounding artifact."
do_3 = "Reject a negative elapsed interval or a batch timestamp earlier than the last settlement for that stream with NON_MONOTONIC_TIME and move no funds."
do_4 = "Reject an accrual product that would exceed the maximum representable fixed-width value with ACCRUAL_OVERFLOW rather than wrapping, saturating or truncating."
do_5 = "Stop accrual exactly at the recorded end timestamp or total cap and cap the lifetime paid amount at the recorded total cap."
do_6 = "Add a replay test running an identical timestamp history twice and asserting byte-identical accrued amounts and residues."
verify_cmd = "make test-stream-accrual"
symbols = ["lx_stream_accrue","lx_stream_elapsed_ms","lx_stream_carry_apply"]
touches = ["src/modules/stream/lx_stream_accrue.c","include/layerx/lx_stream.h","tests/modules/test_stream_accrue.c"]
reqs = ["17.2","17.3","17.4","17.10","5.5"]

[task.13.3]
title = "Implement metered accrual from signed usage attestations"
status = "done"
wave = 7
requires = ["13.2"]
do_1 = "Implement lx_stream_meter_execute to accept a cumulative meter reading and accrue the delta multiplied by the recorded unit price under the same floor rounding rule as time-based accrual."
do_2 = "Implement lx_stream_meter_authority_check rejecting an attestation not signed by a meter authority recorded for the stream with UNAUTHORIZED_METER, verified over the canonical attestation bytes under its domain tag."
do_3 = "Reject a cumulative reading lower than the last recorded reading with METER_REGRESSION and accrue nothing, so meters are monotone by protocol rule."
do_4 = "Emit zero transfer legs from the meter activity itself, leaving all value movement to the settlement path."
do_5 = "Add tests for a regressed reading, an unauthorised meter, a duplicate reading and a reading that overflows the accrual product."
verify_cmd = "make test-stream-meter"
symbols = ["lx_stream_meter_execute","lx_stream_meter_authority_check","lx_stream_metered_accrue"]
touches = ["src/modules/stream/lx_stream_meter.c","include/layerx/lx_stream.h","tests/modules/test_stream_meter.c"]
reqs = ["17.5","17.2","17.4","7.8"]

[task.13.4]
title = "Settle streams through the ledger kernel with underfunded clamping"
status = "done"
wave = 7
requires = ["13.2"]
do_1 = "Implement lx_stream_settle_amount as newly accrued minus already settled, and lx_stream_settle_execute emitting one leg from the payer stream subaccount to the recipient main account with reason LXP_REASON_STREAM_DRAW."
do_2 = "Clamp a settlement that exceeds the stream subaccount balance to the available balance, mark the stream underfunded through lx_stream_mark_underfunded, and suspend further accrual from that settlement timestamp."
do_3 = "Create no debt and never allow any account to become negative, and resume accrual only from the timestamp of a later top-up activity."
do_4 = "Return the original receipt with no additional funds moved when a settlement is resubmitted under an idempotency key that already produced an economic result."
do_5 = "Add tests for exact-balance settlement, an underfunded clamp followed by a top up, and a settlement against a closed stream returning STREAM_CLOSED."
verify_cmd = "make test-stream-settle"
symbols = ["lx_stream_settle_execute","lx_stream_settle_amount","lx_stream_mark_underfunded"]
touches = ["src/modules/stream/lx_stream_settle.c","include/layerx/lx_stream.h","tests/modules/test_stream_settle.c"]
reqs = ["17.3","17.6","17.9","7.5","13.4"]

[task.13.5]
title = "Implement pause, resume and cancellation"
status = "done"
wave = 7
requires = ["13.4"]
do_1 = "Implement lx_stream_pause_execute to settle accrual up to the batch timestamp of the pause and accrue nothing for the paused interval."
do_2 = "Implement lx_stream_resume_execute to accrue again only from the batch timestamp of the resume, with no retroactive catch-up for the paused window."
do_3 = "Implement lx_stream_close_execute as one atomic transfer set paying the accrued unsettled amount to the recipient and returning the entire remaining stream subaccount balance to the payer main account, leaving the subaccount at zero and the stream closed."
do_4 = "Roll the whole set back and leave the stream open if any leg violates an invariant, and omit a zero-amount return leg rather than emitting it."
do_5 = "Reject a pause, resume, settle or cancel against an already closed stream with STREAM_CLOSED and restrict pause and resume to an authorised party."
do_6 = "Add a property test over randomised pause, resume, meter, settle and close sequences asserting conservation and a zero terminal subaccount balance."
verify_cmd = "make test-stream-lifecycle"
symbols = ["lx_stream_pause_execute","lx_stream_resume_execute","lx_stream_close_execute"]
touches = ["src/modules/stream/lx_stream_lifecycle.c","include/layerx/lx_stream.h","tests/modules/test_stream_lifecycle.c"]
reqs = ["17.7","17.8","17.9","11.1","7.5"]

[task.14]
title = "Build the service module covering the complete agent work lifecycle"
status = "pending"

[task.14.1]
title = "Implement offers and agreement formation"
status = "done"
wave = 8
requires = ["11.2"]
do_1 = "Register the service module as module id 5 declaring service.offer_publish, service.offer_withdraw, service.agreement_propose, service.agreement_accept, service.commit_task, service.commit_abandon, service.tool_exec_attest, service.progress_report, service.deliver, service.accept, service.reject, service.dispute_open and service.dispute_resolve."
do_2 = "Implement lx_service_offer_publish_execute recording the offering agent, asset, price, terms hash, deliverable specification hash, delivery deadline, acceptance window, dispute window, default outcome and offer expiry, assigning it a global sequence and including it in the activity merkle root of its batch."
do_3 = "Implement lx_service_agreement_accept_execute binding the acceptance to the offer activity id and requiring an exact match of the recorded terms hash, rejecting an expired, already accepted or withdrawn offer with OFFER_UNAVAILABLE and a differing hash with TERMS_MISMATCH."
do_4 = "Emit zero transfer legs from every activity in this subtask, and reject with MODULE_MAY_NOT_WRITE_BALANCE any service payload that attempts to encode a balance mutation of its own."
do_5 = "Reference any escrow hold only by identifier through the agreement record, leaving the hold itself to the escrow module and the ledger."
verify_cmd = "make test-service-offer"
symbols = ["lx_service_module_iface","lx_service_offer_publish_execute","lx_service_offer_withdraw_execute","lx_service_agreement_accept_execute"]
touches = ["src/modules/service/lx_service_offer.c","include/layerx/lx_service.h","tests/modules/test_service_offer.c"]
reqs = ["18.2","18.3","18.1","7.8","4.3"]

[task.14.2]
title = "Record work commitments as ordered attested activities"
status = "done"
wave = 8
requires = ["14.1"]
do_1 = "Implement lx_service_commit_task_execute recording the committing provider identity, the agreement id, the task hash, the deadline as a protocol timestamp bound, the declared resource bound and the identifier of any referenced escrow hold."
do_2 = "Move no funds from the commitment even when an escrow hold is named, so the commitment is evidence and the hold is a separate authenticated transfer."
do_3 = "Implement lx_service_commit_abandon_execute to close a commitment with a reason code, leaving any referenced hold to expire or be resolved by the escrow module on its own rules."
do_4 = "Reject a commitment from an identity that is not a party to the agreement, or against an agreement not in an admitting state, with LX_ERR_AGREEMENT_STATE."
do_5 = "Add tests asserting the receipt effect list for a commitment contains zero transfer effects and that the commitment record is reproduced byte-identically on replay."
verify_cmd = "make test-service-commit"
symbols = ["lx_service_commit_task_execute","lx_service_commit_abandon_execute","lx_service_commitment_put"]
touches = ["src/modules/service/lx_service_commit.c","include/layerx/lx_service.h","tests/modules/test_service_commit.c"]
reqs = ["18.4","18.1","7.8","2.9"]

[task.14.3]
title = "Implement tool execution attestations"
status = "done"
wave = 8
requires = ["14.2"]
do_1 = "Implement lx_service_tool_exec_attest_execute requiring a tool identifier, an input commitment hash, an output commitment hash, execution start and end timestamp bounds, resource units and an attestor identity."
do_2 = "Implement lx_service_attestor_verify to check the signature under a key holding attestation authority for the committing agent, rejecting an invalid signature, a revoked or expired key, or an absent required commitment hash with INVALID_ATTESTATION."
do_3 = "Order the attestation in the activity log and commit its exact payload bytes into the activity merkle root, storing the commitment on chain and the bytes behind the data availability reference."
do_4 = "Emit zero transfer legs, so an attestation is replayable evidence that a tool ran on a named input and produced a named output, never a payment trigger."
do_5 = "Add tests for a revoked attestor key, a missing output hash, a duplicate attestation and canonical round-trip of the attestation record."
verify_cmd = "make test-service-attest"
symbols = ["lx_service_tool_exec_attest_execute","lx_service_attestor_verify","lx_service_execution_put"]
touches = ["src/modules/service/lx_service_attest.c","include/layerx/lx_service.h","tests/modules/test_service_attest.c"]
reqs = ["18.5","3.7","4.10","7.8"]

[task.14.4]
title = "Implement delivery with deliverable hashes and availability references"
status = "done"
wave = 8
requires = ["14.2"]
do_1 = "Implement lx_service_deliver_execute recording every deliverable hash with its artifact size and data availability reference, in canonical sorted order."
do_2 = "Implement lx_service_deliverable_check rejecting a deliverable hash that does not match the deliverable specification hash committed in the accepted offer with DELIVERABLE_MISMATCH."
do_3 = "Reject a delivery whose batch timestamp is later than the recorded delivery deadline with DELIVERY_DEADLINE_PASSED, evaluated only against the sealed batch timestamp."
do_4 = "Emit zero transfer legs and require any resulting payment to execute as a separate escrow capture or ledger transfer referencing the same agreement id through context_hash."
do_5 = "Add tests for a mismatched deliverable, a late delivery, a redelivery after rejection and a delivery whose availability reference is absent."
verify_cmd = "make test-service-deliver"
symbols = ["lx_service_deliver_execute","lx_service_deliverable_check","lx_service_delivery_put"]
touches = ["src/modules/service/lx_service_deliver.c","include/layerx/lx_service.h","tests/modules/test_service_deliver.c"]
reqs = ["18.6","18.1","23.1","7.8","6.4"]

[task.14.5]
title = "Implement acceptance, rejection and the deterministic deadline outcome"
status = "done"
wave = 8
requires = ["14.4"]
do_1 = "Implement lx_service_accept_execute to mark the agreement accepted and closed for delivery purposes when the buyer accepts within the acceptance window, emitting zero transfer legs."
do_2 = "Implement lx_service_reject_execute recording the rejection reason code and the contested deliverable hashes, and holding the agreement in a disputable state until the recorded dispute window closes."
do_3 = "Implement lx_service_acceptance_default to apply the default outcome recorded in the accepted offer terms deterministically during the first batch whose timestamp crosses the acceptance window end, driven from lx_service_epoch_begin."
do_4 = "Require any resulting payment to execute as a separate 402LXP escrow capture or transfer rather than as an effect of the acceptance activity itself."
do_5 = "Add a replay test proving the default outcome applies identically on every replay of the same history and is never influenced by when the replay is run."
verify_cmd = "make test-service-acceptance"
symbols = ["lx_service_accept_execute","lx_service_reject_execute","lx_service_acceptance_default","lx_service_epoch_begin"]
touches = ["src/modules/service/lx_service_acceptance.c","include/layerx/lx_service.h","tests/modules/test_service_acceptance.c"]
reqs = ["18.7","18.8","18.9","7.8","27.10"]

[task.14.6]
title = "Implement service disputes and prove zero direct monetary effect"
status = "done"
wave = 8
requires = ["14.5"]
do_1 = "Implement lx_service_dispute_open_execute ordering the dispute and recording the evidence hashes it carries when the raiser is a party and the batch timestamp is inside the recorded dispute window."
do_2 = "Reject a raiser who is not a party with UNAUTHORIZED_DISPUTANT and a dispute past the window end with DISPUTE_WINDOW_CLOSED."
do_3 = "Implement lx_service_dispute_resolve_execute to record the ruling and its basis points, and to reference the escrow dispute resolution that actually moves the funds rather than moving them here."
do_4 = "Implement lx_service_effect_audit asserting across every service activity type that the emitted effect list contains zero transfer effects, and wire it into the module test target."
do_5 = "Add an end-to-end test running offer, accept, commit, attest, deliver, accept-delivery and escrow capture, asserting that every unit of value moved through the ledger kernel and none through the service module."
verify_cmd = "make test-service-dispute"
symbols = ["lx_service_dispute_open_execute","lx_service_dispute_resolve_execute","lx_service_effect_audit"]
touches = ["src/modules/service/lx_service_dispute.c","include/layerx/lx_service.h","tests/modules/test_service_dispute.c"]
reqs = ["18.10","18.1","7.8","13.1"]

[task.15]
title = "Build the oracle adapter and signed oracle activity intake"
status = "pending"

[task.15.1]
title = "Keep the Crossverse adapter entirely outside execution"
status = "done"
wave = 8
requires = ["8.3"]
do_1 = "Implement lx_oracle_adapter_run in the network layer so it polls Crossverse, canonically encodes each observation and submits it as an ordinary signed activity, with no code path from a state transition out to the network."
do_2 = "Implement lx_oracle_observation_sign over the domain-separated canonical bytes of the observation using the oracle key, and lx_oracle_activity_encode to wrap it as a standard activity envelope with its own account sequence and fee limit."
do_3 = "Include market id, observation sequence, price scaled by the market quote decimals, observed-at timestamp and source identifier as integer fields only, with no floating point anywhere in the encoding."
do_4 = "Add lx_oracle_adapter_isolation_check to the build so no translation unit under src/modules, src/ledger, src/state or src/protocol links any socket, HTTP or DNS symbol, keeping the adapter strictly a submitter."
do_5 = "Add a test that runs a full replay with the adapter binary absent, proving execution outcomes depend only on already accepted oracle activities."
verify_cmd = "make test-oracle-adapter"
symbols = ["lx_oracle_adapter_run","lx_oracle_observation_sign","lx_oracle_activity_encode","lx_oracle_adapter_isolation_check"]
touches = ["src/network/lx_oracle_adapter.c","include/layerx/lx_oracle.h","tests/network/test_oracle_adapter.c"]
reqs = ["4.5","19.5","27.8","5.8"]

[task.15.2]
title = "Admit oracle observations as ordered signed activities"
status = "done"
wave = 8
requires = ["15.1"]
do_1 = "Implement lx_oracle_push_execute to accept an observation only when signed by a key in the market permitted oracle key set, verified by lx_oracle_key_set_check over the canonical observation bytes."
do_2 = "Store the exact accepted payload bytes through lx_oracle_store_put so the observation becomes part of replayable history rather than a derived value."
do_3 = "Emit zero transfer legs from the observation itself, so external data can never directly alter a balance and any effect executes later as a separately authorised activity."
do_4 = "Implement lx_oracle_observation_decode rejecting a non-canonical encoding, an unknown market, an unregistered oracle key and any non-integer price representation."
do_5 = "Add tests proving an accepted observation changes no balance and that an unregistered oracle key is rejected outright rather than downgraded."
verify_cmd = "make test-oracle-intake"
symbols = ["lx_oracle_push_execute","lx_oracle_key_set_check","lx_oracle_store_put","lx_oracle_observation_decode"]
touches = ["src/modules/perps/lx_oracle_intake.c","include/layerx/lx_oracle.h","tests/modules/test_oracle_intake.c"]
reqs = ["19.5","13.11","4.5","7.8"]

[task.15.3]
title = "Enforce staleness, monotonicity, bounds and deviation limits"
status = "done"
wave = 8
requires = ["15.2"]
do_1 = "Implement lx_oracle_staleness_check comparing the observation time against the sealed batch timestamp and the configured maximum staleness for the market, with no wall-clock read."
do_2 = "Reject an observation sequence that is not strictly greater than the last accepted one for that market, so replayed or reordered observations cannot be admitted twice."
do_3 = "Implement lx_oracle_bounds_check and lx_oracle_deviation_check to reject a price outside the configured bounds or beyond the configured deviation limit from the last accepted price, using integer basis points only."
do_4 = "Implement lx_oracle_store_latest returning the most recently accepted in-bound observation for a market, and never substitute, extrapolate or reuse a last-known price for a rejected one."
do_5 = "Add tests for a stale observation, a regressed sequence, an out-of-bounds price and a price at the exact deviation boundary."
verify_cmd = "make test-oracle-bounds"
symbols = ["lx_oracle_staleness_check","lx_oracle_bounds_check","lx_oracle_deviation_check","lx_oracle_store_latest"]
touches = ["src/modules/perps/lx_oracle_checks.c","include/layerx/lx_oracle.h","tests/modules/test_oracle_checks.c"]
reqs = ["19.6","19.1","21.4","5.7"]

[task.15.4]
title = "Commit accepted oracle inputs into the batch oracle root"
status = "done"
wave = 8
requires = ["15.2"]
do_1 = "Implement lx_oracle_leaf_encode as the canonical encoding of an accepted observation including its signature, and lx_oracle_root_compute as a merkle root over those leaves in canonical order."
do_2 = "Use distinct domain tags for oracle leaves and internal nodes and a single deterministic rule for leaf counts that are not a power of two, so a node can never be reinterpreted as a leaf."
do_3 = "Wire lx_batch_header_set_oracle_root into batch sealing so oracle_root is one of the header commitments a guarantor independently recomputes."
do_4 = "Include the accepted oracle inputs in the data availability bundle so a guarantor attesting possession is attesting to the exact bytes that were executed."
do_5 = "Add a test recomputing oracle_root from served availability bytes and asserting byte equality with the sealed header value."
verify_cmd = "make test-oracle-root"
symbols = ["lx_oracle_root_compute","lx_oracle_leaf_encode","lx_batch_header_set_oracle_root"]
touches = ["src/sequencer/lx_oracle_root.c","include/layerx/lx_batch.h","tests/sequencer/test_oracle_root.c"]
reqs = ["21.1","6.5","23.1","22.3"]

[task.15.5]
title = "Fail closed by halting an affected market rather than guessing"
status = "done"
wave = 8
requires = ["15.3"]
do_1 = "Implement lx_oracle_fail_closed_eval so that when no acceptable observation exists for a market within the required freshness window the market is halted through lx_oracle_market_halt rather than falling back to any substituted price."
do_2 = "Implement lx_oracle_market_halted as the single query every market-touching path consults, rejecting new orders, position increases and liquidations for a halted market with MARKET_HALTED."
do_3 = "Suspend funding accrual for a halted market and permit only order cancellation and margin additions while it stays halted."
do_4 = "Resume a market only when a fresh in-bound observation from a permitted oracle key is accepted, never on elapsed time and never by operator action outside the ordered activity log."
do_5 = "Add tests for halt on staleness, rejection of every disallowed activity while halted, and resumption on the first valid fresh observation."
verify_cmd = "make test-oracle-failclosed"
symbols = ["lx_oracle_fail_closed_eval","lx_oracle_market_halt","lx_oracle_market_halted"]
touches = ["src/modules/perps/lx_oracle_halt.c","include/layerx/lx_oracle.h","tests/modules/test_oracle_halt.c"]
reqs = ["19.7","19.6","13.11","27.9"]

[task.16]
title = "Build the perps module with real margin accounts and atomic liquidation"
status = "pending"

[task.16.1]
title = "Define markets as integer parameter records"
status = "done"
wave = 9
requires = ["15.4"]
do_1 = "Register the perps module as module id 6 declaring perps.market_create, perps.market_halt, perps.oracle_push, perps.order_place, perps.order_cancel, perps.position_open, perps.position_increase, perps.position_close, perps.funding_tick, perps.liquidate and perps.adl."
do_2 = "Implement lx_perps_market_create_execute recording market id, quote asset, contract size, tick size, lot size, initial and maintenance margin ratios in basis points, funding interval, maximum oracle staleness, price bounds and the permitted oracle key set, all as integer parameters."
do_3 = "Reject a duplicate market id with MARKET_ALREADY_EXISTS and reject any parameter outside its declared governance bounds before recording anything."
do_4 = "Implement lx_perps_market_put and lx_perps_market_lookup over the module namespace with canonical byte-lexicographic iteration order."
do_5 = "Add tests for a duplicate market, an out-of-bounds margin ratio and canonical round-trip of the market record."
verify_cmd = "make test-perps-market"
symbols = ["lx_perps_module_iface","lx_perps_market_create_execute","lx_perps_market_put","lx_perps_market_lookup"]
touches = ["src/modules/perps/lx_perps_market.c","include/layerx/lx_perps.h","tests/modules/test_perps_market.c"]
reqs = ["19.1","4.3","5.2","20.1"]

[task.16.2]
title = "Implement the order book and deterministic matching rules"
status = "done"
wave = 9
requires = ["16.1","15.5"]
do_1 = "Implement the resting book keyed by market id, side, price and global sequence, iterated in canonical order with no dependence on pointer values, allocation order or hash seeds."
do_2 = "Implement lx_perps_book_match applying fixed price then sequence priority in canonical global sequence order, so matching is a pure function of the ordered history."
do_3 = "Implement lx_perps_order_place_execute rejecting an order that would require more initial margin than the agent can fund with INSUFFICIENT_MARGIN before touching the book or any balance."
do_4 = "Implement lx_perps_order_cancel_execute and permit cancellation even while the market is halted, while rejecting placements and increases with MARKET_HALTED."
do_5 = "Emit zero transfer legs from order placement and cancellation, leaving margin movement to the position path."
do_6 = "Add a determinism test replaying an identical order stream twice and asserting byte-identical books and fills."
verify_cmd = "make test-perps-book"
symbols = ["lx_perps_order_place_execute","lx_perps_order_cancel_execute","lx_perps_book_match","lx_perps_book_iter"]
touches = ["src/modules/perps/lx_perps_book.c","include/layerx/lx_perps.h","tests/modules/test_perps_book.c"]
reqs = ["19.2","19.7","5.4","5.9"]

[task.16.3]
title = "Hold position margin in a real margin subaccount"
status = "done"
wave = 9
requires = ["16.2","10.3"]
do_1 = "Implement lx_perps_position_open_execute and lx_perps_position_increase_execute to move initial or additional margin by a transfer from agent main to agent margin subaccount for that position with reason LXP_REASON_MARGIN_POST."
do_2 = "Implement lx_perps_margin_release to move margin back from the margin subaccount to agent main with reason LXP_REASON_MARGIN_RELEASE, and never satisfy a margin requirement by setting a reserved-margin field."
do_3 = "Treat the margin subaccount balance as the collateral of record for the position, and record size, entry notional, margin account and funding index at entry in position state."
do_4 = "Drain the margin subaccount to zero through explicit legs on close, settle or liquidation, and reject any closure that would leave residual value stranded in it."
do_5 = "Reject a debit of a margin subaccount presented under an owner direct signature or a session key with ERR_UNAUTHORIZED_DEBIT, since only the protocol-module capability may debit it."
do_6 = "Add tests asserting that opening a position changes the main and margin balances by exactly the margin amount and that the reserve report shows the margin subaccount as its own line."
verify_cmd = "make test-perps-margin"
symbols = ["lx_perps_position_open_execute","lx_perps_position_increase_execute","lx_perps_margin_post","lx_perps_margin_release"]
touches = ["src/modules/perps/lx_perps_position.c","include/layerx/lx_perps.h","tests/modules/test_perps_position.c"]
reqs = ["19.3","10.7","10.8","7.7","13.1"]

[task.16.4]
title = "Compute PnL and funding deterministically and settle funding transfers"
status = "done"
wave = 9
requires = ["16.3"]
do_1 = "Implement lx_perps_pnl_compute multiplying into 256 bits and floor-dividing back, with one fixed rounding rule that rounds against the trader and in favour of the protocol, and no floating point anywhere in the path."
do_2 = "Implement lx_perps_funding_rate from the last accepted in-bound oracle price and the market parameters using integer basis points only, and lx_perps_funding_index_update to advance the funding index."
do_3 = "Implement lx_perps_funding_tick_execute to settle funding when the funding interval elapses according to batch timestamps, emitting one sign-determined leg between the long and short funding accounts of that market with reason LXP_REASON_FUNDING."
do_4 = "Require the sum of debits to equal the sum of credits for every funding settlement so funding never mints or burns value, and suspend funding accrual entirely while the market is halted."
do_5 = "Reject any intermediate computation that would overflow with ARITHMETIC_OVERFLOW rather than wrapping, saturating or truncating."
do_6 = "Add exhaustive boundary tests over the funding and PnL routines plus a cross-architecture replay assertion on the resulting balances."
verify_cmd = "make test-perps-funding"
symbols = ["lx_perps_pnl_compute","lx_perps_funding_rate","lx_perps_funding_tick_execute","lx_perps_funding_index_update"]
touches = ["src/modules/perps/lx_perps_funding.c","include/layerx/lx_perps.h","tests/modules/test_perps_funding.c"]
reqs = ["19.4","19.8","7.7","13.5","5.5"]

[task.16.5]
title = "Execute liquidation as one atomic multi-leg transfer set"
status = "done"
wave = 9
requires = ["16.4","9.3"]
do_1 = "Implement lx_perps_maintenance_check to detect a position whose margin ratio falls below the maintenance margin ratio at the most recently accepted in-bound oracle price."
do_2 = "Implement lx_perps_liquidation_legs_build producing the legs in the fixed order margin subaccount to market liquidity, margin subaccount to the liquidation fee accounts, insurance to market liquidity for any deficit, and remaining margin to agent main."
do_3 = "Implement lx_perps_liquidation_fee_split using integer basis points to divide the fee between the liquidator main account and system insurance, rounding up in favour of the protocol and assigning the residue explicitly."
do_4 = "Submit the whole thing as one transfer set with one authorisation context, one execution sequence and one receipt, omitting any computed-zero leg before authorisation."
do_5 = "Roll the entire set back and leave the position open and unchanged if any leg violates an invariant, and reject a liquidation while the market is halted."
do_6 = "Add tests reproducing the worked solvent and deficit cases leg by leg and asserting the margin subaccount reaches exactly zero with conservation holding on gross sums."
verify_cmd = "make test-perps-liquidation"
symbols = ["lx_perps_liquidate_execute","lx_perps_liquidation_legs_build","lx_perps_liquidation_fee_split","lx_perps_maintenance_check"]
touches = ["src/modules/perps/lx_perps_liquidate.c","include/layerx/lx_perps.h","tests/modules/test_perps_liquidate.c"]
reqs = ["19.9","11.3","7.7","13.6","13.4"]

[task.16.6]
title = "Cover deficits from the insurance pool without minting value"
status = "done"
wave = 9
requires = ["16.5"]
do_1 = "Implement lx_perps_insurance_cover emitting the insurance to liquidity leg only for the exact computed deficit, and failing the whole liquidation atomically when the pool cannot cover its net movement."
do_2 = "Implement lx_perps_deficit_record to record an explicit deficit against the insurance account when the loss exceeds both available margin and insurance balance, minting nothing and allowing no balance to become negative."
do_3 = "Implement lx_perps_adl_execute to deleverage counterparties from market liquidity in ascending position id order, one leg each, when a position cannot be liquidated solvently."
do_4 = "Express every monetary effect of the perps module and its oracle adapter exclusively as authenticated transfers, and assert in test that no perps path writes a balance directly."
do_5 = "Add a property test over randomised price paths, liquidations and deficits asserting per-asset conservation and a non-negative balance for every account at every step."
verify_cmd = "make test-perps-insurance"
symbols = ["lx_perps_insurance_cover","lx_perps_deficit_record","lx_perps_adl_execute"]
touches = ["src/modules/perps/lx_perps_insurance.c","include/layerx/lx_perps.h","tests/modules/test_perps_insurance.c"]
reqs = ["19.10","7.7","11.5","13.4","13.1"]

[task.17]
section = "Waves 10-12 - Sequencing, Replay, Guarantors and Availability"
title = "Build the sequencer and the activity batch"
status = "pending"

[task.17.1]
title = "Define the BatchHeader and its canonical serialisation"
status = "done"
wave = 10
requires = ["2.3","3.4","8.5"]
do_1 = "Declare struct lxp_batch_header in include/layerx/lxp_batch.h carrying exactly protocol_version, network_id, epoch, batch_number, first_sequence, last_sequence, previous_state_root, resulting_state_root, activity_merkle_root, receipt_merkle_root, event_merkle_root, data_availability_root, oracle_root, timestamp_ms and sequencer_id, every field a fixed-width integer or a fixed-length hash."
do_2 = "Implement lxp_batch_header_encode and lxp_batch_header_decode on the canonical binary codec, rejecting trailing bytes, non-minimal integers, optional fields and any unknown field identifier with a deterministic result code."
do_3 = "Implement lxp_batch_header_hash under the domain tag `LX:BATCH_HEADER:v1` so a header digest can never be confused with a merkle leaf, an internal node or an activity id."
do_4 = "Add golden encoding vectors and a round-trip test proving that re-encoding any successfully decoded header reproduces the original bytes exactly."
do_5 = "Ship fuzz/fuzz_batch_header.c so the header decoder is total over arbitrary input and never allocates unbounded memory."
verify_cmd = "make test-batch"
symbols = ["lxp_batch_header","lxp_batch_header_encode","lxp_batch_header_decode","lxp_batch_header_hash"]
touches = ["include/layerx/lxp_batch.h","src/sequencer/lxp_batch_header.c","tests/test_batch_header.c","fuzz/fuzz_batch_header.c"]
reqs = ["21.1","6.1","6.4","2.10"]

[task.17.2]
title = "Assign contiguous gap-free global sequence numbers"
status = "done"
wave = 10
requires = ["17.1"]
do_1 = "Implement lxp_seq_allocator over the durable sequence watermark so every admitted activity occupies exactly one global sequence and no number is skipped, duplicated or reused across the whole chain history."
do_2 = "Implement lxp_admission_queue as a single-producer queue whose order is admission order, never verification completion order, so worker threads cannot influence sequencing."
do_3 = "Implement lxp_batch_range_check rejecting a batch whose first_sequence is not the previous last_sequence plus one, whose last_sequence is below first_sequence, or whose previous_state_root does not equal the prior resulting_state_root."
do_4 = "Bind account_sequence consumption to global sequence assignment so an activity that fails admission is never assigned a sequence and never consumes the actor sequence."
do_5 = "Test a million-activity assignment run for contiguity, and test that a rejected activity leaves both counters untouched."
verify_cmd = "make test-sequencer"
symbols = ["lxp_seq_allocator","lxp_seq_assign","lxp_admission_queue","lxp_batch_range_check"]
touches = ["include/layerx/lxp_sequencer.h","src/sequencer/lxp_sequence.c","src/sequencer/lxp_admission.c","tests/test_sequencer_sequence.c"]
reqs = ["21.2","21.3","2.5","13.9"]

[task.17.3]
title = "Supply deterministic batch timestamps to execution"
status = "done"
wave = 10
requires = ["17.1"]
do_1 = "Implement lxp_batch_timestamp_select to choose timestamp_ms once at seal time and freeze it, and lxp_batch_timestamp_validate to reject any batch whose timestamp is not strictly greater than the previous batch or exceeds it by more than the configured forward drift bound."
do_2 = "Implement lxp_exec_clock as the only time source visible to transition functions, reading the sealed batch timestamp and nothing else."
do_3 = "Route every time-dependent decision - timestamp_bound checks, authority and grant expiry, escrow timeout, stream accrual, funding interval and governance activation - through lxp_exec_clock."
do_4 = "Add tools/lxp_check_no_clock.sh to CI to fail the build if any translation unit under src/protocol, src/state, src/ledger or src/modules references time, gettimeofday, clock_gettime or rand."
do_5 = "Test that replaying the same history a year later, on a machine with a wrong system clock, yields identical receipts and roots."
verify_cmd = "make test-batch-time"
symbols = ["lxp_batch_timestamp_select","lxp_batch_timestamp_validate","lxp_exec_clock"]
touches = ["src/sequencer/lxp_batch_time.c","src/protocol/lxp_exec_clock.c","tools/lxp_check_no_clock.sh","tests/test_batch_time.c"]
reqs = ["21.4","21.5","5.7","5.8","27.10"]

[task.17.4]
title = "Seal and sign batches over all five merkle roots"
status = "done"
wave = 10
requires = ["17.2","17.3"]
do_1 = "Implement lxp_batch_roots_compute to build activity_merkle_root, receipt_merkle_root, event_merkle_root, oracle_root and data_availability_root over canonically encoded leaves, with leaves and internal nodes hashed under distinct domain tags and one fixed rule for non-power-of-two leaf counts."
do_2 = "Implement lxp_batch_seal to freeze the sequence range, the timestamp, the state roots and all five commitments into one immutable header."
do_3 = "Implement lxp_batch_sign over the canonical header bytes excluding the signature field, and lxp_batch_verify_signature which rejects an absent, malformed or foreign-key signature and any sequencer_id governance has not authorised for that batch_number."
do_4 = "Prove by test that a one-bit change anywhere in the batch body changes at least one root and invalidates the header signature."
do_5 = "Record the sealed header in the append-only log with an fdatasync before the batch is published to anyone."
verify_cmd = "make test-batch-seal"
symbols = ["lxp_batch_seal","lxp_batch_roots_compute","lxp_batch_sign","lxp_batch_verify_signature"]
touches = ["src/sequencer/lxp_batch_seal.c","src/sequencer/lxp_batch_roots.c","tests/test_batch_seal.c"]
reqs = ["21.1","21.6","6.5","12.4"]

[task.17.5]
title = "Distribute full batches to replicas before checkpoint eligibility"
status = "done"
wave = 10
requires = ["17.4"]
do_1 = "Define struct lxp_batch_body carrying the header, the activities, the receipts, the events, the accepted oracle inputs, the state-diff material and the recovery metadata as one canonically encoded unit."
do_2 = "Implement lxp_batch_publish to push the complete body to the configured replica set over the canonical binary transport, with resumable chunked transfer."
do_3 = "Implement lxp_replica_ack and lxp_batch_eligibility so a batch becomes checkpoint-eligible only once acknowledgements from the configured replica threshold are durably recorded."
do_4 = "Make eligibility a hard gate: a batch with insufficient acknowledgements is never offered to guarantors and never enters a checkpoint."
do_5 = "Test that withholding acknowledgements from a majority of replicas leaves the batch sealed, readable and permanently ineligible until they arrive."
verify_cmd = "make test-batch-distribute"
symbols = ["lxp_batch_body","lxp_batch_publish","lxp_replica_ack","lxp_batch_eligibility"]
touches = ["include/layerx/lxp_batch.h","src/network/lxp_batch_publish.c","src/sequencer/lxp_eligibility.c","tests/test_batch_publish.c"]
reqs = ["21.7","23.1","22.1","1.2"]

[task.17.6]
title = "Recover the sequencer at every write boundary"
status = "done"
wave = 10
requires = ["17.5"]
do_1 = "Implement lxp_log_scan_tail to scan the last log segment forward, validate the record magic and body CRC, and truncate at the first short or invalid record, treating a partial tail as a crash artifact and never as a fork."
do_2 = "Implement lxp_sequencer_recover to establish durable_head as the highest sequence carrying both an activity record and its receipt, load the newest snapshot at or below it, replay forward and hard-halt on any recomputed root mismatch."
do_3 = "Implement lxp_projection_rebuild to drop and rebuild every SQLite projection from the log whenever the projection watermark is ahead of, or behind, durable_head."
do_4 = "Implement lxp_sequencer_equivocation_detect to refuse a second distinct header for a batch_number already sealed and to publish both signed headers as slashable evidence."
do_5 = "Define the sequencer-loss posture: stop accepting activities rather than order out of band, and require a governance handover activity naming the new sequencer_id and its first batch_number."
do_6 = "Add a fault-injection test that aborts the process at each distinct write boundary and asserts each activity is fully present or fully absent after restart."
verify_cmd = "make test-sequencer-recovery"
symbols = ["lxp_sequencer_recover","lxp_sequencer_equivocation_detect"]
touches = ["src/sequencer/lxp_recovery.c","src/storage/lxp_log_recover.c","tests/test_sequencer_recovery.c"]
reqs = ["1.9","5.10","11.10","21.8","21.9","21.10"]

[task.18]
title = "Build the replica, replay engine and state reconstruction"
status = "pending"

[task.18.1]
title = "Ingest and validate batches on a replica"
status = "done"
wave = 11
requires = ["17.5"]
do_1 = "Implement lxp_replica_ingest_batch to decode a received batch body, reject any non-canonical encoding before evaluating anything else, and store it verbatim as received bytes."
do_2 = "Implement lxp_replica_validate_header to check protocol_version, network_id, the sequencer signature and the authorised sequencer_id for that batch_number."
do_3 = "Implement lxp_replica_chain_link to reject a gap in batch_number, a previous_state_root that does not match the locally held resulting_state_root of the prior batch, or a non-contiguous first_sequence."
do_4 = "Serve an acknowledgement only after the body is durably written, so an acknowledgement is a durability statement and not a receipt of packets."
do_5 = "Test ingestion of a truncated, reordered and duplicated batch stream and assert deterministic rejection with no state advance."
verify_cmd = "make test-replica"
symbols = ["lxp_replica_ingest_batch","lxp_replica_validate_header","lxp_replica_chain_link"]
touches = ["include/layerx/lxp_replica.h","src/replica/lxp_replica_ingest.c","tests/test_replica_ingest.c"]
reqs = ["21.3","21.6","23.3","6.2"]

[task.18.2]
title = "Replay every transition independently to the same roots"
status = "done"
wave = 11
requires = ["18.1"]
do_1 = "Implement lxp_replay_engine to execute a batch from previous_state_root using the versioned transition function recorded for that batch, the sealed batch timestamp and the parameter version in force at that epoch."
do_2 = "Implement lxp_replay_batch to recompute resulting_state_root and all five merkle roots without reading any sequencer-supplied result, receipt or effect list."
do_3 = "Implement lxp_replay_verify_roots to compare every recomputed root, result code, fee charged, effect list and resulting balance byte for byte against the published batch."
do_4 = "Guarantee the replay path never reads SQLite: the append-only log and the snapshot are the only inputs."
do_5 = "Test full replay from genesis and from an arbitrary snapshot, asserting both produce identical terminal roots."
verify_cmd = "make test-replay"
symbols = ["lxp_replay_engine","lxp_replay_batch","lxp_replay_verify_roots"]
touches = ["src/replica/lxp_replay.c","include/layerx/lxp_replica.h","tests/test_replica_replay.c"]
reqs = ["5.1","5.6","13.14","22.3"]

[task.18.3]
title = "Detect divergence and halt rather than continue"
status = "done"
wave = 11
requires = ["18.2"]
do_1 = "Implement lxp_divergence_detect to stop at the first differing global sequence rather than accumulating differences or continuing to the end of the batch."
do_2 = "Implement lxp_divergence_report to emit a signed record naming batch_number, the first divergent global_sequence, the expected value and the produced value."
do_3 = "Implement lxp_replica_halt to stop execution, stop acknowledging and stop serving state as current, while continuing to serve already finalised history."
do_4 = "Make the halt unconditional: no build flag, governance parameter or emergency mode may disable divergence checking or resume a halted replica without operator action."
do_5 = "Test a deliberately corrupted receipt, a corrupted state diff and a substituted state root, asserting halt at the exact offending sequence in all three cases."
verify_cmd = "make test-replica-divergence"
symbols = ["lxp_divergence_detect","lxp_divergence_report","lxp_replica_halt"]
touches = ["src/replica/lxp_divergence.c","tests/test_replica_divergence.c"]
reqs = ["13.14","13.16","22.4","5.1"]

[task.18.4]
title = "Snapshot and restore materialised state"
status = "done"
wave = 11
requires = ["18.2"]
do_1 = "Implement lxp_snapshot_write to serialise the account tree, every module namespace, the sequence watermarks and the module state-root vector at an exact global sequence, in canonical encoding."
do_2 = "Implement lxp_snapshot_manifest carrying the snapshot global_sequence, the state root it claims and a domain-separated digest over the snapshot bytes."
do_3 = "Implement lxp_snapshot_load and lxp_snapshot_verify_root so a snapshot is refused unless its recomputed root equals the root recorded in the receipt at that sequence."
do_4 = "Write snapshots alongside history and never rewrite log records, so compaction never destroys replayable history."
do_5 = "Test that a node restored from a snapshot and a node replayed from genesis converge to byte-identical roots at the same sequence."
verify_cmd = "make test-snapshot"
symbols = ["lxp_snapshot_write","lxp_snapshot_load","lxp_snapshot_manifest","lxp_snapshot_verify_root"]
touches = ["src/state/lxp_snapshot.c","src/storage/lxp_snapshot_store.c","tests/test_snapshot.c"]
reqs = ["5.10","23.9","21.9","11.10"]

[task.18.5]
title = "Serve historical queries from the log"
status = "done"
wave = 11
requires = ["18.4"]
do_1 = "Implement lxp_history_query to answer by checkpoint_id, batch_number, global_sequence range or activity_id, returning the canonical binary bytes exactly as they were committed."
do_2 = "Implement lxp_receipt_lookup by transaction_id, idempotency_key and global_sequence, returning byte-identical receipt content for every repeat query."
do_3 = "Build the query index as a rebuildable SQLite projection under migrations, and make the log the authority whenever the two disagree."
do_4 = "Implement lxp_history_serve_range over the canonical binary transport with an explicit maximum response size and no ability to synthesise, normalise or re-encode stored bytes."
do_5 = "Test that re-hashing served bytes reproduces the committed activity, receipt, event and oracle roots exactly."
verify_cmd = "make test-history"
symbols = ["lxp_history_query","lxp_history_serve_range","lxp_receipt_lookup"]
touches = ["src/replica/lxp_history.c","src/network/lxp_history_service.c","migrations/0007_history_index.sql","tests/test_history_query.c"]
reqs = ["23.3","12.6","12.7","7.3"]

[task.18.6]
title = "Prove byte-identical replay across architectures"
status = "done"
wave = 11
requires = ["18.3","18.5"]
do_1 = "Build a fixed replay corpus spanning every activity type and store it as canonical bytes under tests, with the expected terminal root recorded independently of the implementation that produced it."
do_2 = "Implement lxp_replay_fixture_load and lxp_replay_digest to reduce a full replay to one digest over every receipt, event and intermediate batch root."
do_3 = "Run the corpus under at least one 64-bit little-endian and one non-x86 target, plus two compilers and two optimisation levels, via tools/lxp_replay_matrix.sh."
do_4 = "Fail the test on a single differing byte and print the first divergent global sequence rather than a summary count."
do_5 = "Assert intermediate roots match at every batch boundary, not only the terminal root."
verify_cmd = "make test-replay-crossarch"
symbols = ["lxp_replay_fixture_load","lxp_replay_digest","lxp_replay_crossarch_case"]
touches = ["tests/test_replay_crossarch.c","tests/vectors/replay_corpus.lxb","tools/lxp_replay_matrix.sh"]
reqs = ["5.1","13.14","28.1","23.4"]

[task.19]
title = "Build the guarantor node and checkpoint attestation"
status = "pending"

[task.19.1]
title = "Implement the six guarantor duties as one pipeline"
status = "done"
wave = 12
requires = ["18.2"]
do_1 = "Implement lxp_guarantor_process_batch to run the duties in the fixed order: download the complete batch body, verify every signature, replay every transition, recompute all roots, store the availability data, and only then consider signing."
do_2 = "Implement lxp_guarantor_verify_signatures over every actor signature, every session key, capability grant and delegated authority proof, every oracle signature and the sequencer header signature."
do_3 = "Implement lxp_guarantor_recompute_roots to derive resulting_state_root, activity_merkle_root, receipt_merkle_root, event_merkle_root, oracle_root and data_availability_root from first principles, trusting no sequencer-supplied value."
do_4 = "Define struct lxp_guarantor_ctx to hold the guarantor identity, its secp256k1 Paxeer-facing key, its bond view and its independent state, sharing no execution state with the sequencer."
do_5 = "Fail the pipeline closed: a duty that cannot be completed withholds attestation instead of assuming success."
verify_cmd = "make test-guarantor"
symbols = ["lxp_guarantor_ctx","lxp_guarantor_process_batch","lxp_guarantor_verify_signatures","lxp_guarantor_recompute_roots"]
touches = ["include/layerx/lxp_guarantor.h","src/guarantor/lxp_guarantor.c","tests/test_guarantor_duties.c"]
reqs = ["22.1","22.2","22.3","22.5"]

[task.19.2]
title = "Assemble threshold checkpoint certificates"
status = "done"
wave = 12
requires = ["19.1"]
do_1 = "Define struct lxp_guarantor_attestation with checkpoint_id, checkpoint_hash, guarantor_id, a replayed flag, a da_possessed flag, attested_at_ms and a secp256k1 signature."
do_2 = "Implement lxp_guarantor_attest to sign only when replayed and da_possessed are both true, so one signature is simultaneously a correctness and a possession attestation."
do_3 = "Implement lxp_guarantor_cert_assemble to collect attestations sorted by guarantor_id over a byte-identical checkpoint certificate, and lxp_guarantor_cert_verify to count distinct valid signatures against the threshold."
do_4 = "Reserve an explicit optional validity-proof field in the certificate so a later version can add proofs without changing the activity envelope, the receipt format or the batch header."
do_5 = "Augment finalised receipts with activity_inclusion_proof, state_inclusion_proof, checkpoint_id, guarantor_certificate and paxeer_settlement_reference, altering no pre-checkpoint field."
verify_cmd = "make test-guarantor-cert"
symbols = ["lxp_guarantor_attestation","lxp_guarantor_attest","lxp_guarantor_cert","lxp_guarantor_cert_assemble","lxp_guarantor_cert_verify"]
touches = ["src/guarantor/lxp_attestation.c","src/paxeer/lxp_checkpoint_cert.c","tests/test_guarantor_cert.c"]
reqs = ["22.6","22.10","12.7","12.8"]

[task.19.3]
title = "Track bonding state and threshold eligibility"
status = "done"
wave = 12
requires = ["19.2"]
do_1 = "Implement lxp_guarantor_set as versioned state carrying guarantor_id, the secp256k1 public key, bond amount, joined epoch and jail flag, changed only by ordered governance activities."
do_2 = "Implement lxp_guarantor_eligible to exclude any guarantor whose bond was below the minimum at the checkpoint epoch, that is jailed, that is under an unresolved slashing action, or that governance removed - even when its signature verifies."
do_3 = "Implement lxp_checkpoint_finalisable evaluating all finalisation preconditions: threshold of distinct eligible attestations, bonds at or above the minimum, replayed and possession flags set, no equivocation, previous_state_root equal to the finalised root, challenge window elapsed and availability challenges answered."
do_4 = "Leave a checkpoint unfinalised when the threshold is not met before the deadline, settling no withdrawal, no deposit credit and no dispute against it, and keeping the previous finalised checkpoint as the anchor."
do_5 = "Test each precondition independently by making exactly one of them fail and asserting finalisation is refused with no override path."
verify_cmd = "make test-guarantor-bond"
symbols = ["lxp_guarantor_set","lxp_guarantor_bond_state","lxp_guarantor_eligible","lxp_checkpoint_finalisable"]
touches = ["src/guarantor/lxp_bond_state.c","src/paxeer/lxp_finalisation.c","tests/test_guarantor_bond.c"]
reqs = ["22.7","22.8","23.2","1.6"]

[task.19.4]
title = "Produce portable slashable equivocation evidence"
status = "done"
wave = 12
requires = ["19.2"]
do_1 = "Implement lxp_equivocation_detect to recognise two conflicting attestations from one guarantor for the same checkpoint_id or batch height, and the sequencer analogue of two distinct signed headers at one batch_number."
do_2 = "Define struct lxp_equivocation_evidence holding both signed statements verbatim, with lxp_equivocation_encode producing a canonical, self-contained byte string."
do_3 = "Implement lxp_equivocation_verify so any party can validate the contradiction from the evidence alone, with no trusted intermediary and no access to LayerX state."
do_4 = "Make the evidence directly submittable to the Paxeer slashing entry point and remove the offender from the active attesting set on acceptance."
do_5 = "Test that evidence produced by one node verifies unchanged on another and that a non-contradictory pair is rejected as evidence."
verify_cmd = "make test-equivocation"
symbols = ["lxp_equivocation_evidence","lxp_equivocation_detect","lxp_equivocation_encode","lxp_equivocation_verify"]
touches = ["src/guarantor/lxp_equivocation.c","src/paxeer/lxp_slashing_submit.c","tests/test_equivocation.c"]
reqs = ["22.9","21.8","28.8","6.8"]

[task.19.5]
title = "Refuse to sign on any mismatch and publish dissent"
status = "done"
wave = 12
requires = ["19.1","19.4"]
do_1 = "Implement lxp_guarantor_withhold as the single exit path taken when any signature, root, result code, fee, effect list or resulting balance differs by even one byte."
do_2 = "Implement lxp_guarantor_first_divergence to name the batch_number and the first divergent global_sequence rather than reporting an aggregate mismatch."
do_3 = "Implement lxp_guarantor_dissent to publish a signed dissent record and halt attestation for the epoch."
do_4 = "Add a disagreement test harness running three guarantors: one replaying to a different root, one refusing to sign, and one signing two conflicting checkpoints for the same batch number."
do_5 = "Assert the harness leaves finality withheld below threshold and yields a portable equivocation proof for the double signer."
verify_cmd = "make test-guarantor-disagreement"
symbols = ["lxp_guarantor_dissent","lxp_guarantor_withhold","lxp_guarantor_first_divergence"]
touches = ["src/guarantor/lxp_dissent.c","tests/test_guarantor_disagreement.c"]
reqs = ["22.4","22.7","28.8","13.14"]

[task.20]
title = "Build the data availability layer"
status = "pending"

[task.20.1]
title = "Commit the availability bundle in the batch header"
status = "done"
wave = 12
requires = ["17.4"]
do_1 = "Define the five availability classes as the complete activity batch, every receipt, every accepted oracle input with its signature, the state-diff material and the recovery metadata."
do_2 = "Implement lxp_da_bundle_build to serialise those classes in fixed order into bounded chunks of struct lxp_da_chunk, each with batch_number, chunk_index, length and a domain-separated chunk hash."
do_3 = "Implement lxp_da_bundle_root as a merkle root over chunk hashes in ascending chunk_index, and write it into the header field data_availability_root at seal time."
do_4 = "Include in the recovery metadata the module state-root vector, the account-tree frontier and the sequence watermarks, sufficient to rebuild every SQLite projection from the log alone."
do_5 = "Test that a bundle rebuilt from served bytes reproduces the committed data_availability_root exactly."
verify_cmd = "make test-da"
symbols = ["lxp_da_chunk","lxp_da_class","lxp_da_bundle_build","lxp_da_bundle_root"]
touches = ["include/layerx/lxp_da.h","src/network/lxp_da_bundle.c","tests/test_da_bundle.c"]
reqs = ["23.1","21.1","6.5","23.9"]

[task.20.2]
title = "Require guarantor possession attestation over every class"
status = "done"
wave = 12
requires = ["20.1","19.2"]
do_1 = "Implement lxp_da_store_bundle so a guarantor durably persists the full bundle before any attestation step that mentions availability."
do_2 = "Implement lxp_da_possession_attest to cover all five availability classes named in the certificate and the exact data_availability_root being attested."
do_3 = "Implement lxp_da_possession_verify to reject any attestation covering fewer classes or a different availability root as invalid rather than partial."
do_4 = "Make a guarantor that replayed correctly but stored no availability data uncountable towards the finality threshold."
do_5 = "Test that possession cannot be asserted for data the guarantor does not hold, by challenging immediately after attestation."
verify_cmd = "make test-da-possession"
symbols = ["lxp_da_possession_attest","lxp_da_possession_verify","lxp_da_store_bundle"]
touches = ["src/guarantor/lxp_da_possession.c","tests/test_da_possession.c"]
reqs = ["23.2","22.5","22.6","28.9"]

[task.20.3]
title = "Publish a retrieval interface agents can replay from"
status = "done"
wave = 12
requires = ["20.1"]
do_1 = "Implement lxp_da_serve_chunk to answer requests by batch_number and chunk_index over the canonical binary transport, returning committed bytes verbatim."
do_2 = "Define struct lxp_da_retrieval_request supporting lookup by checkpoint_id, batch_number, global_sequence range and activity_id."
do_3 = "Implement lxp_da_fetch as the client side used by layerx-verify to reconstruct state from genesis using only retrieved bundles and the public transition function, with no index database as input."
do_4 = "Implement lxp_da_verify_served_bytes so a retriever re-hashes what it received and compares against every committed root before using it."
do_5 = "Test an end-to-end retrieve-and-replay run on a second machine reproducing byte-identical receipts and roots."
verify_cmd = "make test-da-retrieval"
symbols = ["lxp_da_serve_chunk","lxp_da_fetch","lxp_da_retrieval_request","lxp_da_verify_served_bytes"]
touches = ["src/network/lxp_da_service.c","cmd/layerx-verify/lxp_verify_fetch.c","tests/test_da_retrieval.c"]
reqs = ["23.3","23.4","23.10","12.8"]

[task.20.4]
title = "Run deterministic availability sampling challenges"
status = "done"
wave = 12
requires = ["20.3"]
do_1 = "Implement lxp_da_challenge_indices to derive challenged chunk indices deterministically from the checkpoint hash and the chunk count, so no randomness ever enters execution."
do_2 = "Implement lxp_da_challenge_issue and lxp_da_challenge_respond so a challenged guarantor returns the chunk plus an inclusion proof against data_availability_root inside the configured response window."
do_3 = "Implement lxp_da_challenge_judge to treat a missing, late, non-hashing or non-proving response as an availability failure rather than a satisfied request."
do_4 = "Record failures on Paxeer, make them slashable under the availability policy, and exclude the guarantor from later availability and finality thresholds until governance restores it."
do_5 = "Publish the mismatching signed commitment together with the served bytes as evidence whenever a response fails to verify."
verify_cmd = "make test-da-challenge"
symbols = ["lxp_da_challenge_indices","lxp_da_challenge_issue","lxp_da_challenge_respond","lxp_da_challenge_judge"]
touches = ["src/guarantor/lxp_da_challenge.c","src/paxeer/lxp_da_challenge_record.c","tests/test_da_challenge.c"]
reqs = ["23.5","23.6","23.10","5.8"]

[task.20.5]
title = "Simulate unavailability and block finalisation"
status = "done"
wave = 12
requires = ["20.2","20.4"]
do_1 = "Build tools/lxp_da_withhold.sh and lxp_da_withhold_sim to withhold, one class at a time, the activity batch, the receipts, the oracle inputs, the state-diff material or the recovery metadata."
do_2 = "Implement lxp_checkpoint_block_on_da so an unfinalised checkpoint whose data is unavailable can never be finalised, and no withdrawal, deposit credit or dispute settles against it."
do_3 = "Implement lxp_da_unavailable_mode so unavailability behind an already finalised checkpoint keeps that checkpoint valid for emergency exit, halts further finalisation and resumes only on restored availability or a governance reconstitution of the guarantor set."
do_4 = "Assert in the test that agents can detect the unavailability from public information alone."
do_5 = "Assert that no availability attestation can be produced for withheld data in any of the five withholding scenarios."
verify_cmd = "make test-da-unavailable"
symbols = ["lxp_da_withhold_sim","lxp_da_unavailable_mode","lxp_checkpoint_block_on_da"]
touches = ["src/paxeer/lxp_finalisation.c","tools/lxp_da_withhold.sh","tests/test_da_unavailable.c"]
reqs = ["23.7","23.8","28.9","24.8"]

[task.21]
title = "Build the governance module, fees and resource metering"
status = "pending"

[task.21.1]
title = "Hold protocol parameters as versioned state"
status = "done"
wave = 12
requires = ["17.3"]
do_1 = "Implement lxp_param_table mapping parameter identifiers to value, activation epoch, proposed value and proposal id, iterated in canonical byte-lexicographic key order."
do_2 = "Implement lxp_param_version so every execution resolves parameters through an explicit version and every receipt records the parameter_version in force."
do_3 = "Implement lxp_param_set_bounds declaring the legal range and target module of every key, so an out-of-bounds or unknown key is rejected rather than clamped."
do_4 = "Implement lxp_param_get as the only read path, forbidding any module from caching a parameter across an epoch boundary."
do_5 = "Test that replaying a historical batch resolves the parameter values of that batch epoch, not the newest values."
verify_cmd = "make test-governance"
symbols = ["lxp_param_table","lxp_param_get","lxp_param_version","lxp_param_set_bounds"]
touches = ["include/layerx/lxp_governance.h","src/modules/governance/lxp_params.c","tests/test_governance_params.c"]
reqs = ["20.1","20.10","4.9","5.6"]

[task.21.2]
title = "Enact parameter changes with activation delay and staged rollout"
status = "done"
wave = 12
requires = ["21.1"]
do_1 = "Implement lxp_gov_param_propose to record target_module, parameter_key, proposed_value, activation_epoch and rollout_scope as an ordered activity signed by the governance authority."
do_2 = "Reject a proposal naming an activation_epoch earlier than the current epoch plus the minimum activation delay, an unknown module or key, or a value outside declared bounds, with no state change beyond sequence and fee."
do_3 = "Implement lxp_gov_activation_apply to apply the new value atomically at the first batch whose epoch equals the pending activation epoch and to increment parameter_version at that deterministic boundary."
do_4 = "Implement lxp_gov_stage_cohort so a staged rollout applies only to the enumerated cohort by module, market or account set, leaving every other cohort on the prior value until its own stage activates."
do_5 = "Reject retroactive application outright and preserve the original versioned transition function for sequences already sealed."
do_6 = "Test that two replicas activating from different starting snapshots compute the identical resulting_state_root at the activation boundary."
verify_cmd = "make test-governance-activation"
symbols = ["lxp_gov_param_propose","lxp_gov_param_enact","lxp_gov_activation_apply","lxp_gov_stage_cohort"]
touches = ["src/modules/governance/lxp_gov_param.c","tests/test_governance_activation.c"]
reqs = ["20.2","20.3","20.4","20.5"]

[task.21.3]
title = "Implement scoped emergency pause and emergency mode"
status = "done"
wave = 12
requires = ["21.1"]
do_1 = "Implement lxp_gov_emergency_halt to apply a pause at the declared scope of one module, one market or the whole network, recorded as an ordered activity."
do_2 = "Implement lxp_pause_scope_check so every submitted activity inside a paused scope is rejected with the paused-scope result code while activities outside that scope remain fully executable."
do_3 = "Keep ordering, sealing, distribution and checkpointing running while paused, and keep receipts, inclusion proofs and balance proofs servable so withdrawal and exit paths stay usable."
do_4 = "Implement lxp_gov_emergency_resume requiring an explicit signed governance activity, never elapsed wall-clock time and never an implicit background timer."
do_5 = "Reject any governance activity that would write a balance, mint or burn supply, alter an issued receipt, rewrite a sealed batch or substitute a state root."
do_6 = "Test module-scoped, market-scoped and network-scoped pauses independently, including that a paused perps market still permits order cancellation paths defined by that module."
verify_cmd = "make test-governance-emergency"
symbols = ["lxp_gov_emergency_halt","lxp_gov_emergency_resume","lxp_pause_scope_check","lxp_gov_module_enable"]
touches = ["src/modules/governance/lxp_emergency.c","tests/test_governance_emergency.c"]
reqs = ["20.6","20.7","20.8","20.9"]

[task.21.4]
title = "Compute fees deterministically and charge them as a transfer"
status = "done"
wave = 12
requires = ["21.1"]
do_1 = "Implement lxp_fee_compute as a versioned integer-only function of activity type, canonical encoded byte length, metered execution units and metered storage units, using checked fixed-width arithmetic only."
do_2 = "Apply exactly one rounding rule that rounds any fractional base unit upward, and prove bit-identical results across compilers, optimisation levels and architectures."
do_3 = "Implement lxp_fee_charge to move the fee as a 402LXP transfer from the actor main account to system fees, through the single balance-mutation primitive and no other path."
do_4 = "Record the charged amount in the receipt field fee_charged and guarantee it never exceeds the declared fee_limit."
do_5 = "Implement lxp_fee_schedule resolved from governance parameters at the batch epoch so historical fees stay reproducible after later parameter changes."
do_6 = "Test per-asset conservation: the sum of fee debits equals the sum of fee treasury credits over a long replay."
verify_cmd = "make test-fees"
symbols = ["lxp_fee_charge","lxp_fee_schedule","lxp_fee_treasury_account"]
touches = ["src/protocol/lxp_fee.c","src/modules/governance/lxp_fee_params.c","tests/test_fees.c"]
reqs = ["26.1","26.4","7.4","13.1"]

[task.21.5]
title = "Meter execution and storage against fee_limit"
status = "done"
wave = 12
requires = ["21.4"]
do_1 = "Implement lxp_meter_ctx accumulating execution units and net state storage bytes for the activity currently executing on the single writer."
do_2 = "Implement lxp_meter_charge_exec and lxp_meter_charge_storage at every metered site, pricing storage growth at the storage rate parameter in force for that batch."
do_3 = "Implement lxp_meter_exhausted to abort with the deterministic resource exhaustion result code when either metered quantity exceeds its governance ceiling."
do_4 = "Reject any envelope lacking fee_limit or encoding it as a floating point or ambiguous numeric value, and require the actor spendable balance to be at least fee_limit at admission."
do_5 = "Reject with the arithmetic overflow result code rather than wrapping, saturating or truncating at any step of metering accumulation or treasury credit."
do_6 = "Add fuzz/fuzz_meter.c driving adversarial payload shapes at the metering path."
verify_cmd = "make test-metering"
symbols = ["lxp_meter_ctx","lxp_meter_charge_exec","lxp_meter_charge_storage","lxp_meter_exhausted"]
touches = ["src/protocol/lxp_meter.c","tests/test_metering.c","fuzz/fuzz_meter.c"]
reqs = ["26.2","26.3","26.7","26.8"]

[task.21.6]
title = "Apply one fee rejection policy and prove fee replay"
status = "done"
wave = 12
requires = ["21.5"]
do_1 = "Implement lxp_fee_admission_check so an activity failing admission is assigned no global sequence, consumes no account sequence and is charged nothing."
do_2 = "Implement lxp_fee_rejection_policy so an admitted activity that fails execution consumes its account sequence exactly once, is charged the computed fee up to fee_limit, and yields a durable failure receipt with zero module effects."
do_3 = "Charge the fee before applying any module effect so the fee transfer can never fail because of the activity own spending."
do_4 = "Implement lxp_fee_replay_check asserting that replay from the log reproduces every fee_charged value, the fee treasury balance and every resulting state root exactly."
do_5 = "Test the boundary where the computed fee equals fee_limit exactly, and the case where it exceeds it by one base unit."
verify_cmd = "make test-fee-replay"
symbols = ["lxp_fee_rejection_policy","lxp_fee_admission_check","lxp_fee_replay_check"]
touches = ["src/protocol/lxp_fee_policy.c","tests/test_fee_replay.c"]
reqs = ["26.5","26.6","26.9","26.10","13.1"]

[task.22]
section = "Wave 13 - Paxeer Settlement and Gateways"
title = "Build the Paxeer contracts and the bridge module"
status = "pending"

[task.22.1]
title = "Deploy custody and checkpoint registration contracts"
status = "done"
wave = 13
requires = ["19.3"]
do_1 = "Define the custody contract surface to accept exactly six inputs: a finalised checkpoint certificate, membership or balance proofs against its state root, withdrawal nullifiers, guarantor signatures, challenge-window state and emergency-exit eligibility."
do_2 = "Implement lxp_checkpoint_register to submit only the header commitments of a checkpoint, never individual activity envelopes, payloads or receipts."
do_3 = "Implement lxp_paxeer_verify_cert on the contract side to count distinct bonded guarantor signatures over the checkpoint hash and advance the registered finalised state root."
do_4 = "Keep the contract free of any LayerX business logic: no perps order, no market or funding rule, no service agreement, no escrow or budget rule and no ordinary transfer is interpretable by it."
do_5 = "Test that a checkpoint carrying millions of underlying activities registers as one submission whose payload size is independent of activity count."
verify_cmd = "make test-paxeer"
symbols = ["lxp_checkpoint_register","lxp_checkpoint_registry_state","lxp_paxeer_verify_cert","lxp_paxeer_custody_abi"]
touches = ["contracts/LayerXCustody.sol","contracts/CheckpointRegistry.sol","src/paxeer/lxp_paxeer_client.c","tests/test_paxeer_custody.c"]
reqs = ["24.1","1.2","1.3","1.4"]

[task.22.2]
title = "Hold guarantor bonds and execute slashing on Paxeer"
status = "done"
wave = 13
requires = ["22.1","19.4"]
do_1 = "Implement the bond contract holding per-guarantor bonds with an explicit minimum expressed as a fraction of custodied value, and expose lxp_paxeer_bond_state to LayerX as read-only mirrored state."
do_2 = "Implement lxp_paxeer_slash_submit to accept a portable equivocation proof from any party and slash without any judgement about correctness, because the contradiction is self-evident."
do_3 = "Implement lxp_paxeer_jail_guarantor to remove a slashed or under-bonded guarantor from the active attesting set immediately."
do_4 = "Exclude signatures from jailed, removed or under-bonded guarantors from every threshold count even when cryptographically valid."
do_5 = "Test slashing driven purely by the evidence bytes produced in the guarantor task, submitted by a third party with no privileged access."
verify_cmd = "make test-paxeer-bond"
symbols = ["lxp_paxeer_bond_deposit","lxp_paxeer_bond_state","lxp_paxeer_slash_submit","lxp_paxeer_jail_guarantor"]
touches = ["contracts/GuarantorBond.sol","src/paxeer/lxp_bond_client.c","tests/test_paxeer_bond.c"]
reqs = ["22.8","22.9","1.6","28.8"]

[task.22.3]
title = "Credit deposits only against a finalised custody proof"
status = "done"
wave = 13
requires = ["22.1"]
do_1 = "Implement lxp_deposit_proof_verify to require a Paxeer deposit proof bound to a finalised checkpoint, matching network_id and protocol_version, before any credit is considered."
do_2 = "Implement lxp_bridge_deposit_credit to perform the credit as one authenticated transfer from system paxeer-reserve to the agent main account, through the single balance-mutation primitive."
do_3 = "Implement lxp_deposit_nullifier and record consumed deposit identifiers so a replayed proof credits at most once."
do_4 = "Reject a deposit whose proof is absent, unverifiable, references an unfinalised checkpoint or reuses a consumed identifier, crediting nothing."
do_5 = "Assert the credit is conserving: total LayerX supply of the asset is unchanged because the reserve mirror already held the units."
verify_cmd = "make test-bridge-deposit"
symbols = ["lxp_bridge_deposit_credit","lxp_deposit_proof_verify","lxp_deposit_nullifier"]
touches = ["include/layerx/lxp_bridge.h","src/modules/bridge/lxp_bridge_deposit.c","tests/test_bridge_deposit.c"]
reqs = ["24.2","24.3","13.12","7.6","11.6"]

[task.22.4]
title = "Settle withdrawals under nullifiers and challenge windows"
status = "done"
wave = 13
requires = ["22.3"]
do_1 = "Implement lxp_bridge_withdraw_request to debit the agent by an authenticated transfer into system paxeer-withdrawals, so the LayerX debit is committed and checkpointed before any payout is claimable."
do_2 = "Implement lxp_withdrawal_nullifier as a domain-separated deterministic function of network_id, withdrawal_id, account, asset, amount and checkpoint_id, recorded identically on both ledgers."
do_3 = "Implement the Paxeer claim path to require a balance or membership proof against a finalised checkpoint state root plus threshold guarantor signatures, and to reject an already-recorded nullifier before doing any other work."
do_4 = "Implement lxp_paxeer_challenge_window to hold payouts while the window is open and to cancel payouts and slash attesting guarantors when a fraud, availability or equivocation challenge succeeds."
do_5 = "Implement lxp_bridge_withdraw_finalize to retire the withdrawal against the reserve mirror exactly once per nullifier."
do_6 = "Test double payment attempts by resubmission, by an alternative inclusion proof, by a later checkpoint and after a settlement-chain reorganisation."
verify_cmd = "make test-bridge-withdraw"
symbols = ["lxp_bridge_withdraw_request","lxp_bridge_withdraw_finalize","lxp_withdrawal_nullifier","lxp_paxeer_challenge_window"]
touches = ["contracts/WithdrawalClaims.sol","src/modules/bridge/lxp_bridge_withdraw.c","tests/test_bridge_withdraw.c"]
reqs = ["24.4","24.5","24.6","24.7","13.13"]

[task.22.5]
title = "Execute emergency exit against the last finalised checkpoint"
status = "done"
wave = 13
requires = ["22.4"]
do_1 = "Implement lxp_exit_eligibility to declare exit availability when no checkpoint has finalised within the liveness bound, when governance declares an emergency, or when a fraud claim against the latest checkpoint is accepted."
do_2 = "Implement lxp_exit_claim_build to assemble a claim from the finalised certificate, its state root, a balance proof and the recorded guarantor signatures, and nothing else."
do_3 = "Implement lxp_exit_verify_balance_proof on the contract side so the exit requires no sequencer cooperation, no newly sealed batch and no ongoing guarantor liveness."
do_4 = "Compute exits from finalised state only and state plainly that activities sequenced after the last finalised checkpoint are discarded, not honoured."
do_5 = "Share the nullifier space between ordinary withdrawals and exits so one withdrawal_id can never pay through both routes."
verify_cmd = "make test-emergency-exit"
symbols = ["lxp_exit_declare","lxp_exit_claim_build","lxp_exit_eligibility","lxp_exit_verify_balance_proof"]
touches = ["contracts/EmergencyExit.sol","src/modules/bridge/lxp_bridge_exit.c","tests/test_emergency_exit.c"]
reqs = ["24.8","24.9","1.10","13.13"]

[task.22.6]
title = "Reconcile reserves at the custody boundary"
status = "done"
wave = 13
requires = ["22.5"]
do_1 = "Implement lxp_reserve_reconcile to compute, per asset, the sum of every agent account, subaccount, escrow, budget, margin, liquidity, insurance, fee treasury and withdrawal balance."
do_2 = "Implement lxp_supply_invariant_check to reject any payout or credit that would leave custody below LayerX balances plus outstanding claims, or make any account negative."
do_3 = "Run the check at every checkpoint boundary and refuse to produce a batch whose resulting state root violates full reserve accounting."
do_4 = "Implement lxp_reserve_report as an operator-facing per-asset report with zero tolerance and an explicit list of contributing account classes."
do_5 = "Test that an injected phantom credit is caught at the first checkpoint boundary rather than at audit time."
verify_cmd = "make test-reserve"
symbols = ["lxp_reserve_reconcile","lxp_reserve_report","lxp_supply_invariant_check"]
touches = ["src/modules/bridge/lxp_reserve.c","tools/lxp_reserve_report.c","tests/test_reserve_reconcile.c"]
reqs = ["24.10","14.10","11.8","13.5"]

[task.23]
title = "Build the optional JSON and HTTP 402 gateway and operator tooling"
status = "pending"

[task.23.1]
title = "Define and validate the LXP payment requirement"
status = "done"
wave = 13
requires = ["19.2"]
do_1 = "Define struct lxp_payment_requirement carrying exactly network_id, recipient, asset, amount, invoice_id, purpose_hash, expiry, acceptable_conditions and service_signature."
do_2 = "Implement lxp_payment_requirement_verify to reject a requirement missing any field, encoding amount as anything other than a non-negative integer in the asset base unit, naming a foreign network_id, or carrying a service signature that does not verify against the declared recipient service key."
do_3 = "Implement lxp_gateway_translate so JSON is converted to canonical bytes before any signature is verified, and a submission whose JSON does not translate to exactly the signed bytes is rejected."
do_4 = "State and test the gateway boundary: disabling, changing or removing the gateway alters no accepted outcome, receipt field, result code or state root."
do_5 = "Add a fuzz target over the JSON translation layer so malformed input cannot reach the kernel as anything but canonical bytes or a rejection."
verify_cmd = "make test-gateway"
symbols = ["lxp_payment_requirement","lxp_payment_requirement_verify","lxp_gateway_translate"]
touches = ["include/layerx/lxp_gateway.h","src/network/lxp_gateway_http.c","fuzz/fuzz_gateway_json.c","tests/test_gateway_requirement.c"]
reqs = ["25.1","25.2","25.9","6.7"]

[task.23.2]
title = "Settle the SEND-first payment model end to end"
status = "done"
wave = 13
requires = ["23.1"]
do_1 = "Implement lxp_gateway_send_settle to build a SEND whose to, asset and amount equal the requirement, whose context_hash equals purpose_hash, whose expires_at is not later than the requirement expiry and whose conditions are a subset of acceptable_conditions."
do_2 = "Execute the payment through the single transfer-set primitive and return exactly one signed receipt to the paying agent."
do_3 = "Implement lxp_gateway_invoice_state so the same invoice_id and idempotency_key pair produces at most one economic result and returns the original receipt for every subsequent attempt."
do_4 = "Reject settlement attempted after the requirement expiry or after the batch timestamp exceeds the SEND expires_at, crediting nothing."
do_5 = "Test the identical payment submitted directly over the canonical binary transport and assert the outcome, the receipt bytes and the state root are the same as through the gateway."
verify_cmd = "make test-gateway-send"
symbols = ["lxp_gateway_send_settle","lxp_gateway_invoice_state","lxp_gateway_receipt_return"]
touches = ["src/network/lxp_gateway_send.c","tests/test_gateway_send.c"]
reqs = ["25.3","25.6","25.7","8.1","7.4"]

[task.23.3]
title = "Settle the RECEIVE-claim payment model against payer grants"
status = "done"
wave = 13
requires = ["23.1"]
do_1 = "Implement lxp_gateway_receive_claim so the service submits a RECEIVE accompanied by the signed payer grant naming authorised recipient, asset, maximum amount, total or recurring allowance, expiration, permitted purpose, service or invoice identifier and revocation sequence."
do_2 = "Debit the payer only inside those bounds and only for that invoice_id, executing the identical internal transfer function used by SEND."
do_3 = "Reject a claim from a non-authorised recipient, above the maximum or remaining allowance, after expiration, for another purpose, or against a consumed revocation sequence, leaving every balance unchanged."
do_4 = "Implement lxp_gateway_grant_present so a client can hand the service a grant instead of a receipt, and both models converge on one ledger outcome."
do_5 = "Test a repeated exchange mixing one SEND and one RECEIVE for the same invoice and assert exactly one economic result."
verify_cmd = "make test-gateway-receive"
symbols = ["lxp_gateway_receive_claim","lxp_gateway_grant_present","lxp_gateway_grant_bounds_check"]
touches = ["src/network/lxp_gateway_receive.c","tests/test_gateway_receive.c"]
reqs = ["25.4","25.5","9.3","9.5","13.3"]

[task.23.4]
title = "Verify receipts offline in a service-side library"
status = "done"
wave = 13
requires = ["23.2","23.3"]
do_1 = "Implement lxp_receipt_verify_offline to check the sequencer signature over the canonical receipt bytes using only the published sequencer key, with no call back to LayerX."
do_2 = "Implement lxp_receipt_match_requirement to confirm asset, amount, recipient, network_id and context_hash match the requirement the service issued."
do_3 = "Implement lxp_receipt_verify_checkpointed to recompute the activity merkle root from activity_inclusion_proof, verify state_inclusion_proof against resulting_state_root, count threshold bonded signatures in the guarantor certificate and match the Paxeer settlement reference."
do_4 = "Ship the verifier as a small standalone unit a paid service can link without the node, and reuse it inside layerx-verify."
do_5 = "Test verification of a pre-checkpoint receipt and the same receipt after augmentation, asserting no pre-checkpoint field changed."
verify_cmd = "make test-receipt-offline"
symbols = ["lxp_receipt_verify_offline","lxp_receipt_match_requirement","lxp_receipt_verify_checkpointed"]
touches = ["src/protocol/lxp_receipt_verify.c","cmd/layerx-verify/lxp_verify_receipt.c","tests/test_receipt_offline.c"]
reqs = ["25.8","25.10","12.4","12.8"]

[task.23.5]
title = "Ship the layerxd daemon with its three roles"
status = "done"
wave = 13
requires = ["19.1","20.3"]
do_1 = "Implement lxp_daemon_main selecting exactly one role per process: sequencer, replica or guarantor, with no combined mode that could blur the independence of attestation."
do_2 = "Implement lxp_daemon_config as an explicit file-driven configuration with no environment-dependent consensus input."
do_3 = "Wire the threading model as specified: one executor thread touching consensus state, plus verify, network, projection and checkpoint workers that never mutate it."
do_4 = "Add the nightly serial-execution mode reducing all worker pools to zero and asserting byte-identical output."
do_5 = "Implement lxp_daemon_shutdown for a clean stop at a write boundary, and test start, stop and restart under load with no sequence gap."
verify_cmd = "make test-layerxd"
symbols = ["lxp_daemon_main","lxp_daemon_config","lxp_daemon_role","lxp_daemon_shutdown"]
touches = ["cmd/layerxd/lxp_daemon_main.c","cmd/layerxd/lxp_daemon_config.c","tests/test_layerxd.c"]
reqs = ["21.7","22.1","5.9","23.3"]

[task.23.6]
title = "Ship layerxctl, layerx-verify and layerx-genesis"
status = "done"
wave = 13
requires = ["23.4","23.5"]
do_1 = "Implement layerxctl as an operator CLI that submits ordered activities and reads state, and that offers no path to mutate state outside the ordered activity log."
do_2 = "Implement layerx-verify to replay finalised history from retrieved availability bundles and verify receipts, inclusion proofs and guarantor certificates independently of any node."
do_3 = "Implement layerx-genesis as the manifest build and reconciliation entry point, leaving manifest semantics to the migration task."
do_4 = "Give every tool machine-readable canonical output so operator tooling never becomes an alternative interpretation of protocol state."
do_5 = "Test that layerx-verify run by a third party against served bytes reproduces the roots in the checkpoint certificate."
verify_cmd = "make test-tools"
symbols = ["lxp_ctl_main","lxp_ctl_submit_activity","lxp_verify_main","lxp_genesis_cli_main"]
touches = ["cmd/layerxctl/lxp_ctl_main.c","cmd/layerx-verify/lxp_verify_main.c","cmd/layerx-genesis/lxp_genesis_cli.c","tests/test_tools.c"]
reqs = ["4.9","23.4","27.1","28.14"]

[task.24]
section = "Waves 14-15 - Migration and Qualification"
title = "Build the genesis manifest, migration and shadow replay"
status = "pending"

[task.24.1]
title = "Define the explicit signed genesis manifest"
status = "done"
wave = 14
requires = ["23.6"]
do_1 = "Define struct lxp_genesis_manifest declaring protocol_version, network_id, genesis timestamp, the full parameter set, the initial guarantor set with bonds, the complete imported account and module state and the genesis state root."
do_2 = "Implement lxp_genesis_parse and lxp_genesis_verify_signature over the canonical binary encoding, refusing to start if any existing database, index or log is presented in place of a manifest."
do_3 = "Implement lxp_genesis_state_root to compute the root from the manifest content and reject a manifest whose declared root does not match."
do_4 = "Materialise every imported locked value as a real account or subaccount, never as a hidden balance column."
do_5 = "Register the genesis state root on Paxeer as the first checkpoint before any activity is accepted."
verify_cmd = "make test-genesis"
symbols = ["lxp_genesis_manifest","lxp_genesis_parse","lxp_genesis_verify_signature","lxp_genesis_state_root"]
touches = ["include/layerx/lxp_genesis.h","src/protocol/lxp_genesis.c","cmd/layerx-genesis/lxp_genesis_main.c","tests/test_genesis_manifest.c"]
reqs = ["27.1","27.4","5.1","6.1"]

[task.24.2]
title = "Account separately for every import category"
status = "done"
wave = 14
requires = ["24.1"]
do_1 = "Define struct lxp_import_section so each category is a distinct enumerated section with its own item count and per-asset totals."
do_2 = "Import USDX balances, vault reserves, open holds, queued withdrawals, liquidity pools and insurance pools as explicit sections into their real accounts and subaccounts."
do_3 = "Import open perps positions, pending orders and funding state, moving every position margin into the margin subaccount named for that position rather than a reserved field."
do_4 = "Import DID to EVM bindings with no authority to move, debit or authorise any balance."
do_5 = "Carry outstanding receipts and previously anchored roots forward as immutable historical commitments that are verifiable but not re-executable."
do_6 = "Implement lxp_import_totals to publish per-section counts and per-asset totals for reconciliation input."
verify_cmd = "make test-genesis-import"
symbols = ["lxp_import_section","lxp_import_balances","lxp_import_positions","lxp_import_bindings","lxp_import_totals"]
touches = ["cmd/layerx-genesis/lxp_import.c","migrations/0001_genesis_sections.sql","tests/test_genesis_import.c"]
reqs = ["27.2","27.4","10.1","13.5"]

[task.24.3]
title = "Reconcile the manifest against Paxeer custody or reject it"
status = "done"
wave = 14
requires = ["24.2"]
do_1 = "Implement lxp_custody_attestation_verify to read the Paxeer attested custody amount per asset from a finalised custody state, never from an operator assertion."
do_2 = "Implement lxp_genesis_reconcile to require, for every asset, exact equality between the attested custody amount and the sum of imported account, subaccount, hold, margin and pool balances."
do_3 = "Implement lxp_genesis_reject to fail the entire manifest on any mismatch, importing nothing and permitting no partial or best-effort import."
do_4 = "Report a mismatch as the asset, the attested amount, the computed amount and the difference, with zero tolerance."
do_5 = "Test a manifest that is short by one base unit and assert total rejection."
verify_cmd = "make test-genesis-reconcile"
symbols = ["lxp_genesis_reconcile","lxp_custody_attestation_verify","lxp_genesis_reject"]
touches = ["cmd/layerx-genesis/lxp_reconcile.c","tests/test_genesis_reconcile.c"]
reqs = ["27.3","14.10","24.10","13.5"]

[task.24.4]
title = "Hold the legacy LayerX read-only behind guardrails"
status = "done"
wave = 14
requires = ["24.1"]
do_1 = "Implement lxp_legacy_stream_open to consume the legacy accepted activity stream through a strictly read-only reader, with no write path to legacy state or custody."
do_2 = "Implement lxp_legacy_readonly_guard to abort the harness immediately on any attempted write to the reference system."
do_3 = "Assert the non-ported boundaries in CI: no PostgreSQL structure is treated as a protocol definition and no HTTP endpoint is treated as the canonical wire protocol."
do_4 = "Assert no in-memory authentication challenge becomes authority state and no direct Crossverse access exists inside execution."
do_5 = "Assert no development settlement fallback and no process-local event stream can satisfy a settlement or event requirement, so an unavailable settlement path fails closed."
verify_cmd = "make test-legacy-readonly"
symbols = ["lxp_legacy_reader","lxp_legacy_stream_open","lxp_legacy_readonly_guard"]
touches = ["tools/lxp_legacy_reader.c","tests/test_legacy_readonly.c"]
reqs = ["27.5","27.7","27.8","27.9"]

[task.24.5]
title = "Compare shadow replay outcomes activity by activity"
status = "done"
wave = 14
requires = ["24.3","24.4"]
do_1 = "Implement lxp_shadow_harness to feed the identical accepted activity stream into the C implementation while the legacy system remains the sole authority."
do_2 = "Implement lxp_shadow_compare_outcome to compare, per activity, the accepted or rejected classification, the ordering, the resulting balance, the fee charged, the result code and every receipt field."
do_3 = "Implement lxp_shadow_divergence_record to capture the activity identifier, the expected value and the produced value for every difference."
do_4 = "Block promotion to authority while any unexplained divergence exists, and require an intentional specified difference to be recorded explicitly rather than tolerated."
do_5 = "Implement lxp_shadow_report summarising a comparison window as counts by category plus the full divergence list."
do_6 = "Assert that every time-dependent outcome in the comparison derives from batch timestamps rather than legacy background timers."
verify_cmd = "make test-shadow"
symbols = ["lxp_shadow_harness","lxp_shadow_compare_outcome","lxp_shadow_divergence_record","lxp_shadow_report"]
touches = ["tools/lxp_shadow_compare.c","tests/test_shadow_replay.c"]
reqs = ["27.6","27.10","28.12","13.14"]

[task.24.6]
title = "Build the canonical Solidity primitive and cryptography layer"
status = "done"
wave = 15
requires = ["24.5"]
do_1 = "Implement Arithmetic.sol, Constants.sol and Types.sol with checked fixed-width conversions, full-precision multiplication and division, explicit rounding direction and one canonical definition for each Paxeer contract constant and cross-domain type."
do_2 = "Implement Bytes.sol and Encoding.sol with bounded calldata and memory readers plus one canonical encoding for deployment, message and migration commitments."
do_3 = "Implement Hashing.sol, CryptographyPrimitives.sol and MerkleLib.sol with domain separation, low-s signature recovery, index-aware binary proof verification and proof-shape bounds."
do_4 = "Implement DecimalsConverterHelper.sol with checked powers of ten, exact remainder reporting and exact-only conversion for custody credits."
do_5 = "Add unit and fuzz tests for every boundary, malformed input, arithmetic overflow, signature malleability, invalid proof and decimal-dust case."
verify_cmd = "forge test --root . --match-path test/ContractPrimitives.t.sol"
symbols = ["Arithmetic","Bytes","Encoding","Hashing","CryptographyPrimitives","MerkleLib","DecimalsConverterHelper"]
touches = ["contracts/libraries/Arithmetic.sol","contracts/libraries/Bytes.sol","contracts/libraries/Constants.sol","contracts/libraries/Types.sol","contracts/libraries/Encoding.sol","contracts/libraries/Hashing.sol","contracts/libraries/CryptographyPrimitives.sol","contracts/libraries/MerkleLib.sol","contracts/libraries/DecimalsConverterHelper.sol","test/ContractPrimitives.t.sol"]
reqs = ["29.1","29.2","29.12","29.15"]

[task.24.7]
title = "Build the Solidity call, caller, storage and upgrade-safety layer"
status = "done"
wave = 15
requires = ["24.6"]
do_1 = "Implement SafeCall.sol and Burn.sol with bounded return-data copying, exact call-result classification, code-presence checks and value-disposal semantics that cannot strand accounting state."
do_2 = "Implement EOA.sol and CallerChecker.sol with explicit EIP-7702-aware direct-origin rules and no tx.origin authorization."
do_3 = "Implement DelegateCallAware.sol and UUPSNotUpgradeable.sol so direct-only, delegated-only and permanently non-upgradeable execution postures are mechanically enforced."
do_4 = "Implement Storage.sol and TransientContext.sol with namespaced slots, collision checks, scoped context clearing and a Paxeer-compatible persistent fallback."
do_5 = "Add malicious-target and call-context tests covering oversized reverts, empty-code calls, constructor callers, delegated calls, nested context and attempted UUPS upgrades."
verify_cmd = "forge test --root . --match-path test/ContractExecutionSafety.t.sol"
symbols = ["SafeCall","Burn","EOA","CallerChecker","DelegateCallAware","UUPSNotUpgradeable","Storage","TransientContext"]
touches = ["contracts/libraries/SafeCall.sol","contracts/libraries/Burn.sol","contracts/libraries/EOA.sol","contracts/libraries/CallerChecker.sol","contracts/security/DelegateCallAware.sol","contracts/security/UUPSNotUpgradeable.sol","contracts/libraries/Storage.sol","contracts/libraries/TransientContext.sol","test/ContractExecutionSafety.t.sol"]
reqs = ["29.3","29.4","29.5","29.6","29.13","29.16"]

[task.24.8]
title = "Build deterministic blueprints, predeploys and static configuration"
status = "done"
wave = 15
requires = ["24.7"]
do_1 = "Implement Blueprint.sol for versioned CREATE2 salts, init-code commitments, predicted addresses and runtime code-hash validation."
do_2 = "Implement Predeploys.sol and Preinstalls.sol as exhaustive typed manifests of critical LayerX roles and their expected addresses, interfaces, code hashes and configuration hashes, with no foreign-chain address assumed to exist."
do_3 = "Implement StaticConfig.sol as the canonical commitment to chain, protocol, governance, genesis, timing, asset and feature configuration."
do_4 = "Implement Features.sol and SemverComp.sol with explicit supported-feature masks, rejected foreign facilities and strict semantic-version parsing and ordering."
do_5 = "Add deployment-collision, wrong-code, wrong-chain, unknown-feature, malformed-version and deterministic-address vectors."
verify_cmd = "forge test --root . --match-path test/ContractDeployment.t.sol"
symbols = ["Blueprint","Predeploys","Preinstalls","StaticConfig","Features","SemverComp"]
touches = ["contracts/deployment/Blueprint.sol","contracts/deployment/Predeploys.sol","contracts/deployment/Preinstalls.sol","contracts/config/StaticConfig.sol","contracts/config/Features.sol","contracts/libraries/SemverComp.sol","test/ContractDeployment.t.sol"]
reqs = ["29.7","29.8","29.13","29.14","29.15"]

[task.24.9]
title = "Build the contracts manager, staged migrator and validators"
status = "done"
wave = 15
requires = ["24.8"]
do_1 = "Implement BlockErrors.sol and StandardValidatorUtils.sol with stable errors and reusable validation of targets, selectors, interface support, code hashes, configuration hashes and storage-layout versions."
do_2 = "Implement ManagerContainer.sol as the enumerable role-to-contract manifest, rejecting duplicate roles, duplicate addresses, absent code and mismatched component attestations."
do_3 = "Implement ManagerMigrator.sol as a timelocked commit-validate-execute state machine binding versions, configuration, ordered operation root, code hashes, values, calldata, deadline and one-shot completion."
do_4 = "Ensure the complete migration is validated before the first operation, each operation is checked immediately before and after execution, and a failed transaction leaves no partial migration state."
do_5 = "Add downgrade, replay, expiry, reordered-operation, selector, target, value, code-hash, storage-layout and partial-execution adversarial tests."
verify_cmd = "forge test --root . --match-path test/ContractManager.t.sol"
symbols = ["BlockErrors","StandardValidatorUtils","ManagerContainer","ManagerMigrator"]
touches = ["contracts/manager/BlockErrors.sol","contracts/manager/StandardValidatorUtils.sol","contracts/manager/ManagerContainer.sol","contracts/manager/ManagerMigrator.sol","test/ContractManager.t.sol"]
reqs = ["29.9","29.10","29.14","29.15","29.16"]

[task.24.10]
title = "Integrate and qualify the professional Paxeer contract system"
status = "done"
wave = 15
requires = ["24.9"]
do_1 = "Route custody, checkpoint, challenge, withdrawal, emergency-exit, nullifier and timelock contracts through the canonical constants, hashing, Merkle, safe-call, error and non-upgradeability layers without changing their externally committed wire bytes."
do_2 = "Deploy the complete suite through the deterministic blueprint and register every component in the manager container under one static configuration hash."
do_3 = "Add the canonical MessageTypes.sol envelope and assert chain, protocol, sender, target, nonce, value, gas and payload domain separation against cross-chain and cross-manager replay."
do_4 = "Add invariants for custody conservation, one-time withdrawals, checkpoint continuity, bounded governance authority, immutable implementation posture and manager manifest completeness."
do_5 = "Run the complete Foundry suite at the configured fuzz and invariant depths, inspect every external state-changing function, and record a Codify trace from each requested contract-system dimension to symbols, files, tests and snapshots."
verify_cmd = "make test-contracts"
symbols = ["MessageTypes","LayerXVault","CheckpointRegistry","ManagerContainer","ManagerMigrator"]
touches = ["contracts/libraries/MessageTypes.sol","contracts/CheckpointRegistry.sol","contracts/custody/LayerXVault.sol","contracts/governance/LayerXTimelock.sol","contracts/storage/WithdrawalNullifierRegistry.sol","test/*.t.sol"]
reqs = ["29.1","29.2","29.3","29.4","29.5","29.6","29.7","29.8","29.9","29.10","29.11","29.12","29.13","29.14","29.15","29.16"]

[task.25]
title = "Qualify the protocol with conformance suites and fuzzing"
status = "pending"

[task.25.1]
title = "Qualify deterministic replay at scale and across architectures"
status = "done"
wave = 15
requires = ["24.10"]
do_1 = "Implement lxp_qual_corpus_generate to build a corpus of at least ten million activities spanning every activity type, stored as canonical bytes with an independently recorded terminal root."
do_2 = "Implement lxp_qual_replay_matrix to replay that corpus across machines, operating systems, compilers, optimisation levels and CPU architectures including at least one 64-bit and one non-x86 target."
do_3 = "Compare activity encodings, receipts, batch headers and state roots byte for byte and fail qualification on any single differing byte."
do_4 = "Implement lxp_qual_root_ledger to record intermediate roots at every batch boundary and compare them, not only the terminal root."
do_5 = "Publish the corpus digest so a third party can reproduce the run independently."
verify_cmd = "make qualify-replay"
symbols = ["lxp_qual_replay_matrix","lxp_qual_corpus_generate","lxp_qual_root_ledger"]
touches = ["tests/qualification/lxp_qual_replay.c","tools/lxp_corpus_gen.c","tests/test_qual_replay.c"]
reqs = ["28.1","28.2","5.1","13.14"]

[task.25.2]
title = "Inject crashes, partitions and sequencer loss"
status = "done"
wave = 15
requires = ["25.1"]
do_1 = "Implement lxp_fault_inject_point at every distinct durable write boundary in the append-only log, the index and the checkpoint store."
do_2 = "Implement lxp_fault_crash_at_boundary to abort the process at each point in turn and assert on restart that each activity is fully contained or fully excluded, with no partially applied transfer set ever observable."
do_3 = "Assert the global sequence resumes with no gap, duplication or reordering after every injected crash."
do_4 = "Implement lxp_partition_sim for replica isolation, message reordering and message loss, asserting no conflicting state is ever finalised for one sequence range."
do_5 = "Implement lxp_sequencer_loss_sim for prolonged unavailability, asserting the network halts rather than forks and resumes from the last checkpointed root."
verify_cmd = "make qualify-faults"
symbols = ["lxp_fault_inject_point","lxp_fault_crash_at_boundary","lxp_partition_sim","lxp_sequencer_loss_sim"]
touches = ["tests/qualification/lxp_qual_faults.c","src/storage/lxp_fault_hooks.c","tests/test_qual_recovery.c"]
reqs = ["28.3","28.4","11.10","21.10"]

[task.25.3]
title = "Fuzz malformed activities and signatures to exhaustion"
status = "done"
wave = 15
requires = ["25.1"]
do_1 = "Implement fuzz targets driving malformed, truncated, over-long, deeply nested, non-canonically encoded and randomly mutated activity bytes at the decoder and the state machine."
do_2 = "Assert every non-canonical input is rejected with a deterministic result code, with no crash, hang, out-of-bounds read or unbounded allocation and no state change."
do_3 = "Implement lxp_fuzz_signature_mutate covering malleated, truncated, all-zero, wrong-curve, wrong-domain-separator and replayed-from-another-network signatures."
do_4 = "Assert a signature valid on another network_id is treated as invalid, and that verification always runs over the exact canonical bytes."
do_5 = "Implement lxp_fuzz_transfer_set to fuzz leg counts, orders and asset combinations against the transfer-set legality rules."
do_6 = "Seed corpora from the qualification corpus so fuzzing starts from realistic structures rather than noise."
verify_cmd = "make qualify-fuzz"
symbols = ["lxp_fuzz_activity_decode","lxp_fuzz_signature_mutate","lxp_fuzz_transfer_set","lxp_fuzz_corpus_seed"]
touches = ["fuzz/fuzz_activity.c","fuzz/fuzz_signature.c","fuzz/fuzz_transfer_set.c","tests/test_fuzz_smoke.c"]
reqs = ["28.5","28.6","6.2","6.8"]

[task.25.4]
title = "Prove overflow and rounding behaviour and ship sanitizer-clean"
status = "in_progress"
wave = 15
requires = ["25.3"]
do_1 = "Implement lxp_u128_proof_harness and lxp_u256_boundary_case for exhaustive boundary testing of every checked addition, subtraction, multiplication and division."
do_2 = "Add machine-checked proof obligations for the 128-bit and 256-bit routines asserting each returns an exact checked result or a deterministic overflow error."
do_3 = "Implement lxp_rounding_direction_check to assert every rounding site rounds in the single specified direction and that rounding never creates or destroys value."
do_4 = "Assert by build-time check that no floating-point instruction or libm symbol appears anywhere in consensus-critical code."
do_5 = "Run the entire test, fuzz and replay corpus under address, undefined-behaviour, thread and memory sanitizers plus a leak checker, failing on any suppressed finding without a recorded justification."
verify_cmd = "make qualify-arith"
symbols = ["lxp_u128_proof_harness","lxp_u256_boundary_case","lxp_rounding_direction_check"]
touches = ["tests/qualification/lxp_qual_arith.c","tools/lxp_arith_proof.sh","tests/test_qual_arith.c"]
reqs = ["28.7","28.16","5.2","5.3","26.8"]

[task.25.5]
title = "Drill settlement, exits, unavailability and reserve audit"
status = "pending"
wave = 15
requires = ["25.2"]
do_1 = "Implement lxp_qual_guarantor_disagree to run the three-guarantor disagreement scenario and assert finality is withheld and a portable equivocation proof is produced."
do_2 = "Implement lxp_qual_da_withhold to withhold each availability class in turn and assert the checkpoint stays unfinalizable and no possession attestation can be signed."
do_3 = "Implement lxp_qual_exit_drill to execute an emergency exit end to end against a live custody deployment with the sequencer offline and uncooperative for the entire test."
do_4 = "Assert the exit needs only a finalised checkpoint certificate, a balance proof against its state root and an unspent withdrawal nullifier."
do_5 = "Implement lxp_qual_reserve_audit to show per asset that all account classes sum exactly to the Paxeer custody balance with zero tolerance and that every historical batch preserved debits equal to credits."
verify_cmd = "make qualify-settlement"
symbols = ["lxp_qual_exit_drill","lxp_qual_reserve_audit","lxp_qual_guarantor_disagree","lxp_qual_da_withhold"]
touches = ["tests/qualification/lxp_qual_settlement.c","tools/lxp_reserve_audit.c","tests/test_qual_settlement.c"]
reqs = ["28.8","28.9","28.10","28.11","24.9"]

[task.25.6]
title = "Run the canary and the independent conformance suites"
status = "pending"
wave = 15
requires = ["25.4","25.5"]
do_1 = "Implement lxp_conformance_run_wire so an independently written implementation accepts every valid canonical vector, rejects every listed non-canonical and adversarial vector, and reproduces the exact encodings, identifiers, merkle roots and receipt bytes."
do_2 = "Implement lxp_conformance_run_capi to check every documented contract of the public headers: return codes, ownership and lifetime rules, thread-safety declarations, and the rule that exactly one primitive writes a balance."
do_3 = "Make the C API suite detect and fail any direct balance write attempted outside that primitive."
do_4 = "Publish struct lxp_conformance_vector and the vector corpus so the suites can be executed by an independent party with no access to the implementation."
do_5 = "Implement lxp_canary_exposure_limit to run real agents with real value under a bounded exposure cap for the declared canary period."
do_6 = "Declare general availability only after the canary shows no reserve discrepancy, no unrecoverable crash, no missed checkpoint deadline and no receipt failing independent verification."
verify_cmd = "make qualify-conformance"
symbols = ["lxp_conformance_vector","lxp_conformance_run_wire","lxp_conformance_run_capi","lxp_canary_exposure_limit"]
touches = ["tests/conformance/lxp_conformance_wire.c","tests/conformance/lxp_conformance_capi.c","tests/vectors/wire_vectors.lxv","tools/lxp_canary_monitor.c"]
reqs = ["28.13","28.14","28.15","6.10","7.2"]
